---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/organizations/handlers.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo test compiles and runs across the whole workspace without errors"
    - "The two existing inline unit tests in organizations/handlers.rs still pass"
  artifacts:
    - backend/src/organizations/handlers.rs
  key_links:
    - "CreateOrganizationRequest struct literal in tests module matches the struct definition in organizations/models.rs (7 fields including department: Option<String>)"
---

<objective>
Fix the broken `cargo test` compile: the inline unit test module in `backend/src/organizations/handlers.rs` constructs `CreateOrganizationRequest` struct literals that are missing the `department` field, which was added to the struct in `backend/src/organizations/models.rs` (`department: Option<String>`). This is a compile error that blocks the entire crate's test suite.

Purpose: Restore a green `cargo test` so the full backend test suite (unit + phase 9-12 integration binaries) compiles and runs again.
Output: `backend/src/organizations/handlers.rs` test module updated with `department: None,` added to both struct literals; crate compiles; existing tests still pass; zero regression in the resolver_parity, digital_resources_api_test, and security_hardening_test integration binaries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@backend/src/organizations/models.rs
@backend/src/organizations/handlers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing department field to inline unit test struct literals</name>
  <files>backend/src/organizations/handlers.rs</files>
  <action>
    In the `#[cfg(test)] mod tests` block at the bottom of `backend/src/organizations/handlers.rs`, both `CreateOrganizationRequest { ... }` struct literals (in `test_create_organization_validation` at line ~275 and `test_create_organization_invalid_email` at line ~289) are missing the `department` field, which is `Option<String>` per `backend/src/organizations/models.rs`. This causes a compile error (E0063 missing field) that fails `cargo test` for the whole crate.

    Add `department: None,` as a new field to both struct literals (after the `contract_number` field, matching struct field order). Use `None` since the tests only assert email/general validation behavior and `department` is optional — do not fabricate a department value or change any other field, assertion, or test logic. Do not touch any other file or any other part of this file.
  </action>
  <verify>
    <automated>cd backend && cargo test --lib organizations::handlers::tests -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>Both struct literals include `department: None,`; `cargo test --lib organizations::handlers::tests` compiles and both tests (`test_create_organization_validation`, `test_create_organization_invalid_email`) pass.</done>
</task>

</tasks>

<verification>
Run the full backend test suite to confirm the crate-wide compile error is resolved and no regression was introduced in the phase 9-12 integration test binaries:

```bash
cd backend && cargo test --lib 2>&1 | tail -20
cd backend && cargo test --test resolver_parity --test digital_resources_api_test --test security_hardening_test 2>&1 | tail -40
```

Both commands must exit 0 with all tests passing (no compile errors, no failed assertions).
</verification>

<success_criteria>
- `cargo test --lib` compiles and passes for the `organizations` module (and the whole crate, since the crate-wide compile error is gone).
- `cargo test --test resolver_parity --test digital_resources_api_test --test security_hardening_test` passes with zero regressions.
- No file other than `backend/src/organizations/handlers.rs` was modified.
</success_criteria>

<output>
Create `.planning/quick/260703-u5o-fix-broken-cargo-test-backend-src-organi/260703-u5o-SUMMARY.md` when done
</output>
