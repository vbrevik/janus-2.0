---
milestone: v2.1
milestone_name: Physical Access Zones (demo)
audit_date: 2026-06-01
status: passed
phases: [5, 6, 7, 8]
---

# Milestone Audit — v2.1 Physical Access Zones (demo)

**Verdict: PASSED.** All four phases carry `status: passed` VERIFICATION reports; every requirement is verified in its phase; deferrals and out-of-scope are documented and intentional.

## Requirements coverage

| Group | Requirements | Phase | Status |
|-------|--------------|-------|--------|
| ZONE | ZONE-01..05 | 5 | ✅ Verified (`isValidZoneTypeCombination` ceiling rule, zone hierarchy types) |
| ACCESS | ACCESS-01..05 | 5–6 | ✅ Verified (5-tier ladder; CONTROLLED/RESTRICTED/SECURED rules; two-gate resolution) |
| GRANT | GRANT-01..04 | 6 | ✅ Verified (`isGrantActive`, `resolveGrant` most-specific + explicit-auth short-circuit) |
| DELEG | DELEG-01..03 | 6 | ✅ Verified (type + `isDelegateActive`; `canIssueGrant()` enforcement deferred to Phase 8 UI by design) |
| LOG | LOG-01..03 | 7 | ✅ Verified (`ZoneEntryLog`, CARD/ESCORT, mandatory-for-SECURED) |
| VISIT | VISIT-01..03 | 7 | ✅ Verified (`ZoneVisitorPass`, `getActiveVisitorPasses`) |
| SEED | SEED-01..09 | 8 | ✅ Verified (6-unit dataset: all zone types, inheritance, exclusions, delegates, CARD+ESCORT, passes, active/expired/future grants) |
| UI | UI-01..06 | 8 | ✅ Verified (Zone Browser, Access Resolution Explorer w/ prose trace, Entry Log view; 5 browser-confirmed) |

**Note:** REQUIREMENTS.md checkboxes and the Traceability table still read unchecked/"Pending" for the model requirements — this is unmaintained bookkeeping, not a coverage gap. The phase VERIFICATION reports are authoritative and all read PASSED. The archive step will tick the boxes.

## Cross-phase integration

Linear, well-wired dependency chain: Phase 5 model → Phase 6 resolution/delegation builds on the access-rule functions → Phase 7 entry log/passes build on the model → Phase 8 dataset + UI exercise the full stack. Phase 8 browser-confirmed the end-to-end demo flows (browse hierarchy → resolve a person/zone with an explained trace → toggle grants to observe inheritance/deny → inspect entry log with visitor-pass badges). No broken seams found.

## Deferrals (intentional, documented)

- `canIssueGrant()` delegation enforcement — typed in Phase 6, enforcement deferred to Phase 8 UI per the SPEC interview lock.
- Out of scope (documented in REQUIREMENTS.md): Rust/PostgreSQL backend, real card-reader hardware, cross-entity federation of zone access, NDA/screening prerequisites.

## Tech debt / gaps

None blocking. v2.1 is demo/mock-only by design; backend implementation is explicitly a future milestone.

## Test posture

Vitest suite green across phases (Phase 7 reports 116/116; Phase 8 tsc clean + 17/17 criteria). All within the isolated `frontend/src/demo/` track — no `routeTree.gen.ts` changes, no production-app coupling.
