---
phase: 14-mock-dataset-worldstate
plan: 2
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/demo/lib/dataset-selectors.ts
  - frontend/src/demo/lib/dataset-selectors.test.ts
autonomous: true
requirements: [DATA-SEED-01, DATA-SEED-02, DATA-SEED-03, DATA-SEED-04, DATA-SEED-05, DATA-SEED-06]

must_haves:
  truths:
    - "`datasetsForApplication(DATASET_NODES, \"rsrc-milapp-1\")` returns exactly the 4 milapp-1-attached datasets (ds-mailbox-dana, ds-mailbox-sam, ds-archive-caserecords, ds-docsite-ops); `datasetsForApplication(DATASET_NODES, \"rsrc-intapp-1\")` returns exactly ds-docsite-intel (the application_id join, R6)"
    - "every DATASET_NODES[].application_ids entry resolves to an existing APPLICATIONS id -- a passing structural test confirms zero orphan application_id references (R6 covered edge)"
    - "subj-1 and subj-2 each have >=2 active DatasetAccessGrant records on 2 DISTINCT MAILBOX-type datasets (own + shared), confirmed via activeDatasetGrantsForPerson (DATA-SEED-01)"
    - "ds-archive-caserecords' active DatasetAccessGrants cover all three ArchiveRole values (READER, CASE_HANDLER, ADMIN) across 3 distinct people (DATA-SEED-02)"
    - ">=2 DOCUMENT_SITE DatasetNodes exist with >=2 distinct DocumentSiteLevel values represented across their active grants (DATA-SEED-03)"
    - "resolveDatasetAt(..., subj-1, SECRET, ds-archive-caserecords, ADMIN, NOW) returns allow:true, visible:true -- the full prerequisite-chain-success scenario against REAL seed data (DATA-SEED-04)"
    - "resolveDatasetAt(..., ds-deny-subj, SECRET, ds-archive-caserecords, READER, NOW) returns visible:true, allow:false with DATASET_GRANT as the failing gate -- the dataset-gate-denial scenario against real seed data (DATA-SEED-05)"
    - "deny-matrix case (a): resolveDatasetAt(..., subj-3, CONFIDENTIAL, ds-archive-caserecords, READER, NOW) returns allow:false with CLEARANCE as the SOLE failing gate -- APP_GRANT_OR and DATASET_GRANT both independently assert pass:true in the SAME test (DATA-SEED-06, edge-probe R5 covered)"
    - "deny-matrix case (b): resolveDatasetAt(..., subj-2, TOP_SECRET, ds-archive-caserecords, CASE_HANDLER, NOW) returns allow:false, visible:false with APP_GRANT_OR as the SOLE failing gate -- CLEARANCE and DATASET_GRANT both independently assert pass:true in the SAME test (DATA-SEED-06, edge-probe R5 covered)"
    - "deny-matrix case (c): the SAME ds-deny-subj/ds-archive-caserecords/READER call as DATA-SEED-05 is asserted a second time with the fuller edge-probe rigor -- DATASET_GRANT is the SOLE failing gate, CLEARANCE and APP_GRANT_OR both independently assert pass:true (DATA-SEED-06, edge-probe R5 covered)"
  artifacts:
    - "frontend/src/demo/lib/dataset-selectors.ts -- datasetsForApplication, activeDatasetGrantsForPerson, resolveDatasetAt"
    - "frontend/src/demo/lib/dataset-selectors.test.ts -- join/structural tests + DATA-SEED-01..06 seed-integration scenario tests"
  key_links:
    - "resolveDatasetAt calls resolveDatasetAccess (Phase 13, model.ts) directly and returns its DatasetAccessResult verbatim -- never re-derives allow/visible or transforms the gates[] trace"
    - "every seed-integration test in dataset-selectors.test.ts imports DATASET_NODES/DATASET_GRANTS/APPLICATIONS/PLATFORMS/RESOURCE_GRANTS from Plan 14-01's real seed.ts fixtures -- no inline/standalone fixtures for these scenarios (D-08)"
---

<objective>
Build `dataset-selectors.ts` -- the read path over Plan 14-01's fixtures, mirroring `digital-resource-selectors.ts`'s pure-function/explicit-now style but with individual array params (D-11's deliberate divergence, since the join spans two separate WorldState sub-objects) -- and `dataset-selectors.test.ts`, which proves DATA-SEED-01 through DATA-SEED-06 against the REAL seed.ts fixtures (not inline standalone ones, per D-08/D-09).

Purpose: This is the proof layer. Phase 13 already built and tested `resolveDatasetAccess` in isolation; this plan proves the specific combination of real people, real grants, and real datasets Plan 14-01 seeded actually produces the intended allow/deny/visible outcomes -- including all three deny-matrix gates isolated as sole deciders.
Output: `dataset-selectors.ts` (3 exported pure functions) and `dataset-selectors.test.ts` (join/structural checks + 8 seed-integration scenario tests) both exist and pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mock-dataset-worldstate/14-SPEC.md
@.planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md
@.planning/phases/14-mock-dataset-worldstate/14-RESEARCH.md

# Source of truth -- Plan 14-01's fixtures this plan reads and Phase 13's resolver this plan wraps
@frontend/src/demo/lib/seed.ts
@frontend/src/demo/lib/model.ts
@frontend/src/demo/lib/digital-resource-selectors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dataset-selectors.ts -- application_id join + active-grant filter + resolveDatasetAt wrapper</name>
  <files>frontend/src/demo/lib/dataset-selectors.ts</files>
  <read_first>
    - frontend/src/demo/lib/digital-resource-selectors.ts (full file -- the sibling analog; note it takes a single `world` param, which D-11 deliberately diverges from for dataset-selectors.ts)
    - frontend/src/demo/lib/model.ts:1499-1650 (DatasetAccessResult + resolveDatasetAccess -- the function this file wraps, exact param order/types)
    - frontend/src/demo/lib/model.ts:803-812 (isWindowActive)
    - .planning/phases/14-mock-dataset-worldstate/14-RESEARCH.md "Pitfall 1" section (why individual array params, not a single world/sub-object param, are required here)
  </read_first>
  <behavior>
    - `datasetsForApplication(datasets, "rsrc-milapp-1")` returns only the DatasetNodes whose `application_ids` includes that exact string
    - `activeDatasetGrantsForPerson(grants, personId, now)` returns only grants matching `person_id` AND currently window-active at `now` (excludes expired/future-dated grants)
    - `resolveDatasetAt(...)` with a `datasetId` that does not exist in `datasets` returns `{ allow: false, visible: false, gates: [] }` without throwing
    - `resolveDatasetAt(...)` with a valid `datasetId` delegates to `resolveDatasetAccess` and returns its result unchanged (no post-processing of allow/visible/gates)
  </behavior>
  <action>
    Create `frontend/src/demo/lib/dataset-selectors.ts` with a file-header comment matching `digital-resource-selectors.ts`'s style -- state that these are pure read selectors over the dataset fixtures and that every timestamp is supplied by the caller rather than read from the system clock internally (paraphrase the concept; do not restate the literal disallowed-call syntax verbatim in the comment). Import `resolveDatasetAccess, isWindowActive, type DatasetNode, type DatasetAccessGrant, type DatasetAccessResult, type Clearance, type ApplicationNode, type PlatformNode, type ResourceAccessGrant` from "./model". Export exactly 3 functions, each taking explicit individual array params (D-11 -- deliberately NOT a single world/sub-object param, since the join spans two separate WorldState sub-objects): (1) `datasetsForApplication(datasets: DatasetNode[], applicationId: string): DatasetNode[]` -- returns `datasets.filter(d => d.application_ids.includes(applicationId))`; (2) `activeDatasetGrantsForPerson(grants: DatasetAccessGrant[], personId: string, now: Date): DatasetAccessGrant[]` -- returns `grants.filter(g => g.person_id === personId && isWindowActive(g.valid_from, g.valid_until, now))`; (3) `resolveDatasetAt(datasets: DatasetNode[], applications: ApplicationNode[], platforms: PlatformNode[], appGrants: ResourceAccessGrant[], datasetGrants: DatasetAccessGrant[], subject: string, subjectClearance: Clearance, datasetId: string, requiredLevel: string, now: Date): DatasetAccessResult` -- finds the dataset by id in `datasets`; if not found returns `{ allow: false, visible: false, gates: [] }` (`DatasetAccessResult` has no `reason` field, unlike `ResourceAccessResult` -- do not add one); otherwise delegates directly to `resolveDatasetAccess(subject, subjectClearance, dataset, applications, platforms, appGrants, datasetGrants, requiredLevel, now)` and returns its result verbatim, with zero post-processing of `allow`/`visible`/`gates`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc -b --noEmit` reports zero errors. `dataset-selectors.ts` exports exactly `datasetsForApplication`, `activeDatasetGrantsForPerson`, `resolveDatasetAt` -- no additional exported symbols. None of the 3 function BODIES (excluding comment lines) read the system clock internally: `grep -v '^\s*//' frontend/src/demo/lib/dataset-selectors.ts | grep -c 'new Date()'` returns 0 and `grep -v '^\s*//' frontend/src/demo/lib/dataset-selectors.ts | grep -c 'Date.now()'` returns 0.
  </acceptance_criteria>
  <done>`dataset-selectors.ts` exists, mirrors `digital-resource-selectors.ts`'s pure-function/explicit-now style with the D-11-mandated individual-array-params divergence, and `resolveDatasetAt` delegates to Phase 13's `resolveDatasetAccess` without re-deriving any gate logic.</done>
</task>

<task type="auto">
  <name>Task 2: dataset-selectors.test.ts -- join/structural tests + DATA-SEED-01..06 seed-integration scenarios</name>
  <files>frontend/src/demo/lib/dataset-selectors.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/digital-resource.test.ts:1-70, 888-972 (imports pattern + "seed integration: digital-resource fixtures" describe block -- the exact structural convention D-08/D-09 mandate this new file follow)
    - frontend/src/demo/lib/dataset.test.ts:507-660 (the "sole deciding gate" gate-trace assertion idiom -- reuse this EXACT pattern against real seed data instead of inline fixtures)
    - frontend/src/demo/lib/seed.ts (Plan 14-01's finished output -- DATASET_NODES/DATASET_GRANTS/DATASET_DELEGATES, the additive RESOURCE_GRANTS entries, and the new `ds-deny-subj` Subject; read the FINISHED file, not this plan's prose, to confirm exact literal ids/values)
    - frontend/src/demo/lib/dataset-selectors.ts (this plan's Task 1 output)
    - .planning/phases/14-mock-dataset-worldstate/14-SPEC.md Requirements 1-6 and the Edge Coverage table rows R5/R6
  </read_first>
  <action>
    Create `frontend/src/demo/lib/dataset-selectors.test.ts`. Import `describe, it, expect` from "vitest"; import `datasetsForApplication, activeDatasetGrantsForPerson, resolveDatasetAt` from "./dataset-selectors"; import `DATASET_NODES, DATASET_GRANTS, APPLICATIONS, PLATFORMS, RESOURCE_GRANTS` from "./seed". Define one fixed `const NOW = new Date("2026-07-01T12:00:00Z")` (matches dataset.test.ts's convention; Sam's expired grant at 2026-05-01 sits before it).
    First describe block `"dataset-selectors: application_id join + structural integrity"`: (a) `datasetsForApplication(DATASET_NODES, "rsrc-milapp-1")` returns exactly the 4 datasets attached to it -- assert `.length === 4` and assert each of ds-mailbox-dana, ds-mailbox-sam, ds-archive-caserecords, ds-docsite-ops is present via `.some()`; (b) `datasetsForApplication(DATASET_NODES, "rsrc-intapp-1")` returns exactly `[ds-docsite-intel]` (`.length === 1`); (c) a structural test asserting `DATASET_NODES.every(d => d.application_ids.every(id => APPLICATIONS.some(a => a.id === id)))` is `true` -- zero orphan application_id references (R6 covered edge).
    Second describe block `"seed integration: dataset fixtures (DATA-SEED-01..06)"`, one `it` per case: DATA-SEED-01 (mailbox) -- for both `"subj-1"` and `"subj-2"`, filter `activeDatasetGrantsForPerson(DATASET_GRANTS, personId, NOW)` to MAILBOX-type dataset ids and assert the resulting distinct `dataset_id` count is `>= 2`; DATA-SEED-02 (archive) -- filter `DATASET_GRANTS` to `dataset_id === "ds-archive-caserecords"` and assert the set of `.level` values equals `{"READER","CASE_HANDLER","ADMIN"}`; DATA-SEED-03 (document sites) -- assert `DATASET_NODES.filter(d => d.dataset_type === "DOCUMENT_SITE").length >= 2` and the set of `.level` values among grants on those dataset ids has size `>= 2`; DATA-SEED-04 -- `resolveDatasetAt(DATASET_NODES, APPLICATIONS, PLATFORMS, RESOURCE_GRANTS, DATASET_GRANTS, "subj-1", "SECRET", "ds-archive-caserecords", "ADMIN", NOW)` asserts `allow: true, visible: true`; DATA-SEED-05 -- same call shape with `"ds-deny-subj"` / `"SECRET"` / `"READER"` asserts `visible: true, allow: false` and `gates.find(g => g.kind === "DATASET_GRANT")?.pass === false`; deny-matrix (a) -- `"subj-3"` / `"CONFIDENTIAL"` / `"READER"` asserts `allow: false`, `gates.find(g => g.kind === "CLEARANCE")?.pass === false`, AND `gates.find(g => g.kind === "APP_GRANT_OR")?.pass === true`, AND `gates.find(g => g.kind === "DATASET_GRANT")?.pass === true` (all in the SAME test); deny-matrix (b) -- `"subj-2"` / `"TOP_SECRET"` / `"CASE_HANDLER"` asserts `allow: false, visible: false`, `gates.find(g => g.kind === "APP_GRANT_OR")?.pass === false`, AND `gates.find(g => g.kind === "CLEARANCE")?.pass === true`, AND `gates.find(g => g.kind === "DATASET_GRANT")?.pass === true`; deny-matrix (c) -- reuse DATA-SEED-05's exact call (`"ds-deny-subj"` / `"SECRET"` / `"READER"`) in a SEPARATE `it` and additionally assert `gates.find(g => g.kind === "CLEARANCE")?.pass === true` AND `gates.find(g => g.kind === "APP_GRANT_OR")?.pass === true` alongside `DATASET_GRANT`'s `pass === false` -- the fuller edge-probe rigor distinct from DATA-SEED-05's simpler assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/demo/lib/dataset-selectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run src/demo/lib/dataset-selectors.test.ts` passes, 0 failures, covering 11 `it` blocks (3 join/structural + 8 seed-integration). Every deny-matrix `it` block asserts the target gate's `pass === false` AND explicitly asserts the OTHER TWO gates' `pass === true` in the SAME test (not a separate test) -- matching SPEC.md's edge-probe R5 requirement literally. The deny-matrix case-(b) test explicitly asserts `visible === false`, and the DATA-SEED-05 test explicitly asserts `visible === true` -- proving these are NOT the same outcome (RESEARCH.md Pitfall 3).
  </acceptance_criteria>
  <done>`dataset-selectors.test.ts` exists with passing tests for the application_id join, the no-orphan-reference structural check, and all of DATA-SEED-01 through DATA-SEED-06 resolved against Plan 14-01's real seed.ts fixtures (D-08/D-09).</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `frontend/src/demo/lib/seed.ts` (additive, Plan 14-01): `DATASET_NODES`, `DATASET_GRANTS`, `DATASET_DELEGATES` (new exported constants); 3 new additive entries in `RESOURCE_GRANTS`; new `phase14DatasetSubjects` const merged into `SUBJECTS` (new Subject id `ds-deny-subj`)
- `frontend/src/demo/lib/model.ts` (additive, Plan 14-03): new `DatasetAuditEntry` interface
- `frontend/src/demo/lib/dataset-selectors.ts` (new file, Plan 14-02): `datasetsForApplication`, `activeDatasetGrantsForPerson`, `resolveDatasetAt`
- `frontend/src/demo/lib/dataset-selectors.test.ts` (new file, Plan 14-02)
- `frontend/src/demo/store/world-state.tsx` (additive, Plan 14-03): `WorldState.datasets` field; new `ISSUE_DATASET_GRANT` `Action` variant + reducer case
- `frontend/src/demo/store/world-state.test.tsx` (additive, Plan 14-03): new `"ISSUE_DATASET_GRANT action"` describe block

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future Phase 15 UI -> `dataset-selectors.ts`'s exported functions | Untrusted caller input (person/dataset/level/datetime selections) crossing into the resolver wrapper |
| `resolveDatasetAt`'s `{visible, allow}` output -> future Phase 15 UI consumers | Output-semantics boundary -- `visible` must never be read downstream as `allow`; this plan's tests are what makes the two fields' independence observable against real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Elevation of Privilege | `resolveDatasetAt`'s wrapper logic | high | mitigate | Task 1's action explicitly forbids any post-processing of `allow`/`visible`/`gates` -- the function returns `resolveDatasetAccess`'s result verbatim; Task 2's deny-matrix tests assert `visible` and `allow` diverge in exactly the cases the resolver's own contract predicts (case b: `visible:false`; DATA-SEED-05: `visible:true`), proving no selector-level reinterpretation occurred |
| T-14-02 | Denial of Service (fail-closed throw) | `datasetsForApplication`, `resolveDatasetAt` consuming a dataset with an orphan `application_id` | low | mitigate | Task 2's structural test asserts zero orphan `application_id` references across all of Plan 14-01's `DATASET_NODES` (R6 covered edge) -- an orphan reference would otherwise cause `resolveDatasetAccess`'s internal application lookup to throw |

</threat_model>

<verification>
Run `cd frontend && npx tsc -b --noEmit` after Task 1. Run `cd frontend && npx vitest run src/demo/lib/dataset-selectors.test.ts` after Task 2.
</verification>

<success_criteria>
`dataset-selectors.ts` exists with 3 pure functions matching the D-11 individual-array-params style. `dataset-selectors.test.ts` passes 11 tests proving the application_id join, the no-orphan-reference structural check, and all of DATA-SEED-01 through DATA-SEED-06 against Plan 14-01's real seed.ts fixtures.
</success_criteria>

<output>
Create `.planning/phases/14-mock-dataset-worldstate/14-02-SUMMARY.md` when done
</output>
