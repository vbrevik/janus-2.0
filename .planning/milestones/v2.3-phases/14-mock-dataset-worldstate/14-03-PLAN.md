---
phase: 14-mock-dataset-worldstate
plan: 3
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/demo/lib/model.ts
  - frontend/src/demo/store/world-state.tsx
  - frontend/src/demo/store/world-state.test.tsx
autonomous: true
requirements: [DATA-SEED-04, DATA-SEED-05, DATA-SEED-06]

must_haves:
  truths:
    - "WorldState carries a `datasets: { nodes, grants, delegates, auditLog }` sub-object populated eagerly from seed.ts's DATASET_NODES/DATASET_GRANTS/DATASET_DELEGATES inside seedWorld() -- mirrors the zones/grants/delegates eager-array pattern, NOT the digitalResources empty-then-fetch pattern (D-10)"
    - "ISSUE_DATASET_GRANT calls canIssueDatasetGrant as the reducer case's first statement and returns `state` completely unchanged (reference-equal) when it returns false -- no DatasetAccessGrant and no DatasetAuditEntry are created on a gate-failing call (SPEC R7, T-14-01)"
    - "a permitted ISSUE_DATASET_GRANT call (actorOrgId === dataset.admin_org_id) appends exactly one new DatasetAccessGrant to state.datasets.grants AND exactly one new DatasetAuditEntry to state.datasets.auditLog, with the audit entry's actor_person_id/actor_org_id/dataset_id/person_id/level fields matching the action payload exactly (SPEC R7, D-06)"
    - "DatasetAuditEntry carries BOTH seq (shares WorldState.seq's numbering) AND timestamp (the explicit `now` passed on the action payload, never new Date()/Date.now() read internally by the reducer) (D-07)"
  artifacts:
    - "frontend/src/demo/lib/model.ts -- new DatasetAuditEntry interface, appended to the Phase 13 Dataset-domain section"
    - "frontend/src/demo/store/world-state.tsx -- WorldState.datasets field, seedWorld() population, new ISSUE_DATASET_GRANT Action variant + reducer case"
    - "frontend/src/demo/store/world-state.test.tsx -- new 'ISSUE_DATASET_GRANT action' describe block"
  key_links:
    - "the reducer's ISSUE_DATASET_GRANT case passes state.datasets.grants and state.datasets.delegates (not digitalResources.*) into canIssueDatasetGrant -- the delegate-cap check operates purely within the dataset sub-object"
    - "seedWorld()'s datasets field is populated directly from Plan 14-01's DATASET_NODES/DATASET_GRANTS/DATASET_DELEGATES seed.ts exports via the array-spread convention -- no lazy/fetch-populated placeholder"
---

<objective>
Give Plan 14-01's fixtures a live home in `WorldState` (mirroring the eager `zones`/`grants`/`delegates` seeding pattern, not the backend-fetch `digitalResources` pattern), and add the one minimal write-path action SPEC.md's Requirement 7 locks in: `ISSUE_DATASET_GRANT`, gated by Phase 13's `canIssueDatasetGrant` as its first statement, producing a new `DatasetAuditEntry` on success and touching nothing on failure.

Purpose: Phase 15's live UI needs a real `WorldState.datasets` sub-object to render against, and needs a working, gated issuing action to demo `canIssueDatasetGrant`'s enforcement live -- not just as a pure function proven in Phase 13's unit tests.
Output: `model.ts` gains `DatasetAuditEntry`; `world-state.tsx` gains the `datasets` field, its `seedWorld()` population, and the `ISSUE_DATASET_GRANT` action + reducer case; `world-state.test.tsx` proves both SPEC R7 acceptance rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mock-dataset-worldstate/14-SPEC.md
@.planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md
@.planning/phases/14-mock-dataset-worldstate/14-RESEARCH.md

# Source of truth -- Plan 14-01's seed fixtures this plan wires in, and the exact files this plan modifies
@frontend/src/demo/lib/seed.ts
@frontend/src/demo/lib/model.ts
@frontend/src/demo/store/world-state.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model.ts DatasetAuditEntry + world-state.tsx datasets wiring + ISSUE_DATASET_GRANT reducer</name>
  <files>frontend/src/demo/lib/model.ts, frontend/src/demo/store/world-state.tsx</files>
  <read_first>
    - frontend/src/demo/lib/model.ts:1740-1743 (end of file -- exact append point for the new interface, right after canIssueDatasetGrant's closing brace)
    - frontend/src/demo/lib/model.ts:564-571 (AttrEvent -- the pattern, not literal shape, DatasetAuditEntry mirrors per D-05)
    - frontend/src/demo/store/world-state.tsx:76-147 (WorldState interface + seedWorld() -- the `zones`/`grants`/`delegates` eager-array fields at 92-96 / 126-131 to mirror; the `digitalResources` empty-then-fetch pattern at 98 / 132-145 to explicitly NOT mirror)
    - frontend/src/demo/store/world-state.tsx:149-197 (Action union -- exact append point, alongside UPSERT_RESOURCE_GRANT/UPSERT_RESOURCE_DELEGATE at 196-197)
    - frontend/src/demo/store/world-state.tsx:344-360 (WITHDRAW_AUTHORIZATION_ACTION -- the conditional-no-op idiom) and :509-522 (UPSERT_RESOURCE_GRANT -- the append-shape idiom) -- ISSUE_DATASET_GRANT combines both
    - .planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md decisions D-05, D-06, D-07, D-10
  </read_first>
  <behavior>
    - `actorOrgId === dataset.admin_org_id` (e.g. "MILITARY_1" issuing on ds-archive-caserecords) -> reducer appends a new DatasetAccessGrant + DatasetAuditEntry, `state.seq` increments by 1
    - `actorOrgId` not the dataset's admin_org and not an active delegate (e.g. "INTEL" on ds-archive-caserecords, whose admin_org_id is "MILITARY_1") -> reducer returns the SAME `state` object (reference-equal), zero new grants, zero new audit entries, `seq` unchanged
    - `datasetId` referencing a non-existent dataset -> reducer returns `state` unchanged (defensive guard; `canIssueDatasetGrant` requires a resolved `DatasetNode` argument, so a lookup miss must short-circuit before calling it)
  </behavior>
  <action>
    Append `export interface DatasetAuditEntry { seq: number; timestamp: Date; actor_person_id: string; actor_org_id: string; dataset_id: string; person_id: string; level: string; }` to model.ts immediately after `canIssueDatasetGrant`'s closing brace (line 1743), under a `// --- Phase 14: DatasetAuditEntry (mock audit-log entry for issueDatasetGrant) ---` comment. Field names/semantics per D-06 (`actor_person_id`/`actor_org_id` match `canIssueDatasetGrant`'s own parameter names) and D-07 (`seq` shares `WorldState.seq`'s numbering; `timestamp` is the explicit `now` argument, never internally generated).
    In `world-state.tsx`: import `type DatasetNode, type DatasetAccessGrant, type DatasetAccessDelegate, type DatasetAuditEntry, canIssueDatasetGrant` from "../lib/model", and `DATASET_NODES, DATASET_GRANTS, DATASET_DELEGATES` from "../lib/seed". Add a `datasets: { nodes: DatasetNode[]; grants: DatasetAccessGrant[]; delegates: DatasetAccessDelegate[]; auditLog: DatasetAuditEntry[] };` field to the `WorldState` interface (after the existing `digitalResources: DigitalResourceWorld;` field). In `seedWorld()`, add `datasets: { nodes: [...DATASET_NODES], grants: [...DATASET_GRANTS], delegates: [...DATASET_DELEGATES], auditLog: [] },` using the SAME eager-array-spread convention as `zones`/`grants`/`delegates` (NOT the `digitalResources` empty-then-fetch pattern -- datasets are pure frontend mock this phase, per D-10). Add `| { type: "ISSUE_DATASET_GRANT"; actorOrgId: string; actorPersonId: string; datasetId: string; personId: string; level: string; now: Date }` to the `Action` union, alongside the existing `UPSERT_RESOURCE_GRANT`/`UPSERT_RESOURCE_DELEGATE` variants. Add a new `case "ISSUE_DATASET_GRANT":` to the `reducer()` switch: look up `const dataset = state.datasets.nodes.find(d => d.id === action.datasetId)`; if `!dataset`, `return state;` unchanged; otherwise call `canIssueDatasetGrant(action.actorOrgId, action.actorPersonId, dataset, action.level, state.datasets.grants, state.datasets.delegates, action.now)` as the case body's FIRST substantive statement; if it returns `false`, `return state;` unchanged (SPEC R7 -- silent refusal, no grant, no audit); if `true`, construct a new `DatasetAccessGrant` (fresh `id` templated from `action.personId`/`action.datasetId`/`state.seq + 1`, `person_id: action.personId`, `dataset_id: action.datasetId`, `level: action.level`, `valid_from: null`, `valid_until: null`) and a new `DatasetAuditEntry` (`seq: state.seq + 1`, `timestamp: action.now`, `actor_person_id: action.actorPersonId`, `actor_org_id: action.actorOrgId`, `dataset_id: action.datasetId`, `person_id: action.personId`, `level: action.level`), then return a new state object with `datasets: { ...state.datasets, grants: [...state.datasets.grants, grant], auditLog: [...state.datasets.auditLog, auditEntry] }` and `seq: state.seq + 1` (append-only spread, matching `UPSERT_RESOURCE_GRANT`'s shape -- never `.push()`).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc -b --noEmit` reports zero errors. Reading `frontend/src/demo/store/world-state.tsx`'s `ISSUE_DATASET_GRANT` case confirms `canIssueDatasetGrant(...)` is called and its result checked BEFORE any `DatasetAccessGrant`/`DatasetAuditEntry` object is constructed. `seedWorld()`'s `datasets` field uses `[...DATASET_NODES]` / `[...DATASET_GRANTS]` / `[...DATASET_DELEGATES]` spreads, not an empty-array placeholder.
  </acceptance_criteria>
  <done>`DatasetAuditEntry` exists in model.ts; `WorldState.datasets` is populated eagerly by `seedWorld()`; `ISSUE_DATASET_GRANT` is gated by `canIssueDatasetGrant` as its first statement and silently refuses (no grant, no audit) on failure, per SPEC R7.</done>
</task>

<task type="auto">
  <name>Task 2: world-state.test.tsx -- ISSUE_DATASET_GRANT reducer tests</name>
  <files>frontend/src/demo/store/world-state.test.tsx</files>
  <read_first>
    - frontend/src/demo/store/world-state.test.tsx:1-23, 246-283 (file imports + UPSERT_RESOURCE_GRANT describe block -- the exact structural convention to mirror for the new describe block)
    - frontend/src/demo/lib/seed.ts (Plan 14-01's finished output -- confirms `ds-archive-caserecords`' admin_org_id is `MILITARY_1` and which people already hold grants there)
    - frontend/src/demo/store/world-state.tsx (this plan's Task 1 output -- exact ISSUE_DATASET_GRANT action field names and reducer behavior)
    - .planning/phases/14-mock-dataset-worldstate/14-SPEC.md Requirement 7 acceptance criteria
  </read_first>
  <action>
    Add a new `describe("ISSUE_DATASET_GRANT action", ...)` block to `world-state.test.tsx`, after the existing `UPSERT_RESOURCE_GRANT / UPSERT_RESOURCE_DELEGATE actions` block. Define `const NOW = new Date("2026-07-01T12:00:00Z")` locally within this describe block. Test 1 ("creates a grant and an audit entry when the actor is the dataset's admin_org"): call `seedWorld()`, capture `before = state.datasets.grants.length`, dispatch `{ type: "ISSUE_DATASET_GRANT", actorOrgId: "MILITARY_1", actorPersonId: "subj-1", datasetId: "ds-archive-caserecords", personId: "subj-2", level: "READER", now: NOW }`, assert `next.datasets.grants.length === before + 1`, `next.datasets.auditLog.length === 1`, and `next.datasets.auditLog[0]` matches `{ actor_person_id: "subj-1", actor_org_id: "MILITARY_1", dataset_id: "ds-archive-caserecords", person_id: "subj-2", level: "READER", timestamp: NOW }` via `toMatchObject`. Test 2 ("creates neither a grant nor an audit entry when canIssueDatasetGrant returns false"): dispatch `{ type: "ISSUE_DATASET_GRANT", actorOrgId: "INTEL", actorPersonId: "subj-3", datasetId: "ds-archive-caserecords", personId: "subj-2", level: "READER", now: NOW }` (INTEL is not `ds-archive-caserecords`' admin_org "MILITARY_1", and subj-3 is not a delegate -- DATASET_DELEGATES is empty), assert `next.datasets.grants.length === state.datasets.grants.length`, `next.datasets.auditLog.length === 0`, and `expect(next).toBe(state)` (reference-equal -- the reducer returned `state` unchanged, not a fresh clone).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/demo/store/world-state.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run src/demo/store/world-state.test.tsx` passes with 16 tests, 0 failures (the pre-phase baseline of 14 plus these 2 new tests). The gate-failing test asserts `next` is REFERENCE-EQUAL to the pre-dispatch `state` (`toBe`, not `toEqual`).
  </acceptance_criteria>
  <done>`world-state.test.tsx` has a passing `ISSUE_DATASET_GRANT action` describe block proving both SPEC R7 acceptance rows: permitted issuance creates grant+audit, gate-failing issuance creates neither.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `frontend/src/demo/lib/seed.ts` (additive, Plan 14-01): `DATASET_NODES`, `DATASET_GRANTS`, `DATASET_DELEGATES` (new exported constants); 3 new additive entries in `RESOURCE_GRANTS`; new `phase14DatasetSubjects` const merged into `SUBJECTS` (new Subject id `ds-deny-subj`)
- `frontend/src/demo/lib/model.ts` (additive, Plan 14-03): new `DatasetAuditEntry` interface
- `frontend/src/demo/lib/dataset-selectors.ts` (new file, Plan 14-02): `datasetsForApplication`, `activeDatasetGrantsForPerson`, `resolveDatasetAt`
- `frontend/src/demo/lib/dataset-selectors.test.ts` (new file, Plan 14-02)
- `frontend/src/demo/store/world-state.tsx` (additive, Plan 14-03): `WorldState.datasets` field; new `ISSUE_DATASET_GRANT` `Action` variant + reducer case
- `frontend/src/demo/store/world-state.test.tsx` (additive, Plan 14-03): new `"ISSUE_DATASET_GRANT action"` describe block

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future Phase 15 issuing-form UI -> `ISSUE_DATASET_GRANT` action dispatch | Untrusted caller input (actorOrgId/actorPersonId/datasetId/personId/level) crossing into the one write path this phase introduces |
| `ISSUE_DATASET_GRANT` reducer -> `state.datasets.grants`/`auditLog` | The only mutation boundary in this phase -- must be impossible to cross without `canIssueDatasetGrant` returning `true` first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | `ISSUE_DATASET_GRANT` reducer case | high | mitigate | `canIssueDatasetGrant` is called as the case body's first substantive statement; on `false` the reducer `return state;` unchanged (no grant, no audit) -- Task 1's action and Task 2's "creates neither" test (reference-equality assertion) are the implementation and the verification |
| T-14-04 | Repudiation | `DatasetAuditEntry` / audit-log richness for the new issuance event | low | accept | Canon-dropped per SPEC.md's Prohibitions section ("owned by code-review/`/gsd-secure-phase`, not minted here") -- `DatasetAuditEntry` captures actor/dataset/person/level/seq/timestamp per D-06/D-07, which is the locked scope; deeper audit-log hardening (tamper-evidence, retention) is out of this phase's scope |

</threat_model>

<verification>
Run `cd frontend && npx tsc -b --noEmit` after Task 1. Run `cd frontend && npx vitest run src/demo/store/world-state.test.tsx` after Task 2.
</verification>

<success_criteria>
`WorldState.datasets` is populated eagerly by `seedWorld()` from Plan 14-01's fixtures. `ISSUE_DATASET_GRANT` is gated by `canIssueDatasetGrant` as its first statement, silently refusing (no grant, no audit) on failure. `world-state.test.tsx` passes 16 tests (14 pre-existing + 2 new), proving both SPEC R7 acceptance rows.
</success_criteria>

<output>
Create `.planning/phases/14-mock-dataset-worldstate/14-03-SUMMARY.md` when done
</output>
