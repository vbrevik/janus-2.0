---
phase: 14-mock-dataset-worldstate
plan: 4
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - frontend/src/demo/lib/dataset-selectors.test.ts
  - frontend/src/demo/store/world-state.test.tsx
autonomous: true
requirements: [DATA-SEED-01, DATA-SEED-02, DATA-SEED-03, DATA-SEED-04, DATA-SEED-05, DATA-SEED-06]

must_haves:
  truths:
    - "npm run test reports 0 failures across the ENTIRE suite -- the pre-phase 300-test whole-repo baseline grows but never shrinks or changes an existing assertion"
    - "every row in SPEC.md's Acceptance Criteria (13 checkboxes), Edge Coverage (3 covered rows), and Prohibitions (2 resolved rows) tables has a directly corresponding passing test somewhere in the phase's test files"
    - "git diff --stat (scoped to the whole phase, against the phase's starting commit) touches ONLY frontend/src/demo/lib/seed.ts, frontend/src/demo/lib/model.ts, frontend/src/demo/lib/dataset-selectors.ts, frontend/src/demo/lib/dataset-selectors.test.ts, frontend/src/demo/store/world-state.tsx, and frontend/src/demo/store/world-state.test.tsx -- no other file in the repository is modified"
  artifacts: []
  key_links:
    - "Confirms the read path (14-02's dataset-selectors.ts/test.ts) and the write path (14-03's world-state.tsx/test.tsx) compose correctly against the SAME seed.ts fixtures (14-01) with no interaction contradictions"
---

<objective>
Close out Phase 14 with a full-suite regression run and a row-by-row audit of SPEC.md's Acceptance Criteria, Edge Coverage, and Prohibitions tables against the tests Plans 14-01/14-02/14-03 actually produced -- appending any missing assertion found, and confirming no file outside the phase's declared scope was touched anywhere across the phase.

Purpose: Plans 14-02 and 14-03 ran in parallel (Wave 2) against the same Wave-1 fixtures; this plan is the single point that proves their combined output is correct and complete, and that v2.2's existing digital-resource/dataset/world-state test suites remain green and unmodified as SPEC.md requires.
Output: `npm run test` and `npm run build` are green for the whole repository; every SPEC.md acceptance/edge/prohibition row has a passing corresponding test; `git diff --stat` confirms the phase touched exactly 6 files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mock-dataset-worldstate/14-SPEC.md

# Source of truth -- the test files this plan audits (Plan 14-01/02/03's finished output)
@frontend/src/demo/lib/dataset-selectors.test.ts
@frontend/src/demo/store/world-state.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression sweep and phase-level acceptance-criteria audit</name>
  <files>frontend/src/demo/lib/dataset-selectors.test.ts, frontend/src/demo/store/world-state.test.tsx</files>
  <read_first>
    - .planning/phases/14-mock-dataset-worldstate/14-SPEC.md -- full "Acceptance Criteria" list (13 checkboxes), "Edge Coverage" table (3 covered rows), "Prohibitions" table (2 resolved rows)
    - frontend/src/demo/lib/dataset-selectors.test.ts (Plan 14-02's finished output, read in full)
    - frontend/src/demo/store/world-state.test.tsx (Plan 14-03's finished output's new describe block, read in full)
  </read_first>
  <behavior>N/A -- this is a verification/completeness task; it audits existing coverage and appends only rows found missing, without restructuring any prior describe block.</behavior>
  <action>
    Walk SPEC.md's 13 Acceptance Criteria checkboxes, the 3 "covered" Edge Coverage rows (R5/DATA-SEED-06's sole-deciding-gate proof, and R6's two-part orphan-reference + selector-join checks), and the 2 resolved Prohibitions rows, confirming each has a directly corresponding passing assertion somewhere across `dataset-selectors.test.ts` (Plan 14-02) and `world-state.test.tsx` (Plan 14-03's new describe block). If any row lacks a direct test, APPEND the missing assertion to the most relevant existing `describe` block in the corresponding file -- do not restructure or rename any prior block. Run the full frontend test suite (`cd frontend && npm run test`) and confirm the pre-phase whole-repo baseline of 300 passing tests is present and green, with the new tests from Plans 14-02/14-03 additionally passing (net growth only). Run `cd frontend && npm run build` and confirm zero TypeScript errors. Confirm via `git diff --stat` (scoped to this phase's commits, against the phase's starting commit) that only `frontend/src/demo/lib/seed.ts`, `frontend/src/demo/lib/model.ts`, `frontend/src/demo/lib/dataset-selectors.ts`, `frontend/src/demo/lib/dataset-selectors.test.ts`, `frontend/src/demo/store/world-state.tsx`, and `frontend/src/demo/store/world-state.test.tsx` were modified across the entire phase -- no other file (including any v2.2 digital-resource file, or Phase 13's `dataset.test.ts`) was touched.
  </action>
  <verify>
    <automated>cd frontend && npm run test</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npm run test` reports 0 failures across the ENTIRE suite, with the total passing-test count >= 300 (the confirmed pre-phase baseline) plus this phase's additions. `cd frontend && npm run build` reports zero TypeScript errors. `git diff --stat` (against the phase's starting commit) shows exactly the 6 files listed above and no others.
  </acceptance_criteria>
  <done>Every SPEC.md Acceptance Criteria, applicable Edge Coverage, and Prohibitions row has a passing corresponding test. `npm run test` and the TypeScript build are green for the whole repository. No file outside the 6 files touched across the whole phase (`seed.ts`, `model.ts`, `dataset-selectors.ts`, `dataset-selectors.test.ts`, `world-state.tsx`, `world-state.test.tsx`) was modified anywhere in Phase 14.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `frontend/src/demo/lib/seed.ts` (additive, Plan 14-01): `DATASET_NODES`, `DATASET_GRANTS`, `DATASET_DELEGATES` (new exported constants); 3 new additive entries in `RESOURCE_GRANTS`; new `phase14DatasetSubjects` const merged into `SUBJECTS` (new Subject id `ds-deny-subj`)
- `frontend/src/demo/lib/model.ts` (additive, Plan 14-03): new `DatasetAuditEntry` interface
- `frontend/src/demo/lib/dataset-selectors.ts` (new file, Plan 14-02): `datasetsForApplication`, `activeDatasetGrantsForPerson`, `resolveDatasetAt`
- `frontend/src/demo/lib/dataset-selectors.test.ts` (new file, Plan 14-02; this plan may append missing assertions only)
- `frontend/src/demo/store/world-state.tsx` (additive, Plan 14-03): `WorldState.datasets` field; new `ISSUE_DATASET_GRANT` `Action` variant + reducer case
- `frontend/src/demo/store/world-state.test.tsx` (additive, Plan 14-03; this plan may append missing assertions only)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-wide regression boundary | The entire pre-existing v2.2/Phase-13 test surface (300 tests) must remain green and unmodified after this phase's additive changes -- this plan is the single gate confirming that boundary held |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | `ISSUE_DATASET_GRANT` reducer (Plan 14-03) | high | mitigate | Re-verified here: `npm run test` includes Plan 14-03's "creates neither" reference-equality test in the full-suite run, confirming the gate-bypass mitigation holds in the fully-assembled codebase, not just in isolation |
| T-14-06 | Tampering | Full-suite regression (v2.2/Phase 13 tests silently weakened or deleted) | medium | mitigate | This plan's `git diff --stat` check plus the explicit "count only grows" assertion on the 300-test baseline is the structural guard against silent test removal/weakening |

</threat_model>

<verification>
Run `cd frontend && npm run test` and `cd frontend && npm run build`. Run `git diff --stat` against the phase's starting commit.
</verification>

<success_criteria>
`npm run test` passes with 0 failures across the whole repository (>= 300 tests, net growth only). `npm run build` reports zero TypeScript errors. Every SPEC.md Acceptance Criteria, Edge Coverage, and Prohibitions row has a passing test. `git diff --stat` for the whole phase shows exactly 6 modified/created files.
</success_criteria>

<output>
Create `.planning/phases/14-mock-dataset-worldstate/14-04-SUMMARY.md` when done
</output>
