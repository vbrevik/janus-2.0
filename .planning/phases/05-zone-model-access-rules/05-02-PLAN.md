---
phase: 05-zone-model-access-rules
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/demo/lib/physical-access.test.ts
autonomous: true
requirements: [ZONE-03, ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04]

must_haves:
  truths:
    - "The new 5-tier clearance ranks are asserted (RESTRICTED===1, CONFIDENTIAL===2)"
    - "The ZONE-03 ceiling rule is proven for SECURED-at-SITE/AREA (false) and SECURED-at-BUILDING (true)"
    - "CONTROLLED access is proven for both grant and no-grant branches"
    - "RESTRICTED access is proven across all 4 branches (no-grant, sufficient clearance, escort path, insufficient+no-escort)"
    - "SECURED access is proven for >=3 branches including escorted-but-under-cleared DENY"
    - "Tree helpers getAncestors (parent-first order) and getDescendants (transitive) are proven"
    - "The full Vitest suite passes with the new tests added"
  artifacts:
    - path: "frontend/src/demo/lib/physical-access.test.ts"
      provides: "Vitest unit tests for all Phase 5 functions + clearance rank assertions"
      min_lines: 80
      contains: "describe(\"evaluateSecuredAccess\""
  key_links:
    - from: "frontend/src/demo/lib/physical-access.test.ts"
      to: "frontend/src/demo/lib/model.ts"
      via: "named imports of functions + ZoneNode type"
      pattern: "from \"\\./model\""
---

<objective>
Create `frontend/src/demo/lib/physical-access.test.ts` — a Vitest unit-test file covering every Phase 5 addition from Plan 01: the updated `CLEARANCE_RANK` values, `isValidZoneTypeCombination`, the three access-rule functions (CONTROLLED/RESTRICTED/SECURED), and the two tree-traversal helpers (getAncestors/getDescendants). Tests use inline fixtures only (no seed.ts imports), following the canonical `policy.test.ts` style.

Purpose: Prove the model layer behaves exactly per SPEC before any downstream phase builds on it. The escort-semantics distinction (RESTRICTED allows escort as an alternate path; SECURED does not) is the highest-value behavior to lock down with tests.

Output: New test file with per-function `describe` blocks (≥2 `it` cases each, ≥4 for evaluateRestrictedAccess, ≥3 for evaluateSecuredAccess). The full Vitest run passes including the new tests; project still type-checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-zone-model-access-rules/05-SPEC.md
@.planning/phases/05-zone-model-access-rules/05-CONTEXT.md
@.planning/phases/05-zone-model-access-rules/05-PATTERNS.md

<interfaces>
<!-- Contracts created by Plan 01 (model.ts) that this test file imports. Use directly. -->
From frontend/src/demo/lib/model.ts (after Plan 01):
  export const CLEARANCE_RANK: Record<Clearance, number>; // RESTRICTED:1, CONFIDENTIAL:2, SECRET:3, TOP_SECRET:4
  export interface ZoneNode { id; name; level: ZoneLevel; zone_type: ZoneType; parent_id: string | null; admin_org_id; asset_owner_org_id; requires_explicit_auth; }
  export interface ZoneAccessResult { allow: boolean; gate: ZoneAccessGate; reason: ZoneAccessReason; detail?: string; }
  export function isValidZoneTypeCombination(level: ZoneLevel, zone_type: ZoneType): boolean;
  export function evaluateControlledAccess(hasGrant: boolean): ZoneAccessResult;       // GRANT_FOUND / NO_GRANT
  export function evaluateRestrictedAccess(hasGrant: boolean, clearance: Clearance, hasValidEscort: boolean): ZoneAccessResult;
  export function evaluateSecuredAccess(hasGrant: boolean, clearance: Clearance, isEscorted: boolean): ZoneAccessResult;
  export function getAncestors(zoneId: string, allZones: ZoneNode[]): ZoneNode[];      // parent-first: [direct-parent, ..., root]
  export function getDescendants(zoneId: string, allZones: ZoneNode[]): ZoneNode[];    // all transitive descendants

Canonical test analog (read for style): frontend/src/demo/lib/policy.test.ts
  - header comment line, `import { describe, it, expect } from "vitest"`, inline fixtures, no seed.ts import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author physical-access.test.ts covering clearance ranks, ceiling rule, and the three access-rule functions</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <behavior>
    - CLEARANCE_RANK: expect(CLEARANCE_RANK["RESTRICTED"]).toBe(1); expect(CLEARANCE_RANK["CONFIDENTIAL"]).toBe(2).
    - isValidZoneTypeCombination: ("SITE","SECURED")→false; ("AREA","SECURED")→false; ("BUILDING","SECURED")→true; ("SITE","CONTROLLED")→true.
    - evaluateControlledAccess: hasGrant=true → allow:true, reason:"GRANT_FOUND", gate:"ZONE_TYPE_RULE"; hasGrant=false → allow:false, reason:"NO_GRANT".
    - evaluateRestrictedAccess (4 branches): (false,"RESTRICTED",false)→DENY NO_GRANT; (true,"RESTRICTED",false)→ALLOW GRANT_FOUND; (true,"UNCLASSIFIED",true)→ALLOW GRANT_FOUND (escort path); (true,"UNCLASSIFIED",false)→DENY INSUFFICIENT_CLEARANCE.
    - evaluateSecuredAccess (≥3 branches): (false,"TOP_SECRET",false)→DENY NO_GRANT; (true,"SECRET",false)→ALLOW GRANT_FOUND with detail containing "entry log mandatory"; (true,"CONFIDENTIAL",false)→DENY INSUFFICIENT_CLEARANCE; (true,"CONFIDENTIAL",true)→DENY INSUFFICIENT_CLEARANCE (escort does NOT unlock SECURED — proves T-05-01).
  </behavior>
  <read_first>
    - frontend/src/demo/lib/policy.test.ts (canonical style: header, vitest import, inline fixtures, describe/it/expect)
    - frontend/src/demo/lib/model.ts (the functions and CLEARANCE_RANK under test, post-Plan-01)
    - .planning/phases/05-zone-model-access-rules/05-SPEC.md (requirements 1,6,7,8,9 acceptance criteria — exact expected values)
    - .planning/phases/05-zone-model-access-rules/05-PATTERNS.md (test file header, import block, describe/it patterns, CLEARANCE_RANK assertion block)
  </read_first>
  <action>
    Create `frontend/src/demo/lib/physical-access.test.ts` with a module-level header comment (`// demo/lib/physical-access.test.ts — Vitest unit tests for Phase 5 zone access rules.`). Import `{ describe, it, expect } from "vitest"` and the functions + `CLEARANCE_RANK` + `type ZoneNode` from `"./model"` (named imports only; no `./seed` import). Write one `describe` block per concern with the `it` cases enumerated in <behavior>:
    (a) `describe("CLEARANCE_RANK — 5-tier ladder")` asserting RESTRICTED===1 and CONFIDENTIAL===2.
    (b) `describe("isValidZoneTypeCombination")` with the 4 SPEC cases.
    (c) `describe("evaluateControlledAccess")` with the 2 branches; assert .allow, .reason, and .gate.
    (d) `describe("evaluateRestrictedAccess")` with all 4 branches; assert .allow and .reason on each.
    (e) `describe("evaluateSecuredAccess")` with the ≥4 branches above; for the SECRET ALLOW case assert .detail contains "entry log mandatory"; explicitly include the escorted-but-CONFIDENTIAL DENY case and comment that escort does not substitute for clearance in SECURED.
    Do NOT modify model.ts; if a test reveals a behavior mismatch, the model is the source of truth per SPEC — flag it rather than editing the test to mask it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/demo/lib/physical-access.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/demo/lib/physical-access.test.ts` exists with a module-level header comment and imports only from "./model" and "vitest".
    - `npx vitest run src/demo/lib/physical-access.test.ts` passes with all listed cases green.
    - The escorted-but-under-cleared SECURED case `evaluateSecuredAccess(true,"CONFIDENTIAL",true)` is asserted to DENY with reason INSUFFICIENT_CLEARANCE.
    - evaluateRestrictedAccess has ≥4 `it` cases; evaluateSecuredAccess has ≥3 `it` cases.
    - No import from `./seed` appears in the file.
  </acceptance_criteria>
  <done>The test file proves clearance ranks, the ZONE-03 ceiling rule, and all three access-rule functions (including the SECURED escort-does-not-unlock case) and passes in isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tree-traversal helper tests and confirm the full suite + type-check pass</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <behavior>
    - Fixture: a small zone tree — root SITE (parent_id null), a BUILDING under it, a ROOM under the building, plus a sibling BUILDING under the SITE.
    - getAncestors(roomId, tree) → [building, site] in parent-first order (direct parent first, root last); first element is the room's direct parent, last is the root SITE.
    - getAncestors(rootSiteId, tree) → [] (root has no ancestors).
    - getDescendants(siteId, tree) → contains the building, the room, and the sibling building (all transitive descendants), length matches the non-root node count.
    - getDescendants(roomId, tree) → [] (leaf has no descendants).
  </behavior>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts (current state from Task 1 — append to it)
    - frontend/src/demo/lib/model.ts (getAncestors / getDescendants signatures and parent-first ordering note, post-Plan-01)
    - .planning/phases/05-zone-model-access-rules/05-CONTEXT.md (D-02 ordering: parent-first for getAncestors; transitive for getDescendants)
    - .planning/phases/05-zone-model-access-rules/05-PATTERNS.md (inline ZoneNode fixture pattern)
  </read_first>
  <action>
    Append two `describe` blocks to `physical-access.test.ts` for the tree helpers. Build a minimal inline `ZoneNode[]` fixture (no seed.ts): a root SITE with parent_id null, a BUILDING child, a ROOM grandchild, and a second BUILDING sibling — give each the required ZoneNode fields with placeholder org ids. Then:
    (a) `describe("getAncestors")` — assert the room's ancestors are [building, site] in that exact order (parent-first), and that the root SITE returns an empty array. Assert ordering by checking the id of element [0] is the direct parent and the last element is the root.
    (b) `describe("getDescendants")` — assert the SITE's descendants include the building, the room, and the sibling building (transitive), and that a leaf ROOM returns an empty array. Compare by id set membership and array length.
    After authoring, run the FULL suite from `frontend/`: `npx vitest run` (all tests including the existing 80 and the new file must pass) and `npx tsc -b --noEmit` (must exit 0).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getAncestors test asserts parent-first ordering (element [0] is the room's direct parent BUILDING; last element is the root SITE) and the root-returns-empty case.
    - getDescendants test asserts transitive membership (building + room + sibling building) and the leaf-returns-empty case.
    - `npx vitest run` passes the entire suite (existing 80 + the new physical-access tests) with zero failures.
    - `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>Tree helpers are proven for ordering and transitivity; the full Vitest suite and project type-check both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → model behavior | Tests are the executable contract proving the access-rule logic; a weak/missing test leaves an allow/deny bug undetected before Phases 6-8 build on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | evaluateSecuredAccess test coverage | mitigate | Explicit `evaluateSecuredAccess(true,"CONFIDENTIAL",true)` → DENY case proves escort cannot unlock a SECURED zone for an under-cleared person (locks down T-05-01 from Plan 01). |
| T-05-07 | Elevation of Privilege | evaluateRestrictedAccess test coverage | mitigate | All 4 branches asserted, including no-grant DENY and the escort alternate-path ALLOW, preventing OR-precedence regressions (T-05-02). |
| T-05-08 | Tampering | clearance rank assertion | mitigate | Tests assert RESTRICTED===1 and CONFIDENTIAL===2, catching any future accidental rank shift in CLEARANCE_RANK. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface introduced. |
</threat_model>

<verification>
- `cd frontend && npx vitest run` passes the full suite (existing 80 + new physical-access tests), zero failures.
- `cd frontend && npx tsc -b --noEmit` exits 0.
- physical-access.test.ts imports only from "./model" and "vitest" (no "./seed").
- evaluateRestrictedAccess has ≥4 it-cases; evaluateSecuredAccess has ≥3 it-cases including the escorted-under-cleared DENY.
</verification>

<success_criteria>
- physical-access.test.ts covers CLEARANCE_RANK assertions, isValidZoneTypeCombination (4 cases), all three access-rule functions (CONTROLLED 2 / RESTRICTED 4 / SECURED ≥3 branches), and both tree helpers.
- Escort semantics distinction is explicitly proven (RESTRICTED escort → ALLOW; SECURED escort under-cleared → DENY).
- Full Vitest suite passes; project type-checks clean.
</success_criteria>

<output>
Create `.planning/phases/05-zone-model-access-rules/05-02-SUMMARY.md` when done. Record the final total Vitest test count (baseline 80 + new cases).
</output>
