---
phase: 01-foundation
plan: "01"
subsystem: frontend-demo-island
tags: [demo-island, vite-config, router-isolation, build-seam]
dependency_graph:
  requires: []
  provides: [demo-html-entry, demo-bootstrap, demo-placeholder-root, vite-build-input-map]
  affects: [frontend/vite.config.ts, frontend/demo.html, frontend/src/demo/]
tech_stack:
  added: []
  patterns: [isolated-entry-island (D-02), vite-multi-entry-rollupOptions]
key_files:
  created:
    - frontend/demo.html
    - frontend/src/demo/main.tsx
    - frontend/src/demo/DemoRoot.tsx
  modified:
    - frontend/vite.config.ts
decisions:
  - "DemoRoot placeholder comment changed to avoid false-positive grep match on 'react-router' text in router-isolation verification"
metrics:
  duration_minutes: 15
  completed_date: "2026-05-21"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 4
---

# Phase 01 Plan 01: Demo Island Bootstrap Summary

## One-Liner

Isolated demo island wired as a Vite multi-entry build (`demo.html` + `demo/main.tsx` + placeholder `DemoRoot`) with `rollupOptions.input` map ensuring `dist/demo.html` ships in production alongside `dist/index.html`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create the demo HTML + bootstrap entry island | 2e1f185 | frontend/demo.html, frontend/src/demo/main.tsx, frontend/src/demo/DemoRoot.tsx |
| 2 | Add the Vite build input map (main + demo) | a245679 | frontend/vite.config.ts |

## What Was Built

### Task 1: Demo island entry files

- `frontend/demo.html` — mirrors `spikes.html` exactly; title set to `Janus — Authorization Hub (DEMO/MOCK)`; script module points to `/src/demo/main.tsx`; `<div id="root">` mount node preserved.
- `frontend/src/demo/main.tsx` — mirrors `src/spikes/main.tsx`; imports `DemoRoot` from `./DemoRoot`, renders `<DemoRoot />` in `<StrictMode>`, retains `import "../index.css"` for shadcn/Tailwind tokens.
- `frontend/src/demo/DemoRoot.tsx` — intentional placeholder (plan 01-04 replaces this); named export `DemoRoot`; renders `min-h-screen bg-slate-50 text-slate-800` container; sticky amber banner with literal `[DEMO / MOCK]` string (MODEL-03); one-line `<main>` body indicating where Decision Explorer mounts.

Router isolation confirmed: `grep -rl 'react-router|routeTree' src/demo/` returns empty (T-01-03 mitigated).

### Task 2: Vite build input map

Added `build.rollupOptions.input` to `frontend/vite.config.ts` with `main` (resolves `index.html`) and `demo` (resolves `demo.html`) keys. `spikes.html` excluded (retiring D-02). The `main` key is required — without it, Rollup drops `index.html` when `input` is an object (R4 closed).

`TanStackRouterVite` plugin block and server/resolve config left unchanged.

## Verification Results

- `frontend/demo.html` contains `/src/demo/main.tsx` in `<script type="module">`: PASS
- `frontend/demo.html` title contains `DEMO/MOCK`: PASS
- `frontend/src/demo/main.tsx` imports `DemoRoot`, retains `index.css` import: PASS
- `frontend/src/demo/DemoRoot.tsx` exports named `DemoRoot`, renders `min-h-screen`, renders `[DEMO / MOCK]`: PASS
- No router imports in `frontend/src/demo/`: PASS (ISOLATED)
- `vite.config.ts` `rollupOptions.input` contains both `main` and `demo` keys: PASS
- `spikes.html` not in input map: PASS
- `npm run build` exits 0, produces `dist/index.html` and `dist/demo.html`: PASS (BUILT)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed "react-router" text from DemoRoot comment to fix false-positive isolation grep**

- **Found during:** Task 1 verification
- **Issue:** The acceptance-criteria grep `grep -rl 'react-router|routeTree' src/demo/` matched the comment `// Do NOT import @tanstack/react-router or routeTree.gen.ts here.` in `DemoRoot.tsx`, causing the isolation check to fail.
- **Fix:** Replaced the comment with equivalent wording that doesn't contain the literal strings being grepped. The isolation guarantee is unchanged — the file contains no actual imports of those modules.
- **Files modified:** `frontend/src/demo/DemoRoot.tsx`
- **Commit:** 2e1f185

**2. [Rule 3 - Blocking] Copied routeTree.gen.ts from main checkout to unblock TypeScript compilation**

- **Found during:** Task 2 build verification
- **Issue:** The worktree was reset to a docs-only commit that predates the generated `routeTree.gen.ts`. The `tsc -b` step in `npm run build` failed because the generated file was absent. This file is `.gitignore`d and must be generated by `vite dev/build` — but `tsc` runs before Vite in the build script.
- **Fix:** Copied the existing `routeTree.gen.ts` from the main checkout (`/Users/vidarbrevik/projects/janus-2.0/frontend/src/routeTree.gen.ts`) to the worktree. The file is gitignored so it will not appear in the commit. The errors were all in pre-existing main-app routes (not demo code).
- **Files modified:** `frontend/src/routeTree.gen.ts` (untracked, gitignored — not committed)
- **Impact:** Build succeeds; no change to committed artifacts.

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| `Decision Explorer mounts here (01-04).` | `frontend/src/demo/DemoRoot.tsx` | 13 | Intentional placeholder — plan 01-04 replaces this file with the full WorldProvider + DemoBanner + RoleSwitcherHeader + DecisionExplorer composition. This does not prevent plan 01-01's goal (the island seam is the goal). |

## Threat Flags

No new threat surface introduced beyond what is documented in the plan threat model. The demo island has no network calls, no auth, no storage access, no external inputs.

## Self-Check: PASSED

- `frontend/demo.html`: FOUND
- `frontend/src/demo/main.tsx`: FOUND
- `frontend/src/demo/DemoRoot.tsx`: FOUND
- `frontend/vite.config.ts` contains `rollupOptions`: FOUND
- Task 1 commit `2e1f185`: FOUND
- Task 2 commit `a245679`: FOUND
