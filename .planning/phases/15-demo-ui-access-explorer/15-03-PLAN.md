---
phase: 15-demo-ui-access-explorer
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - frontend/src/demo/components/datasets-panel.tsx
  - frontend/src/demo/DemoRoot.tsx
autonomous: false

requirements: [DATA-UI-01, DATA-UI-02, DATA-UI-03, DATA-UI-04]

must_haves:
  truths:
    - "Selecting rsrc-milapp-1 (MilApp-1) in the Datasets tab shows its 4 seeded datasets (2 mailboxes, 1 archive role, 1 document site); selecting rsrc-intapp-1 (IntelApp-1) shows its 1 document site; selecting rsrc-tacapp-1 (TacApp-1, zero datasets) shows the explicit empty-state message, never a blank panel."
    - "A dataset spanning 2+ application_ids would appear once per Application it is linked to (no dedup) — structural, generic-loop correctness (no such fixture exists yet this phase, per SPEC's explicit deferred-fixture boundary)."
    - "The Datasets tab performs its own independent useDigitalResourcesWorld fetch — the Application picker works whether or not the Digital Resources tab was ever visited first."
    - "Before any dataset is selected, the explorer and reverse-lookup sections do not render at all (no placeholder-prompt state)."
    - "The explorer/reverse-lookup subtree is wrapped in an ErrorBoundary keyed by the selected dataset's id, so switching datasets always self-recovers from any prior caught error."
    - "All 3 documented deny-matrix scenarios on ds-archive-caserecords render DENY with the single correct failing gate; at least 2 full-chain-success scenarios render ALLOW."
    - "A non-admin login sees only the issuing-form block message, never the '+ Issue new grant' toggle; an admin login can issue a grant that visibly changes the reverse-lookup list and flips a previously-DENY explorer verdict to ALLOW."
    - "Full v2.2 golden-fixture + Phase 13/14 regression suite (314+ tests) stays green; npx tsc -b --noEmit reports zero errors."
  artifacts:
    - "frontend/src/demo/components/datasets-panel.tsx — exports DatasetsPanel; internal ApplicationRow, DatasetRow, DATASET_TYPE_TONE"
    - "frontend/src/demo/DemoRoot.tsx — ActiveView union gains \"datasets\"; new nav button; new <main> branch rendering <DatasetsPanel />"
  key_links:
    - "DatasetsPanel's own useDigitalResourcesWorld(hasToken) + SET_DIGITAL_RESOURCES dispatch on query.isSuccess — mirrors digital-resources-panel.tsx verbatim so the Application picker is populated regardless of tab-visit order (D-04)"
    - "<ErrorBoundary key={selectedDataset.id}> wraps ONLY <DatasetAccessExplorer> + <DatasetReverseLookup> — never the Application/Datasets picker above it (D-12)"
    - "ApplicationRow is a new lightweight Application-only component, not a reuse of resource-browser.tsx's full Network→Platform→Application tree (D-01), rendered as a flat clickable row list — same Pill+name style as ResourceTreeNodeRow but with no expand/collapse chevron (D-02); the orchestrating component is named datasets-panel.tsx, mirroring digital-resources-panel.tsx's naming convention (D-03)"
    - "The Application picker + Datasets list use a grid-cols-3 side-by-side layout matching resource-browser.tsx's tree-left/detail-right precedent (D-06); the explorer and reverse-lookup render together as a single stacked view below, never as inner sub-tabs (D-05)"
  prohibitions:
    - statement: "MUST NOT let the issuing action succeed for a non-admin/non-delegate identity, even if the UI-level isAdmin gate were bypassed."
      status: resolved
      verification: "existing world-state.test.tsx ISSUE_DATASET_GRANT denial test (unmodified, Plan 15-01) + this plan's live-UAT checkpoint confirming the non-admin block visually"
---

<objective>
Wire everything built in Waves 1-2 into a new top-level "Datasets" tab: `datasets-panel.tsx`
(named to match the tab label and mirror `digital-resources-panel.tsx`'s naming
convention, per D-03) orchestrates its own Digital Resources fetch, a flat
Application-only picker, a side-by-side grid Datasets list filtered by the selected
Application (Application list left / Datasets list right, matching `resource-browser.tsx`'s
`grid-cols-3` precedent, per D-06), and — once a dataset is picked — the explorer +
reverse-lookup render together as a single stacked view, never inner sub-tabs (per D-05),
wrapped in the `ErrorBoundary`. `DemoRoot.tsx` gets the new tab entry. Finishes with a
full regression sweep and a live-UAT checkpoint covering the non-admin block and the
full deny-matrix/reverse-lookup/issuing round trip.

Purpose: This is the phase's integration point — the goal ("a developer or stakeholder
can browse datasets nested within an Application, resolve dataset access ... and issue a
new grant") only becomes true once this plan lands.
Output: `datasets-panel.tsx`, edited `DemoRoot.tsx`, a green 314+ test suite, and a
confirmed live UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-demo-ui-access-explorer/15-CONTEXT.md
@.planning/phases/15-demo-ui-access-explorer/15-RESEARCH.md
@.planning/phases/15-demo-ui-access-explorer/15-SPEC.md
@.planning/phases/15-demo-ui-access-explorer/15-UI-SPEC.md
@.planning/phases/15-demo-ui-access-explorer/15-VALIDATION.md
@.planning/phases/15-demo-ui-access-explorer/15-01-SUMMARY.md
@.planning/phases/15-demo-ui-access-explorer/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: datasets-panel.tsx — DatasetsPanel orchestrator</name>
  <files>frontend/src/demo/components/datasets-panel.tsx</files>
  <read_first>
    frontend/src/demo/components/digital-resources-panel.tsx (whole file — the exact 6-state loader shell this component reuses verbatim, per D-04)
    frontend/src/demo/components/resource-browser.tsx (lines 43-105, ResourceTreeNodeRow — the row shell D-01/D-02's flat picker strips the chevron/children branch from; lines 253-323, the grid-cols-3 tree-left/detail-right layout precedent)
    frontend/src/demo/lib/dataset-selectors.ts (datasetsForApplication signature)
    frontend/src/demo/lib/model.ts (DatasetType, DatasetNode, ApplicationNode)
    frontend/src/demo/components/ui.tsx (Card, Pill, ErrorBoundary — from Plan 15-01)
  </read_first>
  <action>
    Create frontend/src/demo/components/datasets-panel.tsx. Import `useEffect, useState` from "react"; `Loader2` from "lucide-react"; `hasStoredToken, useDigitalResourcesWorld, classifyLoaderState, type LoaderState` from "../hooks/use-digital-resources"; `useWorld, useWorldDispatch` from "../store/world-state"; `datasetsForApplication` from "../lib/dataset-selectors"; `type ApplicationNode, type DatasetNode, type DatasetType` from "../lib/model"; `Card, ErrorBoundary, Pill` from "./ui"; `DatasetAccessExplorer` from "./dataset-access-explorer"; `DatasetReverseLookup` from "./dataset-reverse-lookup".

    Define module-local `const DATASET_TYPE_TONE: Record<DatasetType, "blue" | "amber" | "slate"> = { MAILBOX: "blue", ARCHIVE_ROLE: "amber", DOCUMENT_SITE: "slate" };` (deliberately avoids "green", reserved for the Application tier's own TIER_TONE, and avoids "red", reserved for DENY).

    Define module-local `function ApplicationRow({ app, selected, onSelect }: { app: ApplicationNode; selected: boolean; onSelect: (id: string) => void })`: a `button` with `className={\`flex w-full items-center gap-2 rounded px-2 py-1 text-left text-sm ${selected ? "bg-slate-200" : "hover:bg-slate-100"}\`}`, `onClick={() => onSelect(app.id)}`, containing `<Pill tone="green">APPLICATION</Pill>` and a `<span className={\`text-sm${selected ? " font-semibold text-slate-900" : ""}\`}>{app.name}</span>` — no chevron/expand affordance (D-02, only one tier to pick from).

    Define module-local `function DatasetRow({ dataset, selected, onSelect }: { dataset: DatasetNode; selected: boolean; onSelect: (id: string) => void })`: same button shell as ApplicationRow, containing `<Pill tone={DATASET_TYPE_TONE[dataset.dataset_type]}>{dataset.dataset_type}</Pill>` (the literal uppercase type string, no title-casing) and the dataset's `name`.

    Export `function DatasetsPanel()`. `const hasToken = hasStoredToken(); const query = useDigitalResourcesWorld(hasToken); const dispatch = useWorldDispatch(); const world = useWorld(); const state: LoaderState = classifyLoaderState(hasToken, query); const [selectedApplicationId, setSelectedApplicationId] = useState<string | null>(null); const [selectedDatasetId, setSelectedDatasetId] = useState<string | null>(null);`. Add `useEffect(() => { if (query.isSuccess && query.data) dispatch({ type: "SET_DIGITAL_RESOURCES", world: query.data }); }, [query.isSuccess, query.data, dispatch]);`. Reproduce the 5 non-success branches (`missing-token`, `loading`, `unauthorized`, `error`, `empty`) VERBATIM from `digital-resources-panel.tsx` (same JSX, same copy, same `Loader2` spinner, same `query.refetch()` Retry button on `error`) — do not alter any of that copy.

    For `state === "success"`: compute `const datasets = selectedApplicationId ? datasetsForApplication(world.datasets.nodes, selectedApplicationId) : [];` and `const selectedDataset = selectedDatasetId ? (datasets.find(d => d.id === selectedDatasetId) ?? null) : null;`. Render a `<div className="space-y-4">` containing: a `grid grid-cols-3 gap-4` with (1) `<Card title="Applications">` mapping `world.digitalResources.applications` to `<ApplicationRow key={app.id} app={app} selected={app.id === selectedApplicationId} onSelect={(id) => { setSelectedApplicationId(id); setSelectedDatasetId(null); }} />`; (2) `<div className="col-span-2"><Card title="Datasets">...</Card></div>` where the Card body is: if `!selectedApplicationId`, `<p className="text-sm text-slate-400">Select an Application to see its datasets.</p>`; else if `datasets.length === 0`, `<p className="text-sm text-slate-400">No datasets for this Application.</p>` (verbatim Copywriting Contract string); else map `datasets` to `<DatasetRow key={d.id} dataset={d} selected={d.id === selectedDatasetId} onSelect={setSelectedDatasetId} />`. After the grid, `{selectedDataset && (<ErrorBoundary key={selectedDataset.id}><DatasetAccessExplorer dataset={selectedDataset} /><DatasetReverseLookup dataset={selectedDataset} /></ErrorBoundary>)}` (D-07: nothing renders here at all when no dataset is selected — no placeholder prompt).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc -b --noEmit` reports zero errors. `grep -c "export function DatasetsPanel" frontend/src/demo/components/datasets-panel.tsx` returns 1. The 5 non-success loader branches are byte-identical in copy to `digital-resources-panel.tsx`'s (diff the two files' JSX strings for "Not logged in.", "Loading digital resource data…", "Session invalid or expired.", "Could not load digital resource data.", "No digital resources found. Seed the database and refresh.").
  </acceptance_criteria>
  <done>DatasetsPanel performs its own independent fetch, renders the flat Application picker + Datasets list, and wraps the explorer/reverse-lookup in a dataset-keyed ErrorBoundary once a dataset is selected.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DemoRoot.tsx + full regression sweep</name>
  <files>frontend/src/demo/DemoRoot.tsx</files>
  <read_first>
    frontend/src/demo/DemoRoot.tsx (whole file, 90 lines)
  </read_first>
  <action>
    In frontend/src/demo/DemoRoot.tsx: add `import { DatasetsPanel } from "./components/datasets-panel";` alongside the existing `DigitalResourcesPanel` import. Extend the `ActiveView` union (currently ending `| "digital-resources"`) with `| "datasets"`. Add a new nav button immediately after the existing "Digital Resources" button, same shell (`rounded px-3 py-1.5 text-sm ${activeView === "datasets" ? "bg-slate-800 text-white" : "border border-slate-300 text-slate-600 hover:bg-slate-50"}`, `onClick={() => setActiveView("datasets")}`), with the label text "Datasets". Add `{activeView === "datasets" && <DatasetsPanel />}` immediately after the existing `{activeView === "digital-resources" && <DigitalResourcesPanel />}` line inside `<main>`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run` reports 314+ tests passing, 0 failures, 21+ test files (20 pre-existing + the new `use-datasets.test.ts`). `npx tsc -b --noEmit` reports zero errors. `grep -c '"datasets"' frontend/src/demo/DemoRoot.tsx` returns at least 3 (union member, button condition, main-swap condition).
  </acceptance_criteria>
  <done>The Datasets tab is reachable from DemoRoot's nav row; the full existing test suite and type-check stay green after all Phase 15 wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — non-admin block + full deny-matrix/reverse-lookup/issuing round trip</name>
  <read_first>
    .planning/phases/15-demo-ui-access-explorer/15-SPEC.md (## Acceptance Criteria)
    .planning/phases/15-demo-ui-access-explorer/15-VALIDATION.md (## Manual-Only Verifications)
  </read_first>
  <what-built>
    A new "Datasets" tab: an Application-only picker + Datasets list, a dataset-level
    Access Resolution Explorer (person + required-level + datetime → 4-gate ALLOW/DENY
    trace), a reverse-lookup ("Who has access") list, and an admin-gated issuing form
    that dispatches a real (in-memory) DatasetAccessGrant with Valid-from/Valid-until
    dates.
  </what-built>
  <how-to-verify>
    1. Start the stack: `docker compose -f docker-compose.dev.yml up -d`, `cd backend && RUST_LOG=info cargo run`, `cd frontend && npm run dev`. Log in at the frontend (port 15510) as `admin` / `password123`, then open the demo module and click the new "Datasets" tab.
    2. Application picker + Datasets list (DATA-UI-01): select "MilApp-1" (rsrc-milapp-1) — confirm exactly 4 datasets appear (2 mailboxes, 1 archive role "Case Records Archive", 1 document site). Select "IntelApp-1" (rsrc-intapp-1) — confirm exactly 1 dataset ("Intel Fusion Document Site"). Select "TacApp-1" (rsrc-tacapp-1) — confirm the empty message "No datasets for this Application." renders (not a blank panel).
    3. Deny-matrix trace (DATA-UI-02): with MilApp-1 selected, click "Case Records Archive". In the explorer, set Person = "Lee Park", Required level = "READER" — confirm verdict DENY with ONLY the Clearance gate red (Application grant and Dataset grant both green). Set Person = "Sam Okafor", Required level = "CASE_HANDLER" — confirm verdict DENY with ONLY the Application grant gate red (Clearance and Dataset grant both green). Set Person = "Priya Nair", Required level = "READER" — confirm verdict DENY with ONLY the Dataset grant gate red (Clearance and Application grant both green).
    4. Allow cases: set Person = "Dana Reyes", Required level = "ADMIN" — confirm verdict ALLOW, all 4 gates green. Select a mailbox dataset (e.g. "Dana Reyes — Mailbox"), Person = "Dana Reyes", Required level = "FULL_ACCESS" — confirm verdict ALLOW, all 4 gates green.
    5. Reverse lookup (DATA-UI-03): back on "Case Records Archive," confirm the "Who has access" list shows exactly one row — "Dana Reyes" at level "ADMIN" (Sam and Lee and Priya are each correctly excluded per step 3's denials).
    6. Non-admin block (DATA-UI-04): log out, log back in as `viewer` / `password123` (or `operator`/`manager`). Return to the Datasets tab, select "Case Records Archive" — confirm the issuing area shows ONLY "Issuing controls require an admin login." and never the "+ Issue new grant" toggle.
    7. Admin issuing round trip (DATA-UI-04): log back in as `admin`. On "Case Records Archive," expand "+ Issue new grant." Set Person = "Priya Nair", Level = "READER", Valid from = today, leave Valid until blank. Click "Issue grant" — confirm the button reads "Issuing…" then returns to idle with no error banner and the form collapses/resets. Confirm "Priya Nair" now appears in the "Who has access" list at level "READER" (she did not appear in step 5). Set the explorer's Person = "Priya Nair", Required level = "READER" — confirm the verdict has flipped from DENY (step 3) to ALLOW.
    8. Confirm `cd frontend && npx vitest run` still reports 314+ passing and `npx tsc -b --noEmit` is clean (already verified automated in Task 2 — re-confirm no regressions from manual testing artifacts).
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 steps confirm, or describe which step failed and what was observed instead.</resume-signal>
  <files>none — manual verification only, no files modified by this task</files>
  <action>No code changes. Follow the 8 numbered steps in &lt;how-to-verify&gt; above against the running stack (frontend :15510, backend :15520, Postgres :15530) built by Tasks 1-2.</action>
  <verify>
    <human-check>All 8 steps in &lt;how-to-verify&gt; confirmed as described; report any deviation instead of approving.</human-check>
  </verify>
  <acceptance_criteria>
    Tester responds "approved" only after independently confirming each of the 8 numbered steps in &lt;how-to-verify&gt; against the running stack — not from reading the plan text alone.
  </acceptance_criteria>
  <done>All 8 live-UAT steps pass: dataset browsing + empty state, all 3 deny-matrix scenarios plus 2 allow scenarios render the correct verdict/gate, reverse-lookup matches exactly, non-admin is blocked, admin issuing round-trip visibly flips a DENY to ALLOW, and the automated suite/type-check remain green.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan (excluded from drift verification):

- `frontend/src/demo/components/datasets-panel.tsx` (new file) — exports `function DatasetsPanel()`; module-local `DATASET_TYPE_TONE`, `function ApplicationRow({ app, selected, onSelect })`, `function DatasetRow({ dataset, selected, onSelect })`
- `frontend/src/demo/DemoRoot.tsx` — `ActiveView` union gains `"datasets"`; new nav button; new `<main>` branch `{activeView === "datasets" && <DatasetsPanel />}`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser devtools console → in-memory reducer | Unchanged — this plan only assembles Wave 1/2's already-gated pieces |
| Non-admin browser session → issuing UI | A logged-in non-admin user's browser renders the Datasets tab; the issuing form must never appear actionable to them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | `DatasetsPanel` → `IssueDatasetGrantSection` → reducer | high | mitigate | Confirmed end-to-end in this plan's live-UAT checkpoint (step 6): non-admin sees only the block message; authoritative enforcement remains the Plan 15-01 reducer gate |
| T-15-02 | Denial of Service (client-side) | `DatasetsPanel`'s `<ErrorBoundary key={selectedDataset.id}>` | medium | mitigate | Scoped to exactly the explorer+reverse-lookup subtree; the Application/Datasets picker above it stays interactive even if a resolver throw occurs; remounts fresh on every dataset switch |

</threat_model>

<verification>
- `cd frontend && npx vitest run` → 314+ passing, 0 failures.
- `cd frontend && npx tsc -b --noEmit` → 0 errors.
- Live UAT checkpoint (Task 3) approved.
</verification>

<success_criteria>
The Datasets tab is live and reachable; all 4 DATA-UI requirements are demonstrated working end-to-end via the checkpoint; the full existing regression suite plus this phase's new tests stay green; the non-admin enforcement gap is confirmed closed both by automated test and live UAT.
</success_criteria>

<output>
Create `.planning/phases/15-demo-ui-access-explorer/15-03-SUMMARY.md` when done
</output>
