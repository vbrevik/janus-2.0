---
phase: 15-demo-ui-access-explorer
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/demo/components/dataset-access-explorer.tsx
  - frontend/src/demo/components/dataset-reverse-lookup.tsx
autonomous: true
requirements: [DATA-UI-02, DATA-UI-03, DATA-UI-04]

must_haves:
  truths:
    - "Dataset explorer renders all 4 gate-chain entries (CLEARANCE, APP_GRANT_OR, DATASET_GRANT, VISIBILITY) unconditionally, ALLOW or DENY, styled consistently with ResourceResolutionTrace (rounded-lg border p-4, green/red verdict background, text-lg font-semibold verdict line)."
    - "The explorer's Required-level field is always populated from the selected dataset's own dataset_type vocabulary (never a free-text or bare-literal value), defaulting to the vocabulary's lowest entry whenever the dataset changes."
    - "Reverse-lookup shows exactly one row per person at their combined effective level (never one row per raw grant record), computed by calling resolveDatasetAt per person — not a separately computed shortcut."
    - "Zero active grants on a dataset renders the explicit empty message 'No one currently has access to this dataset.', never a blank panel."
    - "The admin-gated issuing form is gated on getStoredUserRole() === admin; a non-admin sees only the block message, never the '+ Issue new grant' toggle."
  artifacts:
    - "frontend/src/demo/components/dataset-access-explorer.tsx — exports DatasetAccessExplorer; internal DatasetResolutionTrace, GATE_LABEL, IssueDatasetGrantSection, levelVocabularyFor"
    - "frontend/src/demo/components/dataset-reverse-lookup.tsx — exports DatasetReverseLookup; internal lowestLevelFor, effectiveLevelFor"
  key_links:
    - "IssueDatasetGrantSection's submit passes actorOrgId: dataset.admin_org_id (always — the only reachable authority path from this UI is the admin_org exemption, since there is no demo concept of 'logged in as a specific delegate Subject')"
    - "Both components call the SAME resolveDatasetAt/effectiveRankedLevel/effectiveArchiveCoverage functions Phase 13's resolver uses internally — never a re-derived shortcut (SPEC R3's explicit requirement)"
  prohibitions:
    - statement: "MUST NOT let the issuing action succeed for a non-admin/non-delegate identity, even if the UI-level isAdmin gate were bypassed."
      status: resolved
      verification: "the UI-level gate here is defense-in-depth only (early-return before any dispatch); the authoritative enforcement is the reducer's canIssueDatasetGrant call, built and tested in Plan 15-01"
---

<objective>
Build the dataset-tier Access Resolution Explorer (person + dataset + required-level +
datetime → 4-gate ALLOW/DENY trace, plus the admin-gated issuing form docked directly
under it, per D-08) and the standalone Reverse-Lookup view (dataset → every person with
active access + effective level). Both are new sibling components, not extensions of
the v2.2 Digital Resources components. The issuing form uses the same collapsed-by-default
"+ Issue new grant" toggle as v2.2's `IssueGrantSection` (per D-09).

Purpose: These are the two content pieces DATA-UI-02/03/04 require; Wave 3's
`DatasetsPanel` only needs to import and wire them (plus a dataset selection), it builds
none of the resolution/lookup logic itself.
Output: `dataset-access-explorer.tsx` (DatasetAccessExplorer), `dataset-reverse-lookup.tsx`
(DatasetReverseLookup).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-demo-ui-access-explorer/15-CONTEXT.md
@.planning/phases/15-demo-ui-access-explorer/15-RESEARCH.md
@.planning/phases/15-demo-ui-access-explorer/15-SPEC.md
@.planning/phases/15-demo-ui-access-explorer/15-UI-SPEC.md
@.planning/phases/15-demo-ui-access-explorer/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dataset-access-explorer.tsx — DatasetAccessExplorer + issuing form</name>
  <files>frontend/src/demo/components/dataset-access-explorer.tsx</files>
  <read_first>
    frontend/src/demo/components/resource-access-explorer.tsx (whole file — ResourceResolutionTrace at lines 52-104 is the exact visual pattern to mirror; IssueGrantSection at lines 112-242 is the exact field-shape/toggle/reset/submit pattern to mirror)
    frontend/src/demo/lib/model.ts (lines 1195-1204 DatasetType/MailboxLevel/DocumentSiteLevel/ArchiveRole; 1208-1217 MAILBOX_LEVELS/DOCUMENT_SITE_LEVELS; 1291-1299 DatasetNode; 1351-1365 effectiveDatasetClassification; 1494-1503 DatasetAccessResult; 1679-1743 canIssueDatasetGrant)
    frontend/src/demo/lib/dataset-selectors.ts (resolveDatasetAt signature, lines 48-75)
    frontend/src/demo/hooks/use-datasets.ts (from Plan 15-01 — useIssueDatasetGrant, IssueDatasetGrantVariables)
    frontend/src/demo/hooks/use-digital-resources.ts (getStoredUserRole, lines 46-62)
    frontend/src/demo/components/access-resolution-explorer.tsx (lines 24-32, CLEARANCE_TONE export)
    frontend/src/demo/components/ui.tsx (Card, Field, Pill, Select, MockTag)
  </read_first>
  <action>
    Create frontend/src/demo/components/dataset-access-explorer.tsx. Import `useMemo, useState` from "react"; `resolveDatasetAt` from "../lib/dataset-selectors"; `effectiveDatasetClassification, MAILBOX_LEVELS, DOCUMENT_SITE_LEVELS, assertNeverDatasetType, type DatasetNode, type DatasetType, type DatasetAccessResult` from "../lib/model"; `useWorld` from "../store/world-state"; `useIssueDatasetGrant, type IssueDatasetGrantVariables` from "../hooks/use-datasets"; `getStoredUserRole` from "../hooks/use-digital-resources"; `CLEARANCE_TONE` from "./access-resolution-explorer"; `Card, Field, MockTag, Pill, Select` from "./ui".

    Define module-local `const GATE_LABEL: Record<string, string> = { CLEARANCE: "Clearance", APP_GRANT_OR: "Application grant", DATASET_GRANT: "Dataset grant", VISIBILITY: "Visibility" };` (per-file copy — do not import from resource-access-explorer.tsx's own GATE_LABEL).

    Define module-local `function levelVocabularyFor(datasetType: DatasetType): readonly string[]` that switches on datasetType: `"MAILBOX"` returns MAILBOX_LEVELS, `"DOCUMENT_SITE"` returns DOCUMENT_SITE_LEVELS, `"ARCHIVE_ROLE"` returns the literal array `["READER", "CASE_HANDLER", "ADMIN"]` (low-to-high display order — model.ts's own ARCHIVE_ROLE_CONTAINS key order is not low-to-high, so this is a fresh, correctly-ordered literal, not an import), `default` calls `assertNeverDatasetType(datasetType)`.

    Define module-local `function DatasetResolutionTrace({ result }: { result: DatasetAccessResult })` exactly per 15-RESEARCH.md's "Code Examples" section (`DatasetResolutionTrace (mirrors ResourceResolutionTrace exactly, per DATA-UI-02)`): `rounded-lg border p-4` container toggling `border-green-200 bg-green-50` / `border-red-200 bg-red-50` on `result.allow`; a `text-lg font-semibold` verdict line "✓ ALLOW" / "✗ DENY"; an unconditional `<ul className="mt-3 space-y-1.5">` mapping over ALL of `result.gates` (never filtered/short-circuited) rendering each gate's pass/fail icon, `GATE_LABEL[g.kind] ?? \`Gate: ${g.kind}\`` label, and `g.reason`.

    Define module-local `function todayStr(): string { return new Date().toISOString().slice(0, 10); }`.

    Define module-local `function IssueDatasetGrantSection({ dataset }: { dataset: DatasetNode })`: `const world = useWorld(); const { mutate, isPending, isError } = useIssueDatasetGrant(); const isAdmin = getStoredUserRole() === "admin";` — if `!isAdmin`, return `<p className="text-xs text-slate-400">Issuing controls require an admin login.</p>` (verbatim string, matching IssueGrantSection exactly). Otherwise: `vocab = levelVocabularyFor(dataset.dataset_type)`, `defaultPersonId = world.subjects[0]?.id ?? ""`; state: `expanded` (bool), `formPersonId` (default defaultPersonId), `formLevel` (default `vocab[0]`), `validFrom` (default `todayStr()`), `validUntil` (default `""`), and `submitted` (bool, tracks an in-flight submit for the collapse-on-success effect below). `resetFields()` resets `formPersonId`/`formLevel`/`validFrom`/`validUntil` to their defaults. Add a `useEffect` keyed on `[submitted, isPending, isError]`: when `submitted` is true and `isPending` is false, set `submitted` back to false, and if `isError` is false additionally call `setExpanded(false)` and `resetFields()` (mirrors IssueGrantSection's reset-on-success/collapse behavior, adapted for this hook's non-awaitable `mutate`). The toggle button reads "+ Issue new grant" / "Cancel" exactly like IssueGrantSection. When expanded, render: `Field label="Person"` → `Select` of `world.subjects` (value/label = id/name); `Field label="Level"` → `Select` of `vocab.map(l => ({ value: l, label: l }))`; `Field label="Valid from"` → `input type="date"` bound to `validFrom`; `Field label="Valid until (optional)"` → `input type="date"` bound to `validUntil`, plus a `text-xs text-slate-400` paragraph "Leave blank for permanent grant."; a submit button (`rounded px-3 py-1.5 text-sm bg-slate-800 text-white disabled:opacity-50 disabled:cursor-not-allowed`, `disabled={isPending}`, text `isPending ? "Issuing…" : "Issue grant"`) whose `onClick` sets `submitted(true)` then calls `mutate({ actorOrgId: dataset.admin_org_id, actorPersonId: "ui-admin", datasetId: dataset.id, personId: formPersonId, level: formLevel, validFrom: new Date(validFrom), validUntil: validUntil ? new Date(validUntil) : null })`. Note on `actorOrgId`/`actorPersonId`: `canIssueDatasetGrant`'s admin_org path (model.ts:1691) checks ONLY `actorOrgId === dataset.admin_org_id` — it never inspects `actorPersonId` on that path — and this UI has no concept of "logged in as a specific delegate Subject," so `dataset.admin_org_id` is always passed (the only reachable authority path from an admin-role UI login) and `"ui-admin"` is a fixed sentinel documenting that provenance (the audit log's `actor_person_id` is write-only this phase — nothing renders it back). When `isError` is true, render `<div className="rounded bg-destructive/10 p-3 text-sm text-destructive mt-2">Not authorized to issue this grant.</div>` (verbatim Copywriting Contract string).

    Export `function DatasetAccessExplorer({ dataset }: { dataset: DatasetNode })`. `const world = useWorld();` state: `personId` (default `world.subjects[0]?.id ?? ""`), `requiredLevel` (default `levelVocabularyFor(dataset.dataset_type)[0]` — this initializer naturally re-runs per dataset because the parent wraps this component in `<ErrorBoundary key={dataset.id}>`, remounting it fresh on every dataset change per D-15), `timestampInput` (datetime-local string, default `useMemo(() => new Date(), []).toISOString().slice(0, 16)`). Compute `evalTime = useMemo(() => new Date(timestampInput), [timestampInput])`; `person = world.subjects.find(s => s.id === personId) ?? null`; `effectiveClass = effectiveDatasetClassification(dataset, world.digitalResources.applications, world.digitalResources.platforms)` (let this throw naturally — the parent's ErrorBoundary catches it, per Pitfall 2); `result = useMemo<DatasetAccessResult | null>(() => person ? resolveDatasetAt(world.datasets.nodes, world.digitalResources.applications, world.digitalResources.platforms, world.digitalResources.grants, world.datasets.grants, personId, person.clearance, dataset.id, requiredLevel, evalTime) : null, [world.datasets.nodes, world.digitalResources, personId, person, dataset, requiredLevel, evalTime])`. Render (per UI-SPEC's "must visually match ResourceResolutionTrace exactly", reusing Card/Field/Select/Pill/MockTag): intro `<p className="text-sm text-slate-500">` prose; a `grid gap-4 sm:grid-cols-2` with (1) a `Card` containing `Field label="Person"` Select of `world.subjects` and, when `person` is truthy, `Field label="Clearance"` showing `<Pill tone={CLEARANCE_TONE[person.clearance]}>{person.clearance}</Pill><MockTag />`; (2) a `Card` showing the dataset's `name` as a read-only label, `<Pill tone={CLEARANCE_TONE[effectiveClass]}>{effectiveClass}</Pill>`, and `Field label="Required level"` Select of `vocab.map(l => ({ value: l, label: l }))` bound to `requiredLevel`/`setRequiredLevel`; a `Card title="Evaluation Timestamp"` with `Field label="Evaluation timestamp"` `input type="datetime-local"` bound to `timestampInput`; `{result !== null && <DatasetResolutionTrace result={result} />}`; then `<IssueDatasetGrantSection dataset={dataset} />`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc -b --noEmit` reports zero errors. `grep -c "export function DatasetAccessExplorer" frontend/src/demo/components/dataset-access-explorer.tsx` returns 1. Manually inspecting the file confirms `DatasetResolutionTrace` renders all 4 entries of `result.gates` unconditionally (no `.filter`/`.slice` on the gates array before the `.map`).
  </acceptance_criteria>
  <done>DatasetAccessExplorer renders person/required-level/datetime selection, the full 4-gate trace, and the admin-gated issuing form docked directly beneath it; compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: dataset-reverse-lookup.tsx — DatasetReverseLookup</name>
  <files>frontend/src/demo/components/dataset-reverse-lookup.tsx</files>
  <read_first>
    frontend/src/demo/lib/model.ts (lines 1444-1488, effectiveRankedLevel + effectiveArchiveCoverage; line 822-ish isWindowActive; 1195-1217 the level vocabularies)
    frontend/src/demo/lib/dataset-selectors.ts (resolveDatasetAt signature)
    frontend/src/demo/components/resource-browser.tsx (lines 394-406, "Active grants" Card list-row styling precedent)
  </read_first>
  <action>
    Create frontend/src/demo/components/dataset-reverse-lookup.tsx. Import `useMemo` from "react"; `resolveDatasetAt` from "../lib/dataset-selectors"; `effectiveArchiveCoverage, effectiveRankedLevel, isWindowActive, MAILBOX_LEVELS, DOCUMENT_SITE_LEVELS, assertNeverDatasetType, type ArchiveRole, type DatasetAccessGrant, type DatasetNode` from "../lib/model"; `useWorld` from "../store/world-state"; `Card, Pill` from "./ui".

    Define module-local `function lowestLevelFor(datasetType: DatasetNode["dataset_type"]): string` switching: `"MAILBOX"` → `MAILBOX_LEVELS[0]`, `"DOCUMENT_SITE"` → `DOCUMENT_SITE_LEVELS[0]`, `"ARCHIVE_ROLE"` → the literal `"READER"`, `default` → `assertNeverDatasetType(datasetType)`. This is used ONLY to pick a requiredLevel that lets `resolveDatasetAt` answer "does this person have ANY active access at all" — since holding any active grant's coverage/rank always includes the lowest vocabulary entry, passing it as requiredLevel makes gate 3 pass iff the person has at least one active, in-vocabulary DatasetAccessGrant on this dataset.

    Define module-local `function effectiveLevelFor(dataset: DatasetNode, grants: DatasetAccessGrant[], personId: string, now: Date): string | null`: filter `grants` to `g.dataset_id === dataset.id && g.person_id === personId && isWindowActive(g.valid_from, g.valid_until, now)`; if the filtered array is empty return `null`; otherwise switch on `dataset.dataset_type`: `"MAILBOX"` → `effectiveRankedLevel(MAILBOX_LEVELS, filtered.map(g => g.level))`; `"DOCUMENT_SITE"` → `effectiveRankedLevel(DOCUMENT_SITE_LEVELS, filtered.map(g => g.level))`; `"ARCHIVE_ROLE"` → compute `effectiveArchiveCoverage(filtered.map(g => g.level as ArchiveRole))`, then walk the literal order `["READER", "CASE_HANDLER", "ADMIN"] as const` (a fresh module-local array, NOT imported from dataset-access-explorer.tsx — per-file-copy convention) tracking the last role present in the coverage Set, and return that (the maximal role in the coverage set by this order — for today's single-role-per-person fixtures this equals the literally-held role); `default` → `assertNeverDatasetType(dataset.dataset_type)`.

    Export `function DatasetReverseLookup({ dataset }: { dataset: DatasetNode })`. `const world = useWorld(); const now = useMemo(() => new Date(), []);` Compute `rows = useMemo(() => { const lowest = lowestLevelFor(dataset.dataset_type); return world.subjects.map(s => { const result = resolveDatasetAt(world.datasets.nodes, world.digitalResources.applications, world.digitalResources.platforms, world.digitalResources.grants, world.datasets.grants, s.id, s.clearance, dataset.id, lowest, now); if (!result.allow) return null; const level = effectiveLevelFor(dataset, world.datasets.grants, s.id, now); return { personId: s.id, personName: s.name, level }; }).filter((r): r is { personId: string; personName: string; level: string | null } => r !== null); }, [world.subjects, world.datasets.nodes, world.datasets.grants, world.digitalResources, dataset, now])`. Render `<Card title="Who has access">`: if `rows.length === 0`, `<p className="text-sm text-slate-400">No one currently has access to this dataset.</p>` (verbatim Copywriting Contract string); otherwise `<ul className="space-y-1">` mapping `rows` to `<li key={r.personId} className="flex items-center gap-2 text-sm"><span>{r.personName}</span>{r.level && <Pill tone="slate">{r.level}</Pill>}</li>` — list order is `world.subjects` array order, no new sort.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc -b --noEmit` reports zero errors. `grep -c "export function DatasetReverseLookup" frontend/src/demo/components/dataset-reverse-lookup.tsx` returns 1. `rows` is built by mapping `world.subjects` (not `world.datasets.grants` directly) confirming "one row per person, not one row per raw grant record."
  </acceptance_criteria>
  <done>DatasetReverseLookup lists every person for whom resolveDatasetAt(...).allow is true, one row per person, at their combined effective level, with an explicit zero-match empty state.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan (excluded from drift verification):

- `frontend/src/demo/components/dataset-access-explorer.tsx` (new file) — exports `function DatasetAccessExplorer({ dataset })`; module-local `GATE_LABEL`, `function levelVocabularyFor(datasetType)`, `function DatasetResolutionTrace({ result })`, `function IssueDatasetGrantSection({ dataset })`, `function todayStr()`
- `frontend/src/demo/components/dataset-reverse-lookup.tsx` (new file) — exports `function DatasetReverseLookup({ dataset })`; module-local `function lowestLevelFor(datasetType)`, `function effectiveLevelFor(dataset, grants, personId, now)`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser devtools console → in-memory reducer | Unchanged from Plan 15-01 — this plan adds no new dispatch surface beyond calling the already-gated useIssueDatasetGrant hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | IssueDatasetGrantSection's `isAdmin` early-return | high | mitigate | UI-level gate is defense-in-depth only; authoritative enforcement is Plan 15-01's reducer-level `canIssueDatasetGrant` check, which this component's `mutate()` call always routes through |
| T-15-03 | Tampering (data integrity) | "Required level" / "Level" Select options | low | mitigate | Both fields are populated exclusively from `levelVocabularyFor(dataset.dataset_type)` — never free text — so an out-of-vocabulary value can never reach `resolveDatasetAt`'s throw-on-bad-vocabulary path from this UI |

</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit` → 0 errors on both new files.
- Manual code inspection: `DatasetResolutionTrace` renders all 4 gates unconditionally; `DatasetReverseLookup` iterates `world.subjects`, not raw grants.
</verification>

<success_criteria>
Both new sibling components exist, compile cleanly, reuse the Phase 13/14 resolver and selectors verbatim (no re-derived access logic), and match the UI-SPEC's exact styling/copy contract.
</success_criteria>

<output>
Create `.planning/phases/15-demo-ui-access-explorer/15-02-SUMMARY.md` when done
</output>
