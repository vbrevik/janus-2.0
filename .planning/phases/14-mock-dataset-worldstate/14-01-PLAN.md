---
phase: 14-mock-dataset-worldstate
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/seed.ts
autonomous: true
requirements: [DATA-SEED-01, DATA-SEED-02, DATA-SEED-03, DATA-SEED-04, DATA-SEED-05, DATA-SEED-06]

must_haves:
  truths:
    - "seed.ts's DATASET_NODES includes 2 MAILBOX nodes (`ds-mailbox-dana`, `ds-mailbox-sam`), each with a single-element `application_ids` referencing `rsrc-milapp-1` (DATA-SEED-01)"
    - "DATASET_GRANTS gives subj-1 and subj-2 each one FULL_ACCESS grant on their own mailbox plus one READ grant on the other's mailbox -- 2 distinct mailbox-dataset grants per person (own + shared) (DATA-SEED-01)"
    - "seed.ts's DATASET_NODES includes exactly 1 ARCHIVE_ROLE dataset (`ds-archive-caserecords`) with DATASET_GRANTS distributing all three ArchiveRole values across 3 distinct people: READER to subj-3, CASE_HANDLER to subj-2, ADMIN to subj-1 (DATA-SEED-02)"
    - "seed.ts's DATASET_NODES includes 2 DOCUMENT_SITE datasets (`ds-docsite-ops` on rsrc-milapp-1, `ds-docsite-intel` on rsrc-intapp-1) with DATASET_GRANTS spanning 3 distinct DocumentSiteLevel values (CONTRIBUTE, READ, FULL_CONTROL) (DATA-SEED-03)"
    - "subj-1 (Dana) holds an active DatasetAccessGrant (ADMIN) on `ds-archive-caserecords`, layered on her PRE-EXISTING active ResourceAccessGrant on rsrc-milapp-1 (unmodified) -- the full prerequisite-chain-success fixture for DATA-SEED-04"
    - "the new `ds-deny-subj` fixture holds an active additive ResourceAccessGrant on rsrc-milapp-1 but ZERO DatasetAccessGrant records anywhere in DATASET_GRANTS -- the dataset-gate-denial fixture for DATA-SEED-05 (and deny-matrix case c)"
    - "subj-3 (Lee, CONFIDENTIAL) holds a new additive ACTIVE ResourceAccessGrant on rsrc-milapp-1 (effective classification SECRET) plus her existing active READER DatasetAccessGrant on `ds-archive-caserecords` -- isolates CLEARANCE as the sole failing gate for deny-matrix case (a), since her clearance rank (2) sits below milapp-1's classification rank (3) while the other two gates independently pass"
    - "subj-2 (Sam, TOP_SECRET) holds exactly ONE new additive ResourceAccessGrant on rsrc-milapp-1 and it is EXPIRED (valid_until 2026-05-01, before the phase's fixed NOW of 2026-07-01) -- isolates APP_GRANT_OR as the sole failing gate for deny-matrix case (b), since Sam's clearance passes and his CASE_HANDLER DatasetAccessGrant on `ds-archive-caserecords` remains nominally active"
    - "every new additive RESOURCE_GRANTS entry (subj-2, subj-3, ds-deny-subj) is APPENDED to the end of the existing array -- zero pre-existing RESOURCE_GRANTS or SUBJECTS entries are modified, reordered, or removed"
  artifacts:
    - "frontend/src/demo/lib/seed.ts -- DATASET_NODES (5), DATASET_GRANTS (10), DATASET_DELEGATES ([]) constants; 3 additive RESOURCE_GRANTS entries; new `phase14DatasetSubjects` Subject fixture (`ds-deny-subj`) merged into SUBJECTS"
  key_links:
    - "Every DatasetAccessGrant.dataset_id references an id present in this SAME plan's DATASET_NODES array -- no forward reference to a dataset defined elsewhere"
    - "Every DatasetNode.application_ids entry references rsrc-milapp-1 or rsrc-intapp-1 (existing APPLICATIONS entries) -- no new Application/Platform/Network fixture introduced anywhere in this plan, and no dataset spans both apps (they have different effective classifications -- SECRET vs TOP_SECRET -- which would make effectiveDatasetClassification's assert-all-share check throw)"
  prohibitions:
    - statement: "MUST NOT introduce fixture data (Person names, emails, org names) resembling a real, identifiable individual or organization"
      status: resolved
      verification: manual
    - statement: "MUST NOT correlate role/access-level assignment across new fixture people with a name-implied protected attribute"
      status: resolved
      verification: manual
---

<objective>
Extend `frontend/src/demo/lib/seed.ts` additively with the mock dataset content Phase 14 needs: one new denial-narrative Subject, 3 new additive `ResourceAccessGrant` entries on the existing cast (subj-2, subj-3), and the full dataset fixture set (2 mailboxes, 1 archive-role dataset, 2 document sites) plus their `DatasetAccessGrant` records -- all hand-designed so a SINGLE archive dataset (`ds-archive-caserecords`) doubles as the fixture for DATA-SEED-02, DATA-SEED-04, and all three DATA-SEED-06 deny-matrix cases simultaneously.

Purpose: This is the raw data layer every other Phase 14 plan (selectors, WorldState wiring, tests) consumes. Getting the person/grant/dataset combinations exactly right here is what makes each downstream gate-trace assertion (clearance-fails / app-grant-expired / dataset-grant-missing) land on the correct sole-deciding gate.
Output: `seed.ts` exports `DATASET_NODES`, `DATASET_GRANTS`, `DATASET_DELEGATES`, a new `ds-deny-subj` Subject in `SUBJECTS`, and 3 new entries in `RESOURCE_GRANTS` -- with zero existing seed.ts content modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mock-dataset-worldstate/14-SPEC.md
@.planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md
@.planning/phases/14-mock-dataset-worldstate/14-RESEARCH.md

# Source of truth -- current state of the file this plan extends additively
@frontend/src/demo/lib/seed.ts
@frontend/src/demo/lib/model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cast prep -- new denial-narrative Subject + additive Application-grant entries</name>
  <files>frontend/src/demo/lib/seed.ts</files>
  <read_first>
    - frontend/src/demo/lib/seed.ts:53-100 (BASE_SUBJECTS -- subj-1/2/3 clearance/unit fields and the exact one-line-comment-per-actor documentation convention to replicate)
    - frontend/src/demo/lib/seed.ts:789-816 (fw5Subjects + the "Merge all expansion records" SUBJECTS export -- the exact append point for a new Subject array)
    - frontend/src/demo/lib/seed.ts:1680-1826 (RESOURCE_GRANTS array -- the exact object shape `{id, person_id, resource_id, valid_from, valid_until}`, and the existing subj-1 entries for rsrc-milapp-1/rsrc-intapp-1 to append after)
    - frontend/src/demo/lib/model.ts:387-397 (UnitId union + UNITS -- confirms HOME_GUARD is a valid unit for the new Subject)
    - .planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md decisions D-01, D-02, D-03, D-04 and their "Claude's Discretion" bullets
  </read_first>
  <action>
    Append a new `const phase14DatasetSubjects: Subject[] = [...]` array directly after `fw5Subjects` (seed.ts:801), containing exactly ONE new Subject: id `ds-deny-subj`, name "Priya Nair", unit `HOME_GUARD`, clearance `SECRET`, domainAuth `{ COMPUTER: "STANDARD" }`, compartments `[]`, flags `{ revoked: false, securityHold: false }`. Add a one-line comment above it following the existing subj-1..4 documentation convention (e.g. "ds-deny-subj: Priya Nair -- dataset-denial narrative actor (v2.3 D-04): active Application grant, zero DatasetAccessGrants"). Add `...phase14DatasetSubjects,` to the `SUBJECTS` export array (seed.ts:807-816) immediately after `...fw5Subjects,`.
    Append exactly 3 new entries to the END of the existing `RESOURCE_GRANTS` array (before its closing `];`), under a new `// === Phase 14: additive Application-grant entries for dataset access stories (D-01) ===` comment: (1) id `rsrc-grant-subj2-milapp-expired`, person_id `subj-2`, resource_id `rsrc-milapp-1`, valid_from `null`, valid_until `new Date("2026-05-01T00:00:00Z")` -- Sam's ONLY grant on this Application, deliberately expired (deny-matrix case-b fixture); (2) id `rsrc-grant-subj3-milapp-active`, person_id `subj-3`, resource_id `rsrc-milapp-1`, valid_from `null`, valid_until `null` -- Lee's active grant (deny-matrix case-a fixture); (3) id `rsrc-grant-dsdenysubj-milapp-active`, person_id `ds-deny-subj`, resource_id `rsrc-milapp-1`, valid_from `null`, valid_until `null` -- the DATA-SEED-05 / deny-matrix-case-c fixture (active app grant, paired with zero DatasetAccessGrant records in Task 2). Do not alter, reorder, or remove any existing RESOURCE_GRANTS or SUBJECTS-composing entry -- additions only, appended at the end of each array.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx vitest run src/demo/lib/digital-resource.test.ts src/demo/lib/abac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc -b --noEmit` reports zero errors. `cd frontend && npx vitest run src/demo/lib/digital-resource.test.ts src/demo/lib/abac.test.ts` passes with 43 tests, 0 failures (the pre-phase baseline for these 2 files), confirming the additive RESOURCE_GRANTS/SUBJECTS changes did not regress v2.2's "RSRC-SEED-05: temporal variety" test or any abac.test.ts subject lookup. `git diff --numstat frontend/src/demo/lib/seed.ts` shows 0 deletions at this point in the plan (pure appends only).
  </acceptance_criteria>
  <done>`SUBJECTS` includes the new `ds-deny-subj` fixture and `RESOURCE_GRANTS` includes exactly 3 new additive entries (subj-2 expired, subj-3 active, ds-deny-subj active) on `rsrc-milapp-1`, with zero existing entries modified.</done>
</task>

<task type="auto">
  <name>Task 2: Dataset fixtures -- mailboxes, archive roles, document sites</name>
  <files>frontend/src/demo/lib/seed.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts:1283-1334 (DatasetNode/DatasetAccessGrant/DatasetAccessDelegate interfaces + validateDatasetNode -- exact field shapes to populate)
    - frontend/src/demo/lib/model.ts:1195-1227 (DatasetType, MailboxLevel/DocumentSiteLevel/ArchiveRole literal unions, MAILBOX_LEVELS/DOCUMENT_SITE_LEVELS/ARCHIVE_ROLE_CONTAINS -- exact vocabulary values to use for `level`)
    - frontend/src/demo/lib/seed.ts:1450-1598 (PLATFORMS + APPLICATIONS -- confirms rsrc-milapp-1 -> rsrc-milpl-1 (SECRET) and rsrc-intapp-1 -> rsrc-intpl-1 (TOP_SECRET), the ONLY two Applications this plan's datasets may reference per D-03)
    - frontend/src/demo/lib/seed.ts (this plan's Task 1 output -- the new RESOURCE_GRANTS entries and `ds-deny-subj` this task's DatasetAccessGrants must line up with; read the file after Task 1 completes, not this plan's action text)
    - .planning/phases/14-mock-dataset-worldstate/14-CONTEXT.md decisions D-01..D-04 and D-08/D-09 (why no Application spans both rsrc-milapp-1 and rsrc-intapp-1 in a single dataset -- effectiveDatasetClassification's assert-all-share-then-fail-loud would throw given their different classifications)
  </read_first>
  <action>
    Append, after the RSRC_DELEGATES array (end of file) under a new `// --- Phase 14: Mock dataset fixtures (v2.3) ---` header, three new exported arrays: `DATASET_NODES: DatasetNode[]`, `DATASET_GRANTS: DatasetAccessGrant[]`, `DATASET_DELEGATES: DatasetAccessDelegate[]`. Import `DatasetNode, DatasetAccessGrant, DatasetAccessDelegate` as new named types from "./model" at the top of seed.ts alongside the existing type imports.
    `DATASET_NODES` (5 entries, EVERY entry's `application_ids` is a single-element array referencing ONLY `rsrc-milapp-1` or `rsrc-intapp-1` -- never both in the same dataset): (1) id `ds-mailbox-dana`, name "Dana Reyes -- Mailbox", dataset_type `MAILBOX`, application_ids `["rsrc-milapp-1"]`, classification_override `null`, admin_org_id `MILITARY_1`, asset_owner_org_id `MILITARY_1`; (2) id `ds-mailbox-sam`, name "Sam Okafor -- Mailbox", dataset_type `MAILBOX`, application_ids `["rsrc-milapp-1"]`, classification_override `null`, admin_org_id `MILITARY_1`, asset_owner_org_id `MILITARY_2`; (3) id `ds-archive-caserecords`, name "Case Records Archive", dataset_type `ARCHIVE_ROLE`, application_ids `["rsrc-milapp-1"]`, classification_override `null`, admin_org_id `MILITARY_1`, asset_owner_org_id `MILITARY_1`; (4) id `ds-docsite-ops`, name "Operations Document Site", dataset_type `DOCUMENT_SITE`, application_ids `["rsrc-milapp-1"]`, classification_override `null`, admin_org_id `MILITARY_1`, asset_owner_org_id `MILITARY_1`; (5) id `ds-docsite-intel`, name "Intel Fusion Document Site", dataset_type `DOCUMENT_SITE`, application_ids `["rsrc-intapp-1"]`, classification_override `null`, admin_org_id `INTEL`, asset_owner_org_id `INTEL`.
    `DATASET_GRANTS` (10 entries, all `valid_from: null, valid_until: null` unless noted; every `level` value drawn from the exact vocabulary matching its dataset's `dataset_type`): on `ds-mailbox-dana` -- subj-1 `FULL_ACCESS` (own) + subj-2 `READ` (shared); on `ds-mailbox-sam` -- subj-2 `FULL_ACCESS` (own, reused as deny-matrix case-b's live dataset grant) + subj-1 `READ` (shared); on `ds-archive-caserecords` -- subj-1 `ADMIN` (DATA-SEED-04 success fixture), subj-2 `CASE_HANDLER` (deny-matrix case-b fixture), subj-3 `READER` (deny-matrix case-a fixture) -- deliberately give `ds-deny-subj` ZERO grants on this dataset (DATA-SEED-05 / deny-matrix case-c fixture); on `ds-docsite-ops` -- subj-1 `CONTRIBUTE` + subj-3 `READ`; on `ds-docsite-intel` -- subj-1 `FULL_CONTROL`. Each grant's `id` follows the pattern `ds-grant-<person>-<dataset-suffix>-<level-lowercase>` (e.g. `ds-grant-dana-archive-admin`).
    `DATASET_DELEGATES`: `[]` (empty -- no delegation-issuing scenario is in this phase's locked scope per SPEC.md's "Full CRUD reducer actions beyond issueDatasetGrant... Phase 15 adds the rest").
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc -b --noEmit` reports zero errors (confirms every `DatasetNode`/`DatasetAccessGrant` literal satisfies its interface and every `level` string is a member of its dataset_type's vocabulary). `DATASET_NODES.length === 5` and `DATASET_GRANTS.length === 10` (spot-checkable via a throwaway `node -e` count, or verified fully by Plan 14-02's structural test). Every `DATASET_NODES[].application_ids` entry is a single-element array containing either `"rsrc-milapp-1"` or `"rsrc-intapp-1"`.
  </acceptance_criteria>
  <done>`DATASET_NODES` (5), `DATASET_GRANTS` (10), and `DATASET_DELEGATES` ([]) are exported from seed.ts, fully populated per DATA-SEED-01..06's fixture requirements, ready for Plan 14-02's selectors/tests to consume.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `frontend/src/demo/lib/seed.ts` (additive, Plan 14-01): `DATASET_NODES`, `DATASET_GRANTS`, `DATASET_DELEGATES` (new exported constants); 3 new additive entries in `RESOURCE_GRANTS`; new `phase14DatasetSubjects` const merged into `SUBJECTS` (new Subject id `ds-deny-subj`)
- `frontend/src/demo/lib/model.ts` (additive, Plan 14-03): new `DatasetAuditEntry` interface
- `frontend/src/demo/lib/dataset-selectors.ts` (new file, Plan 14-02): `datasetsForApplication`, `activeDatasetGrantsForPerson`, `resolveDatasetAt`
- `frontend/src/demo/lib/dataset-selectors.test.ts` (new file, Plan 14-02)
- `frontend/src/demo/store/world-state.tsx` (additive, Plan 14-03): `WorldState.datasets` field; new `ISSUE_DATASET_GRANT` `Action` variant + reducer case
- `frontend/src/demo/store/world-state.test.tsx` (additive, Plan 14-03): new `"ISSUE_DATASET_GRANT action"` describe block

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture author -> `seed.ts` literal data | No external input crosses here -- purely hand-authored constants; the boundary of interest is "does this data resemble a real person/org" (a hygiene concern, not an injection concern) |
| This plan's new fixtures -> Plan 14-02's resolver calls | The new `DatasetNode`/`DatasetAccessGrant` records must be internally consistent (valid `application_ids`, in-vocabulary `level` values) so downstream gate resolution behaves as designed, not as an accidental side effect of malformed seed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Information Disclosure (soft) | New `ds-deny-subj` Subject fixture, new Person-adjacent data | low | accept | SPEC.md's Prohibitions section governs this via judgment/code-review spot-check (fictional name "Priya Nair", no resemblance to a real identifiable individual; role/clearance assignment not correlated with any name-implied protected attribute) -- no automated test exists for this class of check, consistent with SPEC.md's own disposition |
| T-14-02 | Denial of Service (fail-closed throw) | `DatasetNode.application_ids` referencing a non-existent Application | low | mitigate | Every new `DatasetNode` in this plan references exactly one of the two pre-verified existing Applications (`rsrc-milapp-1`, `rsrc-intapp-1`) -- Plan 14-02's structural test (R6, orphan-reference check) is the automated verification for this, run against this plan's output |

</threat_model>

<verification>
Run `cd frontend && npx tsc -b --noEmit` after each task. Run `cd frontend && npx vitest run src/demo/lib/digital-resource.test.ts src/demo/lib/abac.test.ts` after Task 1 to confirm the additive RESOURCE_GRANTS/SUBJECTS changes are regression-safe. Plan 14-02's tests are the full functional verification of this plan's fixture correctness (run in the next wave).
</verification>

<success_criteria>
`frontend/src/demo/lib/seed.ts` exports `DATASET_NODES` (5 entries), `DATASET_GRANTS` (10 entries), `DATASET_DELEGATES` ([]), a new `ds-deny-subj` Subject in `SUBJECTS`, and 3 new additive `RESOURCE_GRANTS` entries -- with zero existing seed.ts content modified, zero TypeScript errors, and the existing digital-resource/abac regression suites green.
</success_criteria>

<output>
Create `.planning/phases/14-mock-dataset-worldstate/14-01-SUMMARY.md` when done
</output>
