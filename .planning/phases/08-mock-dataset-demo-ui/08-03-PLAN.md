---
phase: 08-mock-dataset-demo-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/src/demo/components/access-resolution-explorer.tsx
  - frontend/src/demo/components/zone-entry-log-view.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Access Resolution Explorer shows ALLOW/DENY trace with grant, zone_type rule, clearance, and escort rows"
    - "Toggling a grant off via checkbox changes ALLOW to DENY when that grant was the only access path"
    - "Escort selection affects RESTRICTED zone results; escort without valid grant shows escort-check-fails prose"
    - "Entry Log view lists all entries filterable by zone and person; ESCORT rows show visitor pass badge"
  artifacts:
    - path: "frontend/src/demo/components/access-resolution-explorer.tsx"
      provides: "Person/zone/escort selectors, ZoneResolutionTrace, grant toggle checkboxes, live resolution"
      exports: ["AccessResolutionExplorer"]
    - path: "frontend/src/demo/components/zone-entry-log-view.tsx"
      provides: "Filterable entry log list with ESCORT visitor pass badges"
      exports: ["ZoneEntryLogView"]
  key_links:
    - from: "frontend/src/demo/components/AccessResolutionExplorer.tsx"
      to: "frontend/src/demo/lib/model.ts"
      via: "resolveZoneAccess, resolveGrant, isGrantActive, getAncestors"
      pattern: "resolveZoneAccess"
    - from: "frontend/src/demo/components/ZoneEntryLogView.tsx"
      to: "frontend/src/demo/lib/model.ts"
      via: "getActiveVisitorPasses"
      pattern: "getActiveVisitorPasses"
---

<objective>
Build the Access Resolution Explorer and Zone Entry Log View components, replacing the stubs created in plan 08-02.

Purpose: These two components complete the Phase 8 UI surface — the explorer makes zone access decisions interactive and explainable; the entry log view closes the audit trail loop with visitor pass status.
Output: Full AccessResolutionExplorer with ZoneResolutionTrace and TOGGLE_GRANT; full ZoneEntryLogView with filters and pass badges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mock-dataset-demo-ui/08-CONTEXT.md
@.planning/phases/08-mock-dataset-demo-ui/08-PATTERNS.md
@.planning/phases/08-mock-dataset-demo-ui/08-UI-SPEC.md
@.planning/phases/08-mock-dataset-demo-ui/08-01-SUMMARY.md
@.planning/phases/08-mock-dataset-demo-ui/08-02-SUMMARY.md

<interfaces>
<!-- Key types and functions — read model.ts for exact signatures before implementing -->

From frontend/src/demo/lib/model.ts:
  resolveZoneAccess(personId: string, zone: ZoneNode, clearance: Clearance, hasValidEscort: boolean, allZones: ZoneNode[], allGrants: PhysicalAccessGrant[], now: Date): ZoneAccessResult
  resolveGrant(personId: string, zone: ZoneNode, allZones: ZoneNode[], allGrants: PhysicalAccessGrant[], now: Date): PhysicalAccessGrant | null
  isGrantActive(grant: PhysicalAccessGrant, now: Date): boolean
  getAncestors(zoneId: string, allZones: ZoneNode[]): ZoneNode[]
  getActiveVisitorPasses(zoneId: string, allPasses: ZoneVisitorPass[], now: Date): ZoneVisitorPass[]
  CLEARANCE_RANK: Record<Clearance, number>
  Clearance type: "UNCLASSIFIED" | "RESTRICTED" | "CONFIDENTIAL" | "SECRET" | "TOP_SECRET"

  ZoneAccessResult interface:
    allow: boolean
    gate: "GRANT_LOOKUP" | "ZONE_TYPE_RULE"
    reason: "GRANT_FOUND" | "NO_GRANT" | "INSUFFICIENT_CLEARANCE"
    detail?: string

  Subject interface (from model.ts):
    id: string
    name: string
    clearance: Clearance
    (and other fields)

From frontend/src/demo/store/world-state.tsx (post 08-01):
  useWorld(): WorldState — { zones, grants, delegates, entryLogs, visitorPasses, disabledGrantIds, subjects }
  useWorldDispatch(): Dispatch<Action>
  Action includes: { type: "TOGGLE_GRANT"; grantId: string }

From frontend/src/demo/components/ui.tsx:
  Card, Pill, Field, Select — same as plan 08-02

Copywriting from 08-UI-SPEC.md §Copywriting Contract:
  ALLOW verdict: "✓ ALLOW"
  DENY verdict: "✗ DENY"
  Grant gate label (passing): "Grant found"
  Grant gate label (failing): "No grant"
  Zone-type rule label: "Zone type rule"
  Clearance gate label: "Clearance"
  Escort gate label: "Escort"
  Relevant grants card title: "Relevant grants (toggle to simulate)"
  Active visitor pass badge: "Active pass"
  Expired visitor pass badge: "Expired"
  Entry log empty state: "No entry log records match the current filters."
  Escort escort-has-no-grant note: "Escort ({name}) does not hold an active grant for this zone — escort check fails."
  Disabled grant visual: "disabled" appended in parentheses inline
  Explorer intro: "Select a person and zone to compute an access decision. Toggle grants to see how inheritance and explicit-auth rules change the result."
  requires_explicit_auth badge: "Requires explicit grant"

Selector layout from 08-UI-SPEC.md:
  Top: grid gap-4 sm:grid-cols-3 — Person card, Zone card, Escort card
  Middle: ZoneResolutionTrace — full width
  Bottom: grid gap-4 sm:grid-cols-2 — Relevant Grants card, Explanation card

Entry Log layout from 08-UI-SPEC.md:
  method badge: CARD → Pill tone="slate", ESCORT → Pill tone="blue"
  entry list item: border-b border-slate-100 py-2 flex items-start gap-3 text-sm
  exit_at null → rendered as "–"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AccessResolutionExplorer with ZoneResolutionTrace and TOGGLE_GRANT</name>
  <files>frontend/src/demo/components/AccessResolutionExplorer.tsx</files>
  <read_first>
    - frontend/src/demo/components/DecisionExplorer.tsx — read lines 70–120 for useMemo derivation chain pattern (subject selection → principal → result); read lines 225–250 for dispatch-on-checkbox pattern; read lines 120–160 for Card/Field/Select selector grid
    - frontend/src/demo/components/ui.tsx — confirm Select component signature (value, onChange, children); confirm Pill tones available; confirm Field renders children inside a div
    - frontend/src/demo/lib/model.ts — read lines 282–310 to confirm resolveZoneAccess two-gate logic and ZoneAccessResult shape; read lines 243–270 to confirm resolveGrant ancestor walk
    - frontend/src/demo/store/world-state.tsx — confirm world.subjects field exists and is Subject[]; confirm TOGGLE_GRANT action shape
  </read_first>
  <action>
    Replace the stub access-resolution-explorer.tsx entirely with the full implementation.

    File header comment: // access-resolution-explorer.tsx — Person/zone/escort selection + live ALLOW/DENY trace + TOGGLE_GRANT checkboxes. Per 08-UI-SPEC §Access Resolution Explorer Layout.

    IMPORTS: useMemo, useState from react; Clearance, PhysicalAccessGrant, ZoneAccessResult, ZoneNode, isGrantActive, resolveGrant, resolveZoneAccess, getAncestors, unitName, UnitId from ../lib/model; useWorld, useWorldDispatch from ../store/world-state; Card, Field, Pill, Select from ./ui.

    STABLE NOW: const now = useMemo(() => new Date(), []) — once per mount, no dependency (Pitfall 4 guard from RESEARCH.md).

    STATE: personId (useState, default world.subjects[0].id), zoneId (useState, default world.zones[0]?.id ?? ""), escortId (useState, default "none").

    DERIVED (useMemo — three stages per RESEARCH.md Pattern 3):
    Stage 1 — activeGrants: world.grants.filter(g => !world.disabledGrantIds.has(g.id) && isGrantActive(g, now)). Deps: [world.grants, world.disabledGrantIds, now].
    Stage 2 — selectedZone: world.zones.find(z => z.id === zoneId) ?? null. Deps: [world.zones, zoneId].
    Stage 2b — person: world.subjects.find(s => s.id === personId) ?? null. Deps: [world.subjects, personId].
    Stage 2c — escortHasGrant: if escortId !== "none" && selectedZone, call resolveGrant(escortId, selectedZone, world.zones, activeGrants, now) — true if result !== null; else false. Deps: [escortId, selectedZone, world.zones, activeGrants, now].
    Stage 3 — result: if person && selectedZone, call resolveZoneAccess(personId, selectedZone, person.clearance as Clearance, escortHasGrant, world.zones, activeGrants, now); else null. Deps: [personId, selectedZone, person, escortHasGrant, world.zones, activeGrants, now].

    RELEVANT GRANTS (for toggle panel):
    relevantZoneIds = useMemo: if selectedZone, new Set([selectedZone.id, ...getAncestors(selectedZone.id, world.zones).map(z => z.id)]); else empty Set. Deps: [selectedZone, world.zones].
    relevantGrants = world.grants.filter(g => g.person_id === personId && relevantZoneIds.has(g.zone_id)).

    DISPATCH: const dispatch = useWorldDispatch().

    LOCAL COMPONENT — ZoneResolutionTrace({ result: ZoneAccessResult }):
    CRITICAL: Do NOT use DecisionTrace from ui.tsx — it expects a Decision type from abac.ts (different shape). Build ZoneResolutionTrace as a local function component in the same file.
    Render: outer div className={`rounded-lg border p-4 ${result.allow ? "border-green-200 bg-green-50" : "border-red-200 bg-red-50"}`}
    Verdict line: div className="text-lg font-semibold" with text "✓ ALLOW" or "✗ DENY"
    Gate rows: ul className="mt-3 space-y-1.5"
    Each row is: li className="flex gap-2 text-sm"
      - span icon: "✓" in text-green-600 or "✗" in text-red-600 per gate pass/fail
      - span label: w-28 shrink-0 font-medium
      - span detail: text-slate-600

    Row 1 — Grant gate:
    - Pass icon: result.reason !== "NO_GRANT" (i.e., grant was found) → green "✓"; else red "✗"
    - Label: "Grant found" (pass) or "No grant" (fail) — per UI-SPEC §Copywriting
    - Detail: result.reason === "NO_GRANT" ? "No active grant for this zone or matching ancestor." : "Grant resolved via zone ancestry."

    Row 2 — Zone type rule:
    - Pass icon: result.allow || result.gate === "GRANT_LOOKUP" → green; else red
    - Label: "Zone type rule"
    - Detail: result.detail ?? (result.reason === "INSUFFICIENT_CLEARANCE" ? "Clearance below required level." : "Zone type requirement satisfied.")

    Row 3 — Clearance:
    - Pass icon: result.allow || result.reason !== "INSUFFICIENT_CLEARANCE" → green; else red
    - Label: "Clearance"
    - Detail: result.reason === "INSUFFICIENT_CLEARANCE" ? (result.detail ?? "Clearance insufficient for this zone type.") : "Clearance sufficient."

    Row 4 — Escort (shown when selectedZone is RESTRICTED or SECURED):
    - Pass icon: escortHasGrant → green; else red
    - Label: "Escort"
    - Detail: if escortId === "none" then "No escort selected." else if escortHasGrant then "Escort holds a valid grant." else "Escort does not hold an active grant for this zone — escort check fails."
    ZoneResolutionTrace also receives escortId: string, escortHasGrant: boolean, selectedZone: ZoneNode | null as additional props for the escort row.

    SELECTOR GRID RENDER — div className="grid gap-4 sm:grid-cols-3":
    Card 1 (Person): Field label="Person": Select value=personId onChange sets personId; options are world.subjects.map(s => option key/value=s.id text=s.name); Field label="Clearance": Pill tone based on clearance (CONFIDENTIAL→slate, SECRET→amber, TOP_SECRET→red); p className="text-xs text-slate-400 mt-1">[MockTag: demo data]
    Card 2 (Zone): Field label="Zone": Select value=zoneId onChange; options from world.zones.map; Field label="Zone type": Pill with ZONE_TYPE_TONE if selectedZone; if selectedZone?.requires_explicit_auth: Pill tone="amber">"Requires explicit grant"
    Card 3 (Escort): Field label="Escort (optional)": Select value=escortId onChange; first option value="none" "None"; then world.subjects options; if escortId !== "none" && !escortHasGrant: p className="text-xs text-red-500 mt-1" showing escort-no-grant note

    INTRO PROSE: p className="text-sm text-slate-500 mb-4" with the intro text from §Copywriting Contract.

    TRACE: {result ? <ZoneResolutionTrace result={result} escortId={escortId} escortHasGrant={escortHasGrant} selectedZone={selectedZone} /> : null}

    BOTTOM GRID — div className="grid gap-4 sm:grid-cols-2":
    Card title="Relevant grants (toggle to simulate)":
    ul of relevantGrants; each li className="flex items-center gap-2 text-sm py-1":
      input type="checkbox" checked={!world.disabledGrantIds.has(grant.id)} onChange={() => dispatch({ type: "TOGGLE_GRANT", grantId: grant.id })}
      span className={world.disabledGrantIds.has(grant.id) ? "line-through text-slate-400" : ""}: shows grant.zone_id zone name + person name
      if world.disabledGrantIds.has(grant.id): span className="text-xs text-slate-400" "(disabled)"
    Empty state if relevantGrants.length === 0: p className="text-sm text-slate-400" "No relevant grants for this selection."

    Card title="About this decision" (or "Resolution explanation"):
    p className="text-sm text-slate-600" explaining the two-gate resolution in 2–3 sentences: "Access is resolved in two gates: (1) does an active grant exist for this zone or a matching ancestor? (2) does the zone's security type allow entry given the person's clearance?" This is static prose per 08-UI-SPEC §Interaction Contracts.

    Export AccessResolutionExplorer as a named export.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - ZoneResolutionTrace is a local function component in the file — NOT an import from ui.tsx
    - All three useMemo stages are present with correct deps arrays
    - escortHasGrant computed with resolveGrant (not a hardcoded value)
    - TOGGLE_GRANT dispatched with grantId when checkbox onChange fires
    - relevantGrants filtered by personId AND relevantZoneIds (not all grants)
    - disabled grants rendered with line-through text-slate-400 className
    - stable now = useMemo(() => new Date(), []) present
    - Intro prose present above selectors
    - Export: "export function AccessResolutionExplorer" present in file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build ZoneEntryLogView with filters and visitor pass badges</name>
  <files>frontend/src/demo/components/ZoneEntryLogView.tsx</files>
  <read_first>
    - frontend/src/demo/components/AuditView.tsx — read fully for the filterable list pattern: local filter state, sorted/filtered useMemo, scrollable list layout
    - frontend/src/demo/components/ui.tsx — confirm Pill tone "green" is supported (for Active pass badge); confirm Select usage pattern
    - frontend/src/demo/lib/model.ts — confirm getActiveVisitorPasses(zoneId, allPasses, now) signature and return type ZoneVisitorPass[]
    - frontend/src/demo/store/world-state.tsx — confirm entryLogs and visitorPasses field shapes; confirm subjects field for person name lookup
  </read_first>
  <action>
    Replace the stub zone-entry-log-view.tsx entirely with the full implementation.

    File header comment: // zone-entry-log-view.tsx — Zone entry log with zone/person filters and visitor pass badges on ESCORT rows. Per 08-UI-SPEC §Zone Entry Log Layout.

    IMPORTS: useMemo, useState from react; ZoneEntryLog, ZoneNode, getActiveVisitorPasses from ../lib/model; useWorld from ../store/world-state; Card, Field, Pill, Select from ./ui.

    STABLE NOW: const now = useMemo(() => new Date(), []).

    FROM WORLD: const world = useWorld(). Helper lookups:
    - const zoneName = (id: string) => world.zones.find(z => z.id === id)?.name ?? id
    - const personName = (id: string) => world.subjects.find(s => s.id === id)?.name ?? id

    LOCAL FILTER STATE:
    - const [zoneFilter, setZoneFilter] = useState<string>("all")
    - const [personFilter, setPersonFilter] = useState<string>("all")

    FILTERED + SORTED ENTRIES (useMemo):
    world.entryLogs
      .filter(e => (zoneFilter === "all" || e.zone_id === zoneFilter) && (personFilter === "all" || e.person_id === personFilter))
      .sort((a, b) => b.entry_at.getTime() - a.entry_at.getTime())
    Deps: [world.entryLogs, zoneFilter, personFilter]

    RENDER — outer div className="space-y-4":

    FILTERS ROW — div className="flex gap-4 items-end":
    Field label="Zone": Select value=zoneFilter onChange=setZoneFilter; first option value="all" "All zones"; then world.zones.map(z => option key/value=z.id text=z.name)
    Field label="Person": Select value=personFilter onChange=setPersonFilter; first option value="all" "All persons"; then world.subjects.map(s => option key/value=s.id text=s.name)

    ENTRY LIST — Card title="Entry Log":
    if filtered.length === 0: p className="text-sm text-slate-400" "No entry log records match the current filters."
    else: ul className="divide-y divide-slate-100"
    Each entry: li key=entry.id className="py-3 flex items-start gap-3 text-sm"
      - span className="text-slate-400 font-mono text-xs w-36 shrink-0": entry.entry_at.toISOString().slice(0, 16).replace("T", " ")
      - Pill tone={entry.method === "CARD" ? "slate" : "blue"}: {entry.method}
      - span: personName(entry.person_id)
      - span className="text-slate-400": "→"
      - span: zoneName(entry.zone_id)
      - span className="text-slate-400 text-xs": "exit: " + (entry.exit_at ? entry.exit_at.toISOString().slice(0, 16).replace("T", " ") : "–")

    ESCORT VISITOR PASS BADGE (rendered AFTER the exit span, only when entry.method === "ESCORT"):
    const activePasses = getActiveVisitorPasses(entry.zone_id, world.visitorPasses, now).filter(p => p.entry_log_id === entry.id)
    const hasActive = activePasses.length > 0
    Render: Pill tone={hasActive ? "green" : "slate"}: {hasActive ? "Active pass" : "Expired"}

    ESCORT ESCORT PERSON NOTE — if entry.method === "ESCORT" && entry.escort_person_id:
    span className="text-xs text-slate-500": "escorted by " + personName(entry.escort_person_id)

    Export ZoneEntryLogView as a named export.

    NOTE: The visitor pass badge computation (getActiveVisitorPasses inside a map) creates a new Date() call implicitly through the stable `now` reference — do not call new Date() inside the map iteration. Pass the `now` constant from useMemo.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - ZoneEntryLogView exports ZoneEntryLogView as named function export
    - getActiveVisitorPasses called with entry.zone_id, world.visitorPasses, now — not with a new Date() inline
    - Visitor pass badge shows Pill tone="green" for active passes, tone="slate" for expired
    - Entry list sorted descending by entry_at (b.entry_at.getTime() - a.entry_at.getTime())
    - Zone and person filter dropdowns include "All zones" / "All persons" as first option
    - CARD rows rendered with Pill tone="slate"; ESCORT rows with Pill tone="blue"
    - Empty state text present: "No entry log records match the current filters."
    - Exit_at null renders as "–"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorldState → UI | In-memory mock data; no user input; no network |
| resolveZoneAccess → ZoneResolutionTrace | Computation is pure TypeScript; result is display-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | ZoneResolutionTrace | accept | Demo only; no real clearance or PII; all data is mock |
| T-08-06 | Tampering | useMemo deps arrays | mitigate | Each useMemo dep array must include all store fields read inside the memo — stale closure would produce incorrect resolution results; deps listed explicitly in task action above |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
After both tasks complete — final phase verification:
1. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit — 0 errors
2. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/physical-access.test.ts 2>&1 | tail -5 — all pass
3. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/store/world-state.test.tsx 2>&1 | tail -5 — all pass
4. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run --exclude "**/*.e2e.*" --exclude "**/playwright*" 2>&1 | tail -10 — no regressions
5. Structural check: grep -c "resolveZoneAccess" frontend/src/demo/components/AccessResolutionExplorer.tsx — must be >= 1
6. Structural check: grep -c "getActiveVisitorPasses" frontend/src/demo/components/ZoneEntryLogView.tsx — must be >= 1
7. Structural check: grep -c "ZoneResolutionTrace" frontend/src/demo/components/AccessResolutionExplorer.tsx — must be >= 2 (declaration + usage)
8. Structural check: grep -c "DecisionTrace" frontend/src/demo/components/AccessResolutionExplorer.tsx — must be 0 (not imported/used)
</verification>

<success_criteria>
- Access Resolution Explorer: person + zone with valid grant shows ALLOW; toggling that grant via checkbox changes result to DENY; escort selection affects RESTRICTED zone results; ZoneResolutionTrace renders four gate rows (Grant found, Zone type rule, Clearance, Escort)
- Entry Log: all entries render with correct method badges; ESCORT rows show Active pass / Expired badge from getActiveVisitorPasses; zone and person filters work; entries sorted newest-first
- TypeScript compiles clean; all existing tests pass; no regression in physical-access or world-state tests
</success_criteria>

<output>
Create `.planning/phases/08-mock-dataset-demo-ui/08-03-SUMMARY.md` when done
</output>
