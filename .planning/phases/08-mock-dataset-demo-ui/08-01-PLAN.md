---
phase: 08-mock-dataset-demo-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/seed.ts
  - frontend/src/demo/store/world-state.tsx
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04
  - SEED-05
  - SEED-06
  - SEED-07
  - SEED-08
  - SEED-09

must_haves:
  truths:
    - "ZONES constant has ≥3 root Sites, all three zone_type values, no SECURED at SITE/AREA"
    - "GRANTS constant has expired, future, and permanent grant examples; ≤2 site-level grants"
    - "DELEGATES has ≥1 PERSON delegate and ≥1 ORG delegate"
    - "ENTRY_LOGS has both CARD and ESCORT entries; all pass validateEntryLog"
    - "VISITOR_PASSES has ≥1 pass matching an ESCORT entry_log_id"
    - "WorldState interface has zones/grants/delegates/entryLogs/visitorPasses/disabledGrantIds; seedWorld() initializes them"
    - "TOGGLE_GRANT action exists in Action union; reducer handles it with new Set immutability"
  artifacts:
    - path: "frontend/src/demo/lib/seed.ts"
      provides: "ZONES, GRANTS, DELEGATES, ENTRY_LOGS, VISITOR_PASSES appended below SEED-HEAD boundary"
      contains: "export const ZONES"
    - path: "frontend/src/demo/store/world-state.tsx"
      provides: "WorldState zone fields, seedWorld() extension, TOGGLE_GRANT reducer case"
      exports: ["WorldState", "Action", "seedWorld"]
  key_links:
    - from: "frontend/src/demo/lib/seed.ts"
      to: "frontend/src/demo/store/world-state.tsx"
      via: "import ZONES, GRANTS, DELEGATES, ENTRY_LOGS, VISITOR_PASSES"
      pattern: "import.*ZONES.*from.*seed"
    - from: "frontend/src/demo/store/world-state.tsx"
      to: "frontend/src/demo/lib/model.ts"
      via: "ZoneNode, PhysicalAccessGrant, ZoneAccessDelegate, ZoneEntryLog, ZoneVisitorPass type imports"
      pattern: "import type.*ZoneNode"
---

<objective>
Author the 6-unit physical access zone seed dataset and extend WorldState to hold it.

Purpose: Phases 5–7 built all model types and logic. This plan wires seed data into the shared world store so all three UI views (Zone Browser, Access Resolution Explorer, Entry Log) have live data to read and toggle.
Output: Populated ZONES/GRANTS/DELEGATES/ENTRY_LOGS/VISITOR_PASSES constants in seed.ts; WorldState extended with 6 new fields; TOGGLE_GRANT reducer action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mock-dataset-demo-ui/08-CONTEXT.md
@.planning/phases/08-mock-dataset-demo-ui/08-RESEARCH.md
@.planning/phases/08-mock-dataset-demo-ui/08-PATTERNS.md
@.planning/phases/08-mock-dataset-demo-ui/08-SPEC.md

<interfaces>
<!-- Key types from frontend/src/demo/lib/model.ts — read before writing seed data -->

ZoneNode interface:
  id: string
  name: string
  level: "SITE" | "AREA" | "BUILDING" | "ZONE" | "ROOM"
  zone_type: "CONTROLLED" | "RESTRICTED" | "SECURED"
  parent_id: string | null
  admin_org_id: string
  asset_owner_org_id: string
  requires_explicit_auth: boolean

PhysicalAccessGrant interface:
  id: string
  person_id: string
  zone_id: string
  valid_from: Date | null  (null = unbounded start)
  valid_until: Date | null  (null = permanent)

ZoneAccessDelegate interface:
  id: string
  zone_id: string
  delegate_type: "PERSON" | "ORG"
  delegate_person_id: string | null
  delegate_org_id: string | null
  granted_by_org_id: string
  valid_from: Date | null
  valid_until: Date | null

ZoneEntryLog interface:
  id: string
  person_id: string
  zone_id: string
  entry_at: Date
  exit_at: Date | null
  method: "CARD" | "ESCORT"
  escort_person_id: string | null  (required for ESCORT, null for CARD)

ZoneVisitorPass interface:
  id: string
  entry_log_id: string  (must match an ESCORT ZoneEntryLog.id)
  escort_person_id: string
  zone_id: string
  valid_from: Date
  valid_until: Date

Subject (from seed.ts): subj-1=SECRET, subj-2=TOP_SECRET, subj-3=CONFIDENTIAL, subj-4=TOP_SECRET
UnitId values: "MILITARY_1" | "MILITARY_2" | "INTEL" | "INFRA" | "INDUSTRY" | "HOME_GUARD"

isGrantActive(grant, now): checks valid_from <= now <= valid_until (null bounds = unbounded)
validateEntryLog(entry): returns null if valid, error string if ESCORT without escort_person_id
CLEARANCE_RANK: UNCLASSIFIED=0, RESTRICTED=1, CONFIDENTIAL=2, SECRET=3, TOP_SECRET=4

WorldState interface (current last two fields, before Phase 8 additions):
  fedOutbox: Partial<Record<UnitId, OutboxEntry[]>>
  fedVerifyResults: { valid: VerifyResult | null; rogue: VerifyResult | null }

Action union (current last case, before Phase 8):
  | { type: "FEDERATION_RESET" }

seed.ts SEED-HEAD: all new constants go AFTER line ~945 (after SUPPORT_OBLIGATIONS block).
world-state.tsx imports from seed (lines 30–35): AGREEMENTS, HUB_INDEX, INITIAL_EVENTS, RESOURCES, SUBJECTS
world-state.tsx imports from model (lines 7–28): add ZoneNode, PhysicalAccessGrant, ZoneAccessDelegate, ZoneEntryLog, ZoneVisitorPass
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append zone seed constants to seed.ts</name>
  <files>frontend/src/demo/lib/seed.ts</files>
  <read_first>
    - frontend/src/demo/lib/seed.ts — read the FULL file to find the exact last line after SUPPORT_OBLIGATIONS; confirm SEED-HEAD boundary location; do NOT edit anything above it
    - frontend/src/demo/lib/model.ts — confirm ZoneNode, PhysicalAccessGrant, ZoneAccessDelegate, ZoneEntryLog, ZoneVisitorPass, validateEntryLog signatures
  </read_first>
  <action>
    Add zone type imports at the top of seed.ts — extend the existing import from "./model" to include: ZoneNode, PhysicalAccessGrant, ZoneAccessDelegate, ZoneEntryLog, ZoneVisitorPass. Use the import type syntax consistent with the file's existing import block.

    After the SUPPORT_OBLIGATIONS block (the last existing export), append a boundary comment block followed by five typed export constants. Use the fixed reference year 2026 for all dates. The reference point for "now" in the demo is approximately 2026-06-01 (SEED_NOW).

    Zone topology (3 Sites, all three zone_type values, SECURED only at BUILDING/ZONE/ROOM level):

    Site 1 — "Alpha Command" (id: zone-site-alpha, level: SITE, zone_type: CONTROLLED, parent_id: null, admin_org_id: "MILITARY_1", asset_owner_org_id: "MILITARY_1", requires_explicit_auth: false)
      Area — "North Wing" (id: zone-area-north, level: AREA, zone_type: CONTROLLED, parent_id: zone-site-alpha, admin_org_id: "MILITARY_1", asset_owner_org_id: "MILITARY_1", requires_explicit_auth: false)
        Building — "Block A" (id: zone-bldg-block-a, level: BUILDING, zone_type: CONTROLLED, parent_id: zone-area-north, admin_org_id: "MILITARY_1", asset_owner_org_id: "MILITARY_1", requires_explicit_auth: false)
          Zone — "Corridor C1" (id: zone-corr-c1, level: ZONE, zone_type: CONTROLLED, parent_id: zone-bldg-block-a, admin_org_id: "MILITARY_1", asset_owner_org_id: "MILITARY_1", requires_explicit_auth: false)
            Room — "Server Room 1" (id: zone-room-sr1, level: ROOM, zone_type: CONTROLLED, parent_id: zone-corr-c1, admin_org_id: "MILITARY_1", asset_owner_org_id: "MILITARY_1", requires_explicit_auth: false)
          Zone — "Secure Lab" (id: zone-secure-lab, level: ZONE, zone_type: SECURED, parent_id: zone-bldg-block-a, admin_org_id: "MILITARY_1", asset_owner_org_id: "INTEL", requires_explicit_auth: true)  ← SEED-05 explicit exclusion

    Site 2 — "Intel Campus" (id: zone-site-intel, level: SITE, zone_type: RESTRICTED, parent_id: null, admin_org_id: "INTEL", asset_owner_org_id: "INTEL", requires_explicit_auth: false)
      Building — "Analysis Wing" (id: zone-bldg-analysis, level: BUILDING, zone_type: RESTRICTED, parent_id: zone-site-intel, admin_org_id: "INTEL", asset_owner_org_id: "INTEL", requires_explicit_auth: false)
        Room — "SIGINT Suite" (id: zone-room-sigint, level: ROOM, zone_type: SECURED, parent_id: zone-bldg-analysis, admin_org_id: "INTEL", asset_owner_org_id: "INTEL", requires_explicit_auth: true)  ← SECURED at ROOM

    Site 3 — "Logistics Hub" (id: zone-site-logistics, level: SITE, zone_type: CONTROLLED, parent_id: null, admin_org_id: "INFRA", asset_owner_org_id: "INFRA", requires_explicit_auth: false)
      Area — "Yard" (id: zone-area-yard, level: AREA, zone_type: CONTROLLED, parent_id: zone-site-logistics, admin_org_id: "INFRA", asset_owner_org_id: "INFRA", requires_explicit_auth: false)
        Building — "Warehouse A" (id: zone-bldg-warehouse-a, level: BUILDING, zone_type: CONTROLLED, parent_id: zone-area-yard, admin_org_id: "INFRA", asset_owner_org_id: "MILITARY_2", requires_explicit_auth: false)
          Room — "Supply Room" (id: zone-room-supply, level: ROOM, zone_type: CONTROLLED, parent_id: zone-bldg-warehouse-a, admin_org_id: "INFRA", asset_owner_org_id: "MILITARY_2", requires_explicit_auth: false)  ← SEED-04 inheritance target

    GRANTS — design for TOGGLE_GRANT interactivity and temporal variety (per D-06/SEED-09):
    - grant-dana-block-a: person_id "subj-1", zone_id "zone-bldg-block-a", valid_from: null, valid_until: null  (permanent — covers CONTROLLED children by inheritance per SEED-04)
    - grant-dana-secure-lab: person_id "subj-1", zone_id "zone-secure-lab", valid_from: new Date("2026-01-01"), valid_until: null  (active explicit — required for SECURED zone; SEED-05 toggle demo)
    - grant-sam-alpha-site: person_id "subj-2", zone_id "zone-site-alpha", valid_from: null, valid_until: null  (1 of ≤2 site grants per SEED-03)
    - grant-sam-sigint: person_id "subj-2", zone_id "zone-room-sigint", valid_from: new Date("2026-01-01"), valid_until: null  (active — SECURED explicit for TOP_SECRET person)
    - grant-lee-warehouse: person_id "subj-3", zone_id "zone-bldg-warehouse-a", valid_from: null, valid_until: null  (permanent)
    - grant-mara-analysis: person_id "subj-4", zone_id "zone-bldg-analysis", valid_from: null, valid_until: null  (permanent)
    - grant-expired-lee: person_id "subj-3", zone_id "zone-site-logistics", valid_from: new Date("2025-01-01"), valid_until: new Date("2026-03-01")  (EXPIRED — past valid_until per SEED-09)
    - grant-future-dana: person_id "subj-1", zone_id "zone-bldg-analysis", valid_from: new Date("2026-08-01"), valid_until: null  (FUTURE — valid_from after demo date per SEED-09)

    DELEGATES (per SEED-06):
    - delegate-person-1: id "deleg-person-1", zone_id "zone-bldg-block-a", delegate_type "PERSON", delegate_person_id "subj-4", delegate_org_id null, granted_by_org_id "MILITARY_1", valid_from: null, valid_until: null
    - delegate-org-1: id "deleg-org-1", zone_id "zone-site-intel", delegate_type "ORG", delegate_person_id null, delegate_org_id "MILITARY_2", granted_by_org_id "INTEL", valid_from: new Date("2026-01-01"), valid_until: null

    ENTRY_LOGS (per SEED-07 — both CARD and ESCORT, all must pass validateEntryLog):
    - log-card-1: id "log-card-1", person_id "subj-1", zone_id "zone-bldg-block-a", entry_at: new Date("2026-05-15T09:00:00Z"), exit_at: new Date("2026-05-15T17:00:00Z"), method "CARD", escort_person_id null
    - log-card-2: id "log-card-2", person_id "subj-4", zone_id "zone-bldg-analysis", entry_at: new Date("2026-05-16T08:30:00Z"), exit_at: null, method "CARD", escort_person_id null
    - log-escort-1: id "log-escort-1", person_id "subj-3", zone_id "zone-room-sigint", entry_at: new Date("2026-05-17T10:00:00Z"), exit_at: new Date("2026-05-17T12:00:00Z"), method "ESCORT", escort_person_id "subj-2"  ← ESCORT with escort (per SEED-07)
    - log-escort-2: id "log-escort-2", person_id "subj-3", zone_id "zone-bldg-block-a", entry_at: new Date("2026-05-20T14:00:00Z"), exit_at: null, method "ESCORT", escort_person_id "subj-1"

    VISITOR_PASSES (per SEED-08 — each pass must reference an ESCORT log id):
    - pass-1: id "pass-1", entry_log_id "log-escort-1", escort_person_id "subj-2", zone_id "zone-room-sigint", valid_from: new Date("2026-05-17T10:00:00Z"), valid_until: new Date("2026-05-17T12:00:00Z")  (EXPIRED relative to 2026-06-01)
    - pass-2: id "pass-2", entry_log_id "log-escort-2", escort_person_id "subj-1", zone_id "zone-bldg-block-a", valid_from: new Date("2026-05-20T14:00:00Z"), valid_until: new Date("2026-12-31T23:59:59Z")  (ACTIVE relative to 2026-06-01)

    Use the boundary comment header: // ============================================================ // Phase 8: Physical Access Zone Seed Data // ============================================================

    Do NOT modify any content above the SUPPORT_OBLIGATIONS block. The file may have TypeScript compile errors only if the new imports conflict — resolve by checking the existing import block and extending it, not replacing it.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - ZONES.filter(z => z.parent_id === null).length === 3 (verifiable by inspection)
    - All three zone_type values present in ZONES
    - No SECURED node has level "SITE" or "AREA"
    - GRANTS contains grant-expired-lee (valid_until in 2026-03, past 2026-06) and grant-future-dana (valid_from 2026-08) and grant-dana-block-a (permanent nulls) — three temporal varieties
    - GRANTS.filter(g => ZONES.find(z => z.id === g.zone_id)?.level === "SITE").length === 1 (only grant-sam-alpha-site is site-level)
    - DELEGATES has one PERSON and one ORG record
    - All ESCORT ENTRY_LOGS have escort_person_id set; all CARD entries have escort_person_id null
    - Both VISITOR_PASSES reference existing ESCORT log IDs (log-escort-1, log-escort-2)
    - SEED-HEAD records (above SUPPORT_OBLIGATIONS) are unmodified — verify by checking git diff shows only additions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend WorldState with zone fields and TOGGLE_GRANT reducer</name>
  <files>frontend/src/demo/store/world-state.tsx</files>
  <read_first>
    - frontend/src/demo/store/world-state.tsx — read the FULL file: WorldState interface (lines ~64–80), seedWorld() (lines ~84–110), Action union (lines ~110–155), reducer (lines ~160–430), existing imports from seed and model
  </read_first>
  <action>
    Three targeted edits to world-state.tsx:

    1. IMPORTS — extend the existing import from "../lib/seed" to add: DELEGATES, ENTRY_LOGS, GRANTS, VISITOR_PASSES, ZONES (alphabetically with existing imports). Extend the existing import from "../lib/model" (or add a new import type block) to include: PhysicalAccessGrant, ZoneAccessDelegate, ZoneEntryLog, ZoneNode, ZoneVisitorPass.

    2. WorldState INTERFACE — add 6 new fields AFTER the existing fedVerifyResults field (the current last field). Fields to add:
       zones: ZoneNode[]
       grants: PhysicalAccessGrant[]
       delegates: ZoneAccessDelegate[]
       entryLogs: ZoneEntryLog[]
       visitorPasses: ZoneVisitorPass[]
       disabledGrantIds: Set<string>

    3. seedWorld() FUNCTION — add 6 corresponding initializers AFTER the fedVerifyResults initializer in the return statement:
       zones: ZONES,
       grants: GRANTS,
       delegates: DELEGATES,
       entryLogs: ENTRY_LOGS,
       visitorPasses: VISITOR_PASSES,
       disabledGrantIds: new Set<string>(),

    4. Action TYPE UNION — add one new case AFTER the existing last case (FEDERATION_RESET):
       | { type: "TOGGLE_GRANT"; grantId: string }

    5. REDUCER — add a new case BEFORE the default branch, AFTER the FEDERATION_RESET case:
       case "TOGGLE_GRANT": {
         const next = new Set(state.disabledGrantIds);
         if (next.has(action.grantId)) next.delete(action.grantId);
         else next.add(action.grantId);
         return { ...state, disabledGrantIds: next };
       }

    CRITICAL: The Set must be copied with new Set(state.disabledGrantIds) before mutation — direct Set.add/delete on state.disabledGrantIds breaks React reactivity (Pitfall 2 from RESEARCH.md). Never write state.disabledGrantIds.add(...); return { ...state }.

    Do NOT change any existing Action cases, WorldState fields, reducer cases, or seedWorld() fields. Surgical additions only.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - WorldState interface contains all 6 new fields: zones, grants, delegates, entryLogs, visitorPasses, disabledGrantIds
    - seedWorld() initializes all 6 fields
    - Action union contains the TOGGLE_GRANT variant
    - Reducer handles "TOGGLE_GRANT" with immutable Set copy pattern (new Set(...) not mutation)
    - Existing Action cases and WorldState fields are unmodified (confirm via git diff)
    - world-state.test.tsx still passes: cd frontend && npx vitest run src/demo/store/world-state.test.tsx 2>&1 | tail -10
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed data → WorldState | All data is in-memory TypeScript constants; no network, no user input, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | seed.ts SEED-HEAD boundary | accept | No user input; static file edit by developer only; SEED-HEAD invariant enforced via code review and git diff check |
| T-08-02 | Tampering | TOGGLE_GRANT Set mutation | mitigate | Always use new Set(state.disabledGrantIds) — documented in RESEARCH.md Pitfall 2 and in task action above |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; legitimacy audit not required |
</threat_model>

<verification>
After both tasks complete:
1. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit — must report 0 errors
2. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/physical-access.test.ts 2>&1 | tail -5 — existing tests must still pass
3. cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/store/world-state.test.tsx 2>&1 | tail -5 — existing store tests still pass
4. git diff --stat — only seed.ts and world-state.tsx modified; no files above SEED-HEAD boundary changed
</verification>

<success_criteria>
- ZONES, GRANTS, DELEGATES, ENTRY_LOGS, VISITOR_PASSES exported from seed.ts with ≥3 sites, all zone_types, temporal variety, delegation, CARD+ESCORT entries, and linked visitor passes
- WorldState has 6 new zone fields; seedWorld() initializes them; TOGGLE_GRANT reducer action works with immutable Set copy
- TypeScript compiles clean; all existing tests pass
</success_criteria>

<output>
Create `.planning/phases/08-mock-dataset-demo-ui/08-01-SUMMARY.md` when done
</output>
