---
phase: 02-federation-hub
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/demo/store/world-state.tsx
autonomous: true
requirements:
  - FED-01
  - FED-02
  - FED-03
  - FED-04

must_haves:
  truths:
    - "WorldState has five new federation fields: fedCredentials, fedRunStage, fedTranscript, fedInbox, fedOutbox, fedVerifyResults"
    - "Action union has seven new members: CREDENTIALS_READY, CREDENTIAL_VERIFY_RESULTS, FEDERATION_PUBLISH, FEDERATION_DISCOVER, FEDERATION_REQUEST_DETAIL, FEDERATION_RESPOND, FEDERATION_RESET"
    - "FEDERATION_RESET clears fedTranscript and resets fedRunStage to IDLE but does NOT clear fedInbox or fedOutbox"
    - "FEDERATION_RESPOND: only builds Principal from credential if verifyResult.valid is true; otherwise DENY with 'credential not verified'"
    - "seedWorld() initializes all six new federation fields to their zero values"
  artifacts:
    - path: "frontend/src/demo/store/world-state.tsx"
      provides: "Extended single-store with full federation state slices and action handlers"
      contains: "fedCredentials | fedRunStage | fedTranscript | fedInbox | fedOutbox | fedVerifyResults"
    - path: "frontend/src/demo/store/world-state.tsx"
      provides: "InboxEntry and OutboxEntry interfaces"
      exports:
        - InboxEntry
        - OutboxEntry
  key_links:
    - from: "frontend/src/demo/store/world-state.tsx"
      to: "frontend/src/demo/lib/contract.ts"
      via: "buildDiscoverEnvelopes import used in FEDERATION_DISCOVER case"
      pattern: "from '../lib/contract'"
    - from: "frontend/src/demo/store/world-state.tsx"
      to: "frontend/src/demo/lib/abac.ts"
      via: "Principal type for InboxEntry"
      pattern: "from '../lib/abac'"
---

<objective>
Extend the single Phase-1 useReducer world-state with federation state slices, entry types, and seven new action handlers.

Purpose: This is the integration seam all Federation panels read from. After this plan, Wave 3 panels can be built in parallel because they all read from useWorld() and dispatch typed actions.
Output: Extended world-state.tsx with five new WorldState fields, two new interfaces (InboxEntry, OutboxEntry), seven new Action variants, and all reducer cases.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-RESEARCH.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-PATTERNS.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-01-SUMMARY.md

<interfaces>
<!-- Existing WorldState interface (world-state.tsx lines 35-45) — add new fields AFTER existing fields, never remove any: -->
WorldState fields (existing, DO NOT change):
  units, subjects, resources, agreements, events, hubIndex, currentRole, abacTarget, seq

Action union (existing, lines 69-81 — DO NOT change):
  SET_ROLE | SET_TARGET | APPROVE_ATTRIBUTE | REVOKE_ATTRIBUTE | TOGGLE_SECURITY_HOLD | REQUEST_ATTRIBUTE

New imports needed (add to top of file):
  import { buildDiscoverEnvelopes, type DetailResult } from '../lib/contract';
  import type { Credential, VerifyResult } from '../lib/credential';
  import type { Principal } from '../lib/abac';
  import type { Envelope } from '../lib/model';  (Envelope is already in model.ts but not currently imported)

New interfaces to export (above WorldState):
  InboxEntry: { seq: number; from: UnitId; subjectId: string; requester: Principal; verifyResult: VerifyResult; detailResult: DetailResult }
  OutboxEntry: { seq: number; to: UnitId; subjectId: string; granted: boolean; record: Subject | null }

New WorldState fields (add after existing seq field):
  fedCredentials: { valid: Credential | null; rogue: Credential | null }
  fedRunStage: 'IDLE' | 'PUBLISHED' | 'DISCOVERED' | 'REQUESTED' | 'RESPONDED'
  fedTranscript: Envelope[]
  fedInbox: Partial<Record<UnitId, InboxEntry[]>>
  fedOutbox: Partial<Record<UnitId, OutboxEntry[]>>
  fedVerifyResults: { valid: VerifyResult | null; rogue: VerifyResult | null }

New Action union members (append to existing Action type):
  | { type: 'CREDENTIALS_READY'; valid: Credential; rogue: Credential }
  | { type: 'CREDENTIAL_VERIFY_RESULTS'; validResult: VerifyResult; rogueResult: VerifyResult }
  | { type: 'FEDERATION_PUBLISH'; from: UnitId; subjectId: string; domain: Domain }
  | { type: 'FEDERATION_DISCOVER'; from: UnitId; subjectId: string }
  | { type: 'FEDERATION_REQUEST_DETAIL'; from: UnitId; to: UnitId; subjectId: string; requester: Principal }
  | { type: 'FEDERATION_RESPOND'; verifyResult: VerifyResult; detailResult: DetailResult; requesterUnit: UnitId; holderUnit: UnitId; subjectId: string; requester: Principal }
  | { type: 'FEDERATION_RESET' }

seedWorld() zero values (add to the return object):
  fedCredentials: { valid: null, rogue: null }
  fedRunStage: 'IDLE'
  fedTranscript: []
  fedInbox: {}
  fedOutbox: {}
  fedVerifyResults: { valid: null, rogue: null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WorldState with federation slices and action handlers</name>
  <files>frontend/src/demo/store/world-state.tsx</files>
  <read_first>
    - frontend/src/demo/store/world-state.tsx — the full existing file (read before any edits)
    - frontend/src/demo/lib/contract.ts — DetailResult interface + buildDiscoverEnvelopes signature (from plan 02-01)
    - frontend/src/demo/lib/credential.ts — Credential, VerifyResult exports (from plan 02-01)
    - frontend/src/demo/lib/abac.ts — Principal interface (lines 34-40)
    - frontend/src/demo/lib/model.ts — Envelope type (lines 246-271), Domain, UnitId, Subject
    - frontend/src/spikes/components/Spike006Trust.tsx — verify-before-trust pattern (lines 46-48) for reference on how FEDERATION_RESPOND must only build Principal when verifyResult.valid is true
  </read_first>
  <action>
    Edit frontend/src/demo/store/world-state.tsx. Surgical edits only — do NOT change any existing field, action, or reducer case.

    Step 1 — Add new imports at the top of the imports block (after existing imports from './model' and './seed'):
    Import buildDiscoverEnvelopes and DetailResult from '../lib/contract'.
    Import Credential and VerifyResult as type-only from '../lib/credential'.
    Import Principal as type-only from '../lib/abac'.
    Import Envelope as type-only from '../lib/model' (it is already in the model.ts exports).

    Step 2 — Add InboxEntry and OutboxEntry interfaces immediately before the WorldState interface declaration. Export both. InboxEntry: { seq: number; from: UnitId; subjectId: string; requester: Principal; verifyResult: VerifyResult; detailResult: DetailResult }. OutboxEntry: { seq: number; to: UnitId; subjectId: string; granted: boolean; record: Subject | null }.

    Step 3 — Add six new fields to the WorldState interface after the existing `seq: number` field:
    fedCredentials, fedRunStage (literal union 'IDLE'|'PUBLISHED'|'DISCOVERED'|'REQUESTED'|'RESPONDED'), fedTranscript: Envelope[], fedInbox: Partial<Record<UnitId, InboxEntry[]>>, fedOutbox: Partial<Record<UnitId, OutboxEntry[]>>, fedVerifyResults: { valid: VerifyResult | null; rogue: VerifyResult | null }.

    Step 4 — Extend seedWorld() return object with the six zero values listed in the interfaces block above.

    Step 5 — Append seven new union members to the Action type (per interfaces block above). The FEDERATION_RESPOND member MUST include requester: Principal — this field carries the resolved Principal from the ExchangeTranscriptPanel so the reducer can store it directly in InboxEntry.requester without re-deriving it.

    Step 6 — Add seven new case blocks to the reducer switch BEFORE the default case:

    CREDENTIALS_READY: return { ...state, fedCredentials: { valid: action.valid, rogue: action.rogue } }. No seq advance (bootstrap-only, not a user mutation).

    CREDENTIAL_VERIFY_RESULTS: return { ...state, fedVerifyResults: { valid: action.validResult, rogue: action.rogueResult } }. No seq advance.

    FEDERATION_PUBLISH: build envelope { kind: 'PUBLISH', from: action.from, subjectId: action.subjectId, domain: action.domain }. Return { ...state, fedTranscript: [...state.fedTranscript, envelope], fedRunStage: 'PUBLISHED', seq: state.seq + 1 }.

    FEDERATION_DISCOVER: call buildDiscoverEnvelopes(action.from, action.subjectId, state.hubIndex) to get [discoverEnv, resultEnv]. Return { ...state, fedTranscript: [...state.fedTranscript, discoverEnv, resultEnv], fedRunStage: 'DISCOVERED', seq: state.seq + 1 }.

    FEDERATION_REQUEST_DETAIL: build REQUEST_DETAIL envelope. The requester field on the Envelope must cast to unknown (because model.ts Envelope.requester is typed unknown to avoid circular import): cast as `requester: action.requester as unknown`. Append the request envelope to fedTranscript. Set fedRunStage to 'REQUESTED'. Advance seq. Return updated state.

    FEDERATION_RESPOND: build DETAIL_RESPONSE envelope { kind: 'DETAIL_RESPONSE', to: action.requesterUnit, subjectId: action.subjectId, granted: action.detailResult.granted, decision: action.detailResult.decision as unknown, record: action.detailResult.record }. Build InboxEntry: { seq: state.seq + 1, from: action.requesterUnit, subjectId: action.subjectId, requester: action.requester, verifyResult: action.verifyResult, detailResult: action.detailResult }. Build OutboxEntry: { seq: state.seq + 1, to: action.holderUnit, subjectId: action.subjectId, granted: action.detailResult.granted, record: action.detailResult.record }. Return { ...state, fedTranscript: [...state.fedTranscript, responseEnv], fedRunStage: 'RESPONDED', fedInbox: { ...state.fedInbox, [action.holderUnit]: [...(state.fedInbox[action.holderUnit] ?? []), inboxEntry] }, fedOutbox: { ...state.fedOutbox, [action.requesterUnit]: [...(state.fedOutbox[action.requesterUnit] ?? []), outboxEntry] }, seq: state.seq + 1 }.

    FEDERATION_RESET: D2-06 / Pitfall 7 — ONLY clear fedTranscript and reset fedRunStage to 'IDLE'. Do NOT touch fedInbox or fedOutbox (they are append-only durable history). Return { ...state, fedTranscript: [], fedRunStage: 'IDLE' }.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "world-state" | head -10 || echo "No errors in world-state"</automated>
  </verify>
  <acceptance_criteria>
    - grep "fedRunStage" frontend/src/demo/store/world-state.tsx returns matches (field present)
    - grep "FEDERATION_RESET" frontend/src/demo/store/world-state.tsx returns match in both Action union and reducer case
    - grep -c "fedInbox\|fedOutbox" in the FEDERATION_RESET reducer case returns 0 (inbox/outbox NOT cleared on reset)
    - grep "InboxEntry\|OutboxEntry" frontend/src/demo/store/world-state.tsx shows both exported interfaces
    - grep "buildDiscoverEnvelopes" frontend/src/demo/store/world-state.tsx shows the import and usage in FEDERATION_DISCOVER case
    - seedWorld() return object contains fedCredentials, fedRunStage, fedTranscript, fedInbox, fedOutbox, fedVerifyResults
    - TypeScript compiles without errors (npx tsc --noEmit exits 0 or shows 0 errors for this file)
    - All existing reducer cases (SET_ROLE, SET_TARGET, APPROVE_ATTRIBUTE, REVOKE_ATTRIBUTE, TOGGLE_SECURITY_HOLD, REQUEST_ATTRIBUTE) are unchanged
    - grep "FEDERATION_RESPOND" frontend/src/demo/store/world-state.tsx | grep "requester: Principal" returns a match (the Action union member includes requester: Principal)
    - grep -A3 "FEDERATION_RESPOND" frontend/src/demo/store/world-state.tsx shows InboxEntry built with requester: action.requester (not a placeholder)
  </acceptance_criteria>
  <done>world-state.tsx has six new WorldState fields, two new exported interfaces, seven new Action members (FEDERATION_RESPOND includes requester: Principal), and all seven reducer cases; InboxEntry.requester is populated from action.requester; TypeScript clean; existing cases unmodified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FEDERATION_RESPOND action → InboxEntry.requester | Principal built from verified credential claims must not contain escalated claims from an unverified credential |
| FEDERATION_RESET → fedInbox/fedOutbox | Reset must never clear durable inbox/outbox history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | FEDERATION_RESPOND reducer case — verify-before-trust (D2-10) | mitigate | FEDERATION_RESPOND action carries a pre-resolved verifyResult AND a pre-built requester Principal; the ExchangeTranscriptPanel (plan 02-04) is responsible for building Principal from credential claims only if verifyResult.valid===true before dispatching; the reducer stores action.requester directly in InboxEntry.requester |
| T-02-05 | Tampering | FEDERATION_RESET — inbox/outbox preserve | mitigate | FEDERATION_RESET case only resets fedTranscript + fedRunStage; explicitly does NOT touch fedInbox or fedOutbox (Pitfall 7 guard) |
| T-02-06 | Information Disclosure | WorldState.fedCredentials — credentials stored in reducer | accept | fedCredentials stores [MOCK] HMAC-signed demo credentials; no real keys; demo island only with no persistence or transport |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pure file modification only |
</threat_model>

<verification>
Full typecheck: cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 — expect 0 errors.
Spot-check federation fields: grep -A2 "fedRunStage\|fedTranscript\|fedInbox\|fedOutbox\|fedCredentials\|fedVerifyResults" frontend/src/demo/store/world-state.tsx
Confirm reset safety: grep -A5 "FEDERATION_RESET" frontend/src/demo/store/world-state.tsx — must not contain fedInbox or fedOutbox assignments.
Confirm FEDERATION_RESPOND requester field: grep "FEDERATION_RESPOND" frontend/src/demo/store/world-state.tsx | grep "requester: Principal" — must match.
</verification>

<success_criteria>
- WorldState has all 6 new federation fields
- InboxEntry and OutboxEntry are exported interfaces
- 7 new Action union members present; FEDERATION_RESPOND includes requester: Principal
- 7 new reducer cases present, all correct; InboxEntry.requester = action.requester (not a placeholder)
- FEDERATION_RESET does not clear inbox/outbox
- TypeScript compiles cleanly
</success_criteria>

<output>
Create .planning/phases/02-federation-hub/02-02-SUMMARY.md when done
</output>
