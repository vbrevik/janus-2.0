---
phase: 02-federation-hub
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - frontend/src/demo/DemoRoot.tsx
  - frontend/src/demo/components/FederationHub.tsx
  - frontend/src/demo/components/HubDiscoveryPanel.tsx
autonomous: true
requirements:
  - FED-01

must_haves:
  truths:
    - "Viewer can switch between 'Decision Explorer' and 'Federation Hub' via a two-button toggle; the [DEMO / MOCK] banner and role switcher header remain visible in both views"
    - "Selecting a seeded subject in Hub Discovery shows at least one HubPointer (holdingUnit + domain) with NO clearance, tier, compartment, or decision detail"
    - "The 'What the hub does NOT store' callout is always visible on screen with the four struck-through items"
    - "The interim toggle state is local useState in DemoRoot and is NOT dispatched to the world-state store"
    - "Credentials are signed async at FederationHub mount via a useEffect that dispatches CREDENTIALS_READY"
    - "FederationHub renders null for the three panels not yet built (ExchangeTranscriptPanel, CredentialVerifyPanel, UnitConsolePanel) rather than creating stub files owned by plans 02-04/02-05"
  artifacts:
    - path: "frontend/src/demo/DemoRoot.tsx"
      provides: "activeView local state toggle wiring FederationHub alongside DecisionExplorer"
      contains: "useState<'explorer' | 'federation'>"
    - path: "frontend/src/demo/components/FederationHub.tsx"
      provides: "Top-level scrolling Federation surface with async credential bootstrap"
      exports:
        - FederationHub
    - path: "frontend/src/demo/components/HubDiscoveryPanel.tsx"
      provides: "FED-01 hub query panel with pointer list and 'what hub does NOT store' callout"
      exports:
        - HubDiscoveryPanel
  key_links:
    - from: "frontend/src/demo/DemoRoot.tsx"
      to: "frontend/src/demo/components/FederationHub.tsx"
      via: "conditional render based on activeView state"
      pattern: "activeView === 'federation'"
    - from: "frontend/src/demo/components/FederationHub.tsx"
      to: "frontend/src/demo/lib/credential.ts"
      via: "issueCredential + ISSUER_KEYS in async bootstrap useEffect"
      pattern: "from '../lib/credential'"
    - from: "frontend/src/demo/components/HubDiscoveryPanel.tsx"
      to: "frontend/src/demo/store/world-state.tsx"
      via: "hubIndex + subjects read via useWorld()"
      pattern: "useWorld"
---

<objective>
Wire the interim view toggle into DemoRoot, create the FederationHub top-level component with credential bootstrap, and implement HubDiscoveryPanel (FED-01).

Purpose: FED-01 is the entry point of the federation story — the viewer must be able to reach the Federation surface and query the hub. This plan establishes the surface scaffold that plans 02-04 and 02-05 populate with panels.
Output: DemoRoot with two-button toggle, FederationHub with async credential bootstrap, HubDiscoveryPanel with subject picker + pointer list + "what hub does NOT store" callout.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-UI-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor must use. -->

From world-state.tsx (after plan 02-02):
  useWorld(): WorldState  — returns { subjects, hubIndex, fedCredentials, fedRunStage, fedTranscript, fedInbox, fedOutbox, fedVerifyResults, ... }
  useWorldDispatch(): Dispatch<Action>
  Action: includes CREDENTIALS_READY: { type: 'CREDENTIALS_READY'; valid: Credential; rogue: Credential }

From demo/lib/credential.ts (plan 02-01):
  issueCredential(payload: AttrClaims, issuerSecret: string): Promise<Credential>
  ISSUER_KEYS: Record<string, string>  — keys: 'NATIONAL-CLEARANCE-AUTHORITY', 'ROGUE-ISSUER'

From demo/lib/model.ts:
  unitName(id: UnitId): string
  HubPointer: { subjectId: string; holdingUnit: UnitId; domain: Domain }
  Subject: { id: string; name: string; ... }

From demo/components/ui.tsx:
  Card({ title?, children }), Field({ label, children }), Pill({ children, tone? }), Select<T>({ value, onChange, options }), MockTag()

From demo/components/DemoRoot.tsx (existing file to modify):
  WorldProvider, DemoBanner, RoleSwitcherHeader, DecisionExplorer are already imported
  Structure: WorldProvider > div > DemoBanner + RoleSwitcherHeader + main > DecisionExplorer

Async credential payload for NATIONAL-CLEARANCE-AUTHORITY (use subj-1 Dana Reyes as the valid requester):
  { subject: 'subj-1', entity: 'MILITARY_1', clearance: 'SECRET', compartments: ['AURORA'], issuer: 'NATIONAL-CLEARANCE-AUTHORITY' }
  signed with: ISSUER_KEYS['NATIONAL-CLEARANCE-AUTHORITY']

Async credential payload for ROGUE-ISSUER (use fw5-subj as the rogue fixture):
  { subject: 'fw5-subj', entity: 'INDUSTRY', clearance: 'TOP_SECRET', compartments: ['STOCKWATCH'], issuer: 'ROGUE-ISSUER' }
  signed with: ISSUER_KEYS['ROGUE-ISSUER']

UI-SPEC locked copy (use verbatim):
  Hub Discovery intro: "The hub answers who knows what — which units hold authorization info about a subject, in which domain. It deliberately stores pointers only: no clearance, no tiers, no compartments, no decisions. Details never leave the holding unit."
  "What the hub does NOT store" items (struck-through): "clearance level", "domain tiers", "need-to-know compartments", "the access decision itself"
  Footnote: "To learn any of these, a unit must perform an inter-unit request — and the holder runs its own ABAC policy before releasing anything."
  Empty state: "No unit has published a pointer for this subject."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interim view toggle to DemoRoot (D2-04) and create FederationHub scaffold</name>
  <files>frontend/src/demo/DemoRoot.tsx, frontend/src/demo/components/FederationHub.tsx</files>
  <read_first>
    - frontend/src/demo/DemoRoot.tsx — read the full existing file before editing
    - frontend/src/demo/components/DecisionExplorer.tsx — structure analog for FederationHub composition
    - frontend/src/spikes/components/Spike006Trust.tsx — async credential bootstrap pattern (lines 24-48): async function, let cancelled, Promise.all, dispatch on resolve, cleanup return
    - frontend/src/demo/store/world-state.tsx — useWorld, useWorldDispatch, Action types (after plan 02-02)
  </read_first>
  <action>
    Edit DemoRoot.tsx — surgical additions only:

    Add import { useState } from 'react' to the React imports line.
    Add import { FederationHub } from './components/FederationHub'.
    Add type ActiveView = 'explorer' | 'federation' inside the function body (or at module level — either is fine).
    Add const [activeView, setActiveView] = useState<ActiveView>('explorer') inside DemoRoot().
    Insert a two-button toggle row between RoleSwitcherHeader and main:
      div with className "mx-auto max-w-5xl px-6 py-2 flex gap-2" containing two buttons:
      Button "Decision Explorer": onClick sets activeView to 'explorer'; active = bg-slate-800 text-white; inactive = border border-slate-300 text-slate-600 hover:bg-slate-50
      Button "Federation Hub": onClick sets activeView to 'federation'; same active/inactive treatment.
      Both buttons: className includes "rounded px-3 py-1.5 text-sm" base.
    In main, replace static DecisionExplorer with conditional: activeView === 'explorer' ? DecisionExplorer : FederationHub.
    The toggle is per D2-04 — throwaway, NOT dispatched to the store. Add a comment "// Interim view toggle (D2-04) — throwaway; Phase 4 shell replaces this".

    Create frontend/src/demo/components/FederationHub.tsx:
    Module header: "// FederationHub.tsx — top-level federation surface (D2-03). Single scrolling page: Hub Discovery → Exchange Transcript → Credential Verify → Unit Console."
    Imports: useEffect, useState from 'react'; issueCredential, ISSUER_KEYS from '../lib/credential'; useWorld, useWorldDispatch from '../store/world-state'; HubDiscoveryPanel from './HubDiscoveryPanel'.

    FILE OWNERSHIP NOTE: ExchangeTranscriptPanel, CredentialVerifyPanel, and UnitConsolePanel are owned by plans 02-04 and 02-05 which run in the same Wave 3. Do NOT create stub files for those panels — this would create file-write conflicts during parallel execution. Instead, render null conditionally in FederationHub for those three slots. Plan 02-06 (Wave 4) finalizes FederationHub by replacing the null renders with real imports.

    In FederationHub, implement the credential bootstrap useEffect (D2-09):
    let cancelled = false guard for StrictMode double-invoke.
    Promise.all over two issueCredential calls (one for NATIONAL-CLEARANCE-AUTHORITY subj-1, one for ROGUE-ISSUER fw5-subj — payloads in interfaces block).
    On resolve: if (!cancelled) dispatch({ type: 'CREDENTIALS_READY', valid, rogue }).
    On error: if (!cancelled) set an inline error via useState<string | null>(null) errorMsg state.
    Return cleanup: () => { cancelled = true }.
    Dependency array: [] (run once at mount).

    FederationHub render: div with className "space-y-6" containing:
    1. HubDiscoveryPanel (implemented in Task 2 of this plan)
    2. {null} — placeholder for ExchangeTranscriptPanel (plan 02-04 creates this file)
    3. {null} — placeholder for CredentialVerifyPanel (plan 02-04 creates this file)
    4. {null} — placeholder for UnitConsolePanel (plan 02-05 creates this file)
    If errorMsg is set, render the error before the panels: div className "bg-destructive/10 text-destructive p-4 rounded-md" per CLAUDE.md error convention.

    Add a comment above each null: "// {PanelName} — added by plan 02-0N; plan 02-06 wires the real import"
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -E "DemoRoot|FederationHub" | head -10 || echo "No errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "useState" frontend/src/demo/DemoRoot.tsx shows import and usage
    - grep "activeView" frontend/src/demo/DemoRoot.tsx shows local state, NOT any dispatch call
    - grep "CREDENTIALS_READY" frontend/src/demo/components/FederationHub.tsx shows dispatch in the useEffect handler
    - grep "cancelled" frontend/src/demo/components/FederationHub.tsx shows the StrictMode guard pattern
    - grep -c "from '@tanstack\|routeTree" frontend/src/demo/DemoRoot.tsx frontend/src/demo/components/FederationHub.tsx returns 0
    - grep -c "ExchangeTranscriptPanel\|CredentialVerifyPanel\|UnitConsolePanel" frontend/src/demo/components/FederationHub.tsx returns 0 imports (no stub file creation; only null renders with comments)
    - TypeScript compiles without errors for these files
  </acceptance_criteria>
  <done>DemoRoot has a two-button view toggle; FederationHub scaffolded with async credential bootstrap and null placeholders (no stub files) for the three Wave-3 panels; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Create HubDiscoveryPanel — FED-01 pointer hub with "not stored" callout</name>
  <files>frontend/src/demo/components/HubDiscoveryPanel.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike002Hub.tsx — exact analog (full file); use lines 1-65 as the template
    - frontend/src/demo/store/world-state.tsx — useWorld returns { subjects, hubIndex }
    - frontend/src/demo/components/ui.tsx — Card, Field, Pill, Select components
    - frontend/src/demo/lib/model.ts — unitName(id: UnitId), HubPointer fields: subjectId, holdingUnit, domain
  </read_first>
  <action>
    Create frontend/src/demo/components/HubDiscoveryPanel.tsx.
    Module header: "// HubDiscoveryPanel.tsx — FED-01 discovery without disclosure. Renders hub pointers for a subject with explicit 'hub does NOT store' callout."
    Imports: useState from 'react'; unitName from '../lib/model'; useWorld from '../store/world-state'; Card, Field, Pill, Select from './ui'.

    Export function HubDiscoveryPanel(). Inside:
    Destructure { subjects, hubIndex } from useWorld().
    Local state: const [subjId, setSubjId] = useState(subjects[0]?.id ?? '').
    Derive pointers: hubIndex.filter(p => p.subjectId === subjId).

    Render structure:
    Outer div className "space-y-4":
      Section heading (h2): "Hub Discovery" — text-lg font-semibold.
      Intro paragraph (p): render the locked UI-SPEC intro copy verbatim (see interfaces block).
      Card containing Field with label "Look up a subject in the hub" and a Select with options from subjects.map(s => ({ value: s.id, label: s.name })); onChange calls setSubjId.
      Two-column grid (div className "grid grid-cols-2 gap-4"):
        Left: Card with title "Hub index" — if pointers.length === 0, render empty state p. If pointers > 0, render ul className "space-y-2" with li items; each li: Pill tone="blue" holding unitName(p.holdingUnit); span " holds "; Pill tone="amber" holding p.domain; span " authz info". Use index as key (stable list).
        Right: Card with title "What the hub does NOT store" — ul with four li items each with className "text-sm text-slate-500 line-through": "clearance level", "domain tiers", "need-to-know compartments", "the access decision itself". Below the list, a p with className "mt-3 rounded bg-slate-50 p-2 text-xs text-slate-500" containing the locked footnote copy.

    The pointer row renders ONLY holdingUnit name and domain — no clearance, no tier, no compartments, no decision (FED-01: zero detail from hub). This is the hub-does-not-store invariant.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "HubDiscoveryPanel" | head -5 || echo "No errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "clearance\|compartments\|domainAuth\|decision" frontend/src/demo/components/HubDiscoveryPanel.tsx — must NOT appear as rendered fields (only in comments is fine); the pointer row renders holdingUnit and domain only
    - grep "line-through" frontend/src/demo/components/HubDiscoveryPanel.tsx shows the "not stored" callout items are struck-through
    - grep "useWorld" frontend/src/demo/components/HubDiscoveryPanel.tsx shows it reads from the store (not from direct HUB_INDEX import)
    - grep -c "from '@tanstack\|routeTree" frontend/src/demo/components/HubDiscoveryPanel.tsx returns 0
    - The empty state message "No unit has published a pointer for this subject." appears in the file
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>HubDiscoveryPanel renders subject picker, hub pointer list (unit + domain only), and "what hub does NOT store" callout; FED-01 acceptance criteria satisfied in code</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubDiscoveryPanel render → hub pointer data | Pointer display must expose only holdingUnit and domain — never clearance, tiers, compartments, or the decision itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | HubDiscoveryPanel — pointer row rendering | mitigate | FED-01 invariant: pointer rows render ONLY holdingUnit name and domain; clearance/tier/compartments/decision are never read from hubIndex or subjects in this component's render path |
| T-02-08 | Tampering | DemoRoot activeView — world-state bypass | accept | activeView is local useState only (D2-04); not dispatch-able; cannot affect ABAC decisions or federation state; throwaway interim |
| T-02-09 | Tampering | FederationHub bootstrap useEffect — double-dispatch in StrictMode | mitigate | cancelled flag guard prevents double-dispatch; CREDENTIALS_READY is idempotent in the reducer (overwrites with same values) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pure file creation/modification |
</threat_model>

<verification>
Full typecheck: cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 — expect 0 errors.
FED-01 smoke check: verify hubIndex has entries by grepping seed.ts for HUB_INDEX.push entries (subj-2 should have pointers from plan data).
Confirm no router leakage: grep -r "tanstack/react-router\|routeTree.gen" frontend/src/demo/ — expect no matches.
Confirm no stub files created: ls frontend/src/demo/components/ — ExchangeTranscriptPanel.tsx, CredentialVerifyPanel.tsx, UnitConsolePanel.tsx must NOT exist yet (they are owned by plans 02-04/02-05).
</verification>

<success_criteria>
- DemoRoot: two-button toggle, activeView local useState only, conditional FederationHub render
- FederationHub: async credential bootstrap via useEffect dispatching CREDENTIALS_READY; cancelled guard for StrictMode; null renders (no stub files) for the three panels owned by 02-04/02-05
- HubDiscoveryPanel: subject picker reads from store; pointer list shows only holdingUnit + domain; "not stored" callout with struck-through list always visible
- No @tanstack/react-router imports in any new file
- TypeScript compiles cleanly across all three files
- No ExchangeTranscriptPanel.tsx, CredentialVerifyPanel.tsx, or UnitConsolePanel.tsx files created by this plan
</success_criteria>

<output>
Create .planning/phases/02-federation-hub/02-03-SUMMARY.md when done
</output>
