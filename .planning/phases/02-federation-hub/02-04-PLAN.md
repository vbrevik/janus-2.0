---
phase: 02-federation-hub
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - frontend/src/demo/components/ExchangeTranscriptPanel.tsx
  - frontend/src/demo/components/CredentialVerifyPanel.tsx
autonomous: true
requirements:
  - FED-02
  - FED-03

must_haves:
  truths:
    - "Viewer can step through all four stages (Publish, Discover, Request detail, Respond); each stage button is enabled only on its turn and all four typed envelopes appear in the transcript"
    - "A 'New run' button appears after RESPONDED stage and clears only the transcript (dispatches FEDERATION_RESET)"
    - "The Credential Verify panel shows BOTH the rogue-reject (red/REJECTED/[MOCK]) and valid-accept (green/TRUSTED) outcomes side by side without requiring a user click"
    - "The RESPOND step calls verifyCredential BEFORE any ABAC evaluation; a failed verify means DENY 'credential not verified', never run ABAC on unverified claims (D2-10)"
    - "All typed envelope lines use font-mono text-xs format from envLine(); transcript clears on FEDERATION_RESET but fedInbox/fedOutbox do not"
  artifacts:
    - path: "frontend/src/demo/components/ExchangeTranscriptPanel.tsx"
      provides: "FED-02 four-step typed exchange with interactive stage triggers and transcript render"
      exports:
        - ExchangeTranscriptPanel
    - path: "frontend/src/demo/components/CredentialVerifyPanel.tsx"
      provides: "FED-03 side-by-side rogue-reject and valid-accept credential verification"
      exports:
        - CredentialVerifyPanel
  key_links:
    - from: "frontend/src/demo/components/ExchangeTranscriptPanel.tsx"
      to: "frontend/src/demo/lib/credential.ts"
      via: "verifyCredential called in async handleRespond before building Principal"
      pattern: "await verifyCredential"
    - from: "frontend/src/demo/components/ExchangeTranscriptPanel.tsx"
      to: "frontend/src/demo/lib/contract.ts"
      via: "computeDetailResponse called in handleRespond after verify"
      pattern: "computeDetailResponse"
    - from: "frontend/src/demo/components/CredentialVerifyPanel.tsx"
      to: "frontend/src/demo/store/world-state.tsx"
      via: "fedCredentials read from store; CREDENTIAL_VERIFY_RESULTS dispatched"
      pattern: "CREDENTIAL_VERIFY_RESULTS"
---

<objective>
Build the Exchange Transcript panel (FED-02, four-step state machine) and the Credential Verify panel (FED-03, side-by-side rogue/valid outcomes).

Purpose: FED-02 is the interactive core of the demo — the viewer steps through the typed contract. FED-03 proves verify-before-trust with visible rejection and acceptance outcomes.
Output: ExchangeTranscriptPanel with 4-step stage machine + async RESPOND handler; CredentialVerifyPanel with auto-verify on mount showing both outcomes side by side.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-UI-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-RESEARCH.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor must use. -->

From world-state.tsx (after plan 02-02):
  WorldState: fedRunStage ('IDLE'|'PUBLISHED'|'DISCOVERED'|'REQUESTED'|'RESPONDED'), fedTranscript: Envelope[], fedCredentials: { valid: Credential|null; rogue: Credential|null }, fedVerifyResults: { valid: VerifyResult|null; rogue: VerifyResult|null }, subjects: Subject[]
  Actions:
    FEDERATION_PUBLISH: { type, from: UnitId, subjectId: string, domain: Domain }
    FEDERATION_DISCOVER: { type, from: UnitId, subjectId: string }
    FEDERATION_REQUEST_DETAIL: { type, from: UnitId, to: UnitId, subjectId: string, requester: Principal }
    FEDERATION_RESPOND: { type, verifyResult: VerifyResult, detailResult: DetailResult, requesterUnit: UnitId, holderUnit: UnitId, subjectId: string, requester: Principal }
    FEDERATION_RESET: { type }
    CREDENTIAL_VERIFY_RESULTS: { type, validResult: VerifyResult, rogueResult: VerifyResult }

From demo/lib/credential.ts (plan 02-01):
  verifyCredential(cred: Credential): Promise<VerifyResult>  — checks TRUSTED_ISSUERS first
  VerifyResult: { valid: boolean; reason: string }

From demo/lib/contract.ts (plan 02-01):
  computeDetailResponse(requester: Principal, subject: Subject | undefined, holder: UnitId): DetailResult
  DetailResult: { granted: boolean; decision: Decision | null; record: Subject | null }

From demo/lib/abac.ts:
  Principal: { entity: UnitId; clearance: Clearance; domainAuth: Partial<Record<Domain,string>>; compartments: Compartment[]; flags?: Partial<SubjectFlags> }

From demo/lib/model.ts:
  unitName(id: UnitId): string; UnitId, Domain, Subject, Envelope, Pointer, UNITS

From demo/components/ui.tsx:
  Card, Field, Pill, Select, MockTag, DecisionTrace

Analog files to read:
  Spike005Contract.tsx — envLine helper (lines 22-35), stage button pattern (lines 52-115), transcript render (lines 144-148), full 4-step structure
  Spike006Trust.tsx — auto-verify useEffect pattern (lines 24-48), side-by-side card layout (lines 129-142)

UI-SPEC locked copy (verbatim):
  Exchange Transcript intro: "Units never call each other directly — every interaction is a typed envelope routed through the hub. Step the contract in motion: PUBLISH → DISCOVER → REQUEST → RESPONSE."
  Stage button labels: "Publish", "Discover", "Request detail", "Respond", "New run"
  Transcript empty state: "No exchange yet. Press Publish to start a run."
  Credential Verify intro: "A requester's attributes ride in a signed credential the holder verifies before trusting any claim. A forged or untrusted-issuer credential is rejected; a valid trusted-issuer credential passes and unlocks evaluation. Both shown side by side."
  Valid outcome card verdict: "✓ TRUSTED" (green)
  Rogue outcome card verdict: "✗ REJECTED" (red); footnote: "The holder will not evaluate ABAC on claims it cannot verify."
  Loading state: "Signing credentials…"

Default exchange selection (UI-SPEC interaction contract — guaranteed-ALLOW triple):
  requesterUnit default: 'MILITARY_1' (has agreement with MILITARY_2 per seed.ts:14)
  holderUnit default: 'MILITARY_2'
  subjectId default: 'subj-2' (Sam Okafor, held by MILITARY_2)
  domain default: 'DATA'
  These defaults provide a guaranteed-ALLOW path for FED-04 (MILITARY_1↔MILITARY_2 have agreement, subj-2 is TOP_SECRET with full tiers)
  The user can change all four via dropdowns to reach a guaranteed-DENY (e.g. MILITARY_1 requesting from INTEL — no agreement)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExchangeTranscriptPanel — FED-02 four-step interactive transcript</name>
  <files>frontend/src/demo/components/ExchangeTranscriptPanel.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike005Contract.tsx — exact analog; read full file (lines 1-177): envLine helper, stage machine, button disabled logic, transcript render
    - frontend/src/demo/store/world-state.tsx — WorldState shape, Action types (after plan 02-02)
    - frontend/src/demo/lib/contract.ts — computeDetailResponse signature (plan 02-01)
    - frontend/src/demo/lib/credential.ts — verifyCredential signature (plan 02-01)
    - frontend/src/demo/lib/abac.ts — Principal interface (lines 34-40)
    - frontend/src/demo/lib/model.ts — UNITS, unitName, UnitId, Domain, Envelope, Subject
    - frontend/src/demo/components/ui.tsx — Card, Field, Pill, Select
    - frontend/src/spikes/components/Spike006Trust.tsx — async verify pattern in onClick handler (lines 44-48)
  </read_first>
  <action>
    Create frontend/src/demo/components/ExchangeTranscriptPanel.tsx.
    Module header: "// ExchangeTranscriptPanel.tsx — FED-02 four-step typed contract exchange. Stage machine: IDLE→PUBLISHED→DISCOVERED→REQUESTED→RESPONDED."

    Imports: useState from 'react'; unitName, UNITS, type UnitId, type Domain, type Envelope from '../lib/model'; type Principal from '../lib/abac'; computeDetailResponse, type DetailResult from '../lib/contract'; verifyCredential from '../lib/credential'; useWorld, useWorldDispatch from '../store/world-state'; Card, Field, Pill, Select from './ui'.

    envLine helper function (not exported; module-local): switch on e.kind and return formatted string per UI-SPEC:
      'PUBLISH': "PUBLISH  ${unitName(e.from)} → hub: holds ${e.subjectId} (${e.domain})"
      'DISCOVER': "DISCOVER ${unitName(e.from)} → hub: who holds ${e.subjectId}?"
      'DISCOVER_RESULT': "RESULT   hub → ${unitName(e.to)}: [${e.pointers.map(p=>unitName(p.holder)).join(', ')||'none'}]"
      'REQUEST_DETAIL': "REQUEST  ${unitName(e.from)} → ${unitName(e.to)}: detail for ${e.subjectId}"
      'DETAIL_RESPONSE': "RESPONSE → ${unitName(e.to)}: ${e.granted?'RELEASED':'WITHHELD'}"

    Export function ExchangeTranscriptPanel():
    Destructure { fedRunStage, fedTranscript, fedCredentials, subjects } from useWorld().
    const dispatch = useWorldDispatch().

    Local state (dropdowns — view-selection only, NOT dispatched for PUBLISH/DISCOVER/REQUEST pickers):
      [requesterUnit, setRequesterUnit] = useState<UnitId>('MILITARY_1')
      [holderUnit, setHolderUnit] = useState<UnitId>('MILITARY_2')
      [subjectId, setSubjId] = useState('subj-2')
      [domain, setDomain] = useState<Domain>('DATA')
      [isSending, setIsSending] = useState(false) — busy guard for the async RESPOND button

    unitIds: (Object.keys(UNITS) as UnitId[]). domains: ['COMPUTER', 'DATA', 'PHYSICAL'] as Domain[].

    Async handleRespond function:
      Guard: if (!fedCredentials.valid || isSending) return.
      setIsSending(true).
      try:
        const verifyResult = await verifyCredential(fedCredentials.valid).
        // D2-10: build Principal ONLY if verifyResult.valid === true
        const principal: Principal = verifyResult.valid
          ? { entity: fedCredentials.valid.payload.entity, clearance: fedCredentials.valid.payload.clearance, domainAuth: {}, compartments: fedCredentials.valid.payload.compartments }
          : { entity: requesterUnit, clearance: 'UNCLASSIFIED', compartments: [], domainAuth: {} }.
        const subject = subjects.find(s => s.id === subjectId).
        const detailResult = computeDetailResponse(principal, subject, holderUnit).
        dispatch({ type: 'FEDERATION_RESPOND', verifyResult, detailResult, requesterUnit, holderUnit, subjectId, requester: principal }).
      finally: setIsSending(false).

    Stage button helper: a button renders as solid-slate (bg-slate-800 text-white) when it is the current stage's button (enabled), and disabled:opacity-40 with border when not. Enabled conditions:
      "Publish": disabled={fedRunStage !== 'IDLE'}; onClick: dispatch({ type: 'FEDERATION_PUBLISH', from: requesterUnit, subjectId, domain })
      "Discover": disabled={fedRunStage !== 'PUBLISHED'}; onClick: dispatch({ type: 'FEDERATION_DISCOVER', from: requesterUnit, subjectId })
      "Request detail": disabled={fedRunStage !== 'DISCOVERED'}; onClick: dispatch({ type: 'FEDERATION_REQUEST_DETAIL', from: requesterUnit, to: holderUnit, subjectId, requester: { entity: requesterUnit, clearance: 'UNCLASSIFIED', compartments: [], domainAuth: {} } })
        Note: the requester passed in FEDERATION_REQUEST_DETAIL is a minimal/pre-verify principal — the real verified principal is computed in handleRespond. The InboxEntry.requester will come from the FEDERATION_RESPOND dispatch which carries the resolved requester.
      "Respond": disabled={fedRunStage !== 'REQUESTED' || isSending}; onClick: handleRespond()
      "New run": shown only when fedRunStage === 'RESPONDED'; onClick: dispatch({ type: 'FEDERATION_RESET' }); style: border border-slate-300 text-slate-600

    All stage trigger buttons: base className "rounded px-3 py-1.5 text-sm"; active = "bg-slate-800 text-white"; inactive/disabled = "border border-slate-300 text-slate-400 disabled:opacity-40".

    Render structure:
    div className "space-y-4":
      h2 "Exchange Transcript" — text-lg font-semibold.
      p with locked intro copy (verbatim from interfaces block).
      Card with config selectors (four Fields with Selects: requester unit, holder unit, subject, domain). Only enabled when fedRunStage === 'IDLE' so the exchange parameters are locked in during the run.
      Card title "Stage triggers" containing a div className "flex gap-2 flex-wrap" with the four stage buttons + optional "New run".
      Card title "Transcript (typed envelopes)": if fedTranscript.length === 0, render p empty state; else ul className "space-y-0.5 font-mono text-xs" with li items: fedTranscript.map((e, i) => li key={i} className="text-slate-600" containing envLine(e)).
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "ExchangeTranscript" | head -5 || echo "No errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "await verifyCredential" frontend/src/demo/components/ExchangeTranscriptPanel.tsx shows the verify-before-trust call in handleRespond
    - grep "verifyResult.valid" frontend/src/demo/components/ExchangeTranscriptPanel.tsx shows the conditional Principal construction (D2-10)
    - grep "'FEDERATION_RESET'" frontend/src/demo/components/ExchangeTranscriptPanel.tsx shows the "New run" button dispatch
    - grep "font-mono text-xs" frontend/src/demo/components/ExchangeTranscriptPanel.tsx shows the transcript ul styling
    - The envLine function handles all 5 envelope kinds: grep -c "case 'PUBLISH'\|case 'DISCOVER'\|case 'DISCOVER_RESULT'\|case 'REQUEST_DETAIL'\|case 'DETAIL_RESPONSE'" returns 5
    - grep "isSending" frontend/src/demo/components/ExchangeTranscriptPanel.tsx shows the busy guard for the RESPOND button
    - grep -c "from '@tanstack\|routeTree" frontend/src/demo/components/ExchangeTranscriptPanel.tsx returns 0
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ExchangeTranscriptPanel: 4-step stage machine with enabled-only-on-turn buttons, async handleRespond with verify-before-trust, font-mono transcript, "New run" dispatches FEDERATION_RESET; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Create CredentialVerifyPanel — FED-03 side-by-side rogue/valid outcomes</name>
  <files>frontend/src/demo/components/CredentialVerifyPanel.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike006Trust.tsx — exact analog for auto-verify pattern (lines 24-48) and side-by-side card layout (lines 129-142)
    - frontend/src/demo/store/world-state.tsx — WorldState.fedCredentials, WorldState.fedVerifyResults, Action CREDENTIAL_VERIFY_RESULTS
    - frontend/src/demo/lib/credential.ts — verifyCredential, VerifyResult
    - frontend/src/demo/components/ui.tsx — Card, MockTag
  </read_first>
  <action>
    Create frontend/src/demo/components/CredentialVerifyPanel.tsx.
    Module header: "// CredentialVerifyPanel.tsx — FED-03 signed-credential verification. Auto-verifies both credentials on mount (D2-09). Side-by-side rogue-reject and valid-accept."

    Imports: useEffect from 'react'; verifyCredential from '../lib/credential'; useWorld, useWorldDispatch from '../store/world-state'; Card, MockTag from './ui'.

    Export function CredentialVerifyPanel():
    Destructure { fedCredentials, fedVerifyResults } from useWorld().
    const dispatch = useWorldDispatch().

    Auto-verify useEffect (D2-09 — fires when fedCredentials becomes populated after CREDENTIALS_READY):
      if (!fedCredentials.valid || !fedCredentials.rogue) return early.
      let alive = true.
      Promise.all([verifyCredential(fedCredentials.valid), verifyCredential(fedCredentials.rogue)])
        .then(([validResult, rogueResult]) => {
          if (alive) dispatch({ type: 'CREDENTIAL_VERIFY_RESULTS', validResult, rogueResult });
        });
      return () => { alive = false; }.
      Dependency array: [fedCredentials.valid, fedCredentials.rogue] — re-fires if credentials change (idempotent).

    Loading state: if fedCredentials.valid is null, render Card with p className "text-sm text-slate-400" containing locked loading copy "Signing credentials…".

    After credentials are ready but before verify results arrive (fedVerifyResults.valid === null): render the loading Card still.

    Once both fedVerifyResults.valid and fedVerifyResults.rogue are non-null, render the full panel:
    div className "space-y-4":
      h2 "Credential Verification" — text-lg font-semibold.
      p with locked intro copy (verbatim from interfaces block).
      Side-by-side grid: div className "grid grid-cols-2 gap-4":
        Left card — rogue (red): div className "rounded-lg border border-red-200 bg-red-50 p-4 space-y-2":
          div className "font-bold text-xl": "✗ REJECTED"
          div className "text-sm text-slate-600": {fedVerifyResults.rogue.reason}
          div: MockTag component (renders "[MOCK]")
          div className "text-xs text-slate-500 mt-1": locked footnote "The holder will not evaluate ABAC on claims it cannot verify."
        Right card — valid (green): div className "rounded-lg border border-green-200 bg-green-50 p-4 space-y-2":
          div className "font-bold text-xl": "✓ TRUSTED"
          div className "text-sm text-slate-600": {fedVerifyResults.valid.reason}
          div className "text-xs text-slate-500 mt-1": "Evaluation proceeds — ABAC will compute release using these verified claims."

    The [MOCK] tag appears ONLY on the rogue/rejected card (D2-10), not on the valid/trusted card.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "CredentialVerify" | head -5 || echo "No errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CREDENTIAL_VERIFY_RESULTS" frontend/src/demo/components/CredentialVerifyPanel.tsx shows the dispatch in the auto-verify useEffect
    - grep "alive" frontend/src/demo/components/CredentialVerifyPanel.tsx shows the cleanup guard in the useEffect
    - grep "REJECTED\|TRUSTED" frontend/src/demo/components/CredentialVerifyPanel.tsx shows both verdict strings
    - grep "MockTag" frontend/src/demo/components/CredentialVerifyPanel.tsx shows MockTag is rendered exactly once (on the rogue/rejected card only)
    - grep "border-red-200\|bg-red-50" frontend/src/demo/components/CredentialVerifyPanel.tsx shows the rogue card's red styling
    - grep "border-green-200\|bg-green-50" frontend/src/demo/components/CredentialVerifyPanel.tsx shows the valid card's green styling
    - grep -c "from '@tanstack\|routeTree" frontend/src/demo/components/CredentialVerifyPanel.tsx returns 0
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>CredentialVerifyPanel auto-verifies both credentials when populated; shows rogue-reject (red/[MOCK]) and valid-accept (green) side by side; loading state for signing phase; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handleRespond → ABAC evaluation | verifyCredential must resolve valid===true before a full Principal is built; a failed verify must produce a minimal/denied Principal |
| FEDERATION_RESPOND dispatch → InboxEntry | requester Principal in the dispatch must not carry escalated claims from an unverified credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Elevation of Privilege | ExchangeTranscriptPanel.handleRespond — verify-before-trust (D2-10) | mitigate | verifyCredential called before computeDetailResponse; Principal built from credential claims only if verifyResult.valid===true; otherwise UNCLASSIFIED principal is used (which will DENY in ABAC) |
| T-02-11 | Spoofing | CredentialVerifyPanel — auto-verify on mount | mitigate | useEffect keyed on fedCredentials (set by CREDENTIALS_READY); both ROGUE-ISSUER and valid-issuer credentials are always verified and displayed; rogue path is always shown with [MOCK] label (MODEL-03) |
| T-02-12 | Information Disclosure | ExchangeTranscriptPanel stage triggers — FEDERATION_REQUEST_DETAIL sends minimal principal | accept | The pre-verify principal sent in FEDERATION_REQUEST_DETAIL is intentionally minimal/UNCLASSIFIED; the real verified principal is computed and dispatched in FEDERATION_RESPOND — this is the correct two-step flow (request records intent, respond records verified decision) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pure file creation |
</threat_model>

<verification>
Full typecheck: cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 — expect 0 errors.
Verify verify-before-trust order: grep -A10 "handleRespond" frontend/src/demo/components/ExchangeTranscriptPanel.tsx — verifyCredential must appear before computeDetailResponse.
Confirm MockTag on rogue only: grep -B2 "MockTag" frontend/src/demo/components/CredentialVerifyPanel.tsx — must appear in the rogue/rejected block only.
</verification>

<success_criteria>
- ExchangeTranscriptPanel: 4-step stage machine; async handleRespond with verify-before-trust; "New run" dispatches FEDERATION_RESET; font-mono transcript render
- CredentialVerifyPanel: auto-verify useEffect; side-by-side red/green verdict cards; [MOCK] tag on rogue only; loading state
- No @tanstack/react-router in either file
- TypeScript compiles cleanly
</success_criteria>

<output>
Create .planning/phases/02-federation-hub/02-04-SUMMARY.md when done
</output>
