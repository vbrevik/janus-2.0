---
phase: 02-federation-hub
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/contract.ts
  - frontend/src/demo/lib/credential.ts
autonomous: true
requirements:
  - FED-02
  - FED-03
  - FED-04

must_haves:
  truths:
    - "demo/lib/contract.ts exports computeDetailResponse, buildPublishEnvelope, buildDiscoverEnvelopes as pure functions with no class definitions and no internal state arrays"
    - "demo/lib/credential.ts exports issueCredential, verifyCredential, ISSUER_KEYS, TRUSTED_ISSUERS, VerifyResult — lifted verbatim with UnitId re-key"
    - "Neither file imports from @tanstack/react-router or the generated route tree"
    - "computeDetailResponse enforces the record-hold force-DENY (D2-08) before returning a DetailResult"
  artifacts:
    - path: "frontend/src/demo/lib/contract.ts"
      provides: "Pure helper functions for the typed interchange contract"
      exports:
        - computeDetailResponse
        - buildPublishEnvelope
        - buildDiscoverEnvelopes
        - DetailResult
    - path: "frontend/src/demo/lib/credential.ts"
      provides: "Async HMAC credential issue/verify, trusted-issuer registry"
      exports:
        - issueCredential
        - verifyCredential
        - ISSUER_KEYS
        - TRUSTED_ISSUERS
        - VerifyResult
  key_links:
    - from: "frontend/src/demo/lib/contract.ts"
      to: "frontend/src/demo/lib/abac.ts"
      via: "evaluate + releaseRequirementFor imports"
      pattern: "from './abac'"
    - from: "frontend/src/demo/lib/credential.ts"
      to: "frontend/src/demo/lib/model.ts"
      via: "AttrClaims + Credential type imports"
      pattern: "from './model'"
---

<objective>
Lift the pure federation helper logic from the spike libraries into the demo island as stateless utility modules.

Purpose: These two files are the dependency-free foundation every downstream wave depends on. Extracting them first lets Waves 2 and 3 import them without circular risk.
Output: demo/lib/contract.ts (pure sync helpers) and demo/lib/credential.ts (async HMAC helpers) — no classes, no internal state, no router imports.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-SPEC.md

<interfaces>
<!-- Key types from source files the executor must use. -->

From frontend/src/demo/lib/model.ts (lines 246-291):
- Envelope: discriminated union — 'PUBLISH' | 'DISCOVER' | 'DISCOVER_RESULT' | 'REQUEST_DETAIL' | 'DETAIL_RESPONSE'
- Pointer: { holder: UnitId; domain: Domain }
- HubPointer: { subjectId: string; holdingUnit: UnitId; domain: Domain }
- AttrClaims: { subject: string; entity: UnitId; clearance: Clearance; compartments: Compartment[]; issuer: string }
- Credential: { payload: AttrClaims; sig: string }
- NOTE: Envelope.requester is typed `unknown` to avoid circular import — this is intentional; do NOT change model.ts

From frontend/src/demo/lib/abac.ts (lines 34-48, 61, 161):
- Principal: { entity: UnitId; clearance: Clearance; domainAuth: Partial<Record<Domain,string>>; compartments: Compartment[]; flags?: Partial<SubjectFlags> }
- Decision: { decision: 'ALLOW'|'DENY'; rules: Rule[]; overrides: Rule[]; failed: string[] }
- evaluate(principal: Principal, req: Requirement): Decision
- releaseRequirementFor(subject: Subject, holder: UnitId): Requirement

From frontend/src/spikes/lib/contract.ts (lines 47-52, 113-140 — pure logic to extract):
- DetailResult: { granted: boolean; decision: Decision | null; record: Subject | null }
- Network.requestDetail() lines 100-151 — extract ONLY the pure computation (no class state)
- Network.discover() lines 86-98 — extract ONLY the envelope building (no this.pointers push)
- Network.publishAll() lines 70-83 — extract ONLY the envelope building (no this.transcript push)
- D2-08 record-hold override is in lines 120-134

From frontend/src/spikes/lib/credential.ts (full file — lift verbatim except UnitId re-key):
- canonical(), toBase64(), importKey(), sign() — copy verbatim (lines 19-51)
- issueCredential, verifyCredential, ISSUER_KEYS, TRUSTED_ISSUERS, VerifyResult — copy verbatim (lines 53-90)
- Only change: AttrClaims.entity type is UnitId (already the case in model.ts:283) — import AttrClaims from './model' instead of defining it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo/lib/contract.ts — pure interchange helpers</name>
  <files>frontend/src/demo/lib/contract.ts</files>
  <read_first>
    - frontend/src/spikes/lib/contract.ts — the source to extract pure logic from (lines 1-52 for types, 70-151 for method bodies to extract as pure functions)
    - frontend/src/demo/lib/abac.ts — imports: evaluate, releaseRequirementFor, Decision, Principal, Requirement
    - frontend/src/demo/lib/model.ts — imports: UnitId, Domain, Subject, Envelope, Pointer, HubPointer
  </read_first>
  <action>
    Create frontend/src/demo/lib/contract.ts. Module header comment: "// demo/lib/contract.ts — pure interchange helpers lifted from spikes/lib/contract.ts. Network class NOT ported (D2-02)."

    Imports: UnitId, Domain, Subject, Envelope, Pointer from './model'; evaluate, releaseRequirementFor, Decision, Principal from './abac'.

    Export interface DetailResult: { granted: boolean; decision: Decision | null; record: Subject | null }.

    Export function buildPublishEnvelope(from: UnitId, subjectId: string, domain: Domain): Envelope — returns { kind: 'PUBLISH', from, subjectId, domain }. Source: spike Network.publishAll() body (lines 72-75).

    Export function buildDiscoverEnvelopes(from: UnitId, subjectId: string, hubIndex: HubPointer[]): [Envelope, Envelope] — filters hubIndex by subjectId, maps to Pointer[] ({ holder: p.holdingUnit, domain: p.domain }), returns DISCOVER envelope and DISCOVER_RESULT envelope as a tuple. Import HubPointer from './model'. Source: spike Network.discover() body (lines 87-98).

    Export function computeDetailResponse(requester: Principal, subject: Subject | undefined, holder: UnitId): DetailResult — implements D2-08 record-hold force-DENY. If !subject return { granted: false, decision: null, record: null }. Compute base = evaluate(requester, releaseRequirementFor(subject, holder)). Check blocked = subject.flags.securityHold || subject.flags.revoked. If blocked, spread base and override decision to 'DENY' with an additional override rule { name: 'Record hold', pass: false, detail: 'target record is held/revoked' }. Return { granted: decision.decision === 'ALLOW', decision, record: decision.decision === 'ALLOW' ? subject : null }. Source: spike Network.requestDetail() lines 113-140.

    The Network class (spike lines 54-152) is explicitly NOT ported. No class definitions, no internal arrays or Maps, no this references.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "demo/lib/contract" || echo "0 errors in contract.ts"</automated>
  </verify>
  <acceptance_criteria>
    - demo/lib/contract.ts exports computeDetailResponse, buildPublishEnvelope, buildDiscoverEnvelopes, and DetailResult
    - grep -c "class Network" frontend/src/demo/lib/contract.ts returns 0
    - grep -c "from '@tanstack" frontend/src/demo/lib/contract.ts returns 0
    - computeDetailResponse: when subject.flags.securityHold is true, returned detailResult.granted is false and detailResult.decision.overrides contains an entry with name 'Record hold'
    - buildDiscoverEnvelopes: returns a tuple of exactly 2 Envelopes; first has kind 'DISCOVER', second has kind 'DISCOVER_RESULT' with pointers array
    - TypeScript compiles without errors in this file
  </acceptance_criteria>
  <done>demo/lib/contract.ts exists with 3 exported pure functions and 1 exported interface; no class definitions; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Create demo/lib/credential.ts — async HMAC credential helpers</name>
  <files>frontend/src/demo/lib/credential.ts</files>
  <read_first>
    - frontend/src/spikes/lib/credential.ts — lift verbatim (full file, all 90 lines)
    - frontend/src/demo/lib/model.ts — AttrClaims (line 280-286) and Credential (line 288-291) are already declared here; import from there instead of redefining
  </read_first>
  <action>
    Create frontend/src/demo/lib/credential.ts. Module header: "// demo/lib/credential.ts — async HMAC credential helpers lifted from spikes/lib/credential.ts. UnitId re-key only (D2-02)."

    Import AttrClaims, Credential from './model' (do NOT redefine these — they are already in model.ts:280-291 with UnitId already applied).

    Copy verbatim from the spike (lines 19-51): canonical(p: AttrClaims): string, toBase64(buf: ArrayBuffer): string, importKey(secret: string): Promise<CryptoKey>, sign(payload: AttrClaims, secret: string): Promise<string>.

    Copy verbatim (lines 53-57): export async function issueCredential(payload: AttrClaims, issuerSecret: string): Promise<Credential>.

    Copy verbatim (lines 60-65): export const ISSUER_KEYS: Record<string, string> with keys 'NATIONAL-CLEARANCE-AUTHORITY' and 'ROGUE-ISSUER'. Export const TRUSTED_ISSUERS = ['NATIONAL-CLEARANCE-AUTHORITY'].

    Copy verbatim (lines 67-70): export interface VerifyResult { valid: boolean; reason: string }.

    Copy verbatim (lines 72-90): export async function verifyCredential(cred: Credential): Promise<VerifyResult> — checks TRUSTED_ISSUERS first (untrusted issuer path returns early), checks ISSUER_KEYS, then verifies HMAC signature.

    Do NOT import EntityId from anywhere — AttrClaims.entity is already UnitId in model.ts. Remove any AttrClaims or Credential type definitions that would duplicate model.ts.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "demo/lib/credential" || echo "0 errors in credential.ts"</automated>
  </verify>
  <acceptance_criteria>
    - demo/lib/credential.ts exports issueCredential, verifyCredential, ISSUER_KEYS, TRUSTED_ISSUERS, VerifyResult
    - grep -c "interface AttrClaims\|interface Credential" frontend/src/demo/lib/credential.ts returns 0 (types imported from model.ts, not re-declared)
    - grep "TRUSTED_ISSUERS" frontend/src/demo/lib/credential.ts shows only 'NATIONAL-CLEARANCE-AUTHORITY' in the array
    - grep "'ROGUE-ISSUER'" frontend/src/demo/lib/credential.ts shows ROGUE-ISSUER in ISSUER_KEYS (present so signing works) but NOT in TRUSTED_ISSUERS
    - grep -c "from '@tanstack" frontend/src/demo/lib/credential.ts returns 0
    - TypeScript compiles without errors in this file
  </acceptance_criteria>
  <done>demo/lib/credential.ts exists with all async crypto exports; AttrClaims/Credential imported from model.ts; ROGUE-ISSUER is in ISSUER_KEYS but not TRUSTED_ISSUERS; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifyCredential caller → credential claims | Caller must not use credential.payload claims before verifyCredential resolves valid===true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | computeDetailResponse — record-hold override | mitigate | D2-08: blocked = subject.flags.securityHold || subject.flags.revoked forces DENY override before returning DetailResult; implemented in this task |
| T-02-02 | Elevation of Privilege | verifyCredential — ROGUE-ISSUER in ISSUER_KEYS | mitigate | ROGUE-ISSUER is in ISSUER_KEYS (key exists for signing demo) but NOT in TRUSTED_ISSUERS; verifyCredential returns valid:false for any issuer not in TRUSTED_ISSUERS before checking the key |
| T-02-03 | Information Disclosure | demo/lib/contract.ts — Network class not ported | mitigate | D2-02: only pure stateless functions exported; no internal pointers/records Maps that could be mutated by components |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; pure file creation only |
</threat_model>

<verification>
After both tasks: cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "demo/lib/c" — expect zero errors.
Confirm exports: grep "^export" frontend/src/demo/lib/contract.ts and grep "^export" frontend/src/demo/lib/credential.ts list the expected functions and types.
</verification>

<success_criteria>
- demo/lib/contract.ts: pure-only exports (computeDetailResponse, buildPublishEnvelope, buildDiscoverEnvelopes, DetailResult); zero class definitions
- demo/lib/credential.ts: async crypto exports (issueCredential, verifyCredential, ISSUER_KEYS, TRUSTED_ISSUERS, VerifyResult); AttrClaims/Credential imported not redefined
- Both files: no @tanstack/react-router import; no new runtime packages
- TypeScript compiles both files without errors
</success_criteria>

<output>
Create .planning/phases/02-federation-hub/02-01-SUMMARY.md when done
</output>
