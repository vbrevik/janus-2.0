---
phase: 02-federation-hub
plan: "06"
type: execute
wave: 4
depends_on:
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - frontend/src/demo/components/FederationHub.tsx
autonomous: true
requirements:
  - FED-01
  - FED-02
  - FED-03
  - FED-04

must_haves:
  truths:
    - "Running npm run build (Vite production build) exits 0 with the demo.html entry included"
    - "TypeScript compiles the entire frontend without errors (npx tsc --noEmit exits 0)"
    - "No file under frontend/src/demo/ imports @tanstack/react-router or routeTree.gen"
    - "FederationHub wires all four panels (HubDiscoveryPanel, ExchangeTranscriptPanel, CredentialVerifyPanel, UnitConsolePanel) replacing any plan-02-03 stubs"
    - "All 4 FED-01..FED-04 acceptance criteria from 02-SPEC.md are satisfiable in the running demo (smoke-checked by wiring review)"
  artifacts:
    - path: "frontend/src/demo/components/FederationHub.tsx"
      provides: "Fully wired Federation surface with all four panels composed in order"
      contains: "HubDiscoveryPanel ExchangeTranscriptPanel CredentialVerifyPanel UnitConsolePanel"
  key_links:
    - from: "frontend/src/demo/components/FederationHub.tsx"
      to: "frontend/src/demo/components/ExchangeTranscriptPanel.tsx"
      via: "import and render"
      pattern: "ExchangeTranscriptPanel"
    - from: "frontend/src/demo/components/FederationHub.tsx"
      to: "frontend/src/demo/components/CredentialVerifyPanel.tsx"
      via: "import and render"
      pattern: "CredentialVerifyPanel"
    - from: "frontend/src/demo/components/FederationHub.tsx"
      to: "frontend/src/demo/components/UnitConsolePanel.tsx"
      via: "import and render"
      pattern: "UnitConsolePanel"
---

<objective>
Wire all four Federation panels into FederationHub (replacing plan-02-03 stubs), then run TypeScript and Vite build checks to confirm the phase is complete and shippable.

Purpose: Plans 02-03 through 02-05 may have used placeholder stubs for panels not yet created; this plan replaces them with real imports and runs the full build gate.
Output: Fully wired FederationHub.tsx; clean TypeScript; green Vite production build.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/projects/janus-2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-03-SUMMARY.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-04-SUMMARY.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/02-federation-hub/02-05-SUMMARY.md

<interfaces>
<!-- Files that must exist after plans 02-03..02-05 -->
frontend/src/demo/components/HubDiscoveryPanel.tsx — exports HubDiscoveryPanel
frontend/src/demo/components/ExchangeTranscriptPanel.tsx — exports ExchangeTranscriptPanel
frontend/src/demo/components/CredentialVerifyPanel.tsx — exports CredentialVerifyPanel
frontend/src/demo/components/UnitConsolePanel.tsx — exports UnitConsolePanel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all four panels in FederationHub and replace any stubs</name>
  <files>frontend/src/demo/components/FederationHub.tsx</files>
  <read_first>
    - frontend/src/demo/components/FederationHub.tsx — the current file (from plan 02-03; may have stubs)
    - frontend/src/demo/components/ExchangeTranscriptPanel.tsx — confirm exported symbol name
    - frontend/src/demo/components/CredentialVerifyPanel.tsx — confirm exported symbol name
    - frontend/src/demo/components/UnitConsolePanel.tsx — confirm exported symbol name
  </read_first>
  <action>
    Edit FederationHub.tsx:
    Replace any stub/null/placeholder renders for ExchangeTranscriptPanel, CredentialVerifyPanel, UnitConsolePanel with real imports from their respective files.
    Ensure the four panels render in this exact order (D2-03 scrolling surface contract):
      1. HubDiscoveryPanel
      2. ExchangeTranscriptPanel
      3. CredentialVerifyPanel
      4. UnitConsolePanel
    The outer wrapper is div className "space-y-6" (lg spacing between panels per UI-SPEC).
    Async credential bootstrap useEffect from plan 02-03 must remain unchanged.
    Do NOT add any @tanstack/react-router imports.
    Confirm the error boundary for crypto.subtle failure remains (errorMsg state + inline destructive div).
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1; echo "TSC exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "HubDiscoveryPanel\|ExchangeTranscriptPanel\|CredentialVerifyPanel\|UnitConsolePanel" frontend/src/demo/components/FederationHub.tsx shows all four imports and renders
    - grep -c "null\|placeholder\|stub\|TODO" frontend/src/demo/components/FederationHub.tsx in the return JSX returns 0 (no remaining stubs)
    - TypeScript compile: npx tsc --noEmit --project tsconfig.app.json exits 0
  </acceptance_criteria>
  <done>FederationHub renders all four real panels in the correct order; no stubs remain; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Demo-island isolation audit and Vite production build gate</name>
  <files></files>
  <read_first>
    - frontend/vite.config.ts — confirm demo.html is still registered as an input entry
    - frontend/src/demo/DemoRoot.tsx — confirm no router imports leaked in
  </read_first>
  <action>
    Run the following checks in order. Fix any issues found before declaring done.

    Check 1 — Router isolation (SPEC acceptance criterion line 73):
    grep -r "tanstack/react-router\|routeTree.gen" frontend/src/demo/
    Expected: zero matches. If any found: remove the offending import and replace with a demo-safe alternative.

    Check 2 — No new runtime dependencies:
    diff <(cd frontend && cat package.json | node -e "const d=require('/dev/stdin'); console.log(Object.keys({...d.dependencies,...d.devDependencies}).sort().join('\n'))") <(git show HEAD:frontend/package.json 2>/dev/null | node -e "const d=require('/dev/stdin'); console.log(Object.keys({...d.dependencies,...d.devDependencies}).sort().join('\n'))" 2>/dev/null) || echo "No diff available — check manually that package.json is unchanged"
    Expected: no new packages. If any added: warn and document (they should not exist — this phase adds zero new deps).

    Check 3 — TypeScript full compile:
    cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json
    Expected: exit 0. Fix all TypeScript errors before marking done.

    Check 4 — Vite production build:
    cd /Users/vidarbrevik/projects/janus-2.0/frontend && npm run build
    Expected: exit 0 with demo.html in the build output (dist/). If build fails: read the error, fix the root cause, and rerun.

    Check 5 — Existing tests still pass:
    cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run --exclude "**/*.e2e.*" --exclude "**/playwright/**"
    Expected: all existing tests pass; no regressions from the new demo files.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npm run build 2>&1; echo "BUILD EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "tanstack/react-router\|routeTree.gen" frontend/src/demo/ returns zero matches
    - npx tsc --noEmit --project tsconfig.app.json exits 0 (zero TypeScript errors)
    - npm run build exits 0 and dist/ contains demo.html or demo/index.html (demo entry is in the built output)
    - npx vitest run (excluding e2e) exits 0 (no test regressions)
    - No new entries appear in frontend/package.json dependencies vs HEAD:frontend/package.json
  </acceptance_criteria>
  <done>All five checks pass: no router imports in demo/; TypeScript clean; Vite build green; existing tests green; no new packages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite build input → demo.html | demo.html must remain a registered build entry and must not import app-router code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | Demo island isolation — router import regression | mitigate | grep check in Task 2 Check 1; any routeTree.gen or @tanstack/react-router import in demo/ is a build-blocking failure |
| T-02-17 | Information Disclosure | Vite build — demo bundle leaks app auth routes | mitigate | Demo entry is demo.html (separate build input from index.html); demo components under demo/ only; no cross-import from src/routes/ |
| T-02-18 | Tampering | New runtime dependency introduced | mitigate | package.json diff check in Task 2 Check 2; SPEC acceptance criterion "no new runtime dependency added" is a hard gate |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; build check only |
</threat_model>

<verification>
All five checks in Task 2 pass with exit 0. Summary:
1. grep -r "tanstack/react-router\|routeTree.gen" frontend/src/demo/ → 0 matches
2. package.json diff → unchanged
3. npx tsc --noEmit → exit 0
4. npm run build → exit 0, dist/demo.html exists (or equivalent)
5. npx vitest run → exit 0
</verification>

<success_criteria>
- FederationHub wires all four real panels in scroll order (Hub Discovery → Transcript → Credential Verify → Unit Console)
- All TypeScript errors resolved across the entire frontend (tsc exits 0)
- Vite production build succeeds with demo.html in output
- Existing Vitest tests pass (no regressions)
- No @tanstack/react-router import anywhere in frontend/src/demo/
- No new runtime dependencies added to package.json
</success_criteria>

<output>
Create .planning/phases/02-federation-hub/02-06-SUMMARY.md when done
</output>
