---
phase: 13-dataset-model-access-resolver
plan: 2
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/demo/lib/model.ts
  - frontend/src/demo/lib/dataset.test.ts
autonomous: true
requirements: [DATA-ACCESS-01, DATA-ACCESS-02, DATA-ACCESS-03, DATA-ACCESS-04, DATA-DELEG-01]

must_haves:
  truths:
    - "resolveDatasetAccess returns allow:false when gate 2 (Application-grant OR-gate) fails at evaluation time t, even when a DatasetAccessGrant for the same dataset is still nominally active at t (DATA-ACCESS-01, DATA-ACCESS-03)"
    - "resolveDatasetAccess's Application-grant gate is an OR across application_ids — an active grant on any ONE linked Application suffices (DATA-ACCESS-01)"
    - "resolveDatasetAccess returns allow:true only when clearance, Application-grant, and dataset-grant gates all pass, evaluated in that order, with a 4-entry gates[] trace in order CLEARANCE, APP_GRANT_OR, DATASET_GRANT, VISIBILITY (DATA-ACCESS-02, DATA-ACCESS-03, CONTEXT.md D-03)"
    - "visible = true iff an active Application grant exists on >=1 linked Application, independent of clearance and the dataset-level grant; visible:true, allow:false is a distinct, reachable state for a person with only an Application grant (DATA-ACCESS-04)"
    - "A person with zero qualifying Application grants gets visible:false even if they hold a DatasetAccessGrant directly (orphan case), with NO exemption for admin_org or active delegates (DATA-ACCESS-04)"
    - "A dataset referencing a non-existent Application id fails closed (throws) rather than silently resolving; a stored DatasetAccessGrant.level outside its dataset's own vocabulary is excluded from consideration (denied), not thrown, and sibling valid grants still resolve normally"
    - "canIssueDatasetGrant returns true unconditionally for an active admin_org actor, and for an active delegate ONLY when the delegate holds their own active DatasetAccessGrant on that exact dataset AND is issuing at/below what their own grant covers; false for a delegate with no personal grant on the dataset, a non-admin/no-delegate actor, or an expired-delegate actor (DATA-DELEG-01)"
    - "npm run test passes with zero failures and zero TypeScript errors after these additions; the existing v2.2 golden-fixture and digital-resource Vitest suites remain green and unmodified"
    - "BACKSTOP (non-inferable from Phase 13 alone): the full 3-gate short-circuit exercised as each gate being the SOLE deciding gate (clearance-fails / app-grant-expired-with-live-dataset-grant / dataset-grant-missing) is Phase 14's DATA-SEED-06 deny-matrix's job per SPEC.md's Edge Coverage row (backstop) — Phase 13 covers each gate individually and in the 2-of-3 combinations listed above, but the exhaustive single-deciding-gate matrix over real seed data is intentionally deferred, not silently dropped"
  artifacts:
    - "frontend/src/demo/lib/model.ts — DatasetAccessResult, resolveDatasetAccess, canIssueDatasetGrant appended to the Phase 13 section"
    - "frontend/src/demo/lib/dataset.test.ts — extended with resolver, delegation, and full acceptance-criteria/edge/prohibition sweep coverage"
  key_links:
    - "resolveDatasetAccess calls effectiveDatasetClassification (Plan 13-01) for gate 1 and effectiveRankedLevel/effectiveArchiveCoverage (Plan 13-01) for gate 3 — it never re-derives classification or re-implements aggregation logic locally"
    - "canIssueDatasetGrant's delegate-cap check reuses the SAME effectiveArchiveCoverage/rank-comparison logic as resolveDatasetAccess's gate 3 — issuing authority and content access share one aggregation implementation, not two parallel ones"
    - "visible is computed from gate 2 alone, before and independently of gates 1 and 3 — no code path derives visible from allow, and no admin_org/delegate branch exists anywhere in the visible computation"
  prohibitions:
    - statement: "visible=true must never be read/used downstream as allow=true — they are independent fields with independent meanings"
      status: resolved
      verification: test
    - statement: "A delegate must not be able to issue a grant exceeding what their OWN held DatasetAccessGrant covers (no self- or other-escalation via delegation)"
      status: resolved
      verification: test
    - statement: "admin_org's unrestricted ISSUING authority must not be conflated with unrestricted ACCESS — admin_org's own content access still goes through the full 3-gate resolver"
      status: resolved
      verification: test
---

<objective>
Implement the standalone 3-gate `resolveDatasetAccess` resolver (clearance -> Application-grant OR-gate -> dataset-grant, plus an independent existence-visibility gate surfaced both as a top-level field and a 4th trace entry) and the delegate-capped `canIssueDatasetGrant` issuing-authority check, both built on Plan 13-01's type/vocabulary/aggregation foundation. Close out the phase with a full sweep confirming every SPEC.md acceptance-criteria, edge-coverage, and prohibition row has a passing test.

Purpose: This is the actual access-control mechanism the phase goal names — per-type level mechanisms (Plan 13-01) are inert until a resolver enforces them at resolution time, not issue time, and the existence-visibility gate must be structurally incapable of exemption for privileged actors.
Output: `resolveDatasetAccess`, `canIssueDatasetGrant`, and `DatasetAccessResult` exist in `model.ts`; `dataset.test.ts` has complete coverage of all 13 Phase 13 requirements; `npm run test` and the TypeScript build are green across the whole repo, with zero files touched outside `model.ts`/`dataset.test.ts` for the entire phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dataset-model-access-resolver/13-SPEC.md
@.planning/phases/13-dataset-model-access-resolver/13-RESEARCH.md
@.planning/phases/13-dataset-model-access-resolver/13-CONTEXT.md

# Source of truth — Plan 13-01 will have already appended DatasetNode, DatasetAccessGrant,
# DatasetAccessDelegate, the per-type vocab/tables, effectiveDatasetClassification, and the
# two aggregation functions to this file; this plan appends the resolver + delegation check
# to the SAME Phase 13 section.
@frontend/src/demo/lib/model.ts

# Sibling test-file precedent for the named-pitfall-test convention and delegation matrix style
@frontend/src/demo/lib/digital-resource.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DatasetAccessResult + resolveDatasetAccess (3-gate chain + independent visibility gate)</name>
  <files>frontend/src/demo/lib/model.ts, frontend/src/demo/lib/dataset.test.ts</files>
  <read_first>
    - .planning/phases/13-dataset-model-access-resolver/13-RESEARCH.md, "Pattern 3: Fail-closed on unrecognized level" full code block (~lines 292-364) — the literal reference implementation of `resolveDatasetAccess` to follow
    - frontend/src/demo/lib/model.ts:977-1007 (`evaluateParentTierGrantGate` — the flat-lookup-per-id precedent gate 2's OR-across-application_ids logic must follow; NO ancestor walk, NO recursive resolveResourceAccess call)
    - frontend/src/demo/lib/model.ts:778-795 (`ResourceGateResult`, `ResourceAccessResult` — reuse `ResourceGateResult`'s `{kind, pass, reason}` shape directly for the 4 trace entries; do not define a new gate-result type)
    - frontend/src/demo/lib/model.ts:822-831 (`isWindowActive`) and :15-21 (`CLEARANCE_RANK`)
    - .planning/phases/13-dataset-model-access-resolver/13-CONTEXT.md, decision D-03 (visible as BOTH a top-level field AND a 4th `gates[]` trace entry — not one or the other)
    - .planning/phases/13-dataset-model-access-resolver/13-SPEC.md Requirements 4 and 6, Acceptance Criteria rows 6-10 and 15-16, Edge Coverage table row "DATA-ACCESS-03 | backstop"
    - frontend/src/demo/lib/digital-resource.test.ts:104-157, 216-254 (the exactly-named pitfall-test convention and how a deliberately-injected bad value is asserted against without crashing the whole test)
  </read_first>
  <behavior>
    - Person's only qualifying Application grant expired before `now` on EVERY linked Application, while a `DatasetAccessGrant` for the same dataset is still nominally active at `now` -> `allow: false`, with the `APP_GRANT_OR` gate as the failing gate
    - Person has an active Application grant on exactly ONE of two `application_ids`, plus a covering `DatasetAccessGrant` and sufficient clearance -> `allow: true` (proves the OR-gate, not an AND-across-all-ids)
    - Person has an active Application grant but NO `DatasetAccessGrant` -> `visible: true, allow: false` (a distinct, reachable state)
    - Person has zero qualifying Application grants but DOES hold a `DatasetAccessGrant` directly (orphan case) -> `visible: false` — no exemption for any actor
    - `requiredLevel` drawn from the WRONG dataset type's vocabulary (e.g. `"CONTRIBUTE"` against a `MAILBOX` dataset) -> throws (caller/config error)
    - A stored `DatasetAccessGrant.level` outside its own dataset's vocabulary is silently excluded from gate 3's consideration (denied, not thrown) while OTHER valid grants for the same subject/dataset still resolve normally
    - A dataset referencing a non-existent `application_id` -> throws
  </behavior>
  <action>
    Define `DatasetAccessResult` interface in the Phase 13 section: `allow: boolean`, `visible: boolean` (CONTEXT.md D-03: top-level field), `gates: ResourceGateResult[]` (D-03: exactly 4 entries, reusing the existing `ResourceGateResult` type — do not define a new gate-result interface), `reason?: string`. Implement `resolveDatasetAccess(subject: string, subjectClearance: Clearance, dataset: DatasetNode, applications: ApplicationNode[], platforms: PlatformNode[], appGrants: ResourceAccessGrant[], datasetGrants: DatasetAccessGrant[], requiredLevel: string, now: Date): DatasetAccessResult` following RESEARCH.md's Pattern 3 reference implementation in order: (1) if `!isLevelInVocabulary(dataset.dataset_type, requiredLevel)`, throw a descriptive `Error` immediately — this is a caller/config error and must ALWAYS throw, never silently deny; (2) compute `appGrantPass` via `dataset.application_ids.some(...)`, resolving each id against `applications` (throwing a descriptive `Error` if an id does not resolve — mirrors `effectiveClassification`'s fail-closed pattern) and checking `appGrants` for a matching `person_id`/`resource_id`/`isWindowActive(..., now)` entry; set `visible = appGrantPass` directly with NO admin_org/delegate branch anywhere in this computation; (3) compute `clearancePass` via `CLEARANCE_RANK[subjectClearance] >= CLEARANCE_RANK[effectiveDatasetClassification(dataset, applications, platforms)]`; (4) compute `datasetGrantPass` by filtering `datasetGrants` to this `dataset.id` + `person_id === subject` + `isWindowActive(..., now)` + `isLevelInVocabulary(dataset.dataset_type, g.level)` (silently excluding out-of-vocabulary stored grants — the OPPOSITE fail-closed behavior from step 1, never throwing on bad stored data), then dispatching on `dataset.dataset_type`: for `ARCHIVE_ROLE`, call `effectiveArchiveCoverage` on the filtered grants' levels (cast to `ArchiveRole`) and check `.has(requiredLevel as ArchiveRole)`; for `MAILBOX`/`DOCUMENT_SITE`, call `effectiveRankedLevel` with the appropriate levels array and compare the resulting level's rank to `requiredLevel`'s rank; (5) assemble `gates: ResourceGateResult[]` with exactly 4 entries in this order — `{kind: "CLEARANCE", pass: clearancePass, reason: ...}`, `{kind: "APP_GRANT_OR", pass: appGrantPass, reason: ...}`, `{kind: "DATASET_GRANT", pass: datasetGrantPass, reason: ...}`, `{kind: "VISIBILITY", pass: visible, reason: ...}` — choose fresh, self-describing reason strings for the 3 new kinds (e.g. `APP_GRANT_FOUND`/`NO_APP_GRANT`, `DATASET_GRANT_FOUND`/`NO_DATASET_GRANT`, `VISIBLE`/`NOT_VISIBLE`), following the existing `CLEARANCE_OK`/`INSUFFICIENT_CLEARANCE` naming convention; (6) return `{ allow: clearancePass && appGrantPass && datasetGrantPass, visible, gates }`.
    Extend dataset.test.ts with a new `describe` block containing one test per behavior listed above, PLUS two exactly-named tests in the style of digital-resource.test.ts's named pitfall tests: one proving `visible: true, allow: false` and `visible: false` are both independently reachable (the DATA-ACCESS-04 visible/allow-independence prohibition), and one proving a malformed stored grant (out-of-vocabulary `level`) is silently excluded from gate 3 while a sibling valid grant for the same subject/dataset still resolves correctly (Pitfall B from RESEARCH.md — the "one bad grant must not crash resolution for every other grant" case).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/demo/lib/dataset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run src/demo/lib/dataset.test.ts` passes, 0 failures. A test asserts `resolveDatasetAccess(...)` throws (via `expect(() => ...).toThrow()`) when `requiredLevel` is drawn from the wrong dataset_type's vocabulary. A test asserts `result.gates.length === 4` and `result.gates.map(g => g.kind)` equals `["CLEARANCE", "APP_GRANT_OR", "DATASET_GRANT", "VISIBILITY"]` in that exact order. A test with 2 `application_ids` where only ONE has an active grant asserts `appGrantPass`/`visible` is `true` (proving the OR-gate, not an AND).
  </acceptance_criteria>
  <done>`DatasetAccessResult` and `resolveDatasetAccess` are exported from model.ts, implement the full 3-gate + independent-visibility chain per SPEC.md Requirements 4 and 6, and pass tests for every resolver-related SPEC.md acceptance-criteria row (6-10, 15-16).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canIssueDatasetGrant (delegate-capped issuing authority)</name>
  <files>frontend/src/demo/lib/model.ts, frontend/src/demo/lib/dataset.test.ts</files>
  <read_first>
    - .planning/phases/13-dataset-model-access-resolver/13-RESEARCH.md, "canIssueDatasetGrant (DATA-DELEG-01)" code block (~lines 508-559) — the literal reference implementation
    - frontend/src/demo/lib/model.ts:1159-1183 (`canIssueResourceGrant` — the two-path admin/delegate shape precedent; note DATA-04's `admin_org_id` is a bare fixed-field equality check, unlike `canIssueResourceGrant`'s windowed `activeOrgLinksForRole` lookup — do not copy the windowed-lookup pattern for the admin path)
    - .planning/phases/13-dataset-model-access-resolver/13-CONTEXT.md and 13-RESEARCH.md's "Assumption A3" (admin_org_id is non-time-windowed; the admin path is a bare equality test, never `isWindowActive`)
    - .planning/phases/13-dataset-model-access-resolver/13-SPEC.md Requirement 7, Acceptance Criteria rows 12-13, 16, and the Prohibitions table (all 3 rows)
  </read_first>
  <behavior>
    - `actorOrgId === dataset.admin_org_id` -> `true` unconditionally, regardless of whether admin_org holds any personal `DatasetAccessGrant` on the dataset
    - An active delegate holding their own active `ARCHIVE_ROLE` `CASE_HANDLER` grant attempting to issue `READER` -> `true` (issuing at/below own coverage)
    - An active delegate holding ONLY a `CASE_HANDLER` grant attempting to issue `ADMIN` -> `false` (cannot escalate, including issuing to themselves)
    - A delegate with zero grants on the exact dataset -> `false` ("can issue nothing")
    - An expired delegate -> `false`
    - A non-admin, non-delegate actor -> `false`
  </behavior>
  <action>
    Implement `canIssueDatasetGrant(actorOrgId: string, actorPersonId: string, dataset: DatasetNode, requestedLevel: string, datasetGrants: DatasetAccessGrant[], delegates: DatasetAccessDelegate[], now: Date): boolean` per RESEARCH.md's reference implementation: (1) admin path — if `actorOrgId === dataset.admin_org_id`, return `true` immediately (bare equality, no `isWindowActive` check — `admin_org_id` is a fixed field per DATA-04, not time-windowed); (2) delegate path — find an active `DatasetAccessDelegate` (via `isWindowActive`) matching `dataset.id` and `actorPersonId` (delegates are PERSON-only per Plan 13-01's `DatasetAccessDelegate` shape — no `delegate_type` discriminant needed); if none, return `false`; (3) collect the delegate's OWN active, in-vocabulary `DatasetAccessGrant`s on this exact dataset (`dataset_id` match, `person_id === actorPersonId`, `isWindowActive`, `isLevelInVocabulary`); if empty, return `false` ("can issue nothing"); (4) dispatch on `dataset.dataset_type`: for `ARCHIVE_ROLE`, compute `effectiveArchiveCoverage` over the delegate's own grant levels (cast to `ArchiveRole`) and return whether it `.has(requestedLevel as ArchiveRole)` (validating `requestedLevel` is itself in-vocabulary first, returning `false` if not); for ranked types, compute the delegate's own best rank via the appropriate levels array's `.indexOf` (or `effectiveRankedLevel` plus a rank lookup) and return whether `requestedLevel`'s rank is `<=` that (validating `requestedLevel` is in-vocabulary first).
    Extend dataset.test.ts with a new `describe` block covering all 6 behaviors above using inline fixtures, PLUS one prohibition test asserting `admin_org`'s own CONTENT access still goes through the full 3-gate `resolveDatasetAccess` — construct an `admin_org` actor with no clearance/Application-grant/dataset-grant and assert `resolveDatasetAccess(...)` returns `allow: false` for them, WHILE `canIssueDatasetGrant(adminOrgId, ...)` returns `true` for the same actor/dataset — proving the two functions are independent and issuing authority never substitutes for access (this is the "issuing-vs-access conflation" prohibition from SPEC.md).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/demo/lib/dataset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run src/demo/lib/dataset.test.ts` passes, 0 failures. An explicit test asserts a delegate holding only `CASE_HANDLER` cannot issue `ADMIN` to anyone, including themselves. An explicit test asserts an `admin_org` actor with zero clearance/grants gets `allow: false` from `resolveDatasetAccess` while `canIssueDatasetGrant` for the same actor returns `true` — both assertions in the SAME test, proving independence rather than two separately-passing unrelated tests.
  </acceptance_criteria>
  <done>`canIssueDatasetGrant` is exported from model.ts, implements DATA-DELEG-01 fully (admin unrestricted, delegate capped at own coverage, no-grant-issues-nothing, expired-delegate-denied), and all 3 SPEC.md Prohibitions rows have a passing corresponding test.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression sweep and phase-level acceptance-criteria audit</name>
  <files>frontend/src/demo/lib/dataset.test.ts</files>
  <read_first>
    - .planning/phases/13-dataset-model-access-resolver/13-SPEC.md — full "Acceptance Criteria" list (16 rows), "Edge Coverage" table (18 rows), and "Prohibitions" table (3 rows)
    - frontend/src/demo/lib/dataset.test.ts (the file built across Plan 13-01's 3 tasks and this plan's Tasks 1-2 — read in full before this task to build the row-by-row cross-check)
  </read_first>
  <behavior>N/A — this is a verification/completeness task; it audits existing coverage and appends only the rows found missing, without restructuring any prior describe block.</behavior>
  <action>
    Walk SPEC.md's 16 Acceptance Criteria rows, the Edge Coverage table's rows marked "covered" or "backstop" (4 relevant rows — DATA-01 empty-array, DATA-03 unclassified, DATA-05 unclassified, DATA-ACCESS-01/02/04 unclassified — plus the DATA-ACCESS-03 backstop row, which is satisfied by this plan's must_haves note referencing Phase 14's DATA-SEED-06 rather than a new Phase-13 test), and the Prohibitions table's 3 rows, confirming each has a directly corresponding passing assertion somewhere in dataset.test.ts (built across Plan 13-01 and this plan's Tasks 1-2). If any row lacks a direct test, APPEND the missing assertion to the most relevant existing `describe` block in dataset.test.ts — do not restructure or rename prior blocks. Run the full frontend test suite and the full TypeScript build. Confirm via `git diff --stat` (scoped to this phase's commits) that only `frontend/src/demo/lib/model.ts` and `frontend/src/demo/lib/dataset.test.ts` were modified across the entire phase — no `world-state.tsx`, `seed.ts`, or `.tsx` component touched.
  </action>
  <verify>
    <automated>cd frontend && npm run test</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npm run test` reports 0 failures across the ENTIRE suite (not just dataset.test.ts) — confirms the existing v2.2 golden-fixture (`digital-resource-golden-export.test.ts`) and digital-resource/physical-access suites remain green. `cd frontend && npm run build` reports zero TypeScript errors. `git diff --stat` (against the phase's starting commit) shows only `frontend/src/demo/lib/model.ts` and `frontend/src/demo/lib/dataset.test.ts` in the changed-files list.
  </acceptance_criteria>
  <done>Every SPEC.md Acceptance Criteria, applicable Edge Coverage, and Prohibitions row has a passing corresponding test in dataset.test.ts. `npm run test` and the TypeScript build are green for the whole repository. No file outside the two declared `files_modified` was touched anywhere in the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future Phase 15 UI / Phase 14 selector -> `resolveDatasetAccess`'s `person`/`dataset`/`requiredLevel` parameters | Untrusted caller input crossing into the resolver's gate chain |
| Stored `DatasetAccessGrant`/`DatasetAccessDelegate` fixture data -> gate 3 / `canIssueDatasetGrant`'s grant lookups | Untrusted/malformable seed data that must be validated (or safely excluded) before being trusted by a comparator |
| `resolveDatasetAccess`'s `{visible, allow}` output -> future Phase 15 UI consumers | Output-semantics boundary — `visible` must never be read downstream as `allow`; this phase cannot enforce a future consumer's correctness, only make the two fields structurally independent and tested as such |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | `canIssueDatasetGrant` | high | mitigate | Delegate path always compares `requestedLevel` against the delegate's OWN active grant coverage via `effectiveRankedLevel`/`effectiveArchiveCoverage`, never against the dataset's max level — Task 2, tested by the CASE_HANDLER-cannot-issue-ADMIN case |
| T-13-03 | Elevation of Privilege | `resolveDatasetAccess` gate 2 (APP_GRANT_OR) | high | mitigate | Application-grant OR-gate is evaluated at resolution time `now`, inside the same gate chain as gate 3 — never checked only at grant-issue time — Task 1, tested by the expired-app-grant-with-live-dataset-grant case |
| T-13-05 | Elevation of Privilege | `resolveDatasetAccess`'s `visible` computation | high | mitigate | `visible` is computed from gate 2 alone with no admin_org/delegate branch anywhere in the function; an explicit test asserts `visible` and `allow` are independently controllable — Task 1 |
| T-13-06 | Elevation of Privilege (delegate/dataset-id confusion) | `canIssueDatasetGrant`'s delegate lookup | low | accept | IDOR-shaped dataset-id spoofing concerns are explicitly out of scope for this pure demo/mock module per SPEC.md's Prohibitions section ("dropped as canon — owned by code-review/OWASP, not minted here"); no real request-authentication boundary exists in this milestone — revisit if/when a backend milestone introduces real caller authentication |
</threat_model>

<verification>
Run `cd frontend && npx vitest run src/demo/lib/dataset.test.ts` after Task 1 and Task 2. Run the full `cd frontend && npm run test` and `cd frontend && npm run build` after Task 3. Confirm via `git diff --stat` (scoped to the whole phase) that only `frontend/src/demo/lib/model.ts` and `frontend/src/demo/lib/dataset.test.ts` were modified.
</verification>

<success_criteria>
`resolveDatasetAccess` and `canIssueDatasetGrant` exist in `model.ts`, fully implement DATA-ACCESS-01..04 and DATA-DELEG-01, and every SPEC.md Acceptance Criteria/Edge Coverage/Prohibitions row has a passing test in `dataset.test.ts`. `npm run test` passes with zero failures across the whole repo (including the existing v2.2 golden-fixture and digital-resource suites, unmodified and green). `npm run build` reports zero TypeScript errors. No file outside `model.ts`/`dataset.test.ts` touched anywhere in Phase 13.
</success_criteria>

<output>
Create `.planning/phases/13-dataset-model-access-resolver/13-02-SUMMARY.md` when done
</output>
