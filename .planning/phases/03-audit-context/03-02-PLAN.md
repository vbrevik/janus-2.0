---
phase: 03-audit-context
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/demo/store/world-state.tsx
  - frontend/src/demo/components/AuditView.tsx
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "world-state seedWorld() initializes events: INITIAL_EVENTS (4 baseline entries) and seq: 4"
    - "AuditView renders a range slider defaulting to events.length (current state)"
    - "Dragging the slider to T shows only events with seq <= T as applied; events with seq > T are greyed out with '· future' suffix"
    - "Reconstructed State card shows the subject's clearance, compartments, security hold, and authorization status as Pills at the selected T"
    - "Who-can-access card shows subjects that ALLOW for the selected resource as of T"
    - "A 'Current state' Pill appears next to the slider label when asOf === events.length"
    - "An inline note 'In production, Auditor role required.' appears in AuditView (per D3-11)"
    - "whoCanAccess result is memoized on [asOf, resId, events] to prevent redundant recomputation"
  artifacts:
    - path: "frontend/src/demo/store/world-state.tsx"
      provides: "seedWorld() with INITIAL_EVENTS + seq=4"
      contains: "INITIAL_EVENTS"
    - path: "frontend/src/demo/components/AuditView.tsx"
      provides: "AuditView component — slider + event log + reconstructed state + who-can-access"
      exports: ["AuditView"]
  key_links:
    - from: "frontend/src/demo/components/AuditView.tsx"
      to: "frontend/src/demo/lib/auditlog.ts"
      via: "import { reconstructSubject, whoCanAccess, evaluateWithAuth } from '../lib/auditlog'"
      pattern: "from.*lib/auditlog"
    - from: "frontend/src/demo/store/world-state.tsx"
      to: "frontend/src/demo/lib/seed.ts"
      via: "import { INITIAL_EVENTS } from '../lib/seed'"
      pattern: "INITIAL_EVENTS"
---

<objective>
Patch world-state to pre-seed baseline events, then build AuditView.tsx — the full Audit tab.

Purpose: Deliver the AUDIT-01 and AUDIT-02 success criteria as an interactive component. The slider drives point-in-time reconstruction; the reconstructed state and who-can-access panels update live.

Output: world-state.tsx updated (INITIAL_EVENTS seeded, seq=4), AuditView.tsx created.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-RESEARCH.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-UI-SPEC.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-01-SUMMARY.md
</context>

<interfaces>
From frontend/src/demo/lib/auditlog.ts (created in Plan 03-01):
  export function reconstructSubject(subjectId: string, subjects: Subject[], events: AttrEvent[], asOf: number): Subject | null
  export function whoCanAccess(req: Requirement, events: AttrEvent[], subjects: Subject[], asOf: number): Subject[]
  export function evaluateWithAuth(subject: Subject, req: Requirement): Decision

From frontend/src/demo/store/world-state.tsx (current shape):
  WorldState includes: events: AttrEvent[], seq: number, subjects: Subject[], resources: Resource[]
  useWorld() returns WorldState
  seedWorld() initializes state — currently sets events: [], seq: 0

From frontend/src/demo/lib/seed.ts (after Plan 03-01):
  export const INITIAL_EVENTS: AttrEvent[]  (4 entries, seq 1-4)

From frontend/src/demo/components/ui.tsx:
  Card, Pill, Field, Select, DecisionTrace, MockTag — bespoke atoms, no shadcn imports

From frontend/src/demo/lib/abac.ts:
  export function requirementFromResource(resource: Resource): Requirement
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Patch world-state.tsx — seed INITIAL_EVENTS and set seq=4</name>
  <files>frontend/src/demo/store/world-state.tsx</files>
  <read_first>
    - frontend/src/demo/store/world-state.tsx — full file: seedWorld() function, WorldState shape, initial state object, events field initialization
    - frontend/src/demo/lib/seed.ts — confirm INITIAL_EVENTS is exported (post Plan 03-01)
  </read_first>
  <action>
    In world-state.tsx, add import: `import { INITIAL_EVENTS } from '../lib/seed'`

    Locate the seedWorld() function (or the initial state object it returns). Find the line that sets `events: []` and change it to `events: INITIAL_EVENTS`. Find the line that sets `seq: 0` and change it to `seq: INITIAL_EVENTS.length` (= 4).

    These are the ONLY changes to world-state.tsx. Do not add new reducer cases, do not add new action types, do not change any other field. The existing appendEvent() reducer case already uses state.seq+1 for stamping, so the new initial seq=4 ensures subsequent events continue from seq=5 without collision.

    Verify the change does not break the existing world-state.test.tsx (if it exists) — read it before patching to understand any tests that assume events:[] at init.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/store/world-state.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>world-state.tsx seedWorld() returns events: INITIAL_EVENTS (4 entries) and seq: 4. Existing world-state tests pass (or if they assert events:[] length, update those assertions to expect 4 initial events).</done>
</task>

<task type="auto">
  <name>Task 2: Create AuditView.tsx — slider + event log + reconstructed state + who-can-access</name>
  <files>frontend/src/demo/components/AuditView.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike007Audit.tsx — full file: reference UI layout, slider wiring, event list rendering, two-column grid pattern
    - frontend/src/demo/components/ui.tsx — Card, Pill, Field, Select, DecisionTrace, MockTag APIs and prop shapes
    - frontend/src/demo/lib/abac.ts — requirementFromResource signature
    - frontend/src/demo/store/world-state.tsx — useWorld() return type: events, subjects, resources fields
    - frontend/src/demo/lib/model.ts — Subject, Resource field shapes (name, id, clearance, compartments, securityHold, authorization, flags)
    - frontend/src/demo/lib/seed.ts — subject and resource IDs for default picker initial values
    - frontend/src/demo/components/AuditView.tsx — does NOT exist yet; this task creates it
  </read_first>
  <action>
    Create frontend/src/demo/components/AuditView.tsx as a named export `AuditView`.

    Imports: React (useState, useMemo), useWorld from '../store/world-state', reconstructSubject + whoCanAccess from '../lib/auditlog', requirementFromResource from '../lib/abac', Card + Pill + Field + Select from './ui'. No shadcn imports.

    State:
    - asOf: number, useState(events.length) — slider value. Default to events.length (D3-03).
    - subjId: string, useState(subjects[0].id) — subject picker.
    - resId: string, useState(resources[0].id) — resource picker (for who-can-access).

    Derived (useMemo):
    - reconstructed = reconstructSubject(subjId, subjects, events, asOf) — deps: [subjId, subjects, events, asOf]
    - req = requirementFromResource(resources.find(r => r.id === resId)!) — deps: [resId, resources]
    - canAccess = whoCanAccess(req, events, subjects, asOf) — deps: [req, events, subjects, asOf]

    Layout (top to bottom):
    1. Inline role note: &lt;p className="text-xs text-slate-400"&gt;In production, Auditor role required.&lt;/p&gt; (D3-11)
    2. Timeline Card (title: "Timeline — as of T = {asOf}"):
       - Subject picker (Field + Select over subjects array, label "Subject")
       - Slider: &lt;input type="range" min={0} max={events.length} value={asOf} onChange={e =&gt; setAsOf(Number(e.target.value))} className="w-full" /&gt;
       - Inline: when asOf === events.length, render &lt;Pill tone="blue"&gt;Current state&lt;/Pill&gt; (D3-03)
       - Event list: for each event in events, render a &lt;p className="font-mono text-xs"&gt; row. Applied (event.seq &lt;= asOf): text-slate-700 + "✓ applied". Future (event.seq &gt; asOf): text-slate-300 + "· future". Format: "T={event.seq} {event.op} {event.value ?? ''} by {event.actor}".
       - Empty event list: &lt;p className="text-sm text-slate-400"&gt;No events recorded.&lt;/p&gt;
    3. Two-column grid (grid-cols-2 gap-4):
       Left — Reconstructed State Card (title: "Reconstructed state — {reconstructed?.name ?? subjId}"):
         If reconstructed is null: &lt;p className="text-sm text-slate-400"&gt;Unknown subject.&lt;/p&gt;
         Else: Pills for clearance (tone="blue"), each compartment (tone="slate"), securityHold (if true: &lt;Pill tone="red"&gt;SECURITY HOLD&lt;/Pill&gt;), authorization status (&lt;Pill tone={auth.status === "AUTHORIZED" ? "green" : "red"}&gt;{auth.status === "AUTHORIZED" ? "AUTHORIZED" : "NOT AUTHORIZED"}&lt;/Pill&gt; — only if subject.authorization exists)
       Right — Who-Can-Access Card (title: "Who can access — as of T={asOf}"):
         Resource picker (Field + Select over resources array, label "Resource")
         If canAccess.length === 0: &lt;p className="text-sm text-slate-400"&gt;Nobody can access this resource at this point in time.&lt;/p&gt;
         Else: &lt;ul&gt; with &lt;li className="text-green-700"&gt;✓ {subject.name}&lt;/li&gt; for each subject in canAccess

    Card component prop: title string prop (check ui.tsx for exact API — use whatever Card accepts for the header label).

    Copy strings verbatim from 03-UI-SPEC.md Copywriting Contract section.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -i "AuditView\|auditlog" | head -20</automated>
  </verify>
  <done>AuditView.tsx compiles with 0 TypeScript errors. Component exports named AuditView. Slider defaults to events.length. Event list renders with applied/future distinction. Reconstructed state and who-can-access panels render. "Current state" pill visible at slider max. Role note present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → slider/picker | Slider value (asOf int) and subject/resource picker selections are DOM events — used as array indices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Slider onChange value | accept | Input is a numeric range slider (min=0 max=events.length) — clamped by the browser; demo-only in-memory data; no backend |
| T-03-02-02 | Information Disclosure | Reconstructed subject state | accept | All data is intentionally public mock demo data; no real PII or credentials |
| T-03-02-SC | Tampering | npm installs | accept | No new packages; all imports are from internal demo island files |
</threat_model>

<verification>
TypeScript check: `cd frontend && npx tsc --noEmit --project tsconfig.app.json` — 0 errors.

World-state test: `npx vitest run src/demo/store/world-state.test.tsx` — passes.

Manual smoke (after DemoRoot wired in Plan 03-04): navigate to Audit tab, verify slider at max shows "Current state" pill, drag slider to T=1 and confirm events with seq>1 show "· future", check reconstructed state pills update.
</verification>

<success_criteria>
- world-state.tsx seedWorld() starts with events: INITIAL_EVENTS (4 entries), seq: 4
- AuditView exports named AuditView
- Slider default = events.length; "Current state" Pill at max
- Applied events: text-slate-700 "✓ applied"; future events: text-slate-300 "· future"
- Reconstructed state shows clearance, compartments, hold, authorization status as Pills
- who-can-access list shows green ✓ entries or empty-state copy
- Inline "In production, Auditor role required." note present
- 0 TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/03-audit-context/03-02-SUMMARY.md` when done.
</output>
