---
phase: 03-audit-context
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/demo/components/ContextView.tsx
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "Policy divergence panel shows 6 DecisionTrace cells (3-col × 2-row grid), one per UnitId, for the selected subject + resource"
    - "INTEL unit's DecisionTrace shows DENY for SECRET-cleared subjects (strict floor per D3-06); INDUSTRY unit's DecisionTrace shows ALLOW when NTK/affiliation fail (relaxed per D3-06)"
    - "Deployment toggle shows HOME and ABROAD buttons; active state uses blue styling; inactive uses slate styling"
    - "Toggling a subunit from HOME to ABROAD flips evaluateSubunitAccess result from DENY to ALLOW for an obligated requester"
    - "Shielding panel sources resources exclusively from worldState.resources where shielded === true"
    - "Non-allowlisted requester shown DENY by shielding panel; allowlisted requester shown ALLOW"
    - "All intro copy and hint copy from UI-SPEC Copywriting Contract is present"
    - "ContextTrace is a local component (not exported); DecisionTrace is used for policy divergence grid"
  artifacts:
    - path: "frontend/src/demo/components/ContextView.tsx"
      provides: "ContextView component — policy divergence + deployment toggle + shielding panels"
      exports: ["ContextView"]
  key_links:
    - from: "frontend/src/demo/components/ContextView.tsx"
      to: "frontend/src/demo/lib/policy.ts"
      via: "import { evaluateWithPolicy } from '../lib/policy'"
      pattern: "from.*lib/policy"
    - from: "frontend/src/demo/components/ContextView.tsx"
      to: "frontend/src/demo/lib/obligations.ts"
      via: "import { evaluateSubunitAccess, evaluateResourceAccess } from '../lib/obligations'"
      pattern: "from.*lib/obligations"
    - from: "frontend/src/demo/components/ContextView.tsx"
      to: "frontend/src/demo/lib/seed.ts"
      via: "import { POLICIES, SUBUNITS, SUPPORT_OBLIGATIONS } from '../lib/seed'"
      pattern: "from.*lib/seed"
---

<objective>
Build ContextView.tsx — the full Context tab with three sections: policy divergence, deployment-driven support obligation, and directional shielding.

Purpose: Deliver CTX-01, CTX-02, CTX-03 success criteria as an interactive component. Each section demonstrates a distinct context-driven access mechanism. All pure functions were created in Plan 03-01.

Output: ContextView.tsx (single new file). No other files modified.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-RESEARCH.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-UI-SPEC.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-01-SUMMARY.md
</context>

<interfaces>
From frontend/src/demo/lib/policy.ts (Plan 03-01):
  export function evaluateWithPolicy(principal: Principal, req: Requirement, policy: EntityPolicy): Decision

From frontend/src/demo/lib/obligations.ts (Plan 03-01):
  export function evaluateSubunitAccess(requester: UnitId, subunit: Subunit, obligations: { from: UnitId; to: UnitId }[]): Decision
  export function evaluateResourceAccess(requester: UnitId, resource: Resource): Decision

From frontend/src/demo/lib/seed.ts (Plan 03-01):
  export const POLICIES: Record&lt;UnitId, EntityPolicy&gt;   // 6 UnitId keys
  export const SUBUNITS: Subunit[]                         // 3 entries
  export const SUPPORT_OBLIGATIONS: { from: UnitId; to: UnitId }[]

From frontend/src/demo/store/world-state.tsx:
  useWorld() returns: { subjects: Subject[], resources: Resource[], ... }

From frontend/src/demo/components/ui.tsx:
  Card, Pill, Field, Select, DecisionTrace (accepts Decision object), MockTag

From frontend/src/demo/lib/abac.ts:
  principalFromSubject(subject: Subject): Principal
  requirementFromResource(resource: Resource): Requirement

From frontend/src/demo/lib/model.ts:
  UnitId — "MILITARY_1" | "MILITARY_2" | "INTEL" | "INFRA" | "INDUSTRY" | "HOME_GUARD"
  Resource — { id, name, shielded?: boolean, allowlist?: UnitId[], ownerUnit?: UnitId, ... }
  Subunit — { id, name, unit: UnitId, deployment: Deployment }
  Deployment — "HOME" | "ABROAD"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ContextView.tsx — policy divergence section (CTX-01)</name>
  <files>frontend/src/demo/components/ContextView.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike008Policy.tsx — full file: 6-column grid layout, subject/resource pickers, DecisionTrace usage per column
    - frontend/src/demo/components/ui.tsx — Card, Pill, Field, Select, DecisionTrace exact prop API
    - frontend/src/demo/lib/model.ts — UnitId union literal values (exact strings)
    - frontend/src/demo/lib/seed.ts — POLICIES record structure (post Plan 03-01)
    - frontend/src/demo/lib/abac.ts — principalFromSubject, requirementFromResource signatures
    - frontend/src/demo/store/world-state.tsx — useWorld() return type: subjects, resources
  </read_first>
  <action>
    Create frontend/src/demo/components/ContextView.tsx as a named export `ContextView`. This task implements only the Policy Divergence section — the rest of the component is implemented in Task 2 (same file, same task run — you can build the full component in one pass since it is a single file).

    UNIT_IDS constant: const UNIT_IDS: UnitId[] = ["MILITARY_1", "MILITARY_2", "INTEL", "INFRA", "INDUSTRY", "HOME_GUARD"]

    Policy divergence section state:
    - policySubjId: string, useState(subjects[0].id)
    - policyResId: string, useState(resources[0].id)

    Policy divergence derived (useMemo):
    - principal = principalFromSubject(subjects.find(s => s.id === policySubjId)!) — dep: [policySubjId, subjects]
    - req = requirementFromResource(resources.find(r => r.id === policyResId)!) — dep: [policyResId, resources]
    - results = UNIT_IDS.map(uid => ({ uid, policy: POLICIES[uid], decision: evaluateWithPolicy(principal, req, POLICIES[uid]) })) — dep: [principal, req]

    Policy divergence layout:
    - Intro copy: &lt;p className="text-sm text-slate-500"&gt;{verbatim from UI-SPEC Copywriting Contract "Context view — policy panel intro"}&lt;/p&gt;
    - Two-column picker grid (grid-cols-2 gap-4): subject picker (Field+Select) and resource picker (Field+Select)
    - 6-unit grid (grid-cols-3 gap-4 mt-4): for each result, a Card containing:
      - &lt;p className="text-sm font-semibold"&gt;{result.uid}&lt;/p&gt;
      - &lt;p className="text-xs text-slate-400"&gt;{result.policy.label}&lt;/p&gt;
      - &lt;DecisionTrace decision={result.decision} /&gt;  (or whatever prop DecisionTrace accepts — read ui.tsx)

    IMPORTANT: Check DecisionTrace's exact prop shape in ui.tsx before writing the JSX. Do NOT guess the prop name.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -i "ContextView\|policy\|obligations" | head -20</automated>
  </verify>
  <done>ContextView.tsx compiles with 0 TypeScript errors. Policy divergence section renders a 3×2 grid of DecisionTrace components. Intro copy matches UI-SPEC verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ContextView.tsx — deployment toggle (CTX-02) + shielding panel (CTX-03)</name>
  <files>frontend/src/demo/components/ContextView.tsx</files>
  <read_first>
    - frontend/src/spikes/components/Spike009Context.tsx — full file: deployment toggle two-button pattern, shielding panel layout, ContextTrace local component pattern
    - frontend/src/demo/components/ContextView.tsx — (after Task 1): current component state and structure
    - frontend/src/demo/lib/model.ts — Subunit, Deployment, Resource field shapes
    - frontend/src/demo/lib/seed.ts — SUBUNITS, SUPPORT_OBLIGATIONS structures
    - frontend/src/demo/components/ui.tsx — Pill, Field, Select, Card props
  </read_first>
  <action>
    Extend ContextView.tsx to add two more sections below the policy divergence section.

    Local ContextTrace component (not exported — define above ContextView in the same file):
    - Props: { label: string; decision: Decision }
    - Renders: a styled div with the label, ALLOW (green) or DENY (red) verdict text, and a summary of failed rules if any. Match Spike009Context.tsx's ContextTrace layout.

    Section A — Deployment / Support Obligation (CTX-02):
    - Section heading: &lt;h2 className="text-sm font-semibold"&gt;A · Support obligation (deployment-driven grant)&lt;/h2&gt;
    - Obligation intro: &lt;p className="text-sm text-slate-500"&gt;{verbatim from UI-SPEC "Context view — obligation panel intro"}&lt;/p&gt;
    - Two-column grid (grid-cols-2 gap-4):
      Left column — pickers + toggle:
        - Requester picker: Field + Select over UNIT_IDS, label "Requester unit"
        - Subunit picker: Field + Select over SUBUNITS (display subunit.name, value subunit.id)
        - Deployment toggle: two buttons in &lt;div className="flex gap-2"&gt;
          HOME: onClick(() => setDeployment("HOME")), class: deployment === "HOME" ? "border-slate-300 text-slate-500 rounded border px-3 py-1 text-xs" : "border-blue-300 bg-blue-100 text-blue-800 rounded border px-3 py-1 text-xs" (INVERTED — read the class logic from UI-SPEC carefully: ACTIVE is blue, INACTIVE is slate)
          ABROAD: vice versa
          Note: active button = blue, inactive = slate. When deployment === "ABROAD", ABROAD button gets blue classes. When deployment === "HOME", HOME button gets blue classes. Read the UI-SPEC Interaction Contract carefully.
      Right column — ContextTrace:
        - Pass label = "Obligation grant" or the subunit's name
        - Pass decision = evaluateSubunitAccess(requester, {...selectedSubunit, deployment}, SUPPORT_OBLIGATIONS)
        - Hint: &lt;p className="text-xs text-slate-400"&gt;{verbatim from UI-SPEC "Context view — obligation hint"}&lt;/p&gt;

    State for Section A:
    - obligRequester: UnitId, useState("INFRA" as UnitId)
    - obligSubunitId: string, useState(SUBUNITS[0].id)
    - deployment: Deployment, useState("HOME")
    - selectedSubunit derived: SUBUNITS.find(s => s.id === obligSubunitId) — used in evaluateSubunitAccess call with deployment override: { ...selectedSubunit, deployment }

    Section B — Directional Shielding (CTX-03):
    - Section heading: &lt;h2 className="text-sm font-semibold"&gt;B · Directional shielding (deny override on protected data)&lt;/h2&gt;
    - Two-column grid (grid-cols-2 gap-4):
      Left column:
        - Requester picker: Field + Select over UNIT_IDS, label "Requester unit"
        - Shielded resource picker: Field + Select over shieldedResources (filtered from worldState.resources where resource.shielded === true), label "Protected resource"
        - Per D3-08: resources from world-state, NOT from a hardcoded list
        - If no shielded resources, render &lt;p className="text-sm text-slate-400"&gt;No shielded resources in world state.&lt;/p&gt;
        - Allowlist display: &lt;Pill tone="red"&gt;shielded&lt;/Pill&gt; + "allowlist:" text + one &lt;Pill&gt; per unit in selectedResource.allowlist
      Right column:
        - ContextTrace with decision = evaluateResourceAccess(shieldRequester, selectedResource)
        - Hint: &lt;p className="text-xs text-slate-400"&gt;{verbatim from UI-SPEC "Context view — shielding hint"}&lt;/p&gt;

    State for Section B:
    - shieldRequester: UnitId, useState("MILITARY_1" as UnitId)
    - shieldResId: string, useState(shieldedResources[0]?.id ?? "")
    - shieldedResources = useMemo(() => resources.filter(r => r.shielded === true), [resources])
    - selectedResource = shieldedResources.find(r => r.id === shieldResId) — guard null before calling evaluateResourceAccess

    Wrap both sections in &lt;div className="space-y-6"&gt; below the policy divergence section.
    Copy all hint and intro strings verbatim from the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -i "ContextView" | head -20</automated>
  </verify>
  <done>ContextView.tsx compiles with 0 TypeScript errors. Deployment toggle renders HOME and ABROAD buttons with correct active/inactive styling. Shielding panel sources resources from worldState.resources where shielded===true (not a hardcoded list). All intro/hint copy matches UI-SPEC verbatim. ContextTrace local component defined but not exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → picker selections | Subject, resource, unit, subunit picker values — used as lookup keys in in-memory arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Picker onChange values | accept | All selections are constrained to values from in-memory arrays (subjects, resources, UNIT_IDS, SUBUNITS); demo-only; no backend |
| T-03-03-02 | Information Disclosure | Policy POLICIES record | accept | Demo mock data; no real org policies or sensitive criteria |
| T-03-03-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
TypeScript check: `cd frontend && npx tsc --noEmit --project tsconfig.app.json` — 0 errors.

Manual smoke (after DemoRoot wired in Plan 03-04):
- Navigate to Context tab
- Policy panel: select a SECRET-cleared subject; INTEL column should show DENY, INDUSTRY column likely ALLOW
- Deployment: select INFRA as requester, su-1 (MILITARY_1, HOME) — expect DENY; flip to ABROAD — expect ALLOW
- Shielding: select MILITARY_1 as requester, INTEL Threat Brief (allowlisted) — ALLOW; select MILITARY_2 — DENY
</verification>

<success_criteria>
- ContextView.tsx exports named ContextView
- Policy divergence section: 6-unit grid with DecisionTrace per unit
- Deployment toggle: local useState, no world-state dispatch; blue = active, slate = inactive
- Shielding panel reads resources from worldState.resources where shielded===true
- All copy strings from UI-SPEC Copywriting Contract present verbatim
- ContextTrace is local only (not exported)
- 0 TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/03-audit-context/03-03-SUMMARY.md` when done.
</output>
