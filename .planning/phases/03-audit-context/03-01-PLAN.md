---
phase: 03-audit-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/seed.ts
  - frontend/src/demo/lib/auditlog.ts
  - frontend/src/demo/lib/auditlog.test.ts
  - frontend/src/demo/lib/policy.ts
  - frontend/src/demo/lib/policy.test.ts
  - frontend/src/demo/lib/obligations.ts
  - frontend/src/demo/lib/obligations.test.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - CTX-01
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "reconstructSubject(subjectId, subjects, events, asOf) replays AttrOp events up to asOf and returns a mutated subject clone"
    - "AUTHORIZE_SUBJECT and WITHDRAW_AUTHORIZATION ops update subject.authorization.status in the replayed clone"
    - "evaluateWithAuth applies the authorization-status rule as a post-evaluate pass, returning DENY with 'Authorization valid' rule entry when status !== AUTHORIZED"
    - "whoCanAccess(req, events, subjects, asOf) returns the list of subjects that ALLOW evaluate to the given requirement as of asOf"
    - "evaluateWithPolicy(principal, req, policy) honours INTEL strict floor (TOP_SECRET) and INDUSTRY relaxed flags (no NTK / no affiliation)"
    - "POLICIES record covers all 6 UnitIds with correct flavors: MILITARY_1 standard, INTEL strict, INDUSTRY relaxed, MILITARY_2/INFRA/HOME_GUARD standard"
    - "evaluateSubunitAccess grants access when subunit.deployment === ABROAD and requester has a support obligation; denies for HOME or no obligation"
    - "evaluateResourceAccess denies non-allowlisted requesters when resource.shielded === true; always allows the resource ownerUnit"
    - "seed.ts exports INITIAL_EVENTS (4 baseline events), POLICIES (6-unit Record), SUBUNITS array, SUPPORT_OBLIGATIONS array"
    - "All Vitest tests pass for auditlog.test.ts, policy.test.ts, obligations.test.ts"
  artifacts:
    - path: "frontend/src/demo/lib/auditlog.ts"
      provides: "reconstructSubject, whoCanAccess, evaluateWithAuth"
      exports: ["reconstructSubject", "whoCanAccess", "evaluateWithAuth"]
    - path: "frontend/src/demo/lib/auditlog.test.ts"
      provides: "Vitest tests for auditlog functions"
    - path: "frontend/src/demo/lib/policy.ts"
      provides: "evaluateWithPolicy, POLICIES"
      exports: ["evaluateWithPolicy", "POLICIES"]
    - path: "frontend/src/demo/lib/policy.test.ts"
      provides: "Vitest tests for policy functions"
    - path: "frontend/src/demo/lib/obligations.ts"
      provides: "evaluateSubunitAccess, evaluateResourceAccess"
      exports: ["evaluateSubunitAccess", "evaluateResourceAccess"]
    - path: "frontend/src/demo/lib/obligations.test.ts"
      provides: "Vitest tests for obligations functions"
    - path: "frontend/src/demo/lib/seed.ts"
      provides: "INITIAL_EVENTS, POLICIES, SUBUNITS, SUPPORT_OBLIGATIONS"
      contains: "INITIAL_EVENTS"
  key_links:
    - from: "frontend/src/demo/lib/auditlog.ts"
      to: "frontend/src/demo/lib/abac.ts"
      via: "import { evaluate, principalFromSubject } from './abac'"
      pattern: "from.*abac"
    - from: "frontend/src/demo/lib/auditlog.ts"
      to: "frontend/src/demo/lib/model.ts"
      via: "import type { Subject, AttrEvent } from './model'"
      pattern: "from.*model"
    - from: "frontend/src/demo/lib/seed.ts"
      to: "frontend/src/demo/lib/model.ts"
      via: "imports AttrEvent, Subunit, UnitId types"
      pattern: "from.*model"
---

<objective>
Lift spike 007/008/009 library logic into the demo island and extend seed.ts with baseline data.

Purpose: Establish all pure-function building blocks for the Audit and Context views. Every mechanism is already proven in the spikes — this plan lifts and adapts them to the demo's 6-unit UnitId type, injectable subjects pattern (D3-13), and authorization lifecycle ops (D3-04). Tests are ported alongside each lib (D3-14).

Output: 6 new files (3 libs + 3 test files) and 1 extended file (seed.ts). No new npm packages. No UI components. No world-state changes. Purely additive lib layer.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-RESEARCH.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend seed.ts — INITIAL_EVENTS, POLICIES, SUBUNITS, SUPPORT_OBLIGATIONS</name>
  <files>frontend/src/demo/lib/seed.ts</files>
  <read_first>
    - frontend/src/demo/lib/seed.ts — full file: existing subjects, resources, ca5-subj fixture; current events/seq init values
    - frontend/src/demo/lib/model.ts — AttrEvent, AttrOp, Subunit, Deployment, UnitId, EntityPolicy types
    - frontend/src/spikes/lib/obligations.ts — SUBUNITS and SUPPORT_OBLIGATIONS structure to adapt
  </read_first>
  <behavior>
    - INITIAL_EVENTS has exactly 4 entries with seq 1–4, using ops: GRANT_COMPARTMENT (value="BLACKWING", subjectId is the first subject with no BLACKWING compartment in base seed), SET_HOLD (any subject), CLEAR_HOLD (same subject as SET_HOLD), AUTHORIZE_SUBJECT (subjectId="ca5-subj")
    - POLICIES is Record&lt;UnitId, EntityPolicy&gt; with 6 keys: MILITARY_1 standard, MILITARY_2 standard, INTEL strict (minClearanceFloor: "TOP_SECRET"), INFRA standard, INDUSTRY relaxed (needToKnow: false, affiliation: false), HOME_GUARD standard
    - SUBUNITS is Subunit[] with at least 3 entries using unit keys MILITARY_1 and MILITARY_2
    - SUPPORT_OBLIGATIONS is { from: UnitId; to: UnitId }[] with at least 2 entries (INFRA→MILITARY_1, MILITARY_2→MILITARY_1)
    - All exports are named exports; no default exports added
    - Existing exports in seed.ts are NOT removed or changed
  </behavior>
  <action>
    Read seed.ts in full. Find which subject does NOT already have "BLACKWING" in its compartments array (check ca1-subj through ca5-subj and fw1-subj through fw3-subj). Use that subjectId for the GRANT_COMPARTMENT event (seq 1). Use "subj-1" or equivalent for SET_HOLD (seq 2) and CLEAR_HOLD (seq 3). Use "ca5-subj" for AUTHORIZE_SUBJECT (seq 4) — this subject has authorization.status: WITHDRAWN in the base seed, making the event narratively meaningful.

    Append to seed.ts (after existing exports) the following named exports:
    - INITIAL_EVENTS: AttrEvent[] — 4 entries as described in behavior block; actor strings use human-readable labels matching the demo role labels ("Access Approver / AO", "Security Officer", "Manager / Supervisor")
    - POLICIES: Record&lt;UnitId, EntityPolicy&gt; — 6 entries per D3-06. Standard policy: all rules true, no floor. Strict (INTEL): all rules true, minClearanceFloor: "TOP_SECRET". Relaxed (INDUSTRY): clearance true, domainTier true, needToKnow false, affiliation false.
    - SUBUNITS: Subunit[] — 3 entries: { id: "su-1", name: "1st Recon Coy", unit: "MILITARY_1", deployment: "HOME" }, { id: "su-2", name: "Field Hospital", unit: "MILITARY_1", deployment: "ABROAD" }, { id: "su-3", name: "2nd Armoured", unit: "MILITARY_2", deployment: "HOME" }
    - SUPPORT_OBLIGATIONS: { from: UnitId; to: UnitId }[] — [{ from: "INFRA", to: "MILITARY_1" }, { from: "MILITARY_2", to: "MILITARY_1" }, { from: "INFRA", to: "MILITARY_2" }]

    Do NOT import from abac.ts or any file other than model.ts (type imports only). Do NOT change seedWorld() — that is Task 2's job (world-state.tsx). Do NOT reset or duplicate existing exports.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -i "seed" | head -20</automated>
  </verify>
  <done>seed.ts exports INITIAL_EVENTS (4 entries), POLICIES (6 UnitId keys), SUBUNITS (3 entries), SUPPORT_OBLIGATIONS (3 entries). TypeScript compilation reports no errors in seed.ts. Existing exports remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create demo/lib/auditlog.ts and auditlog.test.ts (lift from spike 007, D3-04)</name>
  <files>
    frontend/src/demo/lib/auditlog.ts
    frontend/src/demo/lib/auditlog.test.ts
  </files>
  <read_first>
    - frontend/src/spikes/lib/auditlog.ts — full file: reconstructSubject, whoCanAccess, AttrEvent, AccessRow — lift verbatim then adapt
    - frontend/src/spikes/lib/auditlog.test.ts — full file: test fixtures to port
    - frontend/src/demo/lib/model.ts — Subject, AttrEvent, AttrOp union type (all ops including AUTHORIZE_SUBJECT, WITHDRAW_AUTHORIZATION, SET_REVOKED, CLEAR_REVOKED)
    - frontend/src/demo/lib/abac.ts — evaluate, principalFromSubject, requirementFromResource, Requirement, Decision signatures
    - frontend/src/demo/lib/seed.ts — (after Task 1) subject IDs and compartments for test fixtures
  </read_first>
  <behavior>
    - reconstructSubject(subjectId: string, subjects: Subject[], events: AttrEvent[], asOf: number): Subject | null — clones the matching subject, replays all events where event.subjectId === subjectId AND event.seq &lt;= asOf. Handles all ops: GRANT_COMPARTMENT (dedup), REVOKE_COMPARTMENT, SET_HOLD, CLEAR_HOLD, AUTHORIZE_SUBJECT (sets authorization.status to "AUTHORIZED"), WITHDRAW_AUTHORIZATION (sets authorization.status to "WITHDRAWN"), SET_REVOKED (sets flags.revoked true), CLEAR_REVOKED (sets flags.revoked false)
    - evaluateWithAuth(subject: Subject, req: Requirement): Decision — calls evaluate(principalFromSubject(subject), req) then, if subject.authorization && subject.authorization.status !== "AUTHORIZED", appends { name: "Authorization valid", pass: false, detail: "authorization.status={status} (requires AUTHORIZED)" } to result.rules and "Authorization valid" to result.failed and forces result.decision to "DENY"
    - whoCanAccess(req: Requirement, events: AttrEvent[], subjects: Subject[], asOf: number): Subject[] — iterates all subjects, calls reconstructSubject for each, calls evaluateWithAuth on the reconstructed subject, returns subjects where decision === "ALLOW"
    - Tests: at minimum — reconstructSubject returns null for unknown id; GRANT_COMPARTMENT adds compartment; REVOKE_COMPARTMENT removes it; SET_HOLD sets hold; CLEAR_HOLD clears it; AUTHORIZE_SUBJECT sets AUTHORIZED; WITHDRAW_AUTHORIZATION sets WITHDRAWN; evaluateWithAuth returns DENY with "Authorization valid" rule when status is WITHDRAWN; asOf=0 returns clean base state (no events applied)
  </behavior>
  <action>
    Create frontend/src/demo/lib/auditlog.ts:
    - Import: `import { evaluate, principalFromSubject } from "./abac"` and `import type { Subject, AttrEvent } from "./model"` and `import type { Requirement, Decision } from "./abac"` (or wherever these types are exported — check abac.ts exports).
    - Do NOT import from spike paths or from seed.ts (subjects are injected, not imported).
    - Do NOT modify abac.ts — it is frozen per D-01.
    - The reconstructSubject function must deep-clone the subject before mutating (use structuredClone or manual spread). The switch over AttrOp must handle all 8 ops listed in behavior. Silently ignore unknown ops.
    - evaluateWithAuth is the authorization-gate wrapper (D3-04). Export it.
    - Export: reconstructSubject, whoCanAccess, evaluateWithAuth. Also re-export AttrEvent if needed by consumers (check whether model.ts already exports it — if yes, no re-export needed).

    Create frontend/src/demo/lib/auditlog.test.ts:
    - Import from "./auditlog" and "./model" (types). Build minimal Subject fixtures inline (do not import from seed.ts — keeps tests isolated per D3-13).
    - Port the spike test file structure, replacing EntityId with UnitId and subjects-as-module-import with an inline subjects array parameter.
    - Each test: arrange (build subject + events array), act (call function with asOf), assert (check returned subject fields or decision).
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/auditlog.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>auditlog.ts compiles cleanly. auditlog.test.ts passes all tests (0 failures). reconstructSubject, whoCanAccess, evaluateWithAuth are all exported. Authorization lifecycle ops update subject.authorization.status correctly in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create demo/lib/policy.ts + policy.test.ts and demo/lib/obligations.ts + obligations.test.ts</name>
  <files>
    frontend/src/demo/lib/policy.ts
    frontend/src/demo/lib/policy.test.ts
    frontend/src/demo/lib/obligations.ts
    frontend/src/demo/lib/obligations.test.ts
  </files>
  <read_first>
    - frontend/src/spikes/lib/policy.ts — full file: evaluateWithPolicy, EntityPolicy, POLICIES — lift verbatim then adapt
    - frontend/src/spikes/lib/policy.test.ts — full file: test fixtures to port
    - frontend/src/spikes/lib/obligations.ts — full file: evaluateSubunitAccess, evaluateResourceAccess, SUBUNITS, RESOURCES_CTX, standingAccess, hasObligation — lift verbatim then adapt
    - frontend/src/spikes/lib/obligations.test.ts — full file: test fixtures to port
    - frontend/src/demo/lib/model.ts — UnitId, EntityPolicy, Subunit, Deployment, Resource types; confirm shielded and allowlist fields on Resource
    - frontend/src/demo/lib/abac.ts — evaluate, principalFromSubject, requirementFromResource signatures
    - frontend/src/demo/lib/seed.ts — (after Task 1) POLICIES, SUBUNITS, SUPPORT_OBLIGATIONS for reference; do NOT import POLICIES into policy.ts — policy.ts defines its own evaluateWithPolicy; consumers import POLICIES from seed.ts
  </read_first>
  <behavior>
    policy.ts:
    - evaluateWithPolicy(principal: Principal, req: Requirement, policy: EntityPolicy): Decision — applies the policy's rule toggles. If policy.minClearanceFloor is set and principal.clearance does not meet the floor, force DENY. If policy.rules.needToKnow === false, skip the NTK rule. If policy.rules.affiliation === false, skip the affiliation rule. Delegates to evaluate() for all active rules.
    - EntityPolicy type (if not already in model.ts): { id: string; label: string; rules: { clearance: boolean; domainTier: boolean; needToKnow: boolean; affiliation: boolean }; minClearanceFloor?: string }
    - Does NOT export POLICIES — that lives in seed.ts (D3-06). Does export evaluateWithPolicy and EntityPolicy type.

    obligations.ts:
    - evaluateSubunitAccess(requester: UnitId, subunit: Subunit, obligations: { from: UnitId; to: UnitId }[]): Decision — returns ALLOW if subunit.deployment === "ABROAD" AND obligations contains { from: requester, to: subunit.unit }. Returns DENY otherwise. Returns a Decision-shaped object with decision, rules[], failed[].
    - evaluateResourceAccess(requester: UnitId, resource: Resource): Decision — if resource.shielded !== true, returns ALLOW. If requester === resource.ownerUnit, returns ALLOW (owner always passes). If resource.allowlist includes requester, returns ALLOW. Otherwise returns DENY with rule name "Directional shielding".
    - Exports: evaluateSubunitAccess, evaluateResourceAccess.
    - Does NOT define SUBUNITS or SUPPORT_OBLIGATIONS inside the file — those live in seed.ts. The functions accept them as parameters.

    Tests:
    - policy.test.ts: test INTEL strict floor blocks SECRET clearance; INDUSTRY relaxed passes even without NTK compartment; standard policy applies all rules.
    - obligations.test.ts: test ABROAD + obligation = ALLOW; HOME = DENY; ABROAD without obligation = DENY; shielded resource denies non-allowlisted; shielded resource allows ownerUnit; shielded resource allows allowlisted unit; non-shielded resource always allows.
  </behavior>
  <action>
    Create frontend/src/demo/lib/policy.ts:
    - Imports: evaluate (or a subset of evaluate logic), principalFromSubject (if needed), and types from "./model" and "./abac".
    - Check model.ts for EntityPolicy — if it already defines the type, import from "./model". If not, define it locally and export it.
    - The INTEL floor check: use a clearance order array ["UNCLASSIFIED", "CONFIDENTIAL", "SECRET", "TOP_SECRET"]. If policy.minClearanceFloor is set, compare clearanceOrder.indexOf(principal.clearance) &lt; clearanceOrder.indexOf(policy.minClearanceFloor) — if true, return DENY with rule "Minimum clearance floor".
    - Delegating to evaluate() for remaining rules: check if abac.ts expose a way to toggle rules. If not, replicate the rule subset directly in evaluateWithPolicy based on the policy.rules flags (clearance, domainTier, needToKnow, affiliation). Keep it simple — call evaluate() and post-filter, OR selectively reconstruct. Either approach is valid; choose the one that avoids modifying abac.ts.

    Create frontend/src/demo/lib/obligations.ts:
    - Imports: UnitId, Subunit, Deployment, Resource types from "./model". No abac.ts imports needed unless you use Decision type.
    - Decision type in return: if Decision is exported from abac.ts, import and use it. Otherwise define a local shape { decision: "ALLOW" | "DENY"; rules: { name: string; pass: boolean; detail: string }[]; failed: string[] }.
    - evaluateSubunitAccess signature: (requester: UnitId, subunit: Subunit, obligations: { from: UnitId; to: UnitId }[]) =&gt; Decision
    - evaluateResourceAccess signature: (requester: UnitId, resource: Resource) =&gt; Decision
    - Do not import from seed.ts.

    Create policy.test.ts and obligations.test.ts:
    - Use inline fixtures (no seed.ts imports). Port the spike test structure with entity IDs replaced by UnitId values.
    - Each test covers one behavior assertion from the behavior block above.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/policy.test.ts src/demo/lib/obligations.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>policy.ts and obligations.ts compile cleanly. policy.test.ts and obligations.test.ts both pass (0 failures). evaluateWithPolicy, evaluateSubunitAccess, evaluateResourceAccess are all exported. INTEL strict floor and INDUSTRY relaxed behavior verified by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo island → user input | Slider value (asOf), subject/resource picker selections — used as array indices/filter keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | reconstructSubject / asOf param | accept | asOf is a number derived from a range slider (min=0, max=events.length); never crosses a trust boundary into a backend; demo-only in-memory data |
| T-03-02 | Information Disclosure | POLICIES / INITIAL_EVENTS in seed.ts | accept | Demo island; all data is intentionally public mock data; no real credentials or PII |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all code is lifted from internal spike files |
</threat_model>

<verification>
Run all three test files: `cd frontend && npx vitest run src/demo/lib/auditlog.test.ts src/demo/lib/policy.test.ts src/demo/lib/obligations.test.ts`

All tests pass. TypeScript compilation: `npx tsc --noEmit --project tsconfig.app.json` reports 0 errors.

seed.ts exports confirmed: `grep -n "export const INITIAL_EVENTS\|export const POLICIES\|export const SUBUNITS\|export const SUPPORT_OBLIGATIONS" frontend/src/demo/lib/seed.ts`
</verification>

<success_criteria>
- 6 new files created, seed.ts extended
- All Vitest tests pass (0 failures across all 3 test files)
- TypeScript strict mode: 0 errors in new files
- reconstructSubject handles all 8 AttrOps including AUTHORIZE_SUBJECT / WITHDRAW_AUTHORIZATION
- evaluateWithAuth returns DENY with "Authorization valid" rule when authorization.status !== "AUTHORIZED"
- POLICIES covers all 6 UnitIds with correct flavor differentiation
- No imports from spike paths or shadcn in demo island files
- abac.ts and model.ts are not modified
</success_criteria>

<output>
Create `.planning/phases/03-audit-context/03-01-SUMMARY.md` when done.
</output>
