---
phase: 03-audit-context
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - frontend/src/demo/DemoRoot.tsx
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - CTX-01
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "DemoRoot renders a 4-tab bar with labels 'Decision Explorer', 'Federation Hub', 'Audit', 'Context'"
    - "Active tab uses bg-slate-800 text-white styling; inactive tabs use border-slate-300 text-slate-600"
    - "Selecting 'Audit' renders AuditView; selecting 'Context' renders ContextView"
    - "Decision Explorer and Federation Hub continue to work as before (no regression)"
    - "Default active tab is 'decisions' (unchanged from Phase 2 behavior)"
    - "TypeScript compilation produces 0 errors across the entire demo island"
    - "Vite dev server builds without errors: npm run dev exits without TypeScript/bundle errors"
  artifacts:
    - path: "frontend/src/demo/DemoRoot.tsx"
      provides: "4-tab navigation extending Phase 2 2-tab toggle"
      contains: "ActiveView"
  key_links:
    - from: "frontend/src/demo/DemoRoot.tsx"
      to: "frontend/src/demo/components/AuditView.tsx"
      via: "import { AuditView } from './components/AuditView'"
      pattern: "AuditView"
    - from: "frontend/src/demo/DemoRoot.tsx"
      to: "frontend/src/demo/components/ContextView.tsx"
      via: "import { ContextView } from './components/ContextView'"
      pattern: "ContextView"
---

<objective>
Extend DemoRoot.tsx with 4-tab navigation and wire AuditView + ContextView. Run a full TypeScript + Vite build gate to confirm Phase 3 is complete and shippable.

Purpose: Compose all Phase 3 pieces into the navigable demo shell. This is the integration and verification plan — it changes exactly one file (DemoRoot.tsx) and then runs the build gate.

Output: DemoRoot.tsx extended; npm run build succeeds with 0 errors.
</objective>

<execution_context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-UI-SPEC.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-02-SUMMARY.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/03-audit-context/03-03-SUMMARY.md
</context>

<interfaces>
From frontend/src/demo/components/AuditView.tsx (Plan 03-02):
  export function AuditView(): JSX.Element   // no props

From frontend/src/demo/components/ContextView.tsx (Plan 03-03):
  export function ContextView(): JSX.Element  // no props

From frontend/src/demo/DemoRoot.tsx (current state — Phase 2):
  type ActiveView = 'decisions' | 'federation'   // extend to add 'audit' | 'context'
  useState&lt;ActiveView&gt;('decisions')
  Existing button pattern: bg-slate-800 text-white (active) / border border-slate-300 text-slate-600 hover:bg-slate-50 (inactive)
  Layout: mx-auto max-w-5xl px-6 — matches UI-SPEC tab bar container
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend DemoRoot.tsx to 4-tab navigation + wire AuditView and ContextView</name>
  <files>frontend/src/demo/DemoRoot.tsx</files>
  <read_first>
    - frontend/src/demo/DemoRoot.tsx — full file: current ActiveView type, useState init, tab button JSX, render switch/conditional for existing views
    - frontend/src/demo/components/AuditView.tsx — confirm named export AuditView (no props)
    - frontend/src/demo/components/ContextView.tsx — confirm named export ContextView (no props)
  </read_first>
  <action>
    Read DemoRoot.tsx in full. Make the following surgical changes:

    1. Add imports:
       import { AuditView } from './components/AuditView'
       import { ContextView } from './components/ContextView'

    2. Extend the ActiveView type:
       Change: type ActiveView = 'decisions' | 'federation'
       To:     type ActiveView = 'decisions' | 'federation' | 'audit' | 'context'

    3. Add two tab buttons in the existing tab bar (after the 'Federation Hub' button):
       Each button must use the EXACT same pattern as existing buttons — copy the existing active/inactive class strings verbatim, only change the label text and the view identifier.
       Button 3: label "Audit", onClick={() => setView('audit')}, active when view === 'audit'
       Button 4: label "Context", onClick={() => setView('context')}, active when view === 'context'

    4. Extend the render conditional (if/else chain or switch) to handle 'audit' and 'context':
       When view === 'audit': render &lt;AuditView /&gt;
       When view === 'context': render &lt;ContextView /&gt;

    Do NOT change: the default view ('decisions'), the existing button classes, the existing layout wrapper classes, the WorldProvider wrapping (it must stay), or any other logic.

    These are the ONLY changes. Do not reformat unrelated lines.

    Active tab button classes (from UI-SPEC): "bg-slate-800 text-white rounded px-3 py-2 text-sm"
    Inactive tab button classes (from UI-SPEC): "border border-slate-300 text-slate-600 hover:bg-slate-50 rounded px-3 py-2 text-sm"
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -10</automated>
  </verify>
  <done>DemoRoot.tsx has ActiveView type with 4 values. Two new tab buttons render with correct active/inactive classes. AuditView and ContextView are imported and rendered when selected. 0 TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite + Vite build gate</name>
  <files></files>
  <read_first>
    - frontend/package.json — confirm build script name and test script name
    - frontend/vite.config.ts — confirm demo entry is registered (should be from Phase 1)
  </read_first>
  <action>
    Run the full Vitest test suite for the demo lib files to confirm all tests pass:
    cd frontend && npx vitest run src/demo/lib/

    Then run the Vite production build:
    cd frontend && npm run build

    If the build fails, read the error output and fix TypeScript or import errors in the Phase 3 files only. Do not modify spike files or other pre-existing files.

    Common failure modes:
    - Missing type import: add `import type { X } from './model'`
    - Pill/Card/Select prop type mismatch: read ui.tsx and fix the prop
    - evaluateWithPolicy receiving wrong Decision shape: verify abac.ts Decision type compatibility
    - ContextView shieldedResources empty array: guard with shieldedResources[0]?.id ?? '' in useState init

    The build gate confirms D2-04 requirement (Phase 2 ensured Vite build stability) is not regressed and Phase 3 additions compile in production mode.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/ 2>&1 | tail -15 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All demo lib Vitest tests pass (0 failures). `npm run build` exits with code 0 and no TypeScript errors. The demo entry is in the build output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab click → view state | User selects active tab; useState constrains to 4 known string values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | ActiveView tab state | accept | Constrained to the 4-value union type at compile time; no external input |
| T-03-04-02 | Information Disclosure | Production build artifact | accept | Demo island with intentionally public mock data; [DEMO/MOCK] banner present per MODEL-03 |
| T-03-04-SC | Tampering | npm installs | accept | No new packages; build gate only |
</threat_model>

<verification>
Full test run: `cd frontend && npx vitest run src/demo/`

Build: `cd frontend && npm run build` — exit code 0

Smoke check after build: `ls dist/ | grep demo` — demo entry present in output.

Manual verification (dev server): `npm run dev`, navigate to localhost:15510/demo.html (or equivalent), click Audit tab — slider and event list visible; click Context tab — 3 sections visible.
</verification>

<success_criteria>
- DemoRoot.tsx has 4 tab buttons: "Decision Explorer", "Federation Hub", "Audit", "Context"
- Active/inactive tab classes match UI-SPEC exactly
- Selecting Audit renders AuditView; selecting Context renders ContextView
- Decision Explorer and Federation Hub render unchanged (no regression)
- All demo lib Vitest tests pass (0 failures)
- `npm run build` exits code 0 with no errors
- Phase 3 is complete: AUDIT-01, AUDIT-02, CTX-01, CTX-02, CTX-03 all delivered
</success_criteria>

<output>
Create `.planning/phases/03-audit-context/03-04-SUMMARY.md` when done.
</output>
