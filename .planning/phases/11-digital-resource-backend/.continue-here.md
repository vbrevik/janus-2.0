---
context: phase
phase: 11-digital-resource-backend
task: 0
total_tasks: 1
status: in_progress
last_updated: 2026-07-02T13:20:00.000Z
---

# Phase 11 — Paused between plans (11-03 done, 11-04 next)

Phase 11 is **3 of 4 plans complete**. 11-03 was executed to a green, committed
state this session. Only **11-04 (security hardening, SEC-01..04)** remains.

<current_state>
11-03 fully complete and committed (HEAD `3984a12`). The full dev stack is
running (frontend :15510, backend :15520, postgres :15530). Nothing is mid-edit —
this is a clean pause before starting the final Phase-11 plan, 11-04.
</current_state>

<completed_work>

- 11-01: migration-chain repair + 8 digital-resource tables (RSRC-BE-01/06) — Done
- 11-02: resolver port + sqlx models + golden parity (RSRC-BE-02) — Done
- 11-03: endpoints + seed migration + seedWorld de-hardcode + **IDOR fix** (RSRC-BE-03/04/05) — Done
  - Commits: `5e7bcb5` (IDOR fix, Option B), `987c3d8` (seedWorld de-hardcode), `3984a12` (SUMMARY + SEED-012 + STATE/ROADMAP)
</completed_work>

<remaining_work>

- **11-04** (not started): vendor_relations AuthGuard, per-role RBAC on writes + audit read, JWT fail-loud (remove fallback secret), CORS single-origin.
</remaining_work>

<decisions_made>

- Digital-resource write authz = **Option B (role-based JWT gate)**, not Option A (org-based). No person→org linkage exists in the schema, so Option A is a data-model build; deferred to **SEED-012**. Option B closes the IDOR and matches the codebase's role-based guard convention.
- Completed the paused resolver `NaiveDateTime`→`DateTime<Utc>` migration (DB is TIMESTAMPTZ); `PolicyVersion` stays `NaiveDateTime` for TS golden byte-parity (converted via `.naive_utc()` at the resolver boundary).
- Backed the prematurely-started 11-04 CORS change out to HEAD (wrong `rocket_cors` API; belongs to 11-04).
</decisions_made>

<blockers>
- **Seed migration not applied**: `20260601130001_seed_digital_resources.sql` is committed but the dev DB's `resource_*` tables are empty (`sqlx migrate run` broken on the drifted DB). Seed-dependent tests are `#[ignore]`; demo `/world` returns nothing until applied/repaired.
- **Option A deferred**: SEED-012 dormant (blocked on person→org data model).
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-digital-resource-backend/11-03-SUMMARY.md` — what shipped, the IDOR fix, verification results
2. `.planning/phases/11-digital-resource-backend/11-04-PLAN.md` — the next plan to execute (SEC-01..04)
3. `.planning/seeds/SEED-012-org-based-resource-authz.md` — deferred org-based authz + the DB-linkage gap
4. `.planning/STATE.md` — position (plan 8/10, 80%) + carried concerns

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN] Trusting a client-supplied org/identity from the request body for authorization (the 11-03 IDOR: `actor_org_id`/`granted_by_org_id`). → Always derive the authorizing identity server-side from the AuthGuard/JWT.
- [ANTI-PATTERN] CORS via `AllowedOrigins::exact(...)`/`::all()` on `rocket_cors` 0.6 — those don't exist / are wrong. → Use `AllowedOrigins::some_exact(&["http://localhost:15510"])` when doing SEC-04.
- [ANTI-PATTERN] Double-backgrounding a server (`cmd & ` inside a `run_in_background` shell) — the child gets orphaned/untracked. → Launch long-running servers as a single tracked background process, no trailing `&`.

## Infrastructure State
- **postgres** :15530 — running (docker: `docker compose -f docker-compose.dev.yml`). Data persists; `resource_*` tables EMPTY.
- **frontend** :15510 — vite dev running (background; may not survive the session). URL http://localhost:15510/
- **backend** :15520 — rocket running but **ORPHANED/untracked** (was pid 61763; a 2nd tracked `cargo run` failed "address already in use"). Login probe → HTTP 200. To manage cleanly: `pkill -f 'target/debug/janus-backend'` then `cd backend && RUST_LOG=info cargo run`.
- Backend **lib** compiles clean; the **lib unit-test** target is broken by a pre-existing gap (`organizations/handlers.rs` fixtures miss `department`) — does not affect the integration test binaries.

<context>
Straightforward from here: 11-03 is closed and verified. The only open work in
Phase 11 is 11-04 security hardening. Nothing is half-done in code. The main
thing a resumer should weigh is whether to apply/repair the seed migration so the
demo and seed-dependent tests have data — independent of 11-04.
</context>

<next_action>
Start with: `/gsd-execute-phase 11` to run 11-04-PLAN.md (SEC-01..04). Read
11-04-PLAN.md first. Redo CORS with `AllowedOrigins::some_exact(&["http://localhost:15510"])`.
</next_action>
