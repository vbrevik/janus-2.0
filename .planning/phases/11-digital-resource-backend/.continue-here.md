---
context: phase
phase: 11-digital-resource-backend
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-23T20:28:47.931Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Not suggestions. Discovered through a real failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **client-supplied actor authority** — the committed 11-03 issue handlers authorize against an org taken from the request body, not the authenticated caller. Any fix or re-execution of issue endpoints MUST derive the authorizing identity server-side from `auth.claims.sub` and never from request-body fields (`actor_org_id`, `granted_by_org_id`).

**Do not proceed until that box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Client-supplied actor (IDOR) | `issue_grant`/`issue_delegate` in `backend/src/digital_resources/handlers.rs` discard the AuthGuard principal (`let _ = &auth`) and pass `data.actor_org_id` / `data.granted_by_org_id` (request body) into `can_issue_resource_grant`. Any authenticated user can assert any ADMIN org and issue grants. Committed as `8ea8948`; flagged by background commit security review. Violates RSRC-BE-04. | blocking | The handler must resolve the caller's authorizing org from `auth.claims.sub` via a DB lookup; body-supplied org fields are dropped or only accepted after verifying the caller actually belongs to that org. A POST-403 integration test for a non-authorized caller asserting someone else's org must exist and pass. |
| Re-dispatch over a partial executor | The 11-03 executor was interrupted mid-run; T1+T2 are committed but T3 + SUMMARY are not. Blindly re-running `/gsd-execute-phase 11` would duplicate work. | advisory | Resume by inspecting git state (commits `8ea8948`, `290fd1c`) + uncommitted files first; continue from the IDOR fix, not from Task 1. |

<current_state>
Paused mid-`/gsd-execute-phase 11`, Wave 3 / plan 11-03, Task 3.
The executor ran partially before a user interrupt, then a background security review flagged a real IDOR in the committed handlers. User chose to **pause and inspect** rather than fix-forward or roll back, and asked for **advice** on the authz model before deciding.

HEAD = `290fd1c` on branch `exit`. Worktrees auto-degraded to sequential (unresolved fork base on `exit`).
</current_state>

<completed_work>

- Task 1: handlers (get_world + issue_grant + issue_delegate) + mount — committed `8ea8948` — **DONE BUT INSECURE (IDOR, see blocking constraint)**
- Task 2: idempotent seed migration `20260601130001_seed_digital_resources.sql` — committed `290fd1c` — Done
- Task 3: integration tests + remove seedWorld() hardcoding — **In progress**: `backend/tests/digital_resources_api_test.rs` written but untracked; frontend de-hardcode not started
</completed_work>

<remaining_work>

1. **FIX IDOR first** — rewrite `issue_grant` / `issue_delegate` to derive the authorizing org from `auth.claims.sub` server-side (depends on the authz-model decision below).
2. Finish + commit Task 3 integration tests: GET 200/401, POST 403 (non-ADMIN / expired-delegate / out-of-window-delegate), grant+delegate idempotency.
3. Remove digital-resource fixture hardcoding from `frontend/src/demo/store/world-state.tsx` (`seedWorld()`).
4. Write `11-03-SUMMARY.md`; update STATE.md + ROADMAP.md plan progress.
5. Then Wave 4 / plan 11-04: security hardening SEC-01..04 (was already next; blocked on 11-03).
</remaining_work>

<decisions_made>

- Paused mid-execution instead of fix-forward / rollback (user inspecting personally).
- Commits `8ea8948` / `290fd1c` left in place (not reverted) for review.
</decisions_made>

<blockers>

- **IDOR in handlers.rs** (blocking) — see constraint table. Fix-forward path: derive caller org from `auth.claims.sub`.
- **UNRESOLVED authz-model decision** (user asked to be advised): JWT `Claims = {sub, exp, iat, role}` has **no org field**, but the ported resolver `can_issue_resource_grant(org_id, ...)` authorizes by **org** (ADMIN org-link / delegate). Two options:
  - **A — org-based (matches resolver):** look up the caller's org from `auth.claims.sub` (org_links / relations) and pass that server-derived org in.
  - **B — role-based:** authorize off JWT `role` (e.g. `admin`); simpler but diverges from the resolver's org model.
  - **Investigate before choosing:** do seed users (`admin`/`manager`/`operator`/`viewer`) actually have org-link rows? If not, Option A won't resolve for seed identities — likely why the executor took the insecure shortcut. Check the live DB (`psql` on :15530).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state mirror of this file.
2. `backend/src/digital_resources/handlers.rs` (lines ~142–310) — the insecure issue handlers.
3. `backend/src/auth/jwt.rs` + `backend/src/auth/middleware.rs` — what AuthGuard/Claims actually expose (sub, role only).
4. `.planning/phases/11-digital-resource-backend/11-03-PLAN.md` — the plan + must_haves contract (RSRC-BE-03/04/05).
5. `backend/src/digital_resources/resolver.rs` — `can_issue_resource_grant` signature (org-based authority).

## Critical Anti-Patterns (do NOT repeat these)
- IDOR via client-supplied actor → derive identity from AuthGuard, add a POST-403 test for a caller asserting another org.
- Re-running execute-phase from scratch → resume from the IDOR fix; T1/T2 already committed.

## Infrastructure State
- Branch `exit`, HEAD `290fd1c`. Worktrees degraded to sequential (unresolved fork base).
- Postgres dev on :15530; backend :15520. Live DB drifts from migrations — verify column/FK names before SQL.
- Wide set of `backend/src/**/*.rs` showing as modified in `git status` is **pre-existing drift, not this work** — ignore. Only 11-03 uncommitted files: `digital_resources/models.rs`, `digital_resources/resolver.rs`, `tests/digital_resources_api_test.rs`, `.planning/STATE.md`.

<context>
Backend is becoming source-of-truth for digital resources. The trust-boundary requirement (RSRC-BE-04) is the whole point of the issue endpoints — and it's exactly what got implemented wrong. The blocker isn't the Rust; it's the identity-model gap (JWT role-based vs resolver org-based). Resolve that, then the fix is mechanical.
</context>

<next_action>
Start with: investigate person→org linkage in the live dev DB (do seed users have org_links rows?), then advise the user on Option A vs B, get the decision, then fix the IDOR in `handlers.rs` and finish Task 3.
</next_action>
