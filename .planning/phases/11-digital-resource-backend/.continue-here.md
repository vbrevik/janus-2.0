---
context: phase
phase: 11-digital-resource-backend
task: replan
total_tasks: null
status: in_progress
last_updated: 2026-06-23T12:39:25.749Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Plans 11-01/02/03 predate the SEC-01..04 fold** — they cover RSRC-BE-01..06 only. Executing them as-is ships Phase 11 *without* the security hardening the SPEC now mandates. → Re-plan or amend before any execution.

**Do not proceed to execution until this box is checked and the plans cover SEC-01..04.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale plans vs updated spec | 11-SPEC was re-scored (10 reqs) after the 3 plans were written; the plans don't know about SEC-01..04 | blocking | Diff plans against SPEC reqs 6–9 before executing; amend 11-03 or add a SEC slice |
| Migration repair breaks live DB | RSRC-BE-06 repair could break the already-migrated dev DB on :15530 | blocking | Wave-0 task queries `_sqlx_migrations` FIRST; choose rename-in-place vs compensating migration from live state |
| UI plan-gate false-positive | Backend phase trips the frontend gate because repo has a frontend | advisory | Run plan-phase with `--skip-ui` |

<current_state>
Spec-phase update just completed. `11-SPEC.md` now holds **10 requirements** at **ambiguity 0.18** (gate ≤0.20), having folded the cancelled Phase 13 security work in as **SEC-01..04**. Phase 13 was removed from the roadmap. Plans `11-01/02/03-PLAN.md` exist (untracked) from a prior session but predate this fold.
</current_state>

<completed_work>

This session (2026-06-23):
- Remapped codebase → 7 docs in `.planning/codebase/` — commit `c2c4246`
- Added then folded Phase 13 security into Phase 11 (SEC-01..04); removed Phase 13 — commits `3c152cc`, `baa0101`
- Re-spec'd `11-SPEC.md`: 10 reqs, ambiguity 0.18, edge + prohibition rows for SEC; updated REQUIREMENTS.md + STATE.md

Prior session (carried forward):
- 11-SPEC (RSRC-BE-01..06), 11-CONTEXT, 11-DISCUSSION-LOG (D-01..D-09), 11-RESEARCH (9 migration breakages), 11-PATTERNS
- `11-01/02/03-PLAN.md` written (untracked)
</completed_work>

<remaining_work>

- Re-plan Phase 11 to cover **SEC-01..04** (the 3 existing plans don't)
- Decide: amend `11-03` (handlers/integration) to absorb SEC + add a dedicated SEC slice, or re-run plan-phase
- Commit the untracked plans once reconciled with the SPEC
- Execute: migration repair → schema/seed → Rust domain+resolver → read/issue API → SEC hardening
</remaining_work>

<decisions_made>

- **Fold ALL of Phase 13 into Phase 11**; remove Phase 13 (shares trust boundary with RSRC-BE-04; SEC-03/04 touch `rocket_setup.rs` the phase already edits)
- **RBAC bar**: `AuthGuard` on every handler (incl. `messaging`/`vendor_relations`) + `role_has_permission` on all writes + sensitive (audit) reads — reuse existing primitives, no new framework
- **JWT**: fail-loud everywhere, no fallback; tests/dev set `JWT_SECRET` via env
- **CORS**: `http://localhost:15510`, keep `allow_credentials(true)`
- (prior) Standalone TEXT IDs for the 8 tables; embedded idempotent seed migration; golden-fixture resolver parity; aggregate `GET /api/digital-resources/world`; `--skip-ui`
</decisions_made>

<blockers>
- Plans predate the SEC fold (blocking — see constraints above)
- Migration-chain repair risk (blocking — query live `_sqlx_migrations` first)
- UI plan-gate false-positive (advisory — use `--skip-ui`)
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-digital-resource-backend/11-SPEC.md` — reqs 6–9 (SEC-01..04) are the new work; check edge/prohibition rows
2. `.planning/codebase/CONCERNS.md` — original security findings + exact file/line sites
3. `11-01/02/03-PLAN.md` — existing plans; diff against the SPEC to find the SEC gap
4. `11-RESEARCH.md` + `11-PATTERNS.md` — migration breakages, file→analog mapping

## Infrastructure State
- Postgres dev DB on `:15530` is already migrated (drifted from code); a clean DB still fails to migrate (RSRC-BE-06 target)
- Exact SEC edit sites: `backend/src/shared/rocket_setup.rs:22-23` (JWT fallback), `:58`/`:65` (CORS all + credentials); `backend/src/messaging/handlers.rs` + `backend/src/vendor_relations/handlers.rs` (unguarded); `role_has_permission` lives in `backend/src/shared/rbac.rs`
- No background processes running

<context>
The codebase remap surfaced live server-side security holes; rather than a separate Phase 13, the user chose to fold them into Phase 11 so the new backend ships secure. The resource-backend planning from the prior session is sound but now incomplete against the spec. The cleanest resume is to reconcile the 3 plans with SEC-01..04 before committing/executing them.
</context>

<next_action>
Start with: read `11-SPEC.md` reqs 6–9, then diff the 3 existing PLAN.md files against them. Then either amend `11-03` (+ a SEC slice) or run `/gsd-plan-phase 11 --skip-ui` so planning incorporates the updated SPEC. Do NOT execute the existing plans as-is.
</next_action>
