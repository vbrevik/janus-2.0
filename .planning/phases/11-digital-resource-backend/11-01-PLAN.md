---
phase: 11-digital-resource-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/20250127000000_add_dates_to_vendor_relations.sql
  - backend/migrations/20250128000000_create_nda_table.sql
  - backend/migrations/20250129000000_create_discussions_table.sql
  - backend/migrations/20250129000001_create_document_references_table.sql
  - backend/migrations/20250131000000_create_person_table_unified.sql
  - backend/migrations/20250131000001_drop_old_tables_after_migration.sql
  - backend/migrations/20250131000002_update_relations_entity_type.sql
  - backend/migrations/20251026132437_create_data_access_table.sql
  - backend/migrations/20251026132437_create_physical_access_table.sql
  - backend/migrations/20251101180000_create_unified_relations_table.sql
  - backend/migrations/20251101190000_add_schema_org_relation_types.sql
  - backend/migrations/20251101191000_rename_personnel_to_person.sql
  - backend/migrations/20260601130000_create_digital_resource_tables.sql
autonomous: false
requirements: [RSRC-BE-06, RSRC-BE-01]
must_haves:
  truths:
    - "A freshly-created empty database migrates from empty to current with zero errors (chain repaired on a clean baseline — D-01)"
    - "Re-running migrations against the live drifted dev DB produces no checksum error and no new failures (D-01 verify-against-live)"
    - "A fresh-applied migration creates all 8 digital-resource tables on the repaired baseline (D-02)"
    - "The zombie rename_personnel_to_person migration no longer participates in the authoritative person-table path"
    - "resource_applications has NO classification column (classification is derived from parent platform at resolution time)"
    - "Network/platform classification CHECK includes RESTRICTED (5 levels), distinct from the existing person.clearance_level 4-level constraint which is NOT altered"
  artifacts:
    - path: "backend/migrations/20260601130000_create_digital_resource_tables.sql"
      provides: "8 digital-resource tables (resource_networks, resource_platforms, resource_applications, resource_org_links, resource_policies, resource_policy_assignments, resource_access_grants, resource_access_delegates)"
      contains: "CREATE TABLE resource_networks"
  key_links:
    - from: "backend/migrations (repaired chain)"
      to: "fresh empty Postgres DB"
      via: "sqlx migrate run applies every file in timestamp order with no ALTER-before-CREATE and no duplicate version"
      pattern: "sqlx migrate run"
  prohibitions:
    - statement: "Application classification MUST NOT be stored or editable independently of its Platform (no column, no API field)"
      status: resolved
      verification: "schema audit — resource_applications has no classification column; grep the create migration shows none"
    - statement: "An already-applied migration file (present in live _sqlx_migrations) MUST NOT have its content or filename edited (checksum lock)"
      status: resolved
      verification: "Task 1 queries _sqlx_migrations first; only files whose version is absent there are renamed/edited"
---

<objective>
Repair the broken backend migration chain so a freshly-created empty Postgres database migrates end-to-end with zero errors (RSRC-BE-06), then add a single additive migration creating the 8 digital-resource tables on the repaired baseline (RSRC-BE-01, schema only — models/handlers/seed are later plans).

Purpose: Every downstream Phase 11 task (models, resolver fixtures, handlers, seed) depends on a clean, applicable schema. The chain is currently un-applicable on a fresh DB (ALTER-before-CREATE, duplicate version timestamps, a zombie rename migration, a buggy INSERT), which blocks CI, new-dev onboarding, and the new tables.
Output: A repaired migration set that applies cleanly on a fresh DB AND does not break the live drifted dev DB, plus `20260601130000_create_digital_resource_tables.sql` with all 8 tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-digital-resource-backend/11-SPEC.md
@.planning/phases/11-digital-resource-backend/11-RESEARCH.md
@.planning/phases/11-digital-resource-backend/11-CONTEXT.md
@.planning/phases/11-digital-resource-backend/11-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Bring up Postgres + capture live migration state (read-only, gates the repair strategy)</name>
  <files>(read-only) backend/migrations/, live janus2 DB _sqlx_migrations table</files>
  <read_first>
    - CLAUDE.md §Commands (docker compose -f docker-compose.dev.yml up -d brings up Postgres on :15530)
    - CLAUDE.md §Gotchas (schema drift / migrations)
    - .planning/phases/11-digital-resource-backend/11-RESEARCH.md §Section 1 (Repair Strategy Step 1 — VERIFY FIRST) and §Open Questions Q1, Assumptions A1/A2
    - Project memory project_migrations_fresh_db_broken (reconstruction recipe)
  </read_first>
  <what-built>
    The executor will: (1) run `docker compose -f docker-compose.dev.yml up -d` to start Postgres on :15530; (2) query `SELECT version FROM _sqlx_migrations ORDER BY version;` on the live `janus2` DB; (3) record the exact set of already-applied versions. This is the cut-point that decides which files may be renamed/edited (versions ABSENT from the table) vs which are frozen (versions PRESENT — checksum-locked, repair them only via a NEW migration).
  </what-built>
  <how-to-verify>
    1. Run: `docker compose -f docker-compose.dev.yml up -d` — Postgres becomes reachable on localhost:15530.
    2. Run: `docker exec -e PGPASSWORD=janus_dev_password $(docker ps --filter "publish=15530" -q) psql -U janus -d janus2 -c "SELECT version FROM _sqlx_migrations ORDER BY version;"`
    3. Confirm the printed list. If `_sqlx_migrations` does NOT contain the Jan-misdated versions (20250127000000, 20250128000000, 20250129000000, 20250129000001, 20250131000000/1/2), the rename strategy in Task 1 is safe. If ANY of those versions ARE present, STOP — they are checksum-locked; the repair must shift to a compensating-migration approach and the executor must surface this before editing files.
  </how-to-verify>
  <action>
    Bring up Postgres via `docker compose -f docker-compose.dev.yml up -d`. Query `_sqlx_migrations` on database `janus2` and record the applied version set verbatim into the SUMMARY. Decide the repair mode: RENAME-IN-PLACE (if the broken/misdated versions are absent) vs COMPENSATING-MIGRATION (if any are already applied). Do NOT edit any migration file in this task — this is read-only reconnaissance that gates Task 1.
  </action>
  <verify>
    <automated>docker exec -e PGPASSWORD=janus_dev_password $(docker ps --filter "publish=15530" -q) psql -U janus -d janus2 -tAc "SELECT version FROM _sqlx_migrations ORDER BY version;"</automated>
  </verify>
  <acceptance_criteria>
    - The applied-version set from `_sqlx_migrations` on `janus2` is recorded in the SUMMARY verbatim.
    - A repair-mode decision (RENAME-IN-PLACE vs COMPENSATING-MIGRATION) is stated with the cut-point version.
    - No migration file content or filename was changed in this task.
  </acceptance_criteria>
  <resume-signal>Type "applied versions recorded, mode chosen" or paste the version list + chosen repair mode.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Repair the migration chain so a fresh empty DB migrates end-to-end with zero errors (RSRC-BE-06)</name>
  <files>backend/migrations/*.sql (renames + the buggy unified-relations INSERT fix + zombie deletion — see frontmatter files_modified)</files>
  <read_first>
    - .planning/phases/11-digital-resource-backend/11-RESEARCH.md §Section 1 (the B-01..B-09 breakage table + the concrete repair sequence) and §Common Pitfalls 1 (already-applied checksum lock)
    - backend/migrations/20251101180000_create_unified_relations_table.sql (the buggy `deleted_at IS NULL OR deleted_at IS NOT NULL` INSERT to fix)
    - backend/migrations/20250131000000_create_person_table_unified.sql (the AUTHORITATIVE person-table path)
    - backend/migrations/20251101191000_rename_personnel_to_person.sql (the ZOMBIE to excise)
    - The Task 0 recorded applied-version set (frozen vs free files)
    - project memory project_migrations_fresh_db_broken
  </read_first>
  <action>
    Apply the repair to ONLY the files whose versions were absent from live `_sqlx_migrations` (per Task 0). Concrete moves (RENAME-IN-PLACE mode): rename `20250127000000_add_dates_to_vendor_relations.sql`→`20251026195325_...`, `20250128000000_create_nda_table.sql`→`20251026195326_...`, `20250129000000_create_discussions_table.sql`→`20251026195327_...`, `20250129000001_create_document_references_table.sql`→`20251026195328_...`, `20250131000000_create_person_table_unified.sql`→`20251101192000_...`, `20250131000001_drop_old_tables_after_migration.sql`→`20251101192001_...`, `20250131000002_update_relations_entity_type.sql`→`20251101192002_...`. Fix duplicate version `20251026132437` by renaming `create_data_access_table.sql`→`20251026132438_...` and `create_physical_access_table.sql`→`20251026132439_...` (keep `create_computer_access_table.sql` at 132437). Fix duplicate version `20251101190000` by renaming `add_schema_org_relation_types.sql`→`20251101190001_...` (keep rename_vendors_to_organizations at 190000). DELETE the zombie `20251101191000_rename_personnel_to_person.sql` (superseded by the unified-create authoritative path). In `20251101180000_create_unified_relations_table.sql`, fix the buggy INSERT by removing the `WHERE (deleted_at IS NULL OR deleted_at IS NOT NULL)` clause that references a never-existent `deleted_at` column on `vendor_relations`. Add `IF NOT EXISTS` to any remaining non-idempotent index DDL. If Task 0 chose COMPENSATING-MIGRATION mode for any frozen file, instead add a NEW migration that fixes that file's damage without editing it — surface which files this applied to.
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://janus:janus_dev_password@localhost:15530/janus2_fresh sqlx migrate run --source backend/migrations 2>&1 | grep -iv error | tail -1; echo "exit-check:"; DATABASE_URL=postgresql://janus:janus_dev_password@localhost:15530/janus2_fresh sh -c 'sqlx database drop -y 2>/dev/null; sqlx database create && sqlx migrate run --source backend/migrations' 2>&1 | grep -ic error</automated>
  </verify>
  <acceptance_criteria>
    - Against a freshly-created empty database `janus2_fresh`, `sqlx migrate run --source backend/migrations` completes with ZERO lines matching `error` (grep -ic error == 0) and a non-error exit.
    - Re-running `sqlx migrate run` against the live `janus2` DB reports no new migrations / no checksum mismatch (no `"different checksum"` error).
    - `20251101191000_rename_personnel_to_person.sql` no longer exists in `backend/migrations/`.
    - No two files in `backend/migrations/` share a version-timestamp prefix (`ls backend/migrations | sed 's/_.*//' | sort | uniq -d` prints nothing).
    - `20251101180000_create_unified_relations_table.sql` contains no `deleted_at` token.
  </acceptance_criteria>
  <done>A clean DB migrates from empty to current with zero errors; the live dev DB is not broken; the zombie is gone; duplicate versions resolved; the buggy relations INSERT fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive migration creating the 8 digital-resource tables (RSRC-BE-01 schema)</name>
  <files>backend/migrations/20260601130000_create_digital_resource_tables.sql</files>
  <read_first>
    - .planning/phases/11-digital-resource-backend/11-RESEARCH.md §Section 6 (the 8 CREATE TABLE definitions with exact columns, CHECK constraints, FKs, indexes) and §Section 2 (RESTRICTED clearance handling)
    - backend/migrations/20251026112330_create_vendors_table.sql (the CREATE TABLE analog — column/index style)
    - 11-PATTERNS.md §create_digital_resource_tables.sql (id TEXT vs SERIAL split; JSONB gates; text-to-text FKs)
    - CLAUDE.md §Gotchas (clearance levels are UNCLASSIFIED|CONFIDENTIAL|SECRET|TOP_SECRET in the existing person constraint — do NOT alter it)
  </read_first>
  <action>
    Create `backend/migrations/20260601130000_create_digital_resource_tables.sql` (timestamp must sort AFTER the last safe migration 20260601120300 and is additive on the repaired baseline). Create 8 tables: (1) `resource_networks` — `id TEXT PRIMARY KEY`, `name TEXT NOT NULL`, `classification TEXT NOT NULL CHECK (classification IN ('UNCLASSIFIED','RESTRICTED','CONFIDENTIAL','SECRET','TOP_SECRET'))`, `created_at`/`updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. (2) `resource_platforms` — same + `network_id TEXT NOT NULL` FK to `resource_networks(id) ON DELETE CASCADE`. (3) `resource_applications` — `id TEXT PRIMARY KEY`, `name TEXT NOT NULL`, `platform_id TEXT NOT NULL` FK to `resource_platforms(id) ON DELETE CASCADE`, timestamps — and NO classification column (derived at resolution time). (4) `resource_org_links` — `id SERIAL PRIMARY KEY`, `resource_id TEXT NOT NULL`, `resource_tier TEXT NOT NULL CHECK (resource_tier IN ('NETWORK','PLATFORM','APPLICATION'))`, `org_id TEXT NOT NULL`, `role TEXT NOT NULL`, `valid_from`/`valid_until TIMESTAMPTZ`; index on `resource_id`. (5) `resource_policies` — `id TEXT PRIMARY KEY`, `label TEXT NOT NULL`, `zone_prereq_id TEXT`, `gates JSONB NOT NULL`. (6) `resource_policy_assignments` — `id SERIAL PRIMARY KEY`, `resource_id TEXT NOT NULL`, `resource_tier TEXT NOT NULL CHECK(...)`, `policy_id TEXT NOT NULL REFERENCES resource_policies(id) ON DELETE CASCADE`, `valid_from`/`valid_until TIMESTAMPTZ`; index on `resource_id`. (7) `resource_access_grants` — `id TEXT PRIMARY KEY`, `person_id TEXT NOT NULL`, `resource_id TEXT NOT NULL`, `valid_from`/`valid_until TIMESTAMPTZ`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `CONSTRAINT uq_grant UNIQUE (person_id, resource_id, valid_from, valid_until)`; indexes on `resource_id` and `person_id`. (8) `resource_access_delegates` — `id TEXT PRIMARY KEY`, `resource_id TEXT NOT NULL`, `delegate_type TEXT NOT NULL CHECK (delegate_type IN ('PERSON','ORG'))`, `delegate_person_id TEXT`, `delegate_org_id TEXT`, `granted_by_org_id TEXT NOT NULL`, `valid_from`/`valid_until TIMESTAMPTZ`, plus a CHECK that PERSON-type has delegate_person_id and null org, ORG-type has delegate_org_id and null person; index on `resource_id`. Use standalone TEXT IDs throughout (NOT FKs to organizations/person SERIAL ints) per the CONTEXT discretion lock — verified against the live person/organizations SERIAL-int PKs in 11-RESEARCH §Section 3. Do NOT touch the existing `person.clearance_level` 4-level CHECK constraint.
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://janus:janus_dev_password@localhost:15530/janus2_fresh sh -c 'sqlx database drop -y 2>/dev/null; sqlx database create && sqlx migrate run --source backend/migrations' 2>&1 | grep -ic error; docker exec -e PGPASSWORD=janus_dev_password $(docker ps --filter "publish=15530" -q) psql -U janus -d janus2_fresh -tAc "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('resource_networks','resource_platforms','resource_applications','resource_org_links','resource_policies','resource_policy_assignments','resource_access_grants','resource_access_delegates');"</automated>
  </verify>
  <acceptance_criteria>
    - On the fresh `janus2_fresh` DB after migrate, the table-count query returns exactly 8.
    - `grep -i classification backend/migrations/20260601130000_create_digital_resource_tables.sql` shows the RESTRICTED-inclusive 5-value CHECK on resource_networks/resource_platforms and NO classification column under resource_applications.
    - The migrate run prints zero lines matching `error`.
    - `resource_access_grants` has the `uq_grant UNIQUE (person_id, resource_id, valid_from, valid_until)` constraint (needed for idempotent issue in Plan 03).
  </acceptance_criteria>
  <done>All 8 tables exist on a freshly-migrated DB; resource_applications carries no classification column; resource_networks/platforms allow RESTRICTED; the grant uniqueness constraint exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration files → live dev DB | Editing an already-applied migration corrupts the live DB's checksum state |
| migration files → fresh DB | A non-applicable chain blocks all new environments / CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Already-applied migrations (checksum lock) | mitigate | Task 0 queries `_sqlx_migrations` first; only versions absent from it are renamed/edited; frozen files repaired via NEW compensating migration |
| T-11-02 | Denial of Service | Broken migration chain blocks fresh DB / CI | mitigate | Repair verified against a throwaway `janus2_fresh` DB before touching anything else (RSRC-BE-06 acceptance) |
| T-11-03 | Information Disclosure | resource_applications classification | mitigate | No classification column on resource_applications; effective classification derived from parent platform (prohibition, RSRC-BE-01) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan (RESEARCH §Package Legitimacy Audit: N/A); all deps already in Cargo.toml |
</threat_model>

<verification>
- Fresh empty `janus2_fresh` DB migrates end-to-end with zero `error` lines (RSRC-BE-06).
- Live `janus2` DB shows no checksum mismatch on re-run.
- All 8 digital-resource tables exist on the freshly-migrated DB; resource_applications has no classification column.
- No duplicate version-timestamp prefixes remain; zombie rename migration removed.
</verification>

<success_criteria>
- A freshly-created empty database migrates from empty to current with zero errors (Success Criterion 0).
- A fresh-applied migration creates all 8 digital-resource tables (Success Criterion 1, schema half).
- The live dev DB is not broken by the repair.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

**New files:**
- `backend/migrations/20260601130000_create_digital_resource_tables.sql`

**Renamed migration files (RENAME-IN-PLACE mode):**
- `20250127000000_add_dates_to_vendor_relations.sql` → `20251026195325_...`
- `20250128000000_create_nda_table.sql` → `20251026195326_...`
- `20250129000000_create_discussions_table.sql` → `20251026195327_...`
- `20250129000001_create_document_references_table.sql` → `20251026195328_...`
- `20250131000000_create_person_table_unified.sql` → `20251101192000_...`
- `20250131000001_drop_old_tables_after_migration.sql` → `20251101192001_...`
- `20250131000002_update_relations_entity_type.sql` → `20251101192002_...`
- `20251026132437_create_data_access_table.sql` → `20251026132438_...`
- `20251026132437_create_physical_access_table.sql` → `20251026132439_...`
- `20251101190000_add_schema_org_relation_types.sql` → `20251101190001_...`

**Deleted migration file:**
- `20251101191000_rename_personnel_to_person.sql` (zombie)

**Edited migration file:**
- `20251101180000_create_unified_relations_table.sql` (buggy `deleted_at` INSERT clause removed)

**DB tables created (8):** `resource_networks`, `resource_platforms`, `resource_applications`, `resource_org_links`, `resource_policies`, `resource_policy_assignments`, `resource_access_grants`, `resource_access_delegates`

**DB constraint:** `uq_grant UNIQUE (person_id, resource_id, valid_from, valid_until)` on `resource_access_grants`
</artifacts_produced>

<output>
Create `.planning/phases/11-digital-resource-backend/11-01-SUMMARY.md` when done. Record the live `_sqlx_migrations` applied-version set and the chosen repair mode.
</output>
