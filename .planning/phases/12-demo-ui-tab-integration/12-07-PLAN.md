---
phase: 12-demo-ui-tab-integration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/digital-resource-selectors.ts
  - frontend/src/demo/lib/digital-resource-selectors.test.ts
  - frontend/src/demo/lib/digital-resource.test.ts
  - frontend/src/demo/components/resource-access-explorer.tsx
autonomous: false
gap_closure: true
requirements: [RSRC-UI-05]
must_haves:
  truths:
    - "resolveResourceAt threads real allZones/allPhysicalGrants arrays into resolveResourceAccess instead of hardcoded [], so a policy's zone_prereq_id can resolve against actual zone data"
    - "Selecting IntelNet (whose Enhanced/non-baseline policy carries zone_prereq_id: \"zone-room-sr1\") in the Access Resolution Explorer renders the amber 'Advisory (non-blocking)' row live in the browser, for any person/timestamp inside the policy's active window"
    - "A policy with zone_prereq_id: null, or one whose zone_prereq_id has no matching entry in allZones, still returns zoneAdvisory: null (no false positives introduced by the fix)"
  artifacts:
    - "frontend/src/demo/lib/digital-resource-selectors.ts"
    - "frontend/src/demo/lib/digital-resource-selectors.test.ts"
  key_links:
    - "resource-access-explorer.tsx's resolveResourceAt call now passes world.zones/world.grants (the live WorldState fields already held by this component via useWorld(), not seed.ts statics) so any future physical-grant mutation stays in sync with the advisory computation"
    - "digital-resource-selectors.ts's resolveResourceAt signature grows by 2 params (allZones: ZoneNode[], allPhysicalGrants: PhysicalAccessGrant[]) inserted immediately before now, mirroring resolveResourceAccess's own parameter order in model.ts"
    - "digital-resource.test.ts's pre-existing 'selectors: buildResourceTree, activeGrantsForResource, resolveResourceAt' describe block already imports real seed fixtures (the file's designated 'ONE place this file imports real seed fixtures' section) — its 3 existing resolveResourceAt calls are updated to pass ZONES/GRANTS (physical) from that same import, not touched anywhere else"
  prohibitions:
    - statement: "digital-resource-selectors.ts MUST NOT import ZONES/GRANTS statically from seed.ts as a workaround — allZones/allPhysicalGrants must remain explicit function parameters, preserving the file's 'pure read selectors, explicit inputs' contract and keeping resolveResourceAt unit-testable with inline fixtures (D3-13 pattern, matching digital-resource-mapper.test.ts)"
      status: pending
      verification: "grep -c 'from \"./seed\"' frontend/src/demo/lib/digital-resource-selectors.ts == 0"
---

<objective>
Fix the confirmed Phase 12 UAT gap (test 10, RSRC-UI-05, severity major): `resolveResourceAt` in `digital-resource-selectors.ts` hardcodes empty arrays for both `allZones` and `allPhysicalGrants` when calling the core resolver `resolveResourceAccess` (model.ts), so a policy's `zone_prereq_id` can never resolve against real zone data — the mandatory amber "Advisory (non-blocking)" zone-prerequisite row is permanently dead code in the running app, even though it was grep/JSX-verified as present by 12-05-SUMMARY.md and the underlying model.ts resolver's own tests pass (they call `resolveResourceAccess` directly with real zone data, bypassing this bug entirely).

Purpose: restore RSRC-UI-05 (the zone-prerequisite advisory row) to actually working, live, in the running Access Resolution Explorer — not just structurally present in the JSX — and add a regression test that specifically targets `resolveResourceAt` (the function that was broken), so a future revert of this wiring fails a fast unit test instead of only being catchable by manual UAT.
Output: `resolveResourceAt` accepts real `allZones`/`allPhysicalGrants` parameters and its sole caller supplies the live WorldState data for them; a new `digital-resource-selectors.test.ts` asserting `zoneAdvisory` resolves correctly; a live human-verify checkpoint confirming the amber row renders in the browser.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-UAT.md
@frontend/src/demo/lib/digital-resource-selectors.ts
@frontend/src/demo/lib/model.ts
@frontend/src/demo/lib/seed.ts
@frontend/src/demo/store/world-state.tsx
@frontend/src/demo/components/resource-access-explorer.tsx
@frontend/src/demo/lib/digital-resource-mapper.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread real allZones/allPhysicalGrants through resolveResourceAt + regression test</name>
  <files>frontend/src/demo/lib/digital-resource-selectors.ts, frontend/src/demo/lib/digital-resource-selectors.test.ts, frontend/src/demo/lib/digital-resource.test.ts, frontend/src/demo/components/resource-access-explorer.tsx</files>
  <read_first>
    frontend/src/demo/lib/digital-resource-selectors.ts (the full file, ~140 lines — `resolveResourceAt` at lines 101-139 is the exact function to change; note the current 6-parameter signature ending in `now: Date` and the two hardcoded `[]` literals at the `resolveResourceAccess` call)
    frontend/src/demo/lib/model.ts lines 1084-1157 (`resolveResourceAccess`'s own parameter order — `allZones: ZoneNode[]` and `allPhysicalGrants: PhysicalAccessGrant[]` sit immediately before `now: Date`; the zoneAdvisory block at lines 1131-1145 only ever sets `zoneAdvisory` non-null when `allZones.find(z => z.id === policy.zone_prereq_id)` succeeds)
    frontend/src/demo/lib/model.ts lines 74-79 (`ZoneAccessResult` shape), lines 207-213 (`PhysicalAccessGrant` shape), lines 51-60 (`ZoneNode` shape), lines 801-811 (`DigitalResourceWorld` shape — confirms `world.zones`/`world.grants` (physical) are NOT part of this sub-object; they live on the top-level `WorldState`)
    frontend/src/demo/store/world-state.tsx lines 92-93 (`WorldState.zones: ZoneNode[]` and `WorldState.grants: PhysicalAccessGrant[]` — the live fields to pass in) and lines 126-127 (both are seeded once at init from `seed.ts`'s `ZONES`/`GRANTS` and never reassigned by any reducer case — `TOGGLE_GRANT` only toggles `disabledGrantIds`, confirming these two fields are stable, safe values to read directly from `useWorld()`)
    frontend/src/demo/components/resource-access-explorer.tsx lines 113, 245-311 (`const world = useWorld()` gives this component the FULL WorldState already — `world.zones`/`world.grants` are already in scope, no new import or plumbing needed; only the `resolveResourceAt(...)` call and its `useMemo` deps array change)
    frontend/src/demo/lib/digital-resource.test.ts lines 28-64 (import block — note the comment "Seed integration tests (the SEED-06/07 cases) — the ONE place this file imports real seed fixtures. Unit tests above stay inline (D3-13 pattern)") and lines 1114-1230 (the "selectors: buildResourceTree, activeGrantsForResource, resolveResourceAt" describe block — 3 existing `resolveResourceAt(...)` calls at lines ~1182, ~1205, ~1218, all ending in `net.id, NOW)` or `"nonexistent-resource", NOW)` with only 6 arguments — these will fail to compile once the signature grows and MUST be updated in this same task, since this file already imports from `./seed` in that exact section)
    frontend/src/demo/lib/digital-resource-mapper.test.ts lines 1-35 (the "inline fixtures only, no seed.ts import" convention to mirror in the NEW test file — do not import from `./seed` in `digital-resource-selectors.test.ts`)
  </read_first>
  <behavior>
    - `resolveResourceAt`, given a `DigitalResourceWorld` network whose sole active policy assignment carries a `zone_prereq_id` that matches an entry in the `allZones` array now passed in, returns a non-null `zoneAdvisory` (the exact regression for the confirmed bug — previously impossible because `allZones` was always `[]`).
    - `resolveResourceAt`, given a policy whose `zone_prereq_id` does NOT match any entry in `allZones`, still returns `zoneAdvisory: null` (distinguishes "wired but no match" from "still hardcoded empty" — must not throw).
    - `resolveResourceAt`, given a policy with `zone_prereq_id: null`, returns `zoneAdvisory: null` regardless of `allZones` content (pre-existing behavior, unchanged by this fix).
  </behavior>
  <action>
    In `digital-resource-selectors.ts`: add `type ZoneNode` and `type PhysicalAccessGrant` to the existing `import { ... } from "./model"` block (keep the single existing import statement — do not add a second one). Change `resolveResourceAt`'s signature to insert two new required parameters, `allZones: ZoneNode[]` and `allPhysicalGrants: PhysicalAccessGrant[]`, positioned immediately before the trailing `now: Date` parameter (mirrors `resolveResourceAccess`'s own parameter order in model.ts). In the function body's `resolveResourceAccess(...)` call, replace the two hardcoded `[]` arguments with `allZones` and `allPhysicalGrants` respectively, in the same positions. The "resource not found" early-return path is untouched (it returns before reaching this call).

    In `resource-access-explorer.tsx`: update the sole `resolveResourceAt(...)` call (inside the `result` `useMemo`) to pass `world.zones` and `world.grants` as the two new arguments, positioned immediately before `evalTime` (which remains last). Add `world.zones` and `world.grants` to that `useMemo`'s dependency array alongside the existing `world.digitalResources` entry. No new imports are needed in this file — `world` is already the full `WorldState` returned by `useWorld()`.

    In `digital-resource.test.ts`: add `ZONES` and `GRANTS` to the existing `import { RESOURCE_NODES, RESOURCE_GRANTS, RSRC_DELEGATES, PLATFORMS, APPLICATIONS } from "./seed"` block (the file's designated seed-integration import). Update all 3 existing `resolveResourceAt(...)` calls in the "selectors: buildResourceTree, activeGrantsForResource, resolveResourceAt" describe block to pass `ZONES, GRANTS` as the two new arguments immediately before each call's trailing `NOW` argument. None of these 3 tests assert on `.zoneAdvisory` (they assert `.allow`/`.gates.length`/`.reason`), and the resource under test (`world.networks[0]`, MilNet) does not carry a `zone_prereq_id`, so passing real zones/grants here does not change any existing assertion's outcome.

    Create `frontend/src/demo/lib/digital-resource-selectors.test.ts` (new file, inline fixtures only — no `./seed` import, matching `digital-resource-mapper.test.ts`'s established convention). Build a minimal `DigitalResourceWorld` containing exactly one `NetworkNode` fixture and one `ZoneNode` fixture, constructed inline. Write 3 `it()` cases covering the 3 behaviors listed above, each calling `resolveResourceAt` with the new 8-argument signature and asserting on `result.zoneAdvisory` (`not.toBeNull()` / `toBeNull()` per case).
  </action>
  <acceptance_criteria>
    - `grep -c 'allZones: ZoneNode\[\]' frontend/src/demo/lib/digital-resource-selectors.ts` &gt;= 1 and `grep -c 'allPhysicalGrants: PhysicalAccessGrant\[\]' frontend/src/demo/lib/digital-resource-selectors.ts` &gt;= 1 (signature updated).
    - `grep -c 'from "./seed"' frontend/src/demo/lib/digital-resource-selectors.ts` == 0 (no hidden static import workaround — real params only).
    - `grep -c 'world.zones' frontend/src/demo/components/resource-access-explorer.tsx` &gt;= 1 and `grep -c 'world.grants' frontend/src/demo/components/resource-access-explorer.tsx` &gt;= 1 (caller wires live WorldState fields through).
    - `cd frontend && npx vitest run src/demo/lib/digital-resource-selectors.test.ts` passes with 3/3 tests, all green.
    - `cd frontend && npx vitest run src/demo/lib/digital-resource.test.ts` passes with zero failures (the 3 updated call sites still compile and pass).
    - `cd frontend && npm run build` — zero TypeScript errors.
    - `cd frontend && npm run test` — full suite green; test-file/test count grows by exactly the 3 new tests added here, zero regressions.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build && npx vitest run src/demo/lib/digital-resource-selectors.test.ts src/demo/lib/digital-resource.test.ts</automated>
  </verify>
  <done>resolveResourceAt threads real zone and physical-grant data into resolveResourceAccess; a dedicated regression test on resolveResourceAt itself passes; the pre-existing selector-level tests in digital-resource.test.ts still compile and pass; the full build and Vitest suite are green with no regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the zone-advisory row renders live</name>
  <action>
    All implementation is already done by Task 1 (signature fix, caller wiring, automated regression test — all verified by build + Vitest). This checkpoint is verification ONLY — Claude does not write code here. Start the dev stack so the viewer can confirm the amber row renders in the actual running app (this is exactly the gap the automated build/grep checks previously missed), then pause for human approval or issue report.
  </action>
  <what-built>
    Task 1 fixed resolveResourceAt so the amber, non-blocking zone-advisory row can now actually resolve against real zone data (it was permanently dead code before this fix, despite passing all prior build/grep checks).
  </what-built>
  <how-to-verify>
    1. Ensure the backend (`cd backend && RUST_LOG=info cargo run`) and DB (`docker compose -f docker-compose.dev.yml up -d`) are running, then start the frontend (`cd frontend && npm run dev`) and log in as `admin`/`password123`.
    2. Open the demo (`/demo.html`), go to the "Digital Resources" tab, then the Access Resolution Explorer sub-tab.
    3. Set the Resource selector to "[NETWORK] IntelNet" (any Person, any evaluation timestamp — IntelNet's non-baseline policy is permanent, so no specific window needed).
    4. Confirm an amber `bg-amber-50` block now renders below the gate list, with the label "Zone prerequisite" and a Pill reading exactly "Advisory (non-blocking)" — this is the row that was confirmed silently absent during the original Phase 12 UAT (test 10).
    5. Confirm the ALLOW/DENY verdict banner above it is unaffected by this row (still driven only by the CLEARANCE/OWN_TIER_GRANT/PARENT_TIER_GRANT/REQUIRED_ROLE gates).
    6. Switch the Resource selector to a resource with no zone prerequisite (e.g. "[NETWORK] HomeGuardNet") and confirm the amber row disappears — no false positives.
  </how-to-verify>
  <resume-signal>Type "approved" once the amber row renders live for IntelNet and correctly disappears for a resource with no zone prerequisite, or describe what you see instead.</resume-signal>
  <verify>
    <human-check>The viewer confirms the amber "Advisory (non-blocking)" zone-prerequisite row renders live for IntelNet and correctly disappears for a resource with no zone prerequisite, in the actual running demo at http://localhost:15510/demo.html.</human-check>
  </verify>
  <done>The human viewer has typed "approved", confirming the zone-advisory row is no longer dead code in the running app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorldState → resolveResourceAt | In-memory client state only; no untrusted input crosses this boundary (fix is internal wiring, not a new input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07-01 | Information Disclosure | resolveResourceAt zoneAdvisory | low | accept | zoneAdvisory is a read-only advisory annotation already scoped to data the caller (an authenticated demo session) can already see elsewhere (Physical Access tab); wiring real zones/grants through does not expose any new data the current user couldn't already view |

</threat_model>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors.
- `cd frontend && npm run test` — full suite green, additive-only growth (+3 tests from `digital-resource-selectors.test.ts`).
- Live browser check (Task 2 checkpoint): amber "Advisory (non-blocking)" row renders for IntelNet and disappears for a zone-prereq-free resource.
</verification>

<success_criteria>
The amber, non-blocking zone-advisory row (RSRC-UI-05) renders live in the Access Resolution Explorer whenever the resolved policy's zone_prereq_id points to a real, existing zone — closing UAT gap #1 (test 10) — with a dedicated regression test on resolveResourceAt guarding against a future re-introduction of the hardcoded-empty-arrays bug.
</success_criteria>

<output>
Create `.planning/phases/12-demo-ui-tab-integration/12-07-SUMMARY.md` when done
</output>
