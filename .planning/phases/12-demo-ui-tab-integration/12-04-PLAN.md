---
phase: 12-demo-ui-tab-integration
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - frontend/src/demo/components/resource-browser.tsx
autonomous: true
requirements: [RSRC-UI-01, RSRC-UI-02, RSRC-UI-06]
must_haves:
  truths:
    - "The tree renders Network -> Platform -> Application with correct nesting, using buildResourceTree's pre-nested children (not a re-derived parent_id filter)"
    - "Root Network nodes start expanded; Application nodes start collapsed until their parent Platform is expanded"
    - "Selecting any node populates the detail panel with tier, classification (Applications show the inherited Platform classification plus a ' (inherited)' suffix), org links grouped by role with active/expired badges, active policy summary, active grants, delegates, and (Platform only) NSM annotation badges"
    - "The '+ Issue new delegate' trigger is visible only when getStoredUserRole() === \"admin\" (D-01 correction — admin-only, never admin+manager); otherwise the 'Issuing controls require an admin login.' note renders in its place"
    - "A successful delegate issue collapses the form, resets its fields, and the new delegate appears in the Delegates card (via WorldState, not a locally-constructed optimistic entry)"
    - "The Issue delegate submit button is disabled and reads 'Issuing…' for the full duration of mutation.isPending — a second click cannot fire a concurrent submit"
  artifacts:
    - "frontend/src/demo/components/resource-browser.tsx"
  key_links:
    - "resource-browser.tsx imports CLEARANCE_TONE from ./access-resolution-explorer (12-02 export) and buildResourceTree/activeGrantsForResource/selectActivePolicy from ../lib/digital-resource-selectors and ../lib/model (all pre-existing, Phase 9/10)"
    - "The Issue Delegate form calls useIssueDelegate() (12-03) and getStoredUserRole() (12-03) — no direct fetch/localStorage access in this file"
    - "ResourceBrowser is rendered by 12-06's DigitalResourcesPanel only once the loader is in the 'success' state — this file assumes world.digitalResources always has data and does not need its own empty/error handling"
  prohibitions:
    - statement: "NSM annotation badges MUST NOT render as a gate pass (green) — static annotations, never gates"
      status: resolved
      verification: "grep -c 'tone=\"green\"' resource-browser.tsx section around the NSM annotations card must be 0; both NSM Pills use tone=\"slate\" per 12-UI-SPEC.md"
---

<objective>
Build the Resource Browser: a collapsible Network→Platform→Application tree (left panel) plus a resource detail panel (right panel) showing tier, inherited-aware classification, org-links-by-role, active policy, active grants, delegates, and Platform-only NSM annotation badges — and, role-gated to `admin` only, an inline "Issue new delegate" form.

Purpose: this is the primary read surface for RSRC-UI-01/RSRC-UI-02 — a stakeholder browsing the demo must be able to see the whole hierarchy and drill into any node's authorization posture without a page reload. The Issue Delegate form (RSRC-UI-06) lives here per 12-UI-SPEC.md's placement contract (below the Delegates card), not in the Access Resolution Explorer.
Output: `frontend/src/demo/components/resource-browser.tsx` exporting `ResourceBrowser`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-demo-ui-tab-integration/12-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-RESEARCH.md
@.planning/phases/12-demo-ui-tab-integration/12-CONTEXT.md
@frontend/src/demo/components/zone-browser.tsx
@frontend/src/demo/components/ui.tsx
@frontend/src/demo/lib/digital-resource-selectors.ts
@frontend/src/demo/lib/model.ts
@frontend/src/demo/hooks/use-digital-resources.ts
@frontend/src/demo/store/world-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tree + detail panel</name>
  <files>frontend/src/demo/components/resource-browser.tsx</files>
  <read_first>
    frontend/src/demo/components/zone-browser.tsx (the FULL structural pattern to mirror: `ZoneTreeNode` recursive component + `ZoneDetailPanel` + `ZoneBrowser` owning `expandedIds`/`selectedId` local state, `grid grid-cols-3 gap-4` layout with tree in col 1, detail in `col-span-2`)
    frontend/src/demo/lib/digital-resource-selectors.ts (`buildResourceTree` returns PRE-NESTED `ResourceTreeNode[]` with a `children` array already built — use `node.children` directly; do NOT re-derive parent/child via a `parent_id`-style filter like zone-browser.tsx does, that selector doesn't exist for resources and would duplicate buildResourceTree's own logic. Also: `activeGrantsForResource(world.digitalResources, resourceId, now)`.)
    frontend/src/demo/lib/model.ts lines 651-812 (NetworkNode/PlatformNode/ApplicationNode shapes — `tier` field is already a literal on each node; `selectActivePolicy(policy_assignments, now)`; `effectiveClassification(node, allPlatforms)`; `isWindowActive(valid_from, valid_until, now)`; `unitName(orgId as UnitId)`; `BaselineOrgRole`)
    frontend/src/demo/components/access-resolution-explorer.tsx lines 24-33 (import `CLEARANCE_TONE` from here per 12-02's export fix — do not redeclare it)
    .planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md sections "Resource Browser Layout" and "Resource Detail Panel" (exact copy strings, exact class names, the NSM annotations block, the Select-sentinel note)
  </read_first>
  <action>
    Create `frontend/src/demo/components/resource-browser.tsx`. Local `TIER_TONE: Record<"NETWORK"|"PLATFORM"|"APPLICATION", "slate"|"blue"|"green">` = `{NETWORK:"slate",PLATFORM:"blue",APPLICATION:"green"}` (module-local, mirrors the codebase's existing pattern of per-file tone maps like `ZONE_TYPE_TONE`, duplicated rather than shared — do not create a shared constants file for this).

    `ResourceTreeNodeRow` — recursive component over `ResourceTreeNode` (from `buildResourceTree`), props `{ node, depth, expandedIds, selectedId, onSelect, onToggle }`. Row: `style={{ marginLeft: depth * 16 }}`, button `flex w-full items-center gap-2 rounded px-2 py-1 text-left text-sm`, selected → `bg-slate-200`, else `hover:bg-slate-100`. Expand toggle span (only when `node.children.length > 0`) shows `▼`/`▶`, `text-xs text-slate-400 w-4`, `onClick` calls `e.stopPropagation()` then `onToggle(node.id)`. Row content: `<Pill tone={TIER_TONE[node.tier]}>{node.tier}</Pill>` then the name text. Recurse into `node.children` only when `expandedIds.has(node.id)`.

    `ResourceBrowser` (exported): `const world = useWorld(); const now = useMemo(() => new Date(), []);`. `const tree = useMemo(() => buildResourceTree(world.digitalResources), [world.digitalResources]);`. `expandedIds` initial state MUST be seeded with every root (Network) id — `useState<Set<string>>(() => new Set(tree.map(n => n.id)))` — NOT an empty Set (this is the one deliberate divergence from zone-browser.tsx's pattern, per UI-SPEC: "root Networks start expanded, Applications start collapsed"). `selectedId: string | null` local state, default `null`. Render `grid grid-cols-3 gap-4`: `<Card title="Resource Hierarchy">` (tree, `col-span-1` implicit) beside a `col-span-2` `<Card title="Resource Detail">`.

    Detail panel lookup: `const selected = selectedId ? (world.digitalResources.networks.find(n=>n.id===selectedId) ?? world.digitalResources.platforms.find(p=>p.id===selectedId) ?? world.digitalResources.applications.find(a=>a.id===selectedId) ?? null) : null` (mirrors the exact search-all-three-arrays pattern `resolveResourceAt` already uses in `digital-resource-selectors.ts` — reuse the pattern, don't invent a different lookup). Empty state when `selected === null`: `<p className="text-sm text-slate-400">Select a resource to see details.</p>` (exact copy, matches Zone Browser's equivalent string).

    When `selected` is present, render (all inside a `space-y-3` wrapper):
    - `<Field label="Tier"><Pill tone={TIER_TONE[selected.tier]}>{selected.tier}</Pill></Field>`.
    - Classification: for NETWORK/PLATFORM use `selected.classification` directly; for APPLICATION compute `effectiveClassification(selected, world.digitalResources.platforms)`. Render `<Field label="Classification"><Pill tone={CLEARANCE_TONE[classification]}>{classification}</Pill>{selected.tier === "APPLICATION" && <span className="text-slate-400 text-xs"> (inherited)</span>}</Field>`.
    - `<Card title="Org links">`: group `selected.org_links` by `.role` (`Array.from(new Set(selected.org_links.map(l => l.role)))`, preserve first-seen order); for each role render a `text-xs font-semibold uppercase tracking-wide text-slate-400` heading, then each link in that role group as `unitName(link.org_id as UnitId)` + a Pill (`tone="green"` "active" / `tone="slate"` "expired", decided via `isWindowActive(link.valid_from, link.valid_until, now)`). Show ALL org_links (active and expired), never filtered out — the Pill communicates status, not visibility.
    - `<Card title="Active policy">`: `const assignment = selectActivePolicy(selected.policy_assignments, now)`. If found: `assignment.policy.label` plus a window string `${from} – ${until}` where each bound is `date ? date.toISOString().slice(0,10) : "open"`. If null: `<p className="text-sm text-slate-400">No active policy at this time.</p>`.
    - `<Card title="Active grants">`: `activeGrantsForResource(world.digitalResources, selected.id, now)`, list each grant's person name via `world.subjects.find(s=>s.id===g.person_id)?.name ?? g.person_id`. Empty: `<p className="text-sm text-slate-400">No active grants.</p>` (exact copy).
    - `<Card title="Delegates">`: `world.digitalResources.delegates.filter(d => d.resource_id === selected.id && isWindowActive(d.valid_from, d.valid_until, now))`. CRITICAL: use `isWindowActive(d.valid_from, d.valid_until, now)` directly — do NOT call `isDelegateActive(d, now)` from model.ts, which is typed to `ZoneAccessDelegate` (requires a `zone_id` field `ResourceAccessDelegate` does not have) and will NOT type-check against a `ResourceAccessDelegate`. List each delegate: person name (if `delegate_type === "PERSON"`, via `world.subjects.find`) or `unitName(delegate_org_id as UnitId)` (if `"ORG"`), plus `(${delegate_type})`. Empty: `<p className="text-sm text-slate-400">No delegates.</p>` (exact copy).
    - `{selected.tier === "PLATFORM" && (<Card title="NSM annotations">...)}`: two `<Pill tone="slate">` badges with the exact copy "sikkerhetsgodkjenning (approval-to-operate)" and "forsvarlig sikkerhetsnivå (adequate security level)"; a `text-xs text-slate-400` line with the exact copy "Static annotations — not enforced as access gates in v2.2."; if any `selected.org_links` entry has `role === "SECURITY_APPROVAL"` (first match, regardless of active/expired), an additional line "Authorizing authority: " + `unitName(link.org_id as UnitId)`.
  </action>
  <acceptance_criteria>
    - `grep -c 'buildResourceTree' resource-browser.tsx` >= 1 and `grep -c 'node.children' resource-browser.tsx` >= 1 (confirms the pre-nested selector output is used directly).
    - `grep -c 'isDelegateActive' resource-browser.tsx` is 0 (the type-incompatible helper is never called here).
    - `grep -c '(inherited)' resource-browser.tsx` >= 1.
    - `grep -c 'tone="slate"' resource-browser.tsx` >= 2 (the two NSM Pills) and `grep -c 'not enforced as access gates in v2.2' resource-browser.tsx` == 1.
    - `cd frontend && npm run build` compiles this file with zero TypeScript errors (run again after Task 2 adds the form; this task's own check may show unused-export warnings for `ResourceBrowser` until 12-06 wires it in — that is expected and not a failure at this stage, since `noUnusedLocals` only flags unused local declarations, not unused exports).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>resource-browser.tsx renders the tree (root Networks expanded, Applications collapsed) and a full detail panel for every tier, matching 12-UI-SPEC.md's Resource Browser Layout section exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Issue Delegate form (admin-gated)</name>
  <files>frontend/src/demo/components/resource-browser.tsx</files>
  <read_first>
    frontend/src/demo/hooks/use-digital-resources.ts (12-03 — `useIssueDelegate`, `getStoredUserRole`, `IssueDelegateVariables` shape)
    .planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md sections "Issue Delegate form" and "Role-Gated Issuing Affordances" (exact copy strings, exact submit-button JSX, exact 403/generic error blocks — note the "Correction note (2026-07-02, pre-planning research)" at the top of that section: authorized role is `admin` ONLY, never `admin`+`manager`)
    backend/src/digital_resources/models.rs lines 116-125 (`IssueDelegateRequest` — `delegate_type`, `delegate_person_id`, `delegate_org_id`, `granted_by_org_id` are all REQUIRED fields the payload must include even though only "Delegate person" and "Resource" are user-facing form fields)
  </read_first>
  <action>
    In the same file, below the "Delegates" Card built in Task 1, add: `const isAdmin = getStoredUserRole() === "admin";` (imported from `../hooks/use-digital-resources`, called directly — per D-01, this equality check compares against the single string `admin` only; do not widen it to accept a second role value). If `!isAdmin`: render `<p className="text-xs text-slate-400">Issuing controls require an admin login.</p>` (exact copy) and stop — no form, no trigger. If `isAdmin`: render a collapsible section, local `useState<boolean>(false)` for expanded. Collapsed: a text-button `text-sm text-slate-600 underline hover:text-slate-800` with the exact copy "+ Issue new delegate"; clicking sets expanded `true`. Expanded: same button now reads exact copy "Cancel" and collapses on click (no submit); below it, a `space-y-3` form with: a `Select` labeled "Delegate person" (`Field label="Delegate person"`) over `world.subjects` (value/label = subject id/name), defaulting to `world.subjects[0]?.id`; a `Select` labeled "Resource" over the flat `[...world.digitalResources.networks, ...world.digitalResources.platforms, ...world.digitalResources.applications]` list with option label format `` `[${tier}] ${name}` `` (e.g. "PLATFORM: MilPlatform-1"), defaulting to the CURRENTLY SELECTED browser resource id (`selectedId` from Task 1) if one is selected, else the first resource in the flat list (Select-sentinel rule — never an empty value); a `<input type="date">` for "Valid from" defaulting to `new Date().toISOString().slice(0,10)`; a `<input type="date">` for "Valid until (optional)" defaulting to empty, with hint `text-xs text-slate-400` exact copy "Leave blank for open delegation."

    `const mutation = useIssueDelegate();` Submit button (exact JSX per UI-SPEC): `className="rounded px-3 py-1.5 text-sm bg-slate-800 text-white disabled:opacity-50 disabled:cursor-not-allowed"`, `disabled={mutation.isPending}`, label `mutation.isPending ? "Issuing…" : "Issue delegate"`. `handleIssueDelegate`: builds `IssueDelegateVariables` — `resource_id` = the form's selected resource id; `delegate_type: "PERSON"`; `delegate_person_id` = the form's selected person id; `delegate_org_id: null`; `granted_by_org_id` = that SAME selected person's `.unit` field (`world.subjects.find(s => s.id === delegatePersonId)?.unit` — the server does not use this field for authorization, per `handlers.rs:151-152`'s comment; any syntactically valid org-id string satisfies the Rocket deserialization, and reusing the delegate person's own unit avoids inventing an unrelated magic string); `valid_from` = `new Date(validFromInput).toISOString()`; `valid_until` = `validUntilInput ? new Date(validUntilInput).toISOString() : null`. Calls `mutation.mutateAsync(vars)`; on success, collapse the form (`setExpanded(false)`) and reset all four fields to their defaults (do NOT locally append the new delegate to any local list — the dispatch already happened inside `useIssueDelegate`'s `onSuccess`, per 12-03).

    Error rendering below the submit button: when `mutation.isError && mutation.error instanceof ApiError && mutation.error.status === 403`, render `<div className="rounded bg-destructive/10 p-3 text-sm text-destructive mt-2"><span className="font-semibold">Not authorized.</span> Your current identity does not have issuing authority for this resource.</div>` (exact copy, "Not authorized." bold). When `mutation.isError` and NOT a 403, render the same container with exact copy "Issue failed. Check that the backend is running and retry." (no bold prefix).
  </action>
  <acceptance_criteria>
    - `grep -c '+ Issue new delegate' resource-browser.tsx` == 1.
    - `grep -c 'Issuing controls require an admin login.' resource-browser.tsx` == 1.
    - `grep -c 'Leave blank for open delegation.' resource-browser.tsx` == 1.
    - `grep -c 'Not authorized. Your current identity does not have issuing authority for this resource.' resource-browser.tsx` == 1 (allowing for the JSX split across the bold span — verify by reading the rendered text, not a single literal grep line, if the copy is split across two JSX text nodes).
    - `grep -c '"admin" ||' resource-browser.tsx` is 0 — confirms no `"admin" || "manager"` gate slipped in (D-01).
    - `cd frontend && npm run build` — zero TypeScript errors.
    - `cd frontend && npm run test` — full suite still green, no regressions.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build && npm run test</automated>
  </verify>
  <done>Issue Delegate form exists, is hidden for non-admin roles behind the exact note copy, submits via useIssueDelegate, and surfaces 403/generic errors inline per 12-UI-SPEC.md.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors.
- `cd frontend && npm run test` — full suite green.
- `grep -c "isDelegateActive" frontend/src/demo/components/resource-browser.tsx` — 0.
- `grep -c '"admin" ||' frontend/src/demo/components/resource-browser.tsx` — 0 (D-01 admin-only gate, no manager fallback).
</verification>

<success_criteria>
A stakeholder can browse the full Network→Platform→Application hierarchy, inspect any node's org-links/policy/grants/delegates/NSM badges, and — only when logged in as admin — issue a new delegate that persists server-side and appears in the Delegates card without a page reload.
</success_criteria>

## Artifacts This Phase Produces

(This plan's slice.)

- `frontend/src/demo/components/resource-browser.tsx` (new) — `ResourceBrowser` (exported), `ResourceTreeNodeRow` (local).

<output>
Create `.planning/phases/12-demo-ui-tab-integration/12-04-SUMMARY.md` when done
</output>