---
phase: 12-demo-ui-tab-integration
plan: 05
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - frontend/src/demo/components/resource-access-explorer.tsx
autonomous: true
requirements: [RSRC-UI-03, RSRC-UI-05, RSRC-UI-06]
must_haves:
  truths:
    - "Selecting a person, resource, or moving the evaluation timestamp recomputes the gate-chain trace live via resolveResourceAt — no submit button"
    - "The zone-advisory row renders amber (never green/red) with the exact Pill copy 'Advisory (non-blocking)' and never changes the ALLOW/DENY verdict"
    - "Sliding the evaluation timestamp across a policy-shift boundary (e.g. 2026-03-01 for MilNet) changes the displayed policy-version label; a timestamp with no covering policy renders the fail-closed DENY/no-active-policy trace"
    - "Disabling the sole active grant behind an ALLOW flips the verdict to DENY; re-enabling restores ALLOW; with two independently-covering grants, disabling only one keeps ALLOW"
    - "The '+ Issue new grant' trigger is visible only when getStoredUserRole() === \"admin\" (D-01 correction — admin-only, never admin+manager); otherwise the 'Issuing controls require an admin login.' note renders in its place"
  artifacts:
    - "frontend/src/demo/components/resource-access-explorer.tsx"
  key_links:
    - "resource-access-explorer.tsx imports CLEARANCE_TONE from ./access-resolution-explorer (12-02 export) and resolveResourceAt from ../lib/digital-resource-selectors (pre-existing, Phase 9/10) — the trace NEVER re-derives ALLOW/DENY itself"
    - "The Issue Grant form calls useIssueGrant() (12-03) and getStoredUserRole() (12-03) — no direct fetch/localStorage access in this file"
    - "The grant toggle dispatches TOGGLE_RESOURCE_GRANT (pre-existing action, field name resourceGrantId not grantId) directly via useWorldDispatch — this plan adds no new reducer action"
  prohibitions:
    - statement: "The zone-advisory row MUST NOT change the ALLOW/DENY verdict in the UI trace"
      status: resolved
      verification: "resolveResourceAt/resolveResourceAccess (model.ts, unmodified by this plan) already guarantee zoneAdvisory never feeds allow — this component only renders result.zoneAdvisory as a separate, non-gating block; grep -c 'zoneAdvisory' resource-access-explorer.tsx confirms it is read but never combined into an allow computation in this file"
---

<objective>
Build the Access Resolution Explorer: person + resource + evaluation-timestamp selectors driving a live gate-chain trace (with the mandatory amber, non-blocking zone-advisory row and the applied policy-version label), an interactive grant-toggle list, and — role-gated to `admin` only — an inline "Issue new grant" form.

Purpose: this is the primary interactive surface for RSRC-UI-03/RSRC-UI-05 — it is the only place in the demo where a viewer can watch a policy-shift boundary or a grant toggle change an ALLOW/DENY verdict in real time, and (RSRC-UI-06) where an admin issues a new grant.
Output: `frontend/src/demo/components/resource-access-explorer.tsx` exporting `ResourceAccessExplorer`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-demo-ui-tab-integration/12-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-RESEARCH.md
@.planning/phases/12-demo-ui-tab-integration/12-CONTEXT.md
@frontend/src/demo/components/access-resolution-explorer.tsx
@frontend/src/demo/components/ui.tsx
@frontend/src/demo/lib/digital-resource-selectors.ts
@frontend/src/demo/lib/model.ts
@frontend/src/demo/hooks/use-digital-resources.ts
@frontend/src/demo/store/world-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selectors + live gate-chain trace + grant toggle</name>
  <files>frontend/src/demo/components/resource-access-explorer.tsx</files>
  <read_first>
    frontend/src/demo/components/access-resolution-explorer.tsx (the FULL structural pattern to mirror: local `ZoneResolutionTrace` component, `useMemo`-driven selectors, `Card`/`Field`/`Select`/`Pill`/`MockTag` usage, the "Relevant grants (toggle to simulate)" checkbox-list pattern with `TOGGLE_GRANT` — this plan's toggle list is the resource-domain sibling of that exact pattern, using `TOGGLE_RESOURCE_GRANT` / `resourceGrantId` instead)
    frontend/src/demo/lib/digital-resource-selectors.ts (`resolveResourceAt(world, subject, subjectClearance, subjectOrgId, resourceId, now): ResourceAccessResult` — already filters disabled grants BEFORE resolving per D-06, so this component must NEVER pre-filter `world.digitalResources.grants` itself before passing them to the selector)
    frontend/src/demo/lib/model.ts lines 776-812 (`ResourceGateResult { kind, pass, reason }`, `ResourceAccessResult { allow, gates, zoneAdvisory, policyVersion, reason? }` — the exact shape the local trace component renders)
    IMPORTANT CORRECTION (verified by direct read of model.ts — 12-UI-SPEC.md's Interaction Contract point 4 pseudocode is WRONG on this one detail): the `Subject` interface (model.ts) has NO `org_id` field. The field is named `unit: UnitId`. Call `resolveResourceAt(world.digitalResources, personId, person.clearance, person.unit, resourceId, evalTime)` — using `person.org_id` as UI-SPEC's prose literally states will fail to compile ("Property 'org_id' does not exist on type 'Subject'").
    .planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md sections "Access Resolution Explorer Layout", "ResourceResolutionTrace component", "Gate label copy", and "Access Resolution Explorer (Resource)" interaction contract (exact copy strings, exact class names)
  </read_first>
  <action>
    Create `frontend/src/demo/components/resource-access-explorer.tsx`. Local `TIER_TONE` and `GATE_LABEL` constants: `TIER_TONE: Record<"NETWORK"|"PLATFORM"|"APPLICATION","slate"|"blue"|"green"> = {NETWORK:"slate",PLATFORM:"blue",APPLICATION:"green"}` (module-local duplicate of resource-browser.tsx's map — matches the codebase's existing per-file tone-map convention, do not import cross-component). `GATE_LABEL: Record<string,string> = {CLEARANCE:"Clearance", OWN_TIER_GRANT:"Own-tier grant", PARENT_TIER_GRANT:"Parent-tier grant", REQUIRED_ROLE:"Required role"}`, with a fallback `` GATE_LABEL[kind] ?? `Gate: ${kind}` `` for any unrecognized kind.

    `ResourceResolutionTrace` — local function component, `{ result }: { result: ResourceAccessResult }`. Verdict banner `rounded-lg border p-4`, `border-green-200 bg-green-50` when `result.allow` else `border-red-200 bg-red-50`; `text-lg font-semibold` line showing exact copy "✓ ALLOW" / "✗ DENY". Policy version row (always shown, `text-xs text-slate-400`): if `result.policyVersion` present, `` `Policy: ${from} – ${until}` `` where each bound is `date ? date.toISOString().slice(0,10) : "open"`; if `result.policyVersion` is null, exact copy "No active policy at this time." Gate rows: `<ul className="mt-3 space-y-1.5">`, one `<li className="flex gap-2 text-sm">` per `result.gates` entry — `✓`/`✗` glyph (`text-green-600`/`text-red-600`) from `g.pass`, `<span className="w-28 shrink-0 font-medium">{GATE_LABEL[g.kind] ?? \`Gate: ${g.kind}\`}</span>`, `<span className="text-slate-600">{g.reason}</span>`. Zone advisory block — render ONLY when `result.zoneAdvisory !== null`: `bg-amber-50 border border-amber-200 rounded p-3 mt-2 flex gap-2 text-sm` container, `⚠` glyph `text-amber-700`, `font-medium text-amber-900` label exact copy "Zone prerequisite", `text-amber-800` detail text (`result.zoneAdvisory.detail ?? result.zoneAdvisory.reason`), `<Pill tone="amber">Advisory (non-blocking)</Pill>` (exact copy, MANDATORY on every render of this block), and a `text-xs text-amber-700 block mt-1` line with exact copy "This zone requirement is advisory — it does not affect the ALLOW/DENY verdict." This block MUST NEVER use `bg-green-*`/`bg-red-*`/`text-green-*`/`text-red-*` classes — amber only.

    `ResourceAccessExplorer` (exported): `const world = useWorld(); const dispatch = useWorldDispatch();`. Flat resource list built once per render (or `useMemo` keyed on `world.digitalResources`): `[...world.digitalResources.networks, ...world.digitalResources.platforms, ...world.digitalResources.applications]`, each mapped to a Select option `{ value: id, label: \`[${tier}] ${name}\` }` (e.g. "[NETWORK] MilNet"). `personId` state defaults to `world.subjects[0]?.id ?? ""`; `resourceId` state defaults to the flat list's first entry id (Select-sentinel rule — never empty). Timestamp: `const initialNow = useMemo(() => new Date(), []); const [timestampInput, setTimestampInput] = useState(initialNow.toISOString().slice(0,16)); const evalTime = useMemo(() => new Date(timestampInput), [timestampInput]);` — `<input type="datetime-local" value={timestampInput} onChange={e => setTimestampInput(e.target.value)} />`. Intro prose: `<p className="text-sm text-slate-500">` exact copy "Select a person and resource, then slide the timestamp to evaluate access at any point in time." Timestamp field hint: `text-xs text-slate-400` exact copy "Slide across 2026-03-01 to see MilNet policy shift."

    Selector grid (`grid gap-4 sm:grid-cols-2` per UI-SPEC — Person+Resource cards; the Evaluation Timestamp card is its own full-width Card below the grid, per the Layout section's separate `[Card: Evaluation Timestamp]` block): Person Card — `Select` labeled "Person" over `world.subjects`; below it, the found `person`'s `<Pill tone={CLEARANCE_TONE[person.clearance]}>` + `<MockTag/>` (import `CLEARANCE_TONE` from `./access-resolution-explorer`, per 12-02's export). Resource Card — `Select` labeled "Resource" over the flat list built above; below it, `<Pill tone={TIER_TONE[tier]}>` + classification Pill for the selected resource (classification via `effectiveClassification` for Applications, else `.classification` directly, same pattern as resource-browser.tsx).

    Resolution: `const result = useMemo(() => resolveResourceAt(world.digitalResources, personId, person.clearance, person.unit, resourceId, evalTime), [world.digitalResources, personId, person, resourceId, evalTime]);` — guard `person`/selected-resource being found before calling (mirrors `access-resolution-explorer.tsx`'s `if (!person || !selectedZone) return null;` null-guard pattern, but since Select always has a valid default per the sentinel rule, this should only be relevant transiently). Render `<ResourceResolutionTrace result={result} />` full width below the selectors.

    Grant toggle: `<Card title="Resource grants (toggle to simulate)">` — filter `world.digitalResources.grants` to `g.person_id === personId && g.resource_id === resourceId`. Empty: exact copy "No grants for this person and resource." Each grant: checkbox `checked={!world.digitalResources.disabledResourceGrantIds.has(grant.id)}`, `onChange={() => dispatch({ type: "TOGGLE_RESOURCE_GRANT", resourceGrantId: grant.id })}` (field name is `resourceGrantId`, NOT `grantId` — that is the physical-domain action's field), label with `line-through text-slate-400` + trailing `(disabled)` text-xs when disabled.

    `<Card title="About this decision">` — static prose card (author 2-4 sentences summarizing the CLEARANCE / OWN_TIER_GRANT / PARENT_TIER_GRANT / REQUIRED_ROLE gate chain and the advisory zone check, mirroring the tone/length of `access-resolution-explorer.tsx`'s equivalent card — no exact copy pinned by UI-SPEC for this card's body, only its title).
  </action>
  <acceptance_criteria>
    - `grep -c 'person.org_id' resource-access-explorer.tsx` is 0; `grep -c 'person.unit' resource-access-explorer.tsx` is >= 1 (confirms the model.ts-verified field name is used, not UI-SPEC's incorrect pseudocode).
    - `grep -c 'Advisory (non-blocking)' resource-access-explorer.tsx` == 1.
    - `grep -c 'bg-green' resource-access-explorer.tsx` and the zone-advisory block region contain no `bg-green`/`bg-red` — verified by reading the zone-advisory JSX block back and confirming only `amber` classes appear there.
    - `grep -c 'resourceGrantId' resource-access-explorer.tsx` >= 1; `grep -c ': "TOGGLE_RESOURCE_GRANT", grantId' resource-access-explorer.tsx` is 0 (wrong field name never used).
    - `cd frontend && npm run build` — zero TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>resource-access-explorer.tsx renders a live-updating gate-chain trace with the mandatory amber advisory row and an interactive grant toggle, matching 12-UI-SPEC.md exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Issue Grant form (admin-gated)</name>
  <files>frontend/src/demo/components/resource-access-explorer.tsx</files>
  <read_first>
    frontend/src/demo/hooks/use-digital-resources.ts (12-03 — `useIssueGrant`, `getStoredUserRole`, `IssueGrantVariables` shape)
    .planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md sections "Issue Grant form" and "Role-Gated Issuing Affordances" (exact copy strings, exact submit-button JSX, exact 403/generic error blocks — authorized role is `admin` ONLY per the 2026-07-02 correction, never `admin`+`manager`)
    backend/src/digital_resources/models.rs lines 107-114 (`IssueGrantRequest` — `resource_id, person_id, actor_org_id, valid_from, valid_until` are all REQUIRED fields even though the form only exposes Person/Resource/Valid-from/Valid-until)
  </read_first>
  <action>
    In the same file, below the "Resource grants (toggle to simulate)" Card built in Task 1, add: `const isAdmin = getStoredUserRole() === "admin";` (imported from `../hooks/use-digital-resources`, called directly — per D-01, this equality check compares against the single string `admin` only; do not widen it to accept a second role value). If `!isAdmin`: render `<p className="text-xs text-slate-400">Issuing controls require an admin login.</p>` (exact copy) and stop. If `isAdmin`: collapsible section, local `useState<boolean>(false)` for expanded. Collapsed: text-button `text-sm text-slate-600 underline hover:text-slate-800` exact copy "+ Issue new grant"; expanded: same button reads exact copy "Cancel", collapses without submitting. Expanded form (`space-y-3`): `Select` labeled "Person" over `world.subjects`, defaulting to `world.subjects[0]?.id`; `Select` labeled "Resource" over the same flat `[TIER] Name` list built in Task 1, defaulting to the first entry (Select-sentinel rule); `<input type="date">` "Valid from" defaulting to today's date; `<input type="date">` "Valid until (optional)" defaulting to empty, hint `text-xs text-slate-400` exact copy "Leave blank for permanent grant."

    `const mutation = useIssueGrant();` Submit button (exact JSX per UI-SPEC): `className="rounded px-3 py-1.5 text-sm bg-slate-800 text-white disabled:opacity-50 disabled:cursor-not-allowed"`, `disabled={mutation.isPending}`, label `mutation.isPending ? "Issuing…" : "Issue grant"`. `handleIssueGrant`: builds `IssueGrantVariables` — `resource_id` = the form's selected resource id; `person_id` = the form's selected person id; `actor_org_id` = that SAME selected person's `.unit` field (`world.subjects.find(s => s.id === formPersonId)?.unit` — the server does not use this field for authorization, per `handlers.rs:151-152`'s comment; reusing the grantee's own unit avoids inventing an unrelated magic string); `valid_from` = `new Date(validFromInput).toISOString()`; `valid_until` = `validUntilInput ? new Date(validUntilInput).toISOString() : null`. Calls `mutation.mutateAsync(vars)`; on success, collapse the form and reset all four fields to their defaults (never locally append the new grant to any local list — the dispatch already happened inside `useIssueGrant`'s `onSuccess`, per 12-03).

    Error rendering below the submit button: when `mutation.isError && mutation.error instanceof ApiError && mutation.error.status === 403`, render `<div className="rounded bg-destructive/10 p-3 text-sm text-destructive mt-2"><span className="font-semibold">Not authorized.</span> Your current identity does not have issuing authority for this resource.</div>` (exact copy, "Not authorized." bold). When `mutation.isError` and NOT a 403, render the same container with exact copy "Issue failed. Check that the backend is running and retry."
  </action>
  <acceptance_criteria>
    - `grep -c '+ Issue new grant' resource-access-explorer.tsx` == 1.
    - `grep -c 'Issuing controls require an admin login.' resource-access-explorer.tsx` == 1.
    - `grep -c 'Leave blank for permanent grant.' resource-access-explorer.tsx` == 1.
    - `grep -c '"admin" ||' resource-access-explorer.tsx` is 0 — confirms no `"admin" || "manager"` gate slipped in (D-01).
    - `cd frontend && npm run build` — zero TypeScript errors.
    - `cd frontend && npm run test` — full suite still green, no regressions.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build && npm run test</automated>
  </verify>
  <done>Issue Grant form exists, is hidden for non-admin roles behind the exact note copy, submits via useIssueGrant, and surfaces 403/generic errors inline per 12-UI-SPEC.md.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors.
- `cd frontend && npm run test` — full suite green.
- `grep -c '"admin" ||' frontend/src/demo/components/resource-access-explorer.tsx` — 0 (D-01 admin-only gate, no manager fallback).
- `grep -c 'person.org_id' frontend/src/demo/components/resource-access-explorer.tsx` — 0 (uses the verified `person.unit` field instead).
</verification>

<success_criteria>
A viewer can pick any person/resource/timestamp combination and watch the gate-chain trace, the amber non-blocking advisory row, and the applied-policy-version label update live; toggling a grant flips the verdict predictably; an admin can issue a new grant that persists and appears without a page reload.
</success_criteria>

## Artifacts This Phase Produces

(This plan's slice.)

- `frontend/src/demo/components/resource-access-explorer.tsx` (new) — `ResourceAccessExplorer` (exported), `ResourceResolutionTrace` (local).

<output>
Create `.planning/phases/12-demo-ui-tab-integration/12-05-SUMMARY.md` when done
</output>
