---
phase: 12-demo-ui-tab-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/apply-digital-resource-seed.sh
autonomous: true
requirements: [RSRC-UI-04]
must_haves:
  truths:
    - "GET /api/digital-resources/world against janus2 (as admin) returns 6 networks, 4 platforms, 4 applications, 18 grants, 18 org_links, 3 policies, 15 policy_assignments, 1 delegate"
    - "Re-running the apply script against janus2 a second time is a no-op: resource_networks/resource_platforms/resource_applications/resource_org_links/resource_policies/resource_policy_assignments/resource_access_grants/resource_access_delegates row counts are byte-identical before and after the second run"
  artifacts:
    - "backend/scripts/apply-digital-resource-seed.sh"
  key_links:
    - "apply-digital-resource-seed.sh applies backend/migrations/20260601130001_seed_digital_resources.sql directly via `docker exec ... psql` against the janus2-postgres-dev container on :15530, bypassing the broken `sqlx migrate run` chain (see project memory project_migrations_fresh_db_broken)"
  prohibitions:
    - statement: "Phase 12 MUST NOT relax any Phase 11 server-side guard (AuthGuard, per-role RBAC, CORS) to accommodate the demo"
      status: satisfied-by-scope
      verification: "This plan touches zero files under backend/src/ — it only executes an already-committed, already-reviewed seed migration file via psql; no backend code, guard, or route changes"
---

<objective>
Apply the already-committed digital-resource seed migration (`20260601130001_seed_digital_resources.sql`) to the live `janus2` dev database via an idempotent, checked-in psql script, so every other Phase 12 plan has real data to fetch, render, and mutate against.

Purpose: `janus2` currently has the 8 digital-resource tables but zero rows (`sqlx migrate run` remains broken on the drifted dev DB — direct psql is the established Phase 11 apply path). Per 12-SPEC.md requirement 7 ("first task — everything else renders against it"), this is a Wave 1, no-dependency prerequisite for the live/manual verification of every later plan, even though later plans do not need to wait on it to be *authored* or *executed* against the codebase in parallel.
Output: `backend/scripts/apply-digital-resource-seed.sh` (new, executable) + a seeded `janus2` (6 networks / 4 platforms / 4 applications / 18 grants / 18 org_links / 3 policies / 15 policy_assignments / 1 delegate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-demo-ui-tab-integration/12-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-RESEARCH.md
@backend/migrations/20260601130001_seed_digital_resources.sql
@.planning/phases/11-digital-resource-backend/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the idempotent seed-apply script</name>
  <files>backend/scripts/apply-digital-resource-seed.sh</files>
  <read_first>
    backend/migrations/20260601130001_seed_digital_resources.sql (confirm every INSERT already carries `ON CONFLICT (id) DO NOTHING` or `WHERE NOT EXISTS` — the script must add NO extra idempotency logic of its own, per the file's own tail comment "Re-running this migration is a no-op")
    docker-compose.dev.yml (container_name: janus2-postgres-dev; POSTGRES_DB: janus2; POSTGRES_USER: janus; POSTGRES_PASSWORD: janus_dev_password; host port 15530)
    .planning/phases/11-digital-resource-backend/11-04-SUMMARY.md (key-decisions: "Permission seed applied directly via psql to live janus2 ... sqlx migrate run remains broken on the drifted dev DB")
  </read_first>
  <action>
    Create `backend/scripts/` (new directory) and `backend/scripts/apply-digital-resource-seed.sh`. Bash script, `#!/usr/bin/env bash`, `set -euo pipefail`. Behavior:
    1. `DB_NAME="${1:-janus2}"` — accepts an optional DB-name argument, defaults to `janus2` (this plan only exercises the default; do not add support for anything beyond a single positional arg — no flags parser).
    2. `CONTAINER_NAME="janus2-postgres-dev"` (the fixed `container_name` from `docker-compose.dev.yml` — do not use a `docker ps --filter` lookup; the name is a fixed dev-compose constant).
    3. Resolve `SEED_FILE` as the absolute path to `backend/migrations/20260601130001_seed_digital_resources.sql`, derived from the script's own location (`$(dirname "${BASH_SOURCE[0]}")/../migrations/...`) so it works regardless of the caller's cwd.
    4. Guard: if `docker ps --format '{{.Names}}'` does not contain `$CONTAINER_NAME`, print an actionable error to stderr ("Postgres container '$CONTAINER_NAME' is not running — run: docker compose -f docker-compose.dev.yml up -d") and `exit 1`.
    5. Guard: if `$SEED_FILE` does not exist, print an error to stderr and `exit 1`.
    6. Apply: `docker exec -i -e PGPASSWORD=janus_dev_password "$CONTAINER_NAME" psql -U janus -d "$DB_NAME" -v ON_ERROR_STOP=1 -f - < "$SEED_FILE"`.
    7. Print a completion line noting the script is safe to re-run because the seed file's own `ON CONFLICT DO NOTHING` / `WHERE NOT EXISTS` guards make every statement a no-op on repeat — the script itself adds no additional guard logic (no `SELECT COUNT` pre-check, no lock file).
    Make it executable: `chmod +x backend/scripts/apply-digital-resource-seed.sh`.
  </action>
  <acceptance_criteria>
    - `backend/scripts/apply-digital-resource-seed.sh` exists, is executable (`ls -l` shows `x` bits), and `bash -n backend/scripts/apply-digital-resource-seed.sh` reports no syntax errors.
    - The script contains no hardcoded row-count checks or extra idempotency logic beyond the guards already in the migration SQL file (verified by reading the script back — it should be a thin apply wrapper, not a reimplementation of the seed file's own guards).
  </acceptance_criteria>
  <verify>
    <automated>bash -n backend/scripts/apply-digital-resource-seed.sh</automated>
  </verify>
  <done>Script exists, is executable, passes a bash syntax check, and contains the exact container/DB/credentials constants from docker-compose.dev.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the seed to janus2 and prove idempotency + live API parity</name>
  <files>N/A — execution only, no files modified (reads backend/scripts/apply-digital-resource-seed.sh from Task 1 and backend/tests/digital_resources_api_test.rs, neither of which this task edits)</files>
  <read_first>
    backend/tests/digital_resources_api_test.rs lines 187-222 (`test_get_world_authenticated` — an existing `#[ignore]`d integration test that logs in as `admin`, calls `GET /api/digital-resources/world`, and asserts the exact seeded counts: 6 networks, 4 platforms, 4 applications, 18 grants, 18 org_links, 3 policies, 15 policy_assignments, 1 delegate. Use this test as the live-API verification instead of hand-writing curl/jq — it already encodes the SPEC's literal acceptance numbers.)
  </read_first>
  <action>
    Ensure the Postgres container is running (`docker compose -f docker-compose.dev.yml up -d` if not already up — the dev stack per CLAUDE.md §Commands). Run `backend/scripts/apply-digital-resource-seed.sh janus2` once. Record the row count of `resource_access_grants` in `janus2` via `docker exec -i -e PGPASSWORD=janus_dev_password janus2-postgres-dev psql -U janus -d janus2 -t -c "SELECT count(*) FROM resource_access_grants;"` (expect `18`). Re-run `backend/scripts/apply-digital-resource-seed.sh janus2` a second time and re-capture the same count — it MUST be unchanged (still `18`), proving the apply step is idempotent per the R7 acceptance criterion. Then run the existing ignored integration test against the now-seeded `janus2` (the backend's default `DATABASE_URL` per `cargo run` dev usage) to prove the live API path returns the seeded hierarchy, not just the raw table rows.
  </action>
  <acceptance_criteria>
    - First apply run exits 0.
    - `resource_access_grants` row count in `janus2` is exactly `18` after the first run.
    - Second apply run exits 0 and the `resource_access_grants` row count is still exactly `18` (unchanged) — idempotency proven.
    - `cargo test --test digital_resources_api_test test_get_world_authenticated -- --exact --include-ignored` reports `1 passed; 0 failed`, confirming `GET /api/digital-resources/world` (through AuthGuard + admin login) returns 6/4/4/18/18/3/15/1 as asserted inside the test.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test digital_resources_api_test test_get_world_authenticated -- --exact --include-ignored</automated>
  </verify>
  <done>janus2 is seeded (18 grants, 6 networks confirmed live via the existing integration test), and a second script run leaves row counts unchanged.</done>
</task>

</tasks>

<verification>
- `bash -n backend/scripts/apply-digital-resource-seed.sh` — syntax check passes.
- `cargo test --test digital_resources_api_test test_get_world_authenticated -- --exact --include-ignored` — 1 passed, 0 failed, against the now-seeded `janus2`.
- Manual row-count diff before/after the second script run — unchanged.
</verification>

<success_criteria>
`janus2` is seeded with the 6-unit digital-resource dataset via a checked-in, idempotent, re-runnable script; every later Phase 12 plan (and its eventual live UAT walkthrough) has real data to render and mutate against.
</success_criteria>

## Artifacts This Phase Produces

(This plan's slice — see 12-02 through 12-06 PLAN.md for the rest of the phase's artifacts.)

- `backend/scripts/apply-digital-resource-seed.sh` (new) — idempotent psql-apply wrapper for `20260601130001_seed_digital_resources.sql` against `janus2`.

<output>
Create `.planning/phases/12-demo-ui-tab-integration/12-01-SUMMARY.md` when done
</output>
