---
phase: 12-demo-ui-tab-integration
plan: 06
type: execute
wave: 4
depends_on: ["12-04", "12-05"]
files_modified:
  - frontend/src/demo/components/digital-resources-panel.tsx
  - frontend/src/demo/DemoRoot.tsx
autonomous: true
requirements: [RSRC-UI-01, RSRC-UI-04]
must_haves:
  truths:
    - "Exactly one of six mutually-exclusive states renders inside DigitalResourcesPanel: missing-token, loading, unauthorized(401), error, empty, success — the sub-nav and both sub-views are absent in every state except success"
    - "The missing-token state never fires the GET /world request (query disabled) and shows no Retry button"
    - "On query success, WorldState.digitalResources is populated via SET_DIGITAL_RESOURCES and the Resource Browser / Access Resolution sub-nav renders"
    - "Clicking the new 'Digital Resources' tab in DemoRoot renders DigitalResourcesPanel; the other six existing tabs are unchanged"
    - "git diff frontend/src/routeTree.gen.ts is empty after every commit in this phase"
  artifacts:
    - "frontend/src/demo/components/digital-resources-panel.tsx"
  key_links:
    - "digital-resources-panel.tsx calls useDigitalResourcesWorld + classifyLoaderState + hasStoredToken (12-03), dispatches SET_DIGITAL_RESOURCES (12-02) via useEffect on query success, and renders ResourceBrowser (12-04) / ResourceAccessExplorer (12-05) inside its own internal sub-nav"
    - "DemoRoot.tsx gains a 7th ActiveView variant + tab button wired to DigitalResourcesPanel — no import of any src/routes/** file or routeTree.gen.ts symbol"
  prohibitions:
    - statement: "The loader MUST NOT silently fall back to stale/hardcoded data and present it as live when the API is unreachable or unauthorized"
      status: resolved
      verification: "Every non-success classifyLoaderState branch renders an explicit cause-naming block and returns before reaching the sub-nav/sub-view render — grep -c 'seedWorld' digital-resources-panel.tsx must be 0 (no fallback-to-seed code path)"
    - statement: "Phase 12 MUST NOT relax any Phase 11 server-side guard (AuthGuard, per-role RBAC, CORS) to accommodate the demo"
      status: resolved
      verification: "cd backend && cargo test --test security_hardening_test -- --include-ignored reports 13 passed; 0 failed (unchanged from the pre-Phase-12 baseline) — this plan (and the whole phase) touches zero files under backend/src/"
---

<objective>
Build `DigitalResourcesPanel` — the loader-state gate (six mutually-exclusive states per 12-UI-SPEC.md) that fetches and dispatches the digital-resource world on mount, then hosts the Resource Browser / Access Resolution internal sub-nav — and wire it into `DemoRoot.tsx` as a 7th tab with zero `routeTree.gen.ts` impact. This is the final integration wave: every other Phase 12 plan's artifact becomes reachable through this plan.

Purpose: closes RSRC-UI-04 (the loader's visible state machine) and completes RSRC-UI-01/02/03/05/06's reachability — without this plan, `ResourceBrowser`/`ResourceAccessExplorer` are unreferenced dead code and the phase's own acceptance criteria (tab click renders the panel, build is clean, routeTree is untouched) are unverifiable.
Output: `frontend/src/demo/components/digital-resources-panel.tsx` (new), `frontend/src/demo/DemoRoot.tsx` (modified, 7th tab).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-demo-ui-tab-integration/12-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md
@.planning/phases/12-demo-ui-tab-integration/12-RESEARCH.md
@frontend/src/demo/DemoRoot.tsx
@frontend/src/demo/components/physical-access-panel.tsx
@frontend/src/demo/hooks/use-digital-resources.ts
@frontend/src/demo/store/world-state.tsx
@frontend/src/demo/components/resource-browser.tsx
@frontend/src/demo/components/resource-access-explorer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DigitalResourcesPanel — 6-state loader gate + sub-nav</name>
  <files>frontend/src/demo/components/digital-resources-panel.tsx</files>
  <read_first>
    frontend/src/demo/components/physical-access-panel.tsx (the exact inner sub-nav pattern to mirror: `useState<PhysicalView>`, `px-4 py-2` button row, conditional render of the two sub-views — this file's sub-nav is the "Resource Browser" | "Access Resolution" pair, structurally identical)
    frontend/src/demo/hooks/use-digital-resources.ts (12-03 — `hasStoredToken`, `useDigitalResourcesWorld`, `classifyLoaderState`, `LoaderState`)
    frontend/src/demo/store/world-state.tsx (`useWorldDispatch` — dispatch `SET_DIGITAL_RESOURCES` on query success, per 12-02's reducer case)
    .planning/phases/12-demo-ui-tab-integration/12-UI-SPEC.md section "Hybrid Loader States (RSRC-UI-04)" in full, especially the "State-to-render map" table (exact copy for every one of the 6 states, exact container classes, which states get a Retry button)
    12-RESEARCH.md "Pitfall 3" (React Query v5 has no `onSuccess` on `useQuery` — dispatch via `useEffect` watching `query.isSuccess`/`query.data`, never `useQuery({ onSuccess })`)
  </read_first>
  <action>
    Create `frontend/src/demo/components/digital-resources-panel.tsx`. `const hasToken = hasStoredToken();` (computed directly, no memoization — matches 12-RESEARCH.md's code example; the demo has no login flow so this value is stable for the whole session). `const query = useDigitalResourcesWorld(hasToken);` `const dispatch = useWorldDispatch();` `const state: LoaderState = classifyLoaderState(hasToken, query);` (pass `query` directly — its shape is structurally compatible with `classifyLoaderState`'s snapshot parameter). `useEffect(() => { if (query.isSuccess && query.data) { dispatch({ type: "SET_DIGITAL_RESOURCES", world: query.data }); } }, [query.isSuccess, query.data, dispatch]);`

    Render exactly one of six blocks based on `state`, each returning before any sub-nav renders (early-return style, one `if` per state, matching the "states are mutually exclusive" UI-SPEC rule):
    - `"missing-token"`: `<div className="rounded-md bg-destructive/10 p-4 text-destructive text-sm space-y-1"><p className="font-semibold">Not logged in.</p><p>The demo reuses your main-app session. Log in to the main app first, then reload this page.</p></div>` (exact copy). No Retry button.
    - `"loading"`: `<div className="flex items-center gap-3 py-12 text-sm text-slate-500">` a spinning icon (`animate-spin h-4 w-4 text-slate-400` — use `Loader2` from `lucide-react`, already a project dependency per 12-UI-SPEC.md's Design System table) plus exact copy "Loading digital resource data…" (the ellipsis is the single Unicode character `…`, not three periods).
    - `"unauthorized"`: `<div className="rounded-md bg-destructive/10 p-4 text-destructive text-sm space-y-1"><p className="font-semibold">Session invalid or expired.</p><p>The backend rejected your main-app session. Log in to the main app again, then reload this page.</p></div>` (exact copy). No Retry button.
    - `"error"`: `<div className="rounded-md bg-destructive/10 p-4 text-destructive text-sm space-y-1"><p className="font-semibold">Could not load digital resource data.</p><p>The backend API is unreachable or returned an error. Check that the backend is running on :15520 and retry.</p><button className="underline text-xs mt-1" onClick={() => query.refetch()}>Retry</button></div>` (exact copy; Retry button belongs ONLY to this state).
    - `"empty"`: `<div className="py-12 text-sm text-slate-400 text-center">No digital resources found. Seed the database and refresh.</div>` (exact copy).
    - `"success"`: render the internal sub-nav — local `useState<"resource-browser"|"access-resolution">("resource-browser")`, two buttons styled exactly like `physical-access-panel.tsx`'s (`px-4 py-2`, active `bg-slate-800 text-white`, inactive `border border-slate-300 text-slate-600 hover:bg-slate-50`) labeled exact copy "Resource Browser" and "Access Resolution", then conditionally render `<ResourceBrowser />` or `<ResourceAccessExplorer />`.

    Do NOT read or reference `seedWorld()`, `RESOURCES`, or any hardcoded digital-resource fixture in this file — every non-success state must be reachable ONLY through `classifyLoaderState`'s real query/token state, never a fallback default.
  </action>
  <acceptance_criteria>
    - `grep -c 'seedWorld' digital-resources-panel.tsx` is 0.
    - `grep -c 'Loading digital resource data…' digital-resources-panel.tsx` == 1 (exact ellipsis character, not three periods — verify by reading the string back, not just a byte-count grep).
    - `grep -c 'onSuccess' digital-resources-panel.tsx` is 0 inside a `useQuery(` call (the dispatch happens via `useEffect`, never `useQuery({ onSuccess })` — confirms Pitfall 3 is avoided).
    - `grep -c 'Retry' digital-resources-panel.tsx` == 1, and it appears only inside the `"error"` branch (not missing-token/unauthorized).
    - `cd frontend && npm run build` — zero TypeScript errors (this is the first point where `resource-browser.tsx`/`resource-access-explorer.tsx` are actually imported/used, so their exports finally get exercised by the compiler here).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>digital-resources-panel.tsx exists, renders exactly one of the 6 loader states per 12-UI-SPEC.md's exact copy/classes, dispatches SET_DIGITAL_RESOURCES via useEffect on success, and hosts the Resource Browser / Access Resolution sub-nav.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the "Digital Resources" tab into DemoRoot + final phase verification</name>
  <files>frontend/src/demo/DemoRoot.tsx</files>
  <read_first>
    frontend/src/demo/DemoRoot.tsx (full file — the `ActiveView` union, the 6 existing button blocks' exact className template, the `<main>` conditional-render block)
    .planning/phases/12-demo-ui-tab-integration/12-SPEC.md "Acceptance Criteria" list (the final phase-level bar: routeTree byte-identity, zero build errors, zero test failures, the security_hardening_test no-relax backstop)
  </read_first>
  <action>
    In `DemoRoot.tsx`: add `import { DigitalResourcesPanel } from "./components/digital-resources-panel";`. Extend the local `ActiveView` union with `"digital-resources"` (append after `"physical-access"`). Add a 7th button to the button row, copying the exact template of the existing 6 (`` `rounded px-3 py-1.5 text-sm ${activeView === "digital-resources" ? "bg-slate-800 text-white" : "border border-slate-300 text-slate-600 hover:bg-slate-50"}` ``, `onClick={() => setActiveView("digital-resources")}`), with the exact button label copy "Digital Resources". Add `{activeView === "digital-resources" && <DigitalResourcesPanel />}` to the `<main>` conditional-render block, after the existing `physical-access` line. Do NOT touch any other line in this file — no reordering of the existing 6 tabs, no changes to `WorldProvider`/`DemoBanner`/`RoleSwitcherHeader`.

    Then run the full phase-level verification: `cd frontend && npm run build` (zero TS errors), `cd frontend && npm run test` (full suite green — record the final test-file/test count and compare against the pre-Phase-12 baseline of 15 files / 194 tests to confirm only additive growth, zero regressions), `git diff --stat frontend/src/routeTree.gen.ts` (must produce NO output — byte-identical to before this phase), and `cd backend && cargo test --test security_hardening_test -- --include-ignored` (must report `13 passed; 0 failed`, matching the pre-Phase-12 baseline exactly — this is the SPEC's "no-relax" backstop; Phase 12 touches zero files under `backend/src/`, so this number must be unchanged).
  </action>
  <acceptance_criteria>
    - `grep -c '"digital-resources"' DemoRoot.tsx` >= 3 (union member, button conditional, main conditional).
    - `grep -c 'Digital Resources' DemoRoot.tsx` == 1 (the button label).
    - `git diff --stat frontend/src/routeTree.gen.ts` produces empty output.
    - `cd frontend && npm run build` — zero TypeScript errors.
    - `cd frontend && npm run test` — full suite passes; test-file/test counts are >= the pre-Phase-12 baseline (15 files / 194 tests), never fewer.
    - `cd backend && cargo test --test security_hardening_test -- --include-ignored` — `13 passed; 0 failed`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build && npm run test</automated>
  </verify>
  <done>DemoRoot renders a 7th "Digital Resources" tab wired to DigitalResourcesPanel; routeTree.gen.ts is untouched; the full frontend build/test suite and the backend's security_hardening_test suite are both green at their pre-phase baseline or better.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors.
- `cd frontend && npm run test` — full suite green, no regressions vs. the 15-file/194-test pre-phase baseline.
- `git diff --stat frontend/src/routeTree.gen.ts` — empty (no route-file impact from this phase).
- `cd backend && cargo test --test security_hardening_test -- --include-ignored` — 13 passed; 0 failed (no server-side guard relaxed anywhere in Phase 12).
- Live walkthrough (deferred to the post-execution `/gsd-verify-work` conversational-UAT step per 12-CONTEXT.md's Claude's Discretion — NOT embedded as a plan checkpoint): toggle round-trip, timestamp-boundary policy-shift, issuing flow success/403/duplicate-submit-silence, and role-gated affordance visibility for both an admin and a non-admin login, against the live dev stack (backend :15520 + frontend :15510 + the `janus2` seeded in 12-01).
</verification>

<success_criteria>
A developer or stakeholder opens the demo, clicks "Digital Resources", and — with the backend up and janus2 seeded — sees the full Network→Platform→Application hierarchy load, browses it, resolves access for any person/resource/timestamp with a live gate-chain trace and a non-blocking amber zone-advisory row, toggles a grant to flip a verdict, and (as admin) issues a new grant/delegate that persists — all without a single line changing in routeTree.gen.ts, and with the Phase 11 security suite still fully green.
</success_criteria>

## Artifacts This Phase Produces

(This plan's slice — the phase's final integration point.)

- `frontend/src/demo/components/digital-resources-panel.tsx` (new) — `DigitalResourcesPanel` (exported).
- `frontend/src/demo/DemoRoot.tsx` (modified) — `"digital-resources"` `ActiveView` variant, 7th tab button, `DigitalResourcesPanel` wiring.

### Full phase artifact index (union of all 6 plans, for reference)

- `backend/scripts/apply-digital-resource-seed.sh` (12-01)
- `frontend/src/demo/lib/digital-resource-mapper.ts` + `.test.ts` (12-02)
- `frontend/src/demo/store/world-state.tsx` + `.test.tsx` modified — `SET_DIGITAL_RESOURCES`, `UPSERT_RESOURCE_GRANT`, `UPSERT_RESOURCE_DELEGATE` (12-02)
- `frontend/src/demo/main.tsx` modified — `QueryClientProvider` (12-02)
- `frontend/src/demo/components/access-resolution-explorer.tsx` modified — `CLEARANCE_TONE` exported (12-02)
- `frontend/src/demo/hooks/use-digital-resources.ts` + `.test.ts` (12-03)
- `frontend/src/demo/components/resource-browser.tsx` (12-04)
- `frontend/src/demo/components/resource-access-explorer.tsx` (12-05)
- `frontend/src/demo/components/digital-resources-panel.tsx` (12-06)
- `frontend/src/demo/DemoRoot.tsx` modified — 7th tab (12-06)

<output>
Create `.planning/phases/12-demo-ui-tab-integration/12-06-SUMMARY.md` when done
</output>
