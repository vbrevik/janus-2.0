---
phase: 07-entry-log-visitor-passes
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/demo/lib/physical-access.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - VISIT-01
  - VISIT-02
  - VISIT-03

must_haves:
  truths:
    - "describe('ZoneEntryLog') block exists in physical-access.test.ts with sub-blocks for validateEntryLog and validateSecuredZoneEntry"
    - "describe('ZoneVisitorPass') block exists with sub-block for getActiveVisitorPasses"
    - "All new tests pass (zero failures, zero skips)"
    - "All 104 pre-existing Phase 5/6 tests continue to pass"
    - "tsc -b --noEmit exits 0 after test file extension"
  artifacts:
    - path: "frontend/src/demo/lib/physical-access.test.ts"
      provides: "ZoneEntryLog and ZoneVisitorPass Vitest coverage"
      contains: "describe(\"ZoneEntryLog\""
  key_links:
    - from: "physical-access.test.ts import block"
      to: "model.ts Phase 7 exports"
      via: "named imports: ZoneEntryLog, ZoneVisitorPass, validateEntryLog, validateSecuredZoneEntry, getActiveVisitorPasses"
      pattern: "ZoneEntryLog.*ZoneVisitorPass.*validateEntryLog"
    - from: "describe('ZoneEntryLog') fixtures"
      to: "module-level NOW constant (line 208)"
      via: "direct reference — NOW is module-scope"
      pattern: "entry_at: NOW"
---

<objective>
Extend physical-access.test.ts with Vitest coverage for all five Phase 7 exports: ZoneEntryLog type, ZoneVisitorPass type, validateEntryLog, validateSecuredZoneEntry, and getActiveVisitorPasses.

Purpose: Plan 07-01 added the Phase 7 interfaces and functions to model.ts. This plan proves correctness with unit tests covering all requirement cases from LOG-01 through VISIT-03 and verifies no Phase 5/6 test regressions.

Output: Two new top-level describe blocks appended at the end of physical-access.test.ts. Updated import block with 5 new named imports. All 104 existing tests plus all new tests pass under vitest run. tsc -b --noEmit exits 0.
</objective>

<execution_context>
@/Users/vidarbrevik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-RESEARCH.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Key facts about physical-access.test.ts the executor needs before touching anything. -->

Import block (lines 6–22 in current file — must be extended):
  import { describe, it, expect } from "vitest";
  import {
    CLEARANCE_RANK,
    isValidZoneTypeCombination,
    evaluateControlledAccess,
    evaluateRestrictedAccess,
    evaluateSecuredAccess,
    getAncestors,
    getDescendants,
    isGrantActive,
    resolveGrant,
    resolveZoneAccess,
    isDelegateActive,
    type ZoneNode,
    type PhysicalAccessGrant,
    type ZoneAccessDelegate,
  } from "./model";

Five names to ADD to this import block (per RESEARCH.md Pitfall 2):
  ZoneEntryLog         (interface — import as type)
  ZoneVisitorPass      (interface — import as type)
  validateEntryLog     (function — value import)
  validateSecuredZoneEntry  (function — value import)
  getActiveVisitorPasses    (function — value import)

Module-level fixtures already declared in the file (accessible to new describe blocks):
  Z_SITE     = { id: "z-site", zone_type: "CONTROLLED", ... }    (line ~130)
  Z_BUILDING1 = { id: "z-bldg1", zone_type: "SECURED", ... }    (line ~138)
  Z_BUILDING2 = { id: "z-bldg2", zone_type: "RESTRICTED", ... } (line ~146)
  Z_ROOM1    = { id: "z-room1", zone_type: "SECURED", ... }      (line ~154)
  NOW        = new Date("2026-01-15T12:00:00Z")                  (line ~208)

Last line of file before appending: line 816 (closing brace of describe("resolveZoneAccess",...))
Line 817: (end of file — append here)

Error strings to assert (exact, per D-04):
  "ESCORT entry requires escort_person_id"
  "CARD entry must not have escort_person_id"
  "SECURED zone requires a ZoneEntryLog entry"

Time boundary semantics (D-05, inclusive both ends):
  pass.valid_from <= now && pass.valid_until >= now
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend import block and add describe("ZoneEntryLog") to physical-access.test.ts</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts — read lines 1–22 (import block to extend) and lines 125–215 (module-level fixtures: Z_SITE, Z_BUILDING1, Z_ROOM1, Z_BUILDING2, NOW) and lines 810–817 (last describe block and end of file — append point)
    - .planning/phases/07-entry-log-visitor-passes/07-SPEC.md — §7 (test coverage requirements for entry log)
    - .planning/phases/07-entry-log-visitor-passes/07-RESEARCH.md — "Pattern 4: Test Describe Block Structure" and fixture examples; "Pitfall 2" (import block must be updated); "Pitfall 3" (noUnusedLocals)
    - .planning/phases/07-entry-log-visitor-passes/07-01-SUMMARY.md — confirm the exact exports created in Plan 07-01
  </read_first>
  <behavior>
    - validateEntryLog(ENTRY_CARD) returns null (valid CARD, escort_person_id is null)
    - validateEntryLog(ENTRY_ESCORT) returns null (valid ESCORT, escort_person_id is set)
    - validateEntryLog({method:"ESCORT", escort_person_id:null, ...}) returns "ESCORT entry requires escort_person_id"
    - validateEntryLog({method:"CARD", escort_person_id:"p-1", ...}) returns "CARD entry must not have escort_person_id"
    - validateSecuredZoneEntry(Z_SITE, null) returns null (CONTROLLED zone — no logging required)
    - validateSecuredZoneEntry(Z_BUILDING1, ENTRY_CARD) returns null (SECURED zone, non-null entry)
    - validateSecuredZoneEntry(Z_BUILDING1, null) returns "SECURED zone requires a ZoneEntryLog entry"
  </behavior>
  <action>
Step 1 — Update import block (lines 6–22):

Add to the existing import from "./model":
  - type ZoneEntryLog    (type import, alongside existing type ZoneNode etc.)
  - type ZoneVisitorPass (type import)
  - validateEntryLog     (value import)
  - validateSecuredZoneEntry  (value import)
  - getActiveVisitorPasses    (value import)

Keep all existing imports. Maintain the existing import style (value imports untagged, type imports tagged with `type`).

Step 2 — Append describe("ZoneEntryLog") after line 816 (end of describe("resolveZoneAccess")):

Declare two fixtures at the top of the ZoneEntryLog describe block (SCREAMING_CASE per Phase 6 pattern):

  const ENTRY_CARD: ZoneEntryLog = {
    id: "log-card-1",
    person_id: "p-1",
    zone_id: "z-room1",
    entry_at: NOW,
    exit_at: null,
    method: "CARD",
    escort_person_id: null,
  };

  const ENTRY_ESCORT: ZoneEntryLog = {
    id: "log-escort-1",
    person_id: "p-visitor",
    zone_id: "z-room1",
    entry_at: NOW,
    exit_at: null,
    method: "ESCORT",
    escort_person_id: "p-1",
  };

Both fixtures use module-level NOW (already declared at line ~208 — do NOT redeclare it).

Inner describe block for validateEntryLog (7 cases total for LOG-01 + LOG-02):
  describe("validateEntryLog", () => {
    it("returns null for valid CARD entry (escort_person_id is null)", () => {
      expect(validateEntryLog(ENTRY_CARD)).toBeNull();
    });
    it("returns null for valid ESCORT entry (escort_person_id is set)", () => {
      expect(validateEntryLog(ENTRY_ESCORT)).toBeNull();
    });
    it("returns error string for ESCORT entry missing escort_person_id", () => {
      const entry: ZoneEntryLog = { ...ENTRY_ESCORT, escort_person_id: null };
      expect(validateEntryLog(entry)).toBe("ESCORT entry requires escort_person_id");
    });
    it("returns error string for CARD entry with unexpected escort_person_id", () => {
      const entry: ZoneEntryLog = { ...ENTRY_CARD, escort_person_id: "p-1" };
      expect(validateEntryLog(entry)).toBe("CARD entry must not have escort_person_id");
    });
  });

Inner describe block for validateSecuredZoneEntry (3 cases for LOG-03):
  describe("validateSecuredZoneEntry", () => {
    it("returns null for non-SECURED zone with null entry", () => {
      expect(validateSecuredZoneEntry(Z_SITE, null)).toBeNull();
    });
    it("returns null for SECURED zone with non-null entry", () => {
      expect(validateSecuredZoneEntry(Z_BUILDING1, ENTRY_CARD)).toBeNull();
    });
    it("returns error string for SECURED zone with null entry", () => {
      expect(validateSecuredZoneEntry(Z_BUILDING1, null)).toBe(
        "SECURED zone requires a ZoneEntryLog entry",
      );
    });
  });

Notes:
- Z_SITE and Z_BUILDING1 are module-level fixtures — reference them directly (do NOT redeclare)
- ENTRY_CARD and ENTRY_ESCORT are declared at the ZoneEntryLog describe scope
- Spread syntax for mutated entries in it() blocks keeps tests concise
- noUnusedLocals: only declare ENTRY_CARD and ENTRY_ESCORT if both are used — they are (ENTRY_CARD in validateSecuredZoneEntry test, ENTRY_ESCORT in validateEntryLog test)
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/physical-access.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Import block includes ZoneEntryLog, ZoneVisitorPass, validateEntryLog, validateSecuredZoneEntry, getActiveVisitorPasses
    - describe("ZoneEntryLog") block exists with describe("validateEntryLog") (4 it() cases) and describe("validateSecuredZoneEntry") (3 it() cases)
    - All 7 new ZoneEntryLog tests pass
    - All 104 pre-existing tests continue to pass (total: 111+ green)
    - tsc -b --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add describe("ZoneVisitorPass") to physical-access.test.ts</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts — read the end of the file after Task 1 to confirm current append point; re-read module-level NOW constant (line ~208) to use the same date value for pass fixture alignment
    - .planning/phases/07-entry-log-visitor-passes/07-SPEC.md — §8 (test coverage requirements for visitor passes, VISIT-01/02/03)
    - .planning/phases/07-entry-log-visitor-passes/07-CONTEXT.md — D-05 (inclusive boundary semantics for getActiveVisitorPasses)
    - .planning/phases/07-entry-log-visitor-passes/07-RESEARCH.md — "Pattern 4: Test Describe Block Structure" ZoneVisitorPass section; "Pitfall 1" (no null guards on valid_from/valid_until); "Pitfall 3" (noUnusedLocals)
  </read_first>
  <behavior>
    - getActiveVisitorPasses("z-room1", [PASS_ACTIVE], NOW) returns [PASS_ACTIVE]
    - getActiveVisitorPasses("z-room1", [PASS_EXPIRED], NOW) returns []
    - getActiveVisitorPasses("z-room1", [PASS_FUTURE], NOW) returns []
    - getActiveVisitorPasses("z-room1", [], NOW) returns []
    - getActiveVisitorPasses("z-room1", [PASS_OTHER_ZONE], NOW) returns [] (zone_id mismatch)
  </behavior>
  <action>
Append describe("ZoneVisitorPass") after the closing brace of describe("ZoneEntryLog") (appended in Task 1).

NOW is module-level (new Date("2026-01-15T12:00:00Z")) — do NOT redeclare it.

Declare four pass fixtures at the describe("ZoneVisitorPass") scope:

  const PASS_ACTIVE: ZoneVisitorPass = {
    id: "vp-1",
    entry_log_id: "log-escort-1",
    escort_person_id: "p-1",
    zone_id: "z-room1",
    valid_from: new Date("2026-01-15T10:00:00Z"),   // before NOW
    valid_until: new Date("2026-01-15T18:00:00Z"),  // after NOW
  };

  const PASS_EXPIRED: ZoneVisitorPass = {
    id: "vp-2",
    entry_log_id: "log-escort-2",
    escort_person_id: "p-1",
    zone_id: "z-room1",
    valid_from: new Date("2026-01-14T10:00:00Z"),
    valid_until: new Date("2026-01-14T18:00:00Z"),  // before NOW — expired
  };

  const PASS_FUTURE: ZoneVisitorPass = {
    id: "vp-3",
    entry_log_id: "log-escort-3",
    escort_person_id: "p-1",
    zone_id: "z-room1",
    valid_from: new Date("2026-01-16T10:00:00Z"),   // after NOW — future
    valid_until: new Date("2026-01-16T18:00:00Z"),
  };

  const PASS_OTHER_ZONE: ZoneVisitorPass = {
    id: "vp-4",
    entry_log_id: "log-escort-4",
    escort_person_id: "p-1",
    zone_id: "z-bldg1",                             // different zone
    valid_from: new Date("2026-01-15T10:00:00Z"),
    valid_until: new Date("2026-01-15T18:00:00Z"),
  };

Inner describe block for getActiveVisitorPasses (5 cases for VISIT-03; VISIT-01/02 covered by type-assignability of fixtures):
  describe("getActiveVisitorPasses", () => {
    it("returns active pass within time window", () => {
      expect(getActiveVisitorPasses("z-room1", [PASS_ACTIVE], NOW)).toEqual([PASS_ACTIVE]);
    });
    it("excludes expired pass (valid_until before now)", () => {
      expect(getActiveVisitorPasses("z-room1", [PASS_EXPIRED], NOW)).toEqual([]);
    });
    it("excludes future pass (valid_from after now)", () => {
      expect(getActiveVisitorPasses("z-room1", [PASS_FUTURE], NOW)).toEqual([]);
    });
    it("returns empty array when allPasses is empty", () => {
      expect(getActiveVisitorPasses("z-room1", [], NOW)).toEqual([]);
    });
    it("filters by zoneId — excludes passes for different zone", () => {
      expect(getActiveVisitorPasses("z-room1", [PASS_OTHER_ZONE], NOW)).toEqual([]);
    });
  });

Notes:
- All four pass fixtures are used — no noUnusedLocals violation
- Inclusive boundary check: valid_from 10:00 and valid_until 18:00 bracket NOW at 12:00 — PASS_ACTIVE must return
- PASS_EXPIRED valid_until 14 Jan is before NOW 15 Jan — must not return
- PASS_FUTURE valid_from 16 Jan is after NOW 15 Jan — must not return
- PASS_OTHER_ZONE zone_id "z-bldg1" does not match query zone "z-room1" — must not return
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc -b --noEmit 2>&1 | head -10 && echo "tsc_exit:$?" && npx vitest run src/demo/lib/ 2>&1 | tail -15</automated>
  </verify>
  <done>
    - describe("ZoneVisitorPass") block exists with describe("getActiveVisitorPasses") containing 5 it() cases
    - All 5 new ZoneVisitorPass tests pass
    - Total test count is 104 + 7 (ZoneEntryLog) + 5 (ZoneVisitorPass) = 116 tests passing, 0 failing, 0 skipped
    - tsc -b --noEmit exits 0
    - vitest run src/demo/lib/ exits 0 (full suite green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Test file only; pure Vitest assertions over in-memory TypeScript objects; no I/O, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | Test import block — missing Phase 7 exports causes silent test skip | mitigate | Import block explicitly names all 5 Phase 7 exports; Vitest will fail with ReferenceError (not silently skip) if any import is missing |
| T-07-05 | Tampering | noUnusedLocals — unused fixtures cause tsc error masking real issues | mitigate | Only declare fixtures that are referenced in at least one it() block; RESEARCH Pitfall 3 explicitly addressed |
| T-07-06 | Information Disclosure | Test fixtures — no PII, no real secrets | accept | All fixture data is synthetic (p-1, z-room1, etc.); no real system data |
</threat_model>

<verification>
After both tasks complete:
- grep -c "ZoneEntryLog" frontend/src/demo/lib/physical-access.test.ts returns > 0 (import + describe + fixtures)
- grep -c "ZoneVisitorPass" frontend/src/demo/lib/physical-access.test.ts returns > 0 (import + describe + fixtures)
- grep -c "validateEntryLog\|validateSecuredZoneEntry\|getActiveVisitorPasses" frontend/src/demo/lib/physical-access.test.ts returns 3+ (imports + usage)
- cd frontend && npx tsc -b --noEmit exits 0
- cd frontend && npx vitest run src/demo/lib/ exits 0 with 116 tests (104 pre-existing + 12 new), 0 failures, 0 skipped
</verification>

<success_criteria>
- Import block extended with ZoneEntryLog (type), ZoneVisitorPass (type), validateEntryLog, validateSecuredZoneEntry, getActiveVisitorPasses
- describe("ZoneEntryLog") block with validateEntryLog (4 cases) and validateSecuredZoneEntry (3 cases) sub-blocks — all 7 pass
- describe("ZoneVisitorPass") block with getActiveVisitorPasses (5 cases) sub-block — all 5 pass
- 104 pre-existing tests continue to pass (zero regressions)
- Total: 116 tests green, 0 failed, 0 skipped
- tsc -b --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/07-entry-log-visitor-passes/07-02-SUMMARY.md` when done.
</output>
