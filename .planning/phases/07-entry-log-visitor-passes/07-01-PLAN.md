---
phase: 07-entry-log-visitor-passes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/demo/lib/model.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - VISIT-01
  - VISIT-02
  - VISIT-03

must_haves:
  truths:
    - "ZoneEntryLog is exported from model.ts with id, person_id, zone_id, entry_at (Date), exit_at (Date|null), method ('CARD'|'ESCORT'), escort_person_id (string|null)"
    - "ZoneVisitorPass is exported from model.ts with id, entry_log_id, escort_person_id, zone_id, valid_from (Date), valid_until (Date) — all required, none nullable"
    - "validateEntryLog returns null for valid CARD/ESCORT entries; returns descriptive error string for ESCORT missing escort_person_id or CARD with escort_person_id set"
    - "validateSecuredZoneEntry returns null for non-SECURED zones regardless of entry; returns null for SECURED zone with non-null entry; returns error string for SECURED zone with null entry"
    - "getActiveVisitorPasses returns passes matching zoneId where valid_from <= now <= valid_until (inclusive boundaries, no null guards)"
    - "tsc -b --noEmit exits 0 after additions"
  artifacts:
    - path: "frontend/src/demo/lib/model.ts"
      provides: "ZoneEntryLog, ZoneVisitorPass interfaces + validateEntryLog, validateSecuredZoneEntry, getActiveVisitorPasses functions"
      exports:
        - "ZoneEntryLog"
        - "ZoneVisitorPass"
        - "validateEntryLog"
        - "validateSecuredZoneEntry"
        - "getActiveVisitorPasses"
  key_links:
    - from: "validateSecuredZoneEntry"
      to: "ZoneNode"
      via: "zone.zone_type === 'SECURED' check"
      pattern: "zone\\.zone_type !== .SECURED."
    - from: "getActiveVisitorPasses"
      to: "ZoneVisitorPass"
      via: "array filter on zone_id + time boundaries"
      pattern: "pass\\.valid_from <= now && pass\\.valid_until >= now"
---

<objective>
Append two interfaces and three pure functions to model.ts, completing the Phase 7 entry log and visitor pass type layer.

Purpose: Phases 5 and 6 delivered zone hierarchy and grant resolution. Phase 7 adds the entry-event side: ZoneEntryLog records how a person entered (CARD or ESCORT), ZoneVisitorPass links an escort visitor to their entry event with a bounded time window, and three companion functions validate method constraints, enforce SECURED zone logging requirements, and query active passes at a given moment.

Output: 5 new named exports appended to frontend/src/demo/lib/model.ts — 2 interfaces and 3 functions. tsc -b --noEmit exits 0. No new files created.
</objective>

<execution_context>
@/Users/vidarbrevik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/ROADMAP.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/STATE.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/07-entry-log-visitor-passes/07-RESEARCH.md
</context>

<interfaces>
<!-- Current state of model.ts at the Phase 7 insertion point. -->
<!-- Append Phase 7 section AFTER line 323 (closing brace of isDelegateActive), BEFORE line 325 (// --- D-10: UnitId ---). -->

From frontend/src/demo/lib/model.ts (Phase 6 trailing exports, lines 313–324):

  // --- DELEG-01: isDelegateActive — delegate time-window check ---
  export function isDelegateActive(delegate: ZoneAccessDelegate, now: Date): boolean {
    return (
      (delegate.valid_from === null || delegate.valid_from <= now) &&
      (delegate.valid_until === null || delegate.valid_until >= now)
    );
  }
  // line 324 = closing brace of isDelegateActive
  // line 325 = // --- D-10: UnitId (the 6 canonical units) is the single entity-id type ---

ZoneNode interface (used by validateSecuredZoneEntry):
  export interface ZoneNode {
    id: string;
    name: string;
    level: ZoneLevel;
    zone_type: ZoneType;            // "CONTROLLED" | "RESTRICTED" | "SECURED"
    parent_id: string | null;
    admin_org_id: string;
    asset_owner_org_id: string;
    requires_explicit_auth: boolean;
  }

isGrantActive (Phase 6 — pattern to mirror for getActiveVisitorPasses time boundary):
  export function isGrantActive(grant: PhysicalAccessGrant, now: Date): boolean {
    return (
      (grant.valid_from === null || grant.valid_from <= now) &&
      (grant.valid_until === null || grant.valid_until >= now)
    );
  }
  // NOTE: PhysicalAccessGrant.valid_from/valid_until are Date | null.
  // ZoneVisitorPass.valid_from/valid_until are plain Date (not nullable).
  // getActiveVisitorPasses does NOT need null guards — simpler than isGrantActive.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ZoneEntryLog and ZoneVisitorPass interfaces to model.ts</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts — read lines 313–330 to confirm exact insertion point (end of isDelegateActive, before D-10 UnitId section) and to verify section header style from Phase 6
    - .planning/phases/07-entry-log-visitor-passes/07-SPEC.md — §1 (ZoneEntryLog fields) and §4 (ZoneVisitorPass fields)
    - .planning/phases/07-entry-log-visitor-passes/07-CONTEXT.md — D-01 (append location), D-03 (section header format)
  </read_first>
  <behavior>
    - A valid CARD entry (method: "CARD", escort_person_id: null) is assignable to ZoneEntryLog without TypeScript error
    - A valid ESCORT entry (method: "ESCORT", escort_person_id: "p-escort") is assignable to ZoneEntryLog without TypeScript error
    - A ZoneVisitorPass with entry_log_id pointing to any string is assignable without TypeScript error
    - valid_from and valid_until on ZoneVisitorPass are required Date fields — omitting either causes a TypeScript compile error
    - exit_at and escort_person_id on ZoneEntryLog are Date|null and string|null respectively — they must be provided explicitly (never optional ?)
  </behavior>
  <action>
After the closing brace of isDelegateActive (line 323) and before the "--- D-10: UnitId ---" comment (line 325), insert a blank line and then the Phase 7 section.

Section header (per D-03): // --- Phase 7: Entry log and visitor pass types ---

ZoneEntryLog interface:
- JSDoc: "Records a single zone access event: who entered, which zone, when, and how. ESCORT entries require escort_person_id; CARD entries must not have one."
- export interface ZoneEntryLog
- Fields (all required, snake_case, no ? optional syntax):
    id: string
    person_id: string
    zone_id: string
    entry_at: Date
    exit_at: Date | null
    method: "CARD" | "ESCORT"
    escort_person_id: string | null

ZoneVisitorPass interface:
- JSDoc: "Visitor pass issued when an escort accompanies a person into a zone. Linked to its ZoneEntryLog by entry_log_id. valid_from and valid_until are both required (caller-supplied); neither is nullable."
- export interface ZoneVisitorPass
- Fields (all required, snake_case, no ? optional syntax):
    id: string
    entry_log_id: string
    escort_person_id: string
    zone_id: string
    valid_from: Date
    valid_until: Date

Style rules (per Phase 5/6 pattern):
- Use named `export interface`, never `export type`
- Use `Type | null` for nullable fields, never `field?: Type`
- JSDoc comment immediately above each interface declaration
- Trailing comma on last field of each interface
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc -b --noEmit 2>&1 | head -20 && echo "tsc_exit:$?"</automated>
  </verify>
  <done>
    - grep -c "export interface ZoneEntryLog" frontend/src/demo/lib/model.ts returns 1
    - grep -c "export interface ZoneVisitorPass" frontend/src/demo/lib/model.ts returns 1
    - ZoneEntryLog has exactly 7 fields: id, person_id, zone_id, entry_at, exit_at (Date|null), method ("CARD"|"ESCORT"), escort_person_id (string|null)
    - ZoneVisitorPass has exactly 6 fields: id, entry_log_id, escort_person_id, zone_id, valid_from (Date), valid_until (Date)
    - Both interfaces appear after isDelegateActive and before the D-10 UnitId section
    - tsc -b --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validateEntryLog, validateSecuredZoneEntry, getActiveVisitorPasses to model.ts</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts — read current state after Task 1 to see the insertion point after the ZoneVisitorPass interface; verify ZoneNode is in scope (already declared in Phase 5 section)
    - .planning/phases/07-entry-log-visitor-passes/07-SPEC.md — §2 (validateEntryLog), §3 (validateSecuredZoneEntry), §6 (getActiveVisitorPasses)
    - .planning/phases/07-entry-log-visitor-passes/07-CONTEXT.md — D-04 (exact error strings), D-05 (inclusive boundary semantics)
    - .planning/phases/07-entry-log-visitor-passes/07-RESEARCH.md — "Pattern 2: Validator Function" and "Pattern 3: Array Query Function" sections for exact implementation blueprints
  </read_first>
  <behavior>
    - validateEntryLog(entry with method:"CARD", escort_person_id:null) returns null
    - validateEntryLog(entry with method:"ESCORT", escort_person_id:"p-escort") returns null
    - validateEntryLog(entry with method:"ESCORT", escort_person_id:null) returns "ESCORT entry requires escort_person_id"
    - validateEntryLog(entry with method:"CARD", escort_person_id:"p-escort") returns "CARD entry must not have escort_person_id"
    - validateSecuredZoneEntry({zone_type:"CONTROLLED"}, null) returns null
    - validateSecuredZoneEntry({zone_type:"SECURED"}, validEntry) returns null
    - validateSecuredZoneEntry({zone_type:"SECURED"}, null) returns "SECURED zone requires a ZoneEntryLog entry"
    - getActiveVisitorPasses("z-1", [passForZ1ActiveNow], now) returns [passForZ1ActiveNow]
    - getActiveVisitorPasses("z-1", [expiredPass], now) returns []
    - getActiveVisitorPasses("z-1", [futurePass], now) returns []
    - getActiveVisitorPasses("z-1", [passForZ2ActiveNow], now) returns []
  </behavior>
  <action>
After the ZoneVisitorPass interface block (appended in Task 1), add three functions in this order:

1. validateEntryLog
   - Section header: // --- LOG-02: validateEntryLog — ESCORT/CARD method constraint ---
   - Signature: export function validateEntryLog(entry: ZoneEntryLog): string | null
   - Logic:
       if (entry.method === "ESCORT" && entry.escort_person_id === null)
         return "ESCORT entry requires escort_person_id";
       if (entry.method === "CARD" && entry.escort_person_id !== null)
         return "CARD entry must not have escort_person_id";
       return null;
   - Error strings are EXACT per D-04 — do not paraphrase or shorten

2. validateSecuredZoneEntry
   - Section header: // --- LOG-03: validateSecuredZoneEntry — SECURED zone mandatory logging ---
   - Signature: export function validateSecuredZoneEntry(zone: ZoneNode, entry: ZoneEntryLog | null): string | null
   - Logic (short-circuit pattern):
       if (zone.zone_type !== "SECURED") return null;
       if (entry !== null) return null;
       return "SECURED zone requires a ZoneEntryLog entry";
   - Second parameter is `entry: ZoneEntryLog | null`, NOT `entry?: ZoneEntryLog` — callers must always pass the argument explicitly
   - ZoneNode is already declared earlier in model.ts — no import needed

3. getActiveVisitorPasses
   - Section header: // --- VISIT-03: getActiveVisitorPasses — active pass query ---
   - Signature: export function getActiveVisitorPasses(zoneId: string, allPasses: ZoneVisitorPass[], now: Date): ZoneVisitorPass[]
   - Logic:
       return allPasses.filter(
         (pass) =>
           pass.zone_id === zoneId &&
           pass.valid_from <= now &&
           pass.valid_until >= now,
       );
   - Per D-05: boundaries are inclusive on both ends
   - ZoneVisitorPass.valid_from and valid_until are plain Date (not Date|null) — do NOT add null guards
   - Do NOT use new Date() or Date.now() internally — now is an explicit parameter

All functions:
- Named export function with explicit parameter types and explicit return type
- No side effects
- Section header style: // --- LABEL: name — description ---
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx tsc -b --noEmit 2>&1 | head -20 && echo "tsc_exit:$?" && npx vitest run src/demo/lib/ 2>&1 | tail -15</automated>
  </verify>
  <done>
    - grep -c "export function validateEntryLog" frontend/src/demo/lib/model.ts returns 1
    - grep -c "export function validateSecuredZoneEntry" frontend/src/demo/lib/model.ts returns 1
    - grep -c "export function getActiveVisitorPasses" frontend/src/demo/lib/model.ts returns 1
    - validateSecuredZoneEntry second parameter is typed as "ZoneEntryLog | null" (not optional ?)
    - getActiveVisitorPasses filter has no null guards on valid_from/valid_until
    - tsc -b --noEmit exits 0
    - vitest run src/demo/lib/ passes with 104 tests (no regressions — Phase 7 tests not yet written)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | All code is pure TypeScript functions operating on in-memory objects; no I/O, no external calls, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | validateSecuredZoneEntry — optional vs required null parameter | mitigate | Second parameter typed as `ZoneEntryLog \| null` (not `entry?: ZoneEntryLog`) — callers cannot omit the argument silently; TypeScript enforces explicit null pass |
| T-07-02 | Tampering | getActiveVisitorPasses — null guards on non-nullable Date fields | mitigate | ZoneVisitorPass.valid_from/valid_until are plain Date; no null guards in filter; per RESEARCH Pitfall 1 — TypeScript type enforces this at compile time |
| T-07-03 | Information Disclosure | model.ts — no PII, no secrets | accept | Pure logic file with no network endpoints, no credential handling, no runtime data storage |
</threat_model>

<verification>
After both tasks complete:
- grep -c "export interface ZoneEntryLog" frontend/src/demo/lib/model.ts returns 1
- grep -c "export interface ZoneVisitorPass" frontend/src/demo/lib/model.ts returns 1
- grep -c "export function validateEntryLog" frontend/src/demo/lib/model.ts returns 1
- grep -c "export function validateSecuredZoneEntry" frontend/src/demo/lib/model.ts returns 1
- grep -c "export function getActiveVisitorPasses" frontend/src/demo/lib/model.ts returns 1
- cd frontend && npx tsc -b --noEmit exits 0
- cd frontend && npx vitest run src/demo/lib/ passes with 104 tests (no regressions)
</verification>

<success_criteria>
- ZoneEntryLog exported with 7 fields: id, person_id, zone_id, entry_at (Date), exit_at (Date|null), method ("CARD"|"ESCORT"), escort_person_id (string|null)
- ZoneVisitorPass exported with 6 fields: id, entry_log_id, escort_person_id, zone_id, valid_from (Date), valid_until (Date) — no nullable fields
- validateEntryLog: returns null for valid CARD/ESCORT; returns exact error strings from D-04 for constraint violations
- validateSecuredZoneEntry: short-circuits on non-SECURED; null entry on SECURED returns D-04 error string; second param is ZoneEntryLog|null not optional
- getActiveVisitorPasses: inclusive boundary filter with no null guards; explicit now: Date parameter
- tsc -b --noEmit exits 0; vitest run passes 104 tests
</success_criteria>

<output>
Create `.planning/phases/07-entry-log-visitor-passes/07-01-SUMMARY.md` when done.
</output>
