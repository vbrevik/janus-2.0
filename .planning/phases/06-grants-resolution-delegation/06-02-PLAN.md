---
phase: 06-grants-resolution-delegation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/demo/lib/physical-access.test.ts
autonomous: true
requirements:
  - GRANT-01
  - GRANT-02
  - GRANT-03
  - GRANT-04
  - ACCESS-05
  - DELEG-01
  - DELEG-02

must_haves:
  truths:
    - "isGrantActive is tested for all 4 null/non-null boundary combinations and boundary-exact timestamp"
    - "resolveGrant returns most-specific (leaf-first) grant and returns null when only ancestor grant is inactive or zone_type-mismatched"
    - "resolveGrant skips all ancestors when requires_explicit_auth=true; still finds direct grant on that zone"
    - "resolveZoneAccess produces gate:GRANT_LOOKUP with reason:NO_GRANT when no grant exists"
    - "resolveZoneAccess produces gate:ZONE_TYPE_RULE with reason:INSUFFICIENT_CLEARANCE when grant found but clearance fails"
    - "resolveZoneAccess produces allow:true when both gates pass"
    - "isDelegateActive is tested for all 4 null/non-null boundary combinations and boundary-exact timestamp"
  artifacts:
    - path: "frontend/src/demo/lib/physical-access.test.ts"
      provides: "Phase 6 Vitest tests — 7 describe blocks covering all 8 requirements"
  key_links:
    - from: "physical-access.test.ts Phase 6 describe blocks"
      to: "model.ts Phase 6 exports"
      via: "import { isGrantActive, resolveGrant, resolveZoneAccess, isDelegateActive, type PhysicalAccessGrant, type ZoneAccessDelegate } from './model'"
      pattern: "isGrantActive|resolveGrant|resolveZoneAccess|isDelegateActive"
---

<objective>
Extend physical-access.test.ts with Vitest tests covering all six Phase 6 model.ts exports: PhysicalAccessGrant/ZoneAccessDelegate types (structural tests), isGrantActive, resolveGrant, resolveZoneAccess, and isDelegateActive.

Purpose: Phase 6 functions are untested until this plan runs. The tests prove correctness of the grant time-window check, the zone_type-scoped ancestor walk, the requires_explicit_auth short-circuit, the two-gate resolver gate discriminators, and the delegate time-window check.

Output: physical-access.test.ts extended with ~40 new tests (7 describe blocks). Full Vitest suite must pass at 140+ tests (100 baseline + new).
</objective>

<execution_context>
@/Users/vidarbrevik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vidarbrevik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/06-grants-resolution-delegation/06-SPEC.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/06-grants-resolution-delegation/06-CONTEXT.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/06-grants-resolution-delegation/06-PATTERNS.md
@/Users/vidarbrevik/projects/janus-2.0/.planning/phases/06-grants-resolution-delegation/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key items from physical-access.test.ts that are already in scope for all new describe blocks -->
<!-- (All are declared at file scope before line 196) -->

Already-in-scope file-scope constants (do NOT redeclare):
  Z_SITE: ZoneNode    — id:"z-site", zone_type:"CONTROLLED", level:"SITE", parent_id:null, requires_explicit_auth:false
  Z_BUILDING1: ZoneNode — id:"z-bldg1", zone_type:"SECURED", level:"BUILDING", parent_id:"z-site", requires_explicit_auth:true
  Z_ROOM1: ZoneNode   — id:"z-room1", zone_type:"SECURED", level:"ROOM", parent_id:"z-bldg1", requires_explicit_auth:true
  Z_BUILDING2: ZoneNode — id:"z-bldg2", zone_type:"RESTRICTED", level:"BUILDING", parent_id:"z-site", requires_explicit_auth:false
  ALL_ZONES: ZoneNode[] — [Z_SITE, Z_BUILDING1, Z_ROOM1, Z_BUILDING2]

Existing import statement (lines 5–15) — must be extended with Phase 6 exports:
  Currently imports: CLEARANCE_RANK, isValidZoneTypeCombination, evaluateControlledAccess,
    evaluateRestrictedAccess, evaluateSecuredAccess, getAncestors, getDescendants, type ZoneNode

Phase 6 additions to import list:
  isGrantActive, resolveGrant, resolveZoneAccess, isDelegateActive,
  type PhysicalAccessGrant, type ZoneAccessDelegate

New fixture constants needed for Phase 6 tests (define at describe-scope or as file-scope before first Phase 6 describe block):
  const NOW = new Date("2026-01-15T12:00:00Z")   — reference moment for all time-window tests
  PhysicalAccessGrant fixtures: G_PERMANENT (null/null bounds), G_ACTIVE (within window), G_EXPIRED (valid_until before NOW), G_FUTURE (valid_from after NOW)
  ZoneAccessDelegate fixture: D_PERMANENT (null/null bounds), D_EXPIRED, D_FUTURE

Insertion point: append after line 196 (end of getDescendants describe block, which ends with "});")
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend import statement and add isGrantActive and isDelegateActive describe blocks</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts — read current file to see exact end (line 196) and the import block to extend (lines 5–15)
    - .planning/phases/06-grants-resolution-delegation/06-PATTERNS.md — "Phase 6 describe blocks" section for exact import extension syntax (type keyword on type imports), describe/it/expect style, and fixture patterns
    - .planning/phases/06-grants-resolution-delegation/06-SPEC.md — §2 (isGrantActive acceptance) and §8 (isDelegateActive acceptance) for required test cases
  </read_first>
  <action>
Step 1: Extend the import statement at lines 5–15 of physical-access.test.ts.
Add these items to the existing import from "./model":
  - Value imports (no type keyword): isGrantActive, isDelegateActive
  - Type imports (with type keyword): type PhysicalAccessGrant, type ZoneAccessDelegate

Step 2: After line 196 (end of getDescendants describe block), append a block of file-scope fixture constants:
  const NOW = new Date("2026-01-15T12:00:00Z")
  G_PERMANENT: PhysicalAccessGrant — id:"g-perm", person_id:"p-1", zone_id:"z-room1", valid_from:null, valid_until:null
  G_ACTIVE: PhysicalAccessGrant — id:"g-active", person_id:"p-1", zone_id:"z-room1", valid_from: new Date("2026-01-01T00:00:00Z"), valid_until: new Date("2026-12-31T23:59:59Z")
  G_EXPIRED: PhysicalAccessGrant — id:"g-expired", person_id:"p-1", zone_id:"z-room1", valid_from: new Date("2025-01-01T00:00:00Z"), valid_until: new Date("2025-12-31T23:59:59Z")
  G_FUTURE: PhysicalAccessGrant — id:"g-future", person_id:"p-1", zone_id:"z-room1", valid_from: new Date("2027-01-01T00:00:00Z"), valid_until: null

Step 3: Add describe("isGrantActive", ...) block with these it cases:
  - "returns true for permanent grant (null/null boundaries)" — isGrantActive(G_PERMANENT, NOW) → true
  - "returns true for active grant within window (valid_from <= now <= valid_until)" — isGrantActive(G_ACTIVE, NOW) → true
  - "returns false for expired grant (valid_until before now)" — isGrantActive(G_EXPIRED, NOW) → false
  - "returns false for future grant (valid_from after now)" — isGrantActive(G_FUTURE, NOW) → false
  - "returns true at boundary-exact valid_until (inclusive upper bound)" — create a grant with valid_until === NOW exactly; isGrantActive → true

Step 4: Add delegate fixtures after the grant fixtures:
  D_PERMANENT: ZoneAccessDelegate — id:"d-perm", zone_id:"z-site", delegate_type:"PERSON", delegate_person_id:"p-2", delegate_org_id:null, granted_by_org_id:"org-a", valid_from:null, valid_until:null
  D_EXPIRED: ZoneAccessDelegate — same shape but valid_until set to 2025 date (before NOW)
  D_FUTURE: ZoneAccessDelegate — valid_from set to 2027 date (after NOW), valid_until:null

Step 5: Add describe("isDelegateActive", ...) block with these it cases:
  - "returns true for permanent delegate (null/null boundaries)" — isDelegateActive(D_PERMANENT, NOW) → true
  - "returns false for expired delegate" — isDelegateActive(D_EXPIRED, NOW) → false
  - "returns false for future delegate" — isDelegateActive(D_FUTURE, NOW) → false
  - "returns true at boundary-exact valid_until (inclusive upper bound)" — isDelegateActive with valid_until === NOW → true

Test style: each it block is self-contained (no beforeEach, no shared mutable state). Assertions use expect(result).toBe(value) — assert the discriminating field only.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/physical-access.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - import statement in physical-access.test.ts includes isGrantActive, isDelegateActive, type PhysicalAccessGrant, type ZoneAccessDelegate
    - isGrantActive describe block has ≥5 it cases covering null/null, within-window, expired, future, and boundary-exact
    - isDelegateActive describe block has ≥4 it cases covering null/null, expired, future, and boundary-exact
    - All new tests pass; existing 100 tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add resolveGrant and resolveZoneAccess describe blocks</name>
  <files>frontend/src/demo/lib/physical-access.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts — read current state after Task 1 to see insertion point and existing fixtures in scope
    - .planning/phases/06-grants-resolution-delegation/06-SPEC.md — §3 (zone-type scoping acceptance), §4 (requires_explicit_auth acceptance), §5 (resolveGrant acceptance), §6 (resolveZoneAccess acceptance)
    - .planning/phases/06-grants-resolution-delegation/06-PATTERNS.md — "resolveGrant" fixtures pattern and assertion style (assert grant?.id rather than toEqual the whole object)
    - .planning/phases/06-grants-resolution-delegation/06-RESEARCH.md — Pitfall 1 (zone_type filter on target zone), Pitfall 2 (requires_explicit_auth short-circuit), Pitfall 4 (gate discriminator collision) — each pitfall should have a direct test case
  </read_first>
  <action>
After the isDelegateActive describe block, append additional grant fixtures and two more describe blocks:

Additional fixtures for resolveGrant / resolveZoneAccess tests:
  G_SITE_CONTROLLED: PhysicalAccessGrant — person_id:"p-1", zone_id:"z-site", valid_from:null, valid_until:null  (CONTROLLED ancestor grant)
  G_BLDG1_SECURED: PhysicalAccessGrant — person_id:"p-1", zone_id:"z-bldg1", valid_from:null, valid_until:null  (SECURED building grant; requires_explicit_auth=true on target)
  G_ROOM1_SECURED: PhysicalAccessGrant — person_id:"p-1", zone_id:"z-room1", valid_from:null, valid_until:null  (SECURED room direct grant)
  G_BLDG2_RESTRICTED: PhysicalAccessGrant — person_id:"p-1", zone_id:"z-bldg2", valid_from:null, valid_until:null  (RESTRICTED building grant)

describe("resolveGrant", ...) block — it cases:

  GRANT-04 (most-specific wins):
  - "returns direct zone grant when both direct and ancestor grants exist" — allGrants=[G_ROOM1_SECURED, G_BLDG1_SECURED], target=Z_ROOM1; assert result?.id === "g-room1-secured" (most specific)

  GRANT-04 (ancestor grant fallback):
  - "returns ancestor grant when no direct zone grant and ancestor zone_type matches" — add a new permanent grant on z-bldg2, target=Z_BUILDING2 with a CONTROLLED room child; OR use an alternate fixture: G_BLDG2_RESTRICTED covers Z_BUILDING2 directly, so test an AREA-level ancestor... Instead: create a one-off fixture G_AREA_RESTRICTED for an area ancestor, or simply test "returns null when only WRONG-type ancestor" and "returns ancestor when zone_type matches". Simplest: define a CONTROLLED room that is a child of Z_SITE (the CONTROLLED zone) — or re-use existing topology: Z_SITE is CONTROLLED. Create a new grant G_SITE_CONTROLLED on z-site. Request access for a hypothetical CONTROLLED room under site. Since ALL_ZONES does not have a CONTROLLED room, define a minimal inline fixture Z_CTRL_ROOM inside the describe block: { id:"z-ctrl-room", zone_type:"CONTROLLED", level:"ROOM", parent_id:"z-site", requires_explicit_auth:false, ...other fields }. With allZones=[Z_SITE, Z_CTRL_ROOM] and allGrants=[G_SITE_CONTROLLED], resolveGrant("p-1", Z_CTRL_ROOM, ...) should return G_SITE_CONTROLLED.

  GRANT-02 (zone_type scoping — cross-type inheritance blocked):
  - "returns null when ancestor grant is CONTROLLED but requested zone is RESTRICTED (zone_type mismatch)" — allGrants=[G_SITE_CONTROLLED], target=Z_BUILDING2 (RESTRICTED); expect null

  GRANT-03 (requires_explicit_auth short-circuit):
  - "returns null when target has requires_explicit_auth=true and only ancestor grant exists" — allGrants=[G_BLDG1_SECURED], target=Z_ROOM1 (requires_explicit_auth:true); expect null
  - "returns direct grant when target has requires_explicit_auth=true and direct grant exists" — allGrants=[G_ROOM1_SECURED], target=Z_ROOM1; expect result?.id === G_ROOM1_SECURED.id

  General:
  - "returns null when no grants exist" — allGrants=[], any target; expect null
  - "returns null when only grant belongs to a different person" — grant has person_id:"p-other"; resolveGrant("p-1", ...) → null

describe("resolveZoneAccess", ...) block — it cases:

  ACCESS-05 / GRANT_LOOKUP gate:
  - "returns gate:GRANT_LOOKUP reason:NO_GRANT when no active grant exists" — allGrants=[], CONTROLLED zone; assert { allow:false, gate:"GRANT_LOOKUP", reason:"NO_GRANT" }

  ACCESS-05 / ZONE_TYPE_RULE gate — CONTROLLED:
  - "returns allow:true gate:ZONE_TYPE_RULE for CONTROLLED zone when grant found (any clearance)" — allGrants=[G_SITE_CONTROLLED], target=Z_SITE, clearance:"UNCLASSIFIED"; assert { allow:true, gate:"ZONE_TYPE_RULE" }

  ACCESS-05 / ZONE_TYPE_RULE gate — RESTRICTED:
  - "returns gate:ZONE_TYPE_RULE reason:INSUFFICIENT_CLEARANCE when RESTRICTED zone, grant found, clearance UNCLASSIFIED, no escort" — allGrants=[G_BLDG2_RESTRICTED], target=Z_BUILDING2, clearance:"UNCLASSIFIED", hasValidEscort:false; assert { allow:false, gate:"ZONE_TYPE_RULE", reason:"INSUFFICIENT_CLEARANCE" }
  - "returns allow:true for RESTRICTED zone with sufficient clearance" — same but clearance:"RESTRICTED"; assert allow:true

  ACCESS-05 / ZONE_TYPE_RULE gate — SECURED:
  - "returns gate:ZONE_TYPE_RULE reason:INSUFFICIENT_CLEARANCE when SECURED zone, grant found, clearance CONFIDENTIAL" — allGrants=[G_ROOM1_SECURED], target=Z_ROOM1, clearance:"CONFIDENTIAL", hasValidEscort:false; assert { allow:false, gate:"ZONE_TYPE_RULE", reason:"INSUFFICIENT_CLEARANCE" }
  - "returns allow:true for SECURED zone with SECRET clearance and direct grant" — same but clearance:"SECRET"; assert allow:true

Test style: each it constructs its own inputs. Assert only the discriminating fields (allow, gate, reason) — not the full object with toEqual unless all fields matter. For resolveGrant tests, assert result?.id or result?.zone_id rather than toEqual the whole grant.
  </action>
  <verify>
    <automated>cd /Users/vidarbrevik/projects/janus-2.0/frontend && npx vitest run src/demo/lib/physical-access.test.ts 2>&1 | tail -30 && npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>
    - resolveGrant describe block has ≥7 it cases covering most-specific-wins, ancestor fallback, zone_type mismatch, requires_explicit_auth short-circuit (null), requires_explicit_auth short-circuit (direct grant passes), no grants, wrong person
    - resolveZoneAccess describe block has ≥6 it cases covering GRANT_LOOKUP gate (no grant), ZONE_TYPE_RULE gate for all three zone types (CONTROLLED allow, RESTRICTED deny+allow, SECURED deny+allow)
    - Every new test passes; full Vitest suite exits 0 with all tests passing (100 baseline + all new Phase 6 tests)
    - tsc -b --noEmit exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Test file only; no production code paths, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | resolveZoneAccess gate discriminator — test coverage | mitigate | Explicit it case asserting gate:"GRANT_LOOKUP" on no-grant path, and separate case asserting gate:"ZONE_TYPE_RULE" on clearance-fail path — proves the two gates are distinct at runtime |
| T-06-06 | Tampering | resolveGrant zone_type filter — test coverage | mitigate | Explicit it case: CONTROLLED ancestor grant + RESTRICTED target zone → null (mismatch blocked) |
| T-06-07 | Tampering | requires_explicit_auth short-circuit — test coverage | mitigate | Two explicit it cases: ancestor-only → null; direct-grant → found — proves short-circuit and recovery |
</threat_model>

<verification>
After both tasks complete:
- cd frontend && npx vitest run exits 0
- grep "Tests " output shows 0 failed
- Total test count >= 140 (100 baseline + Phase 6 additions)
- grep -c "describe(\"isGrantActive" frontend/src/demo/lib/physical-access.test.ts returns 1
- grep -c "describe(\"isDelegateActive" frontend/src/demo/lib/physical-access.test.ts returns 1
- grep -c "describe(\"resolveGrant" frontend/src/demo/lib/physical-access.test.ts returns 1
- grep -c "describe(\"resolveZoneAccess" frontend/src/demo/lib/physical-access.test.ts returns 1
- No import from ./seed in the file (grep returns 0 for seed import)
- cd frontend && npx tsc -b --noEmit exits 0
</verification>

<success_criteria>
- isGrantActive tested: 5 cases (null/null, within-window, expired, future, boundary-exact)
- isDelegateActive tested: 4 cases (null/null, expired, future, boundary-exact)
- resolveGrant tested: 7+ cases (most-specific-wins, ancestor fallback, zone_type mismatch, requires_explicit_auth both ways, no grants, wrong person)
- resolveZoneAccess tested: 6+ cases (GRANT_LOOKUP no-grant, CONTROLLED allow, RESTRICTED deny+allow, SECURED deny+allow)
- No seed.ts imports, no beforeEach, all fixtures inline
- Full Vitest suite passes with 0 failures
</success_criteria>

<output>
Create `.planning/phases/06-grants-resolution-delegation/06-02-SUMMARY.md` when done.
</output>
