---
phase: 09-digital-resource-model-policy-engine
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [frontend/src/demo/lib/model.ts]
autonomous: true
requirements: [RSRC-ACCESS-01, RSRC-ACCESS-02, RSRC-ACCESS-03, RSRC-ACCESS-04, RSRC-ACCESS-05, RSRC-POLICY-03, RSRC-GRANT-03, RSRC-DELEG-01]

must_haves:
  truths:
    - "resolveResourceAccess iterates the active policy's gates in list order and dispatches by kind"
    - "an unknown gate kind yields an explicit error result, never a silent ALLOW"
    - "no-active-policy returns fail-closed DENY { allow:false, reason:'NO_ACTIVE_POLICY' }"
    - "OWN_TIER_GRANT matches only grant.resource_id === resource.id (no ancestor walk) — cross-tier inheritance is impossible"
    - "zoneAdvisory is attached separately via reused resolveZoneAccess and never changes allow"
    - "canIssueResourceGrant is true only for an active ADMIN org-link or an active matching delegate"
  artifacts:
    - path: "frontend/src/demo/lib/model.ts"
      provides: "gate evaluators + resolveResourceAccess dispatcher + canIssueResourceGrant (appended)"
      contains: "function resolveResourceAccess"
  key_links:
    - from: "resolveResourceAccess"
      to: "selectActivePolicy + per-gate evaluators"
      via: "loop over policy.gates dispatching by kind"
      pattern: "for .*gates"
    - from: "resolveResourceAccess zoneAdvisory"
      to: "resolveZoneAccess (v2.1, reused unchanged)"
      via: "policy.zone_prereq_id -> resolveZoneAccess call attached to zoneAdvisory"
      pattern: "resolveZoneAccess"
    - from: "canIssueResourceGrant"
      to: "activeOrgLinksForRole('ADMIN') OR active ResourceAccessDelegate"
      via: "authority check closing v2.1 DELEG-03 gap"
      pattern: "canIssueResourceGrant"
---

<objective>
Append the Phase 9 gate-dispatch engine to `frontend/src/demo/lib/model.ts`: the baseline gate evaluators, the `evaluateGate` dispatcher with a fail-closed unknown-kind path, the `resolveResourceAccess` loop-over-gates resolver (with no-active-policy DENY and the advisory zone attachment), and `canIssueResourceGrant`. This is the phase's core deliverable — data-driven, time-versioned, fail-closed authorization with an explainable trace.

Purpose: Convert the policy descriptors (Plan 01) into ALLOW/DENY decisions while structurally preventing the three blocking pitfalls: cross-tier inheritance leak, advisory-treated-as-gate, and silent ALLOW on unknown/missing policy.
Output: Gate evaluators, `evaluateGate`, `resolveResourceAccess`, and `canIssueResourceGrant` appended under the Phase 9 section. Append-only; no v2.1 edits; `resolveZoneAccess` reused as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md
@.planning/phases/09-digital-resource-model-policy-engine/09-CONTEXT.md
@.planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md
@.planning/phases/09-digital-resource-model-policy-engine/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01 appended these to model.ts. Use directly — do not re-derive. -->
- Types: `NetworkNode`, `PlatformNode`, `ApplicationNode`, `OrgLink`, `GateDescriptor`, `ResourcePolicy` (with `gates: GateDescriptor[]`, `zone_prereq_id: string | null`), `PolicyAssignment`, `ResourceAccessGrant`, `ResourceAccessDelegate`, `ResourceGateResult { kind; pass; reason }`, `ResourceAccessResult { allow; gates; zoneAdvisory; policyVersion; reason? }`.
- Helpers: `isWindowActive`, `activeOrgLinks`, `activeOrgLinksForRole(orgLinks, role, now)`, `effectiveClassification(node, allPlatforms)`, `selectActivePolicy(policy_assignments, now)`, `validatePolicyWindows`.

<!-- Existing v2.1 symbols to REUSE unchanged. -->
From frontend/src/demo/lib/model.ts:
- `CLEARANCE_RANK` (15-21): `Record<Clearance, number>` for clearance comparison.
- `resolveZoneAccess(personId, zone, clearance, hasValidEscort, allZones, allGrants, now): ZoneAccessResult` (282-311): call AS-IS for the advisory zone; attach the returned `ZoneAccessResult` to `zoneAdvisory`. NEVER let it touch `allow`.
- `ZoneNode` (51-60), `PhysicalAccessGrant` (207-213) — needed as parameter types for the reused `resolveZoneAccess` call.
- `assertNever(x: never): never` (273-275) — private; use the SAME pattern (a local exhaustive check) over the KNOWN baseline kinds, but pair it with a runtime default that returns an explicit error result for injected unknown kinds.

<!-- DO NOT reuse resolveGrant (243-270): its getAncestors walk is the cross-tier leak. Own-tier = flat find on resource_id; parent-tier = separate flat find on parent id. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baseline gate evaluators + evaluateGate dispatcher</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts lines 96-152 (evaluateControlledAccess/evaluateRestrictedAccess/evaluateSecuredAccess — clearance-rank + structured-result pattern), 232-237 (isGrantActive), 273-275 (assertNever)
    - The Phase 9 types + helpers from Plan 01 (GateDescriptor, ResourceGateResult, activeOrgLinksForRole, effectiveClassification, isWindowActive)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 5, 6, 7; D-01/D-02)
    - .planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md ("Gate evaluators", "Exhaustive switch / fail-closed unknown")
  </read_first>
  <behavior>
    - `CLEARANCE` gate: pass iff `CLEARANCE_RANK[subjectClearance] >= CLEARANCE_RANK[effectiveClassification]`; reason `'INSUFFICIENT_CLEARANCE'` on fail, `'CLEARANCE_OK'` on pass.
    - `OWN_TIER_GRANT` gate: pass iff an active `ResourceAccessGrant` exists with `person_id === subject && resource_id === resource.id` (flat match — NO ancestor walk); reason `'NO_OWN_TIER_GRANT'` on fail.
    - `PARENT_TIER_GRANT` gate: for Platform → require an active grant on `platform.network_id`; for Application → on `app.platform_id`; for Network → pass trivially (no parent); reason `'NO_PARENT_TIER_GRANT'` on fail.
    - `REQUIRED_ROLE` gate: pass iff the subject's org has an active `org_link` on the resource with the descriptor's `role` (via `activeOrgLinksForRole`); reason `'MISSING_REQUIRED_ROLE'` on fail.
    - `evaluateGate` with an unknown `kind` returns `{ kind, pass:false, reason:'UNKNOWN_GATE_KIND' }` — never `pass:true`.
  </behavior>
  <action>
    Append (Phase 9 section) one pure evaluator per baseline kind, each returning a `ResourceGateResult { kind, pass, reason }`. Implement `CLEARANCE`, `OWN_TIER_GRANT`, `PARENT_TIER_GRANT`, and `REQUIRED_ROLE` (D-02 — `REQUIRED_ROLE` is required by SEED-06/07; do NOT implement `CLEARANCE_FLOOR`, A2). Define an evaluation-context object `ctx` carrying everything an evaluator needs (subject personId, subject clearance, subject org-id(s) for `REQUIRED_ROLE`, the derived `effectiveClassification`, the resource node, `allNetworks`/`allPlatforms` for parent lookup, the `ResourceAccessGrant[]` array, and `now`) — this `ctx` is the documented extension point (req 5): adding a gate kind = adding one evaluator + one `case`.
    - `CLEARANCE`: compare `CLEARANCE_RANK` of subject vs `ctx.effectiveClassification` (reuse the map; mirror `evaluateSecuredAccess`).
    - `OWN_TIER_GRANT`: `ctx.grants.find(g => g.person_id === subject && g.resource_id === resource.id && isWindowActive(g.valid_from, g.valid_until, now))`. FLAT match only — do NOT call `resolveGrant` / `getAncestors` (req 7, `cross-tier-inheritance-blocked`).
    - `PARENT_TIER_GRANT`: derive the parent id from the node tier (`network_id` for Platform, `platform_id` for Application), then a SEPARATE flat active-grant find against that parent id. Network → pass (no parent).
    - `REQUIRED_ROLE`: use `activeOrgLinksForRole(resource.org_links, gate.role, now)` and check the subject's org is among them.
    - `evaluateGate(gate: GateDescriptor, ctx): ResourceGateResult`: a `switch (gate.kind)` dispatching the four baseline kinds; the `default` branch returns `{ kind: gate.kind, pass: false, reason: 'UNKNOWN_GATE_KIND' }` (req 5 fail-closed). For compile-time safety over the KNOWN baseline union, narrow first and route unhandled known members through an `assertNever`-style check, but the runtime `default` MUST fail closed for injected string kinds (two layers per PATTERNS). Pure functions, explicit `now`, no internal clock reads.
    Export the evaluators and `evaluateGate`. Append-only.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | head; echo "TSC_EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` reports zero TypeScript errors.
    - model.ts exports `evaluateGate` and the four baseline evaluators.
    - `evaluateGate` default branch returns `pass: false` with reason `'UNKNOWN_GATE_KIND'`: `grep -A2 'default:' ` within the `evaluateGate` body shows `pass: false` / `UNKNOWN_GATE_KIND` (never `pass: true`).
    - `OWN_TIER_GRANT` evaluator body contains no `getAncestors` / `resolveGrant` call: `grep -A10 'OWN_TIER_GRANT' frontend/src/demo/lib/model.ts | grep -cE 'getAncestors|resolveGrant'` is 0.
    - `CLEARANCE_FLOOR` is not implemented as an evaluator (A2): no `case 'CLEARANCE_FLOOR'` in `evaluateGate`.
  </acceptance_criteria>
  <done>Four baseline evaluators + a fail-closed `evaluateGate` dispatcher exported; tsc clean; own-tier gate is a flat resource_id match; unknown kind denies explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveResourceAccess resolver + canIssueResourceGrant</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts lines 282-311 (resolveZoneAccess signature + return — to be reused), 315-323 (isDelegateActive)
    - Task 1's evaluators + `evaluateGate`, and Plan 01's `selectActivePolicy`, `effectiveClassification`, `activeOrgLinksForRole`, `isWindowActive`
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 5, 7, 8, 9, 10; D-03)
    - .planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md ("resolveResourceAccess — gate-loop dispatch", "canIssueResourceGrant")
  </read_first>
  <behavior>
    - With no policy assignment covering `now`, `resolveResourceAccess` returns `{ allow: false, gates: [], zoneAdvisory: null, policyVersion: null, reason: 'NO_ACTIVE_POLICY' }` (D-03 fail-closed).
    - Otherwise it iterates the active policy's `gates` IN LIST ORDER, collecting one `ResourceGateResult` per gate; `allow` is the AND of all gate `pass` values.
    - If the active policy declares `zone_prereq_id`, `zoneAdvisory` is set to the result of the reused `resolveZoneAccess`; `zoneAdvisory` NEVER changes `allow` (req 8 advisory-non-blocking).
    - `policyVersion` reports the selected assignment's `{ valid_from, valid_until }` (req 9).
    - `canIssueResourceGrant` is true iff the actor org holds an active `ADMIN` `org_link` on the resource OR an active matching `ResourceAccessDelegate`; false for non-ADMIN/no-delegate and for expired delegates (req 10).
  </behavior>
  <action>
    Append (Phase 9 section):
    - `resolveResourceAccess(...)` mirroring the `resolveZoneAccess` signature style (explicit `now: Date`, explicit subject personId + subject clearance + subject org-id, arrays passed in: the resource node, `allNetworks`, `allPlatforms`, `allGrants: ResourceAccessGrant[]`, and the v2.1 `allZones`/`allPhysicalGrants` needed by the reused `resolveZoneAccess`). Steps: (1) `selectActivePolicy(resource.policy_assignments, now)` — if `null`, return the D-03 `NO_ACTIVE_POLICY` DENY immediately. (2) Compute `effectiveClassification(resource, allPlatforms)` and build the `ctx` from Task 1. (3) Loop `policy.gates` in array order, calling `evaluateGate(gate, ctx)`, pushing each `ResourceGateResult`; `allow = gates.every(g => g.pass)`. (4) If `policy.zone_prereq_id !== null`, look up the `ZoneNode` and call the existing `resolveZoneAccess(...)` UNCHANGED, attaching its `ZoneAccessResult` to `zoneAdvisory`; otherwise `zoneAdvisory = null`. `zoneAdvisory` must not feed `allow`. (5) Set `policyVersion = { valid_from, valid_until }` of the selected assignment. Return the full `ResourceAccessResult`. No `Date.now()`/`new Date()` inside (PITFALLS #5; D-03/D-04 determinism).
    - `canIssueResourceGrant(actorOrgId: string, resource: NetworkNode|PlatformNode|ApplicationNode, allDelegates: ResourceAccessDelegate[], now: Date): boolean` (per RESEARCH Open-Question 2 — pass arrays explicitly, actor is an org for the ADMIN path). Return true iff `activeOrgLinksForRole(resource.org_links, 'ADMIN', now)` includes `actorOrgId`, OR there is a `ResourceAccessDelegate` with `resource_id === resource.id`, an actor match (`delegate_org_id === actorOrgId` for ORG delegates, or the person path if a personId variant is in scope), and `isWindowActive(d.valid_from, d.valid_until, now)`. Only ADMIN-role orgs may delegate — non-ADMIN/ASSET_OWNER/OPERATOR/SECURITY_APPROVAL-only actors with no delegate return false; expired delegates return false. This closes the v2.1 DELEG-03 gap (`canIssueGrant` was type-only there).
    Export both. Append-only; do not modify `resolveZoneAccess` or any v2.1 symbol.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | head; echo "TSC_EXIT=${PIPESTATUS[0]}"; grep -nE 'new Date\(\)|Date\.now\(\)' frontend/src/demo/lib/model.ts | head</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` reports zero TypeScript errors.
    - model.ts exports `resolveResourceAccess` and `canIssueResourceGrant`.
    - The no-active-policy path returns `reason: 'NO_ACTIVE_POLICY'` with `allow: false`: `grep -A3 'NO_ACTIVE_POLICY' frontend/src/demo/lib/model.ts` shows `allow: false`.
    - `resolveResourceAccess` calls the reused `resolveZoneAccess` for the advisory and `allow` is computed from `gates` only: `grep -A30 'function resolveResourceAccess' frontend/src/demo/lib/model.ts` shows `gates.every` (or equivalent AND over gate pass) and `resolveZoneAccess`, with `zoneAdvisory` assigned independently of `allow`.
    - No internal clock reads in any Phase 9 function: `grep -nE 'new Date\(\)|Date\.now\(\)' frontend/src/demo/lib/model.ts` returns no Phase 9 matches.
    - `git diff frontend/src/demo/lib/model.ts` shows the v2.1 `resolveZoneAccess` body unchanged.
  </acceptance_criteria>
  <done>resolveResourceAccess (loop-dispatch, fail-closed no-policy, advisory-non-blocking, explainable trace) and canIssueResourceGrant exported; tsc clean; resolveZoneAccess reused unchanged; no internal clock reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Demo/mock TypeScript only — no network/auth/persistence. The boundary is **policy + grant + delegate data → the ALLOW/DENY decision and its trace**. Untrusted-shaped data (arbitrary gate `kind`, uncovered timestamps, cross-tier grants) crosses into the resolver and must fail closed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation | unknown gate `kind` in `evaluateGate` | mitigate | `default` branch returns `pass:false, reason:'UNKNOWN_GATE_KIND'`; never `pass:true` (req 5). Test `unknown-gate-kind-errors` (Plan 03) guards it. |
| T-09-06 | Elevation | cross-tier inheritance via `OWN_TIER_GRANT` | mitigate | Own-tier gate is a flat `resource_id` match; no `getAncestors`/`resolveGrant` reuse. Parent gate is a separate explicit check (req 7). Test `cross-tier-inheritance-blocked`. |
| T-09-07 | Spoofing / Information disclosure | advisory zone treated as a blocking gate | mitigate | `zoneAdvisory` is a separate field; `allow` = AND of `gates[]` only; advisory never mutates `allow` (req 8). Test `advisory-non-blocking`. |
| T-09-08 | Elevation | silent ALLOW on uncovered timestamp | mitigate | `selectActivePolicy` null → immediate `NO_ACTIVE_POLICY` DENY (D-03). Test `no-active-policy-denies`. |
| T-09-09 | Tampering | unstable `now` breaking point-in-time determinism | mitigate | All resolvers/evaluators take explicit `now: Date`; no `Date.now()`/`new Date()` internally (PITFALLS #5). |
| T-09-10 | Elevation | non-ADMIN actor issuing grants | mitigate | `canIssueResourceGrant` true only for active ADMIN org-link or active matching delegate (req 10, closes DELEG-03). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero dependencies added this phase. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors.
- `grep -nE 'new Date\(\)|Date\.now\(\)' frontend/src/demo/lib/model.ts` — no Phase 9 matches (stable now).
- `git diff frontend/src/demo/lib/model.ts` — v2.1 `resolveZoneAccess` and all existing symbols unchanged; only Phase 9 appended lines.
</verification>

<success_criteria>
- `resolveResourceAccess` loops policy gates in order, fails closed on no-active-policy and unknown kind, attaches a non-blocking `zoneAdvisory`, and returns an explainable `{ allow, gates, zoneAdvisory, policyVersion }` trace.
- Own-tier gate has no ancestor walk; cross-tier inheritance is structurally impossible.
- `canIssueResourceGrant` enforces ADMIN-or-active-delegate authority.
- `tsc` green; pure functions; append-only.
</success_criteria>

<output>
Create `.planning/phases/09-digital-resource-model-policy-engine/09-02-SUMMARY.md` when done.
</output>
