---
phase: 09-digital-resource-model-policy-engine
plan: 03
type: tdd
wave: 3
depends_on: [09-01, 09-02]
files_modified: [frontend/src/demo/lib/digital-resource.test.ts]
autonomous: true
requirements: [RSRC-ACCESS-02, RSRC-ACCESS-03, RSRC-ACCESS-04, RSRC-ACCESS-05, RSRC-POLICY-01, RSRC-POLICY-02, RSRC-POLICY-03, RSRC-DELEG-01, RSRC-GRANT-02, RSRC-02]

must_haves:
  truths:
    - "cross-tier-inheritance-blocked test passes (Network grant does not satisfy Platform own-tier gate)"
    - "advisory-non-blocking test passes (gates pass, zone prereq fails -> allow:true, zoneAdvisory non-null)"
    - "unknown-gate-kind-errors and no-active-policy-denies tests pass (fail-closed)"
    - "app-classification-inherited test passes (SECRET platform drives the clearance gate; ApplicationNode has no classification)"
    - "every named blocking test from RESEARCH is present and green; npm run test exits 0"
  artifacts:
    - path: "frontend/src/demo/lib/digital-resource.test.ts"
      provides: "blocking Vitest suite, one test per Phase 9 acceptance criterion"
      contains: "cross-tier-inheritance-blocked"
  key_links:
    - from: "digital-resource.test.ts"
      to: "model.ts Phase 9 exports"
      via: "import { resolveResourceAccess, canIssueResourceGrant, ... } from './model'"
      pattern: "from \"./model\""
---

<objective>
Create `frontend/src/demo/lib/digital-resource.test.ts` (new, sibling to `physical-access.test.ts`) with one blocking Vitest test per Phase 9 acceptance criterion. Inline fixtures only, a single fixed `const NOW`, mirroring the `physical-access.test.ts` structure. The named pitfall tests (`cross-tier-inheritance-blocked`, `advisory-non-blocking`, `unknown-gate-kind-errors`, `no-active-policy-denies`, `app-classification-inherited`) are the executable mitigations for this phase's authorization-correctness threats.

Purpose: Lock every critical behavior — fail-closed gates, no cross-tier leak, non-blocking advisory, point-in-time policy selection, delegation authority — behind a grep-able named test before any seed/UI work depends on the engine.
Output: A new test file; `npm run test` green; `npm run build` green. No edits to `model.ts` or `seed.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md
@.planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md
@.planning/phases/09-digital-resource-model-policy-engine/09-01-SUMMARY.md
@.planning/phases/09-digital-resource-model-policy-engine/09-02-SUMMARY.md

<interfaces>
<!-- Phase 9 exports from model.ts (Plans 01-02). Import these; build inline fixtures against them. -->
- Types: `NetworkNode`, `PlatformNode`, `ApplicationNode`, `OrgLink`, `GateDescriptor`, `ResourcePolicy`, `PolicyAssignment`, `ResourceAccessGrant`, `ResourceAccessDelegate`, `ResourceAccessResult`.
- Helpers: `isWindowActive`, `activeOrgLinks`, `activeOrgLinksForRole`, `effectiveClassification`, `selectActivePolicy`, `validatePolicyWindows`.
- Engine: `evaluateGate`, `resolveResourceAccess`, `canIssueResourceGrant`, plus the baseline evaluators.
- Reused v2.1 for advisory fixtures: `ZoneNode`, `PhysicalAccessGrant`, `resolveZoneAccess`.

<!-- Test template conventions (physical-access.test.ts). -->
- Header comment listing coverage; `import { describe, it, expect } from "vitest";` then `import { ... } from "./model";`.
- Inline fixtures, NOT seed.ts imports (the "D3-13 pattern"). Use test-local time constants that exercise each branch of the SEED-06 policy shift, whose boundary is `2026-03-01` per D-04: e.g. `const NOW_A = new Date("2026-02-15T00:00:00Z")` (window A → ALLOW) and `const NOW_B = new Date("2026-04-15T00:00:00Z")` (window B → DENY). Do not use `2026-06-01`.
- Time-window fixtures cover permanent / active / expired / future / exact-boundary.
- One `describe` per function; `it` names state the exact branch + expected outcome.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named pitfall blocking tests (cross-tier, advisory, unknown-kind, no-policy, app-classification)</name>
  <files>frontend/src/demo/lib/digital-resource.test.ts</files>
  <read_first>
    - frontend/src/demo/lib/physical-access.test.ts lines 1-90 (header, imports, inline-fixture + fixed-NOW convention) and a `describe`/`it` example block (e.g. 311-360)
    - The Phase 9 exports in model.ts (Plans 01-02)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 2, 5, 7, 8; acceptance criteria) and 09-RESEARCH.md "Named Blocking Tests" table
  </read_first>
  <action>
    Create `digital-resource.test.ts`. Header comment lists coverage; import `vitest` primitives and the Phase 9 symbols from `./model`. Define a fixed `const NOW` and per-test inline `const` fixtures (nodes, policies, grants, zones). Write these EXACTLY-NAMED `it` blocks (the verifier greps for them):
    - `cross-tier-inheritance-blocked` (req 7): seed a person holding ONLY a Network `ResourceAccessGrant`; build the Platform on that network with a baseline policy (CLEARANCE, OWN_TIER_GRANT, PARENT_TIER_GRANT); call `resolveResourceAccess` for the Platform → assert `result.allow === false` and the `OWN_TIER_GRANT` gate entry has `pass: false`.
    - `advisory-non-blocking` (req 8): build a resource whose active policy declares a `zone_prereq_id` for a zone the subject CANNOT enter, but all access gates PASS (sufficient clearance, active own-tier grant, parent grant). Provide v2.1 `ZoneNode[]`/`PhysicalAccessGrant[]` such that `resolveZoneAccess` denies. Assert `result.allow === true` AND `result.zoneAdvisory !== null` AND `result.zoneAdvisory.allow === false`.
    - `unknown-gate-kind-errors` (req 5): inject a policy with a synthetic gate `{ kind: 'NONEXISTENT_GATE' }`; assert the resulting gate entry has `pass: false` and reason `'UNKNOWN_GATE_KIND'`, and `result.allow === false` (never a silent ALLOW).
    - `no-active-policy-denies` (req 4 / D-03): build a resource whose policy_assignments do not cover `NOW`; assert `result.allow === false`, `result.reason === 'NO_ACTIVE_POLICY'`, `result.policyVersion === null`, `result.gates` empty.
    - `app-classification-inherited` (req 2): seed a Platform classified `SECRET` and an Application on it; resolve with a subject below SECRET → CLEARANCE gate fails using the PLATFORM's classification (not a value on the App). Add a type-level/runtime assertion that the `ApplicationNode` fixture has no `classification` property (e.g. `expect('classification' in app).toBe(false)`).
    Pure inline fixtures; no `seed.ts` import; reuse the fixed `NOW` for determinism.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- digital-resource 2>&1 | tail -20; echo "TEST_EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- digital-resource` exits 0 with all five named tests passing.
    - `grep -c "cross-tier-inheritance-blocked\|advisory-non-blocking\|unknown-gate-kind-errors\|no-active-policy-denies\|app-classification-inherited" frontend/src/demo/lib/digital-resource.test.ts` is 5.
    - The file does NOT import from `./seed`: `grep -c "from \"./seed\"" frontend/src/demo/lib/digital-resource.test.ts` is 0.
    - `cd frontend && npm run build` reports zero TypeScript errors.
    - `model.ts` and `seed.ts` are unchanged: `git diff --stat frontend/src/demo/lib/model.ts frontend/src/demo/lib/seed.ts` is empty.
  </acceptance_criteria>
  <acceptance_criteria>Behavior assertion: each named test fails if the corresponding mitigation is removed (the tests are the threat mitigations, not decorative).</acceptance_criteria>
  <done>Five named pitfall tests present and green; no seed import; tsc clean; model.ts/seed.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-matrix, policy-window, org-link, and delegation tests</name>
  <files>frontend/src/demo/lib/digital-resource.test.ts</files>
  <read_first>
    - The file started in Task 1 (reuse its NOW + fixture style)
    - frontend/src/demo/lib/physical-access.test.ts (per-branch describe/it structure, boundary-window fixtures)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 3, 4, 6, 9, 10) and 09-RESEARCH.md "Named Blocking Tests" table
  </read_first>
  <action>
    Append `describe`/`it` blocks to `digital-resource.test.ts` covering the remaining named blocking tests:
    - `baseline-allow` (req 6): all baseline gates pass → `allow: true`, one gate entry per baseline gate.
    - One `baseline-deny-<gate>` per baseline gate (`baseline-deny-clearance`, `baseline-deny-own-tier`, `baseline-deny-parent-tier`): flip exactly one precondition → `allow: false` with that gate's entry `pass: false` and its naming reason.
    - `gates-evaluate-in-list-order` (req 5): assert `result.gates` order matches the policy's `gates[]` array order.
    - `policy-shift-window-A` / `policy-shift-window-B` (req 4/POLICY-03): a resource with two adjacent non-overlapping `policy_assignments` (policy A then policy B); resolve at a timestamp in window 1 → policy A's gate set + `policyVersion` of A; resolve in window 2 → policy B's gate set + `policyVersion` of B (different gate sets).
    - `overlapping-windows-validator` (req 4): `validatePolicyWindows` returns a non-null error string for overlapping assignments, `null` for adjacent non-overlapping ones.
    - `org-links-active-by-role` (req 3): a resource with two active `OPERATOR` links + one expired `ADMIN` link → `activeOrgLinksForRole(links,'OPERATOR',NOW)` length 2, `activeOrgLinksForRole(links,'ADMIN',NOW)` length 0.
    - `can-issue-admin` / `can-issue-delegate` / `cannot-issue-non-admin` / `cannot-issue-expired-delegate` (req 10): the `canIssueResourceGrant` matrix — active ADMIN org-link → true; active matching delegate → true; OPERATOR/ASSET_OWNER/SECURITY_APPROVAL-only actor with no delegate → false; expired delegate → false.
    - An `explainable-trace` test (req 9): assert `result.gates` lists one entry per baseline gate each with a reason, `zoneAdvisory` is a separate entry, and `policyVersion` matches the selected assignment's window.
    Use inline fixtures and the shared fixed `NOW`; cover exact-boundary windows where relevant (RSRC-GRANT-02 inclusive-boundary).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- digital-resource 2>&1 | tail -25; echo "TEST_EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- digital-resource` exits 0 with all blocking tests passing.
    - `grep -c "baseline-deny-" frontend/src/demo/lib/digital-resource.test.ts` is ≥ 3 (one per baseline gate).
    - `grep -c "policy-shift-window-A\|policy-shift-window-B\|overlapping-windows-validator\|org-links-active-by-role\|gates-evaluate-in-list-order" frontend/src/demo/lib/digital-resource.test.ts` is 5.
    - `grep -c "can-issue-admin\|can-issue-delegate\|cannot-issue-non-admin\|cannot-issue-expired-delegate" frontend/src/demo/lib/digital-resource.test.ts` is 4.
    - `cd frontend && npm run test && npm run build` both exit 0 (full suite green, tsc clean).
  </acceptance_criteria>
  <done>Full gate-matrix, policy-window, org-link, and delegation blocking tests present and green; full Vitest suite + tsc pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The tests ARE the trust boundary for this phase: they assert that policy/seed data crossing into `resolveResourceAccess`/`canIssueResourceGrant` produces fail-closed, non-misrepresenting decisions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Elevation | regression reintroducing cross-tier inheritance | mitigate | `cross-tier-inheritance-blocked` blocking test (named, grep-able). |
| T-09-12 | Spoofing | advisory zone silently flipping the verdict | mitigate | `advisory-non-blocking` blocking test asserts `allow:true` with failing `zoneAdvisory`. |
| T-09-13 | Elevation | silent ALLOW on unknown kind / no policy | mitigate | `unknown-gate-kind-errors` + `no-active-policy-denies` blocking tests. |
| T-09-14 | Tampering | misclassified Application (wrong clearance gate) | mitigate | `app-classification-inherited` asserts the Platform classification drives the gate and the App carries no classification field. |
| T-09-15 | Elevation | non-ADMIN delegation authority | mitigate | `canIssueResourceGrant` matrix tests (admin/delegate true; non-admin/expired false). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero dependencies added; Vitest already present. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run test` — full Vitest suite green (new file included).
- `cd frontend && npm run build` — zero TypeScript errors.
- `git diff --stat frontend/src/demo/lib/model.ts frontend/src/demo/lib/seed.ts` — empty (this plan creates only the test file).
</verification>

<success_criteria>
- Every named blocking test from RESEARCH is present, correctly named, and passing.
- The five pitfall mitigations have grep-able named tests.
- `npm run test` and `npm run build` both exit 0; no edits to model.ts/seed.ts.
</success_criteria>

<output>
Create `.planning/phases/09-digital-resource-model-policy-engine/09-03-SUMMARY.md` when done.
</output>
