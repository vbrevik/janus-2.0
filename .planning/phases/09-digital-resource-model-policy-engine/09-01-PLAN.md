---
phase: 09-digital-resource-model-policy-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/demo/lib/model.ts]
autonomous: true
requirements: [RSRC-01, RSRC-02, RSRC-03, RSRC-04, RSRC-05, RSRC-POLICY-01, RSRC-POLICY-02, RSRC-POLICY-04, RSRC-POLICY-05, RSRC-GRANT-01, RSRC-GRANT-02, RSRC-DELEG-01]

must_haves:
  truths:
    - "ApplicationNode has no classification field (type-level + runtime); Platform/Network do"
    - "org_links is a time-windowed list of role-tagged org associations on every resource node"
    - "policy_assignments selects exactly one active policy by timestamp; no covering window returns null"
    - "overlapping policy windows are surfaced by a validator returning string | null (not a throw)"
    - "the same v2.1 inclusive/null boundary rule governs grants, delegates, org_links, and policy windows"
  artifacts:
    - path: "frontend/src/demo/lib/model.ts"
      provides: "Phase 9 digital-resource types + pure helpers (appended)"
      contains: "Phase 9: Digital Resource hierarchy model"
  key_links:
    - from: "effectiveClassification"
      to: "platform.classification"
      via: "single-hop app -> platform lookup (NOT getAncestors walk)"
      pattern: "effectiveClassification"
    - from: "isWindowActive / activeOrgLinks / selectActivePolicy"
      to: "isGrantActive boundary semantics"
      via: "identical inclusive/null rule"
      pattern: "valid_from === null"
---

<objective>
Append the Phase 9 digital-resource type system and its pure data helpers to `frontend/src/demo/lib/model.ts`. This is the contract layer the resolver (Plan 02) and tests (Plan 03) build against — define every type and every non-gate helper here so downstream plans never explore the codebase for shapes.

Purpose: Establish the strict-tree node types, open-vocabulary org_links, time-versioned parameterized policy descriptors, grant/delegate types, and the result-trace types — plus the window/org-link/classification/policy-selection/validation helpers — all mirroring v2.1 conventions exactly.
Output: New types and pure functions appended under a `// --- Phase 9: Digital Resource hierarchy model (v2.2) ---` section in `model.ts`. Append-only; zero edits to existing v2.1 symbols.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md
@.planning/phases/09-digital-resource-model-policy-engine/09-CONTEXT.md
@.planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md

<interfaces>
<!-- Existing v2.1 symbols in model.ts to mirror/reuse. Extracted from source; do NOT re-explore. -->

From frontend/src/demo/lib/model.ts:
- `Clearance` (lines 8-13): `"UNCLASSIFIED" | "RESTRICTED" | "CONFIDENTIAL" | "SECRET" | "TOP_SECRET"` (5-tier — REUSE, do not redefine).
- `CLEARANCE_RANK` (lines 15-21): `Record<Clearance, number>`, UNCLASSIFIED=0 … TOP_SECRET=4.
- `UnitId` (lines 393-399): the 6 canonical org-id strings (e.g. "MILITARY_1", "INTEL").
- `ZoneNode` (lines 51-60): node-shape analog — flat interface, `id`/`name`/string-FK style, `parent_id: string | null`.
- `PhysicalAccessGrant` (lines 207-213): `{ id; person_id; zone_id; valid_from: Date|null; valid_until: Date|null }` — grant analog; swap `zone_id -> resource_id`.
- `ZoneAccessDelegate` (lines 219-228): `{ id; zone_id; delegate_type:"PERSON"|"ORG"; delegate_person_id:string|null; delegate_org_id:string|null; granted_by_org_id:string; valid_from; valid_until }` — delegate analog; swap `zone_id -> resource_id`.
- `isGrantActive(grant, now): boolean` (lines 232-237): the boundary rule — `(from===null||from<=now) && (until===null||until>=now)`. REUSE these exact semantics.
- `getActiveVisitorPasses(...)` (lines 377-388): inline window-filter pattern to mirror for active-link/active-policy filters.
- `validateEntryLog(entry): string | null` (lines 356-364): validator return-shape to mirror (null = OK, string = config error; never throws).
- `ZoneAccessResult` (lines 74-79): `{ allow; gate; reason; detail? }` — structured-result analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Digital-resource types + result trace types</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts lines 1-100 (Clearance, CLEARANCE_RANK, ZoneNode, ZoneAccessResult shapes) and lines 200-330 (PhysicalAccessGrant, ZoneAccessDelegate, isGrantActive, isDelegateActive)
    - frontend/src/demo/lib/model.ts lines 393-399 (UnitId)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 1, 3, 4, 5, 9, 10)
    - .planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md ("Resource node interfaces", "org_links shape", "ResourceAccessGrant and ResourceAccessDelegate", "ResourcePolicy + parameterized gate descriptors")
  </read_first>
  <behavior>
    - `ApplicationNode` has NO `classification` property (compile error if accessed); `NetworkNode` and `PlatformNode` both carry `classification: Clearance`.
    - `PlatformNode` carries exactly one `network_id: string`; `ApplicationNode` carries exactly one `platform_id: string` (single-valued, strict tree — RSRC-05).
    - Every resource node carries `org_links: OrgLink[]` and `policy_assignments: PolicyAssignment[]`.
    - A gate descriptor is `{ kind: string; ...params }`: baseline kinds `CLEARANCE`/`OWN_TIER_GRANT`/`PARENT_TIER_GRANT` carry no params; `REQUIRED_ROLE` carries `{ kind:'REQUIRED_ROLE', role: string }` (D-01/D-02).
  </behavior>
  <action>
    Append a new section `// --- Phase 9: Digital Resource hierarchy model (v2.2) ---` at the end of `model.ts`. Define:
    - `ResourceTier = 'NETWORK' | 'PLATFORM' | 'APPLICATION'` discriminator.
    - `OrgLink = { org_id: string; role: BaselineOrgRole | (string & {}); valid_from: Date | null; valid_until: Date | null }` where `BaselineOrgRole = 'ADMIN' | 'ASSET_OWNER' | 'OPERATOR' | 'SECURITY_APPROVAL'` (open-vocab union with `(string & {})` for autocomplete per D-discretion; RSRC-04). Use `org_id: string` (accepts `UnitId` values).
    - Gate descriptor union `GateDescriptor`: baseline param-free members `{ kind: 'CLEARANCE' }`, `{ kind: 'OWN_TIER_GRANT' }`, `{ kind: 'PARENT_TIER_GRANT' }`, plus `{ kind: 'REQUIRED_ROLE'; role: string }` (D-02). Keep the union open at the type edge using `| { kind: string & {}; [k: string]: unknown }` so an injected unknown kind is representable at runtime (req 5) — the resolver fails closed on it in Plan 02. Document `CLEARANCE_FLOOR` ONLY as a comment example of the param pattern; do NOT add it to the union (deferred, A2 — SEED-06/07 do not need it).
    - `ResourcePolicy = { id: string; label: string; gates: GateDescriptor[]; zone_prereq_id: string | null }` — the zone prerequisite is declared on the POLICY (req 8, A1: policy-level not node-level), `null` = no advisory zone.
    - `PolicyAssignment = { policy: ResourcePolicy; valid_from: Date | null; valid_until: Date | null }` (reuse the grant window shape; RSRC-POLICY-02/03/05).
    - `NetworkNode = { id; name; tier:'NETWORK'; classification: Clearance; org_links: OrgLink[]; policy_assignments: PolicyAssignment[] }`.
    - `PlatformNode = { id; name; tier:'PLATFORM'; classification: Clearance; network_id: string; org_links: OrgLink[]; policy_assignments: PolicyAssignment[] }`.
    - `ApplicationNode = { id; name; tier:'APPLICATION'; platform_id: string; org_links: OrgLink[]; policy_assignments: PolicyAssignment[] }` — NO `classification` field (RSRC-02, req 2).
    - `ResourceAccessGrant = { id; person_id: string; resource_id: string; valid_from: Date|null; valid_until: Date|null }` mirroring `PhysicalAccessGrant` (RSRC-GRANT-01).
    - `ResourceAccessDelegate = { id; resource_id: string; delegate_type:'PERSON'|'ORG'; delegate_person_id: string|null; delegate_org_id: string|null; granted_by_org_id: string; valid_from: Date|null; valid_until: Date|null }` mirroring `ZoneAccessDelegate` (RSRC-DELEG-01).
    - Result-trace types (req 9): `ResourceGateResult = { kind: string; pass: boolean; reason: string }`; `ResourceAccessResult = { allow: boolean; gates: ResourceGateResult[]; zoneAdvisory: ZoneAccessResult | null; policyVersion: { valid_from: Date|null; valid_until: Date|null } | null; reason?: string }` (the optional top-level `reason` carries `'NO_ACTIVE_POLICY'` per D-03). Reuse the existing `ZoneAccessResult` type for `zoneAdvisory` so the advisory carries the unchanged v2.1 zone outcome.
    Export all types. No fenced code in actual implementation beyond TS source. Do NOT touch any existing v2.1 type.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | grep -v '^$' | head; echo "TSC_EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` reports zero TypeScript errors.
    - `grep -c 'classification' frontend/src/demo/lib/model.ts` confirms `classification` appears on `NetworkNode`/`PlatformNode` only — grep `interface ApplicationNode` block shows no `classification:` line.
    - model.ts contains `export interface ApplicationNode`, `export interface NetworkNode`, `export interface PlatformNode`, `export interface ResourcePolicy`, `export interface ResourceAccessGrant`, `export interface ResourceAccessDelegate`, `ResourceAccessResult`, `GateDescriptor`, `OrgLink`, `PolicyAssignment`.
    - No existing v2.1 symbol (ZoneNode, PhysicalAccessGrant, isGrantActive, resolveZoneAccess, etc.) is modified: `git diff frontend/src/demo/lib/model.ts` shows only appended lines below line 649.
  </acceptance_criteria>
  <done>All Phase 9 types exported and appended; tsc clean; ApplicationNode provably has no classification field; no v2.1 edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure data helpers — window, org-links, classification, policy selection, validator</name>
  <files>frontend/src/demo/lib/model.ts</files>
  <read_first>
    - frontend/src/demo/lib/model.ts lines 230-237 (isGrantActive boundary rule), 315-323 (isDelegateActive), 356-388 (validateEntryLog return-shape, getActiveVisitorPasses filter)
    - The Phase 9 types just appended by Task 1 (OrgLink, PolicyAssignment, ResourcePolicy, NetworkNode/PlatformNode/ApplicationNode, ResourceAccessResult)
    - .planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md ("Active-policy selector + overlapping-window validator", "Application classification derivation", "Shared Patterns")
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (reqs 2, 3, 4; D-03)
  </read_first>
  <behavior>
    - `isWindowActive(valid_from, valid_until, now)` returns true iff `(from===null||from<=now) && (until===null||until>=now)` — byte-identical semantics to `isGrantActive` (no `<`/`>` drift).
    - `activeOrgLinks(orgLinks, now)` returns only links whose window contains `now`; `activeOrgLinksForRole(orgLinks, role, now)` further filters by exact `role` match.
    - `effectiveClassification(node, allPlatforms)`: Network/Platform return own `classification`; Application returns its host platform's `classification` via a single `platform_id` lookup (NO ancestor walk).
    - `selectActivePolicy(policy_assignments, now)` returns the single assignment whose window contains `now`, or `null` when none covers it (D-03 — caller denies; selector itself returns null, not a throw).
    - `validatePolicyWindows(policy_assignments)` returns an error `string` when any two assignment windows overlap, else `null` (mirrors `validateEntryLog`).
  </behavior>
  <action>
    Append (same Phase 9 section) these pure functions — every one takes explicit `now: Date` where time-dependent; NONE call `Date.now()`/`new Date()` internally (Constraint, PITFALLS #5):
    - `isWindowActive(valid_from: Date|null, valid_until: Date|null, now: Date): boolean` — the single shared boundary helper reused by every Phase 9 window check (org_links, policy_assignments, and by Plan 02's grant/delegate checks). Reproduce `isGrantActive`'s inclusive/null rule EXACTLY; do not invent a divergent convention.
    - `activeOrgLinks(orgLinks: OrgLink[], now: Date): OrgLink[]` and `activeOrgLinksForRole(orgLinks: OrgLink[], role: string, now: Date): OrgLink[]` — filter via `isWindowActive` (RSRC-04, req 3).
    - `effectiveClassification(node: NetworkNode | PlatformNode | ApplicationNode, allPlatforms: PlatformNode[]): Clearance` — for `tier:'APPLICATION'`, find `allPlatforms.find(p => p.id === node.platform_id)` and return its `classification`; otherwise return `node.classification`. Single-hop lookup only — explicitly NOT the multi-hop `getAncestors` walk (req 2, PITFALLS App-classification). If the platform is missing, fail closed (treat as highest requirement / throw a clear seed-error string is acceptable — choose the fail-closed option and document it).
    - `selectActivePolicy(policy_assignments: PolicyAssignment[], now: Date): PolicyAssignment | null` — return the single assignment with `isWindowActive(a.valid_from, a.valid_until, now)`, else `null` (RSRC-POLICY-02; D-03 fail-closed handled by Plan 02's resolver).
    - `validatePolicyWindows(policy_assignments: PolicyAssignment[]): string | null` — return a descriptive error string if any two windows overlap (using the inclusive boundary rule), else `null` (req 4 overlap validator; mirrors `validateEntryLog`). This is a seed-config check, not a resolver path.
    Export all. Append-only; no v2.1 edits.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | head; echo "TSC_EXIT=${PIPESTATUS[0]}"; grep -c 'Date.now()\|new Date()' frontend/src/demo/lib/model.ts | sed -n '$p'</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` reports zero TypeScript errors.
    - `grep -nE 'new Date\(\)|Date\.now\(\)' frontend/src/demo/lib/model.ts` returns no match inside any Phase 9 function body (purity / stable-now constraint).
    - model.ts exports `isWindowActive`, `activeOrgLinks`, `activeOrgLinksForRole`, `effectiveClassification`, `selectActivePolicy`, `validatePolicyWindows`.
    - `isWindowActive` source uses `=== null` / `<=` / `>=` exactly as `isGrantActive` (no `<` / `>` strict variants): `grep -A4 'function isWindowActive' frontend/src/demo/lib/model.ts` shows `<= now` and `>= now`.
    - `effectiveClassification` does NOT call `getAncestors`: `grep -A8 'function effectiveClassification' frontend/src/demo/lib/model.ts | grep -c getAncestors` is 0.
  </acceptance_criteria>
  <done>All five helper functions exported, tsc clean, boundary rule identical to v2.1, no internal clock reads, no ancestor walk in classification derivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is demo/mock TypeScript — no network, no auth surface, no persistence. The only meaningful boundary is **policy/seed data → authorization-decision logic**: untrusted-shaped policy descriptors (open-vocabulary gate `kind`, arbitrary window data) cross into the type/helper layer.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / Elevation | `effectiveClassification` for Application | mitigate | Single-hop `app -> platform.classification` only; never reuse `getAncestors` (would leak cross-tier inheritance, req 7). Fail closed if platform missing. |
| T-09-02 | Elevation | `selectActivePolicy` returning a default on uncovered timestamp | mitigate | Selector returns `null` for no-covering-window; never a silent baseline default (D-03; resolver in Plan 02 denies). |
| T-09-03 | Tampering | divergent window boundary semantics | mitigate | Single shared `isWindowActive` reused everywhere with byte-identical inclusive/null rule; no `<`/`>` drift (PITFALLS #6). |
| T-09-04 | Repudiation | overlapping policy windows producing ambiguous decisions | mitigate | `validatePolicyWindows` surfaces overlaps as an explicit seed-config error string. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | None — phase adds ZERO dependencies (RESEARCH STACK confirms append-only, no installs). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run build` — zero TypeScript errors after both tasks.
- `git diff frontend/src/demo/lib/model.ts` — only appended lines; no modification to existing v2.1 symbols.
- No `routeTree.gen.ts` change: `git diff frontend/src/routeTree.gen.ts` is empty.
</verification>

<success_criteria>
- All Phase 9 types and the five pure helpers are exported from `model.ts`.
- `ApplicationNode` has no `classification` field (type-level).
- Window/org-link/policy-selection helpers reuse the v2.1 inclusive/null boundary rule verbatim.
- `tsc` is green; no internal `Date.now()`/`new Date()`; append-only.
</success_criteria>

<output>
Create `.planning/phases/09-digital-resource-model-policy-engine/09-01-SUMMARY.md` when done.
</output>
