---
phase: 09-digital-resource-model-policy-engine
plan: 04
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03]
files_modified: [frontend/src/demo/lib/seed.ts, frontend/src/demo/lib/digital-resource.test.ts]
autonomous: true
requirements: [RSRC-SEED-06, RSRC-SEED-07, RSRC-POLICY-03, RSRC-POLICY-05]

must_haves:
  truths:
    - "seed.ts contains a policy-shift resource (MilNet) with two adjacent non-overlapping policy assignments (SEED-06)"
    - "seed.ts contains a non-baseline-policy resource whose active policy adds REQUIRED_ROLE:SECURITY_APPROVAL (SEED-07)"
    - "the policy-shift resource resolves to ALLOW at a Feb timestamp and DENY at an Apr timestamp (same person)"
    - "the non-baseline resource resolves under the non-baseline policy, not the baseline"
  artifacts:
    - path: "frontend/src/demo/lib/seed.ts"
      provides: "RESOURCE_NODES / RESOURCE_GRANTS / RESOURCE_DELEGATES seed fixtures (appended)"
      contains: "MilNet"
    - path: "frontend/src/demo/lib/digital-resource.test.ts"
      provides: "seed-06-shift-resolves + seed-07-non-baseline-applied tests (appended)"
      contains: "seed-06-shift-resolves"
  key_links:
    - from: "digital-resource.test.ts seed tests"
      to: "seed.ts Phase 9 exports"
      via: "import { ... } from './seed'"
      pattern: "from \"./seed\""
    - from: "SEED-06 policy B"
      to: "REQUIRED_ROLE gate"
      via: "post-incident tightening adds { kind:'REQUIRED_ROLE', role:'SECURITY_APPROVAL' }"
      pattern: "REQUIRED_ROLE"
---

<objective>
Append the two locked real seed fixtures to `frontend/src/demo/lib/seed.ts` — the SEED-06 policy-shift resource (the "tighten after an incident" narrative, D-04) and the SEED-07 non-baseline-policy resource (D-05) — and add the two integration tests that resolve them, in `digital-resource.test.ts`. These are the minimum real fixtures that exercise the policy-shift and non-baseline mechanisms end-to-end; the full 6-unit dataset and WorldState wiring remain Phase 10.

Purpose: Prove the time-versioned, parameterized-gate engine against real seed data, not just inline unit fixtures — the canonical point-in-time demonstration (ALLOW before the incident date, DENY after).
Output: New top-level `export const` arrays in `seed.ts` and two `it` blocks in `digital-resource.test.ts`. Append-only to `seed.ts`; no edits to existing seed exports or to `model.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md
@.planning/phases/09-digital-resource-model-policy-engine/09-CONTEXT.md
@.planning/phases/09-digital-resource-model-policy-engine/09-PATTERNS.md
@.planning/phases/09-digital-resource-model-policy-engine/09-01-SUMMARY.md
@.planning/phases/09-digital-resource-model-policy-engine/09-02-SUMMARY.md

<interfaces>
<!-- Phase 9 types/engine from model.ts (Plans 01-02) and the test file from Plan 03. -->
- Types: `NetworkNode`, `PlatformNode`, `OrgLink`, `ResourcePolicy`, `PolicyAssignment`, `ResourceAccessGrant`, `ResourceAccessDelegate`, `GateDescriptor`.
- Engine: `resolveResourceAccess`, `validatePolicyWindows`.
- Existing seed.ts conventions (lines 1100-1195): top-level `export const ARRAY: Type[] = [ ... ]` with inline object literals, `new Date("YYYY-MM-DD")` windows, `UnitId` strings (`"MILITARY_1"`, `"INTEL"`) for org IDs, `subj-N` person IDs reused from existing subjects. Real zone IDs available for an advisory link (e.g. `zone-room-sigint`, `zone-secure-lab`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEED-06 policy-shift + SEED-07 non-baseline fixtures in seed.ts</name>
  <files>frontend/src/demo/lib/seed.ts</files>
  <read_first>
    - frontend/src/demo/lib/seed.ts lines 1100-1195 (GRANTS/DELEGATES export pattern, inline literals, Date windows, UnitId/subj-N IDs)
    - The Phase 9 types in model.ts (NetworkNode, PlatformNode, ResourcePolicy, PolicyAssignment, OrgLink, ResourceAccessGrant, ResourceAccessDelegate, GateDescriptor)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (req 11) and 09-CONTEXT.md (D-04, D-05) and 09-PATTERNS.md ("seed.ts — SEED-06 / SEED-07 fixtures")
  </read_first>
  <action>
    Append new top-level `export const` arrays to `seed.ts` (append-only; do not touch `ZONES`/`GRANTS`/`DELEGATES`/`ENTRY_LOGS`). Mirror the exact existing style. Define a shared baseline policy descriptor (`gates: [{kind:'CLEARANCE'},{kind:'OWN_TIER_GRANT'},{kind:'PARENT_TIER_GRANT'}]`, `zone_prereq_id: null`) and a non-baseline policy (`baseline gates + {kind:'REQUIRED_ROLE', role:'SECURITY_APPROVAL'}`).
    - RSRC-SEED-06 (D-04, "tighten after incident"): a `NetworkNode` named `MilNet` carrying TWO adjacent non-overlapping `policy_assignments`: policy A `valid_from: null, valid_until: new Date("2026-03-01")` = baseline; policy B `valid_from: new Date("2026-03-01"), valid_until: null` = non-baseline (baseline + `REQUIRED_ROLE: SECURITY_APPROVAL`). Give MilNet an `org_links` list with at least one active `ADMIN` link and provide a person who satisfies clearance + own-tier grant (a `ResourceAccessGrant` on MilNet) but does NOT hold an active `SECURITY_APPROVAL` org-link — so the same person resolves ALLOW at a Feb timestamp (policy A) and DENY at an Apr timestamp (policy B). Choose `classification` (e.g. `SECRET`) and a subject clearance ≥ it. Use `subj-N` IDs reused from existing subjects and `UnitId` org strings.
    - RSRC-SEED-07 (D-05, non-baseline): a separate resource (a `NetworkNode` or `PlatformNode`) whose single active `policy_assignment` is the non-baseline policy (baseline + `REQUIRED_ROLE: SECURITY_APPROVAL`) — the same mechanism as SEED-06's policy B. Provide org_links such that resolution can demonstrate the extra role gate.
    Export the nodes (e.g. `RESOURCE_NODES`), the supporting `RESOURCE_GRANTS: ResourceAccessGrant[]`, and `RESOURCE_DELEGATES: ResourceAccessDelegate[]` if a delegate fixture is useful. Exact IDs/dates are within the locked narratives. Do NOT add the full 6-unit dataset (Phase 10) — these are the two minimum fixtures only.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1 | head; echo "TSC_EXIT=${PIPESTATUS[0]}"; grep -c "MilNet\|REQUIRED_ROLE\|policy_assignments" frontend/src/demo/lib/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` reports zero TypeScript errors (fixtures typecheck against the Phase 9 types).
    - `seed.ts` exports a Phase 9 resource array containing `MilNet` with two `policy_assignments`: `grep -c "MilNet" frontend/src/demo/lib/seed.ts` ≥ 1; `grep -c "REQUIRED_ROLE" frontend/src/demo/lib/seed.ts` ≥ 2 (SEED-06 policy B + SEED-07).
    - SEED-06 windows are adjacent and non-overlapping (policy A `valid_until: new Date("2026-03-01")`, policy B `valid_from: new Date("2026-03-01")`): visible in the appended literals.
    - Existing seed exports are unchanged: `git diff frontend/src/demo/lib/seed.ts` shows only appended lines below the existing `ENTRY_LOGS`/final export.
    - The full 6-unit dataset is NOT added: only the SEED-06 + SEED-07 resources (2 resource nodes, not ≥9).
  </acceptance_criteria>
  <done>SEED-06 (MilNet policy shift across 2026-03-01) and SEED-07 (non-baseline REQUIRED_ROLE) fixtures appended and typechecking; existing seed exports untouched.</done>
</task>

<task type="auto">
  <name>Task 2: seed-resolution integration tests</name>
  <files>frontend/src/demo/lib/digital-resource.test.ts</files>
  <read_first>
    - The SEED-06/07 fixtures just added to seed.ts (Task 1)
    - The test file from Plan 03 (NOW/fixture conventions; this is the ONE place that imports from ./seed)
    - .planning/phases/09-digital-resource-model-policy-engine/09-SPEC.md (req 11 acceptance) and 09-RESEARCH.md "Named Blocking Tests" (`seed-06-shift-resolves`, `seed-07-non-baseline-applied`)
  </read_first>
  <action>
    Append a `describe` block to `digital-resource.test.ts` that DOES import the Phase 9 fixtures from `./seed` (these are the integration cases; per PATTERNS the unit tests stay inline, the seed tests import). Add:
    - `seed-06-shift-resolves` (req 11 / RSRC-SEED-06): resolve the MilNet resource for the seeded person at a Feb-2026 timestamp → `allow: true` (policy A baseline, `policyVersion` = window A); resolve the SAME person at an Apr-2026 timestamp → `allow: false` (policy B adds `REQUIRED_ROLE: SECURITY_APPROVAL` which the person lacks, `policyVersion` = window B). Assert the two `policyVersion` windows differ and the gate sets differ.
    - `seed-07-non-baseline-applied` (req 11 / RSRC-SEED-07): resolve the non-baseline resource and assert the applied policy's `gates` include the `REQUIRED_ROLE` gate (i.e. the non-baseline policy, not the baseline three-gate set, was selected) — e.g. assert a `REQUIRED_ROLE`/`MISSING_REQUIRED_ROLE` gate entry appears in `result.gates`.
    Reuse fixed timestamps consistent with the SEED-06 windows. Do not modify the existing inline tests from Plan 03.
  </action>
  <verify>
    <automated>cd frontend && npm run test 2>&1 | tail -25; echo "TEST_EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test` exits 0 with the full suite green, including `seed-06-shift-resolves` and `seed-07-non-baseline-applied`.
    - `grep -c "seed-06-shift-resolves\|seed-07-non-baseline-applied" frontend/src/demo/lib/digital-resource.test.ts` is 2.
    - The seed `describe` imports from `./seed`: `grep -c "from \"./seed\"" frontend/src/demo/lib/digital-resource.test.ts` is ≥ 1 (the only seed import in the file).
    - Behavior assertion: `seed-06-shift-resolves` asserts ALLOW at the Feb timestamp and DENY at the Apr timestamp for the same person, with differing `policyVersion`.
    - `cd frontend && npm run test && npm run build` both exit 0.
  </acceptance_criteria>
  <done>Two seed-resolution tests present and green; policy-shift ALLOW→DENY across 2026-03-01 demonstrated; non-baseline policy confirmed applied; full suite + tsc pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Demo/mock TypeScript. Boundary: **seed fixture data → the resolver's decision**. The seed fixtures are the first real (non-inline) data crossing into the engine; the integration tests verify the engine treats time-versioned and non-baseline policy data correctly.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | overlapping/ambiguous SEED-06 policy windows | mitigate | SEED-06 windows are adjacent and non-overlapping (shared `2026-03-01` boundary); `validatePolicyWindows` (Plan 01) is available to assert no overlap. |
| T-09-17 | Elevation | non-baseline resource silently resolved under the baseline policy | mitigate | `seed-07-non-baseline-applied` asserts the `REQUIRED_ROLE` gate appears in the trace (correct policy selected). |
| T-09-18 | Elevation | policy shift not actually changing the verdict | mitigate | `seed-06-shift-resolves` asserts ALLOW→DENY for the same person across the incident date with differing `policyVersion`. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero dependencies added. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run test` — full Vitest suite green including the two seed integration tests.
- `cd frontend && npm run build` — zero TypeScript errors.
- `git diff frontend/src/demo/lib/seed.ts` — only appended Phase 9 fixtures; existing exports unchanged.
- `git diff frontend/src/routeTree.gen.ts` — empty.
</verification>

<success_criteria>
- SEED-06 (policy shift) and SEED-07 (non-baseline) fixtures exist in `seed.ts`, typecheck, and are each resolved by a passing test.
- The policy-shift resource flips ALLOW→DENY across the incident date for the same person.
- The non-baseline resource resolves under its non-baseline policy.
- Full `npm run test` + `npm run build` green; append-only.
</success_criteria>

<output>
Create `.planning/phases/09-digital-resource-model-policy-engine/09-04-SUMMARY.md` when done.
</output>
